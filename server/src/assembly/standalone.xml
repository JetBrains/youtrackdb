<?xml version="1.0" encoding="UTF-8"?>
<assembly>
  <id>standalone</id>
  <formats>
    <format>dir</format>
  </formats>
  <fileSets>
    <!--
     | copy server scripts
    -->
    <fileSet>
      <directory>${basedir}/script</directory>
      <outputDirectory>bin</outputDirectory>
      <includes>
        <include>*.sh</include>
      </includes>
      <fileMode>755</fileMode>
      <filtered>true</filtered>
      <lineEnding>unix</lineEnding>
    </fileSet>

    <!--
     | configurations
    -->
    <fileSet>
      <directory>${basedir}/conf</directory>
      <outputDirectory>conf</outputDirectory>
    </fileSet>
    <fileSet>
      <directory>${basedir}/conf/cert</directory>
      <outputDirectory>config/cert</outputDirectory>
    </fileSet>

    <!--
     | Plugins
    -->
    <fileSet>
      <directory>${basedir}/plugins</directory>
      <outputDirectory>plugins</outputDirectory>
    </fileSet>

    <!--
     | Secrets
    -->
    <fileSet>
      <directory>${basedir}/secrets</directory>
      <outputDirectory>secrets</outputDirectory>
    </fileSet>

    <!--
     | creates an empty log directory
    -->
    <fileSet>
      <directory>${basedir}/conf</directory>
      <outputDirectory>log</outputDirectory>
      <excludes>
        <exclude>*</exclude>
      </excludes>
    </fileSet>

    <!--
     | creates an empty databases directory
    -->
    <fileSet>
      <directory>${basedir}/conf</directory>
      <outputDirectory>databases</outputDirectory>
      <excludes>
        <exclude>*</exclude>
      </excludes>
    </fileSet>
  </fileSets>
  <!--
   | includes legals
  -->
  <files>
    <file>
      <source>${basedir}/../license.txt</source>
      <fileMode>444</fileMode>
    </file>
  </files>

  <!--
   | all artifacts in a /lib dir
  -->
  <dependencySets>
    <dependencySet>
      <outputDirectory>/lib</outputDirectory>
      <unpack>false</unpack>
      <scope>runtime</scope>
    </dependencySet>
  </dependencySets>
</assembly>