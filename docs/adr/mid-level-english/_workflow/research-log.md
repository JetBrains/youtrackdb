# Research log — mid-level-english

## Initial request

Make the workflow's house-style rules target mid-level English developers both in conversation, documentation and text of issues.

## Decision Log

- 2026-06-13T15:00Z [ctx=safe] **DR1 — Target is plain-language clarity, not a named proficiency band.** The new rules adopt plain-language moves (short common words, short sentences, active voice, no idiom/phrasal-verb ambiguity, acronyms expanded on first use) without anchoring to CEFR B1–B2 or a reading-grade number.
  - **Why:** A named band or grade implies measurement the project will not run; plain-language moves are teachable and reviewable by eye, which fits the judgment-only enforcement in DR2.
  - **Alternatives rejected:** CEFR B1–B2 anchor (implies measurement, invites scoring debates); reading-grade band like Flesch-Kincaid 8–10 (a number nobody computes during review).
- 2026-06-13T15:00Z [ctx=safe] **DR2 — Judgment guidance only; no NEW mechanical enforcement.** The rules live as prose in the style docs and reviewer lenses. No new `dsc-ai-tell` regex, no sentence-length counter, no new test logic. **Refinement (per adversarial A5):** "no test fixtures" was too absolute — the propagation must still flip the *existing* hook reminder enumeration (`house-style-write-reminder.sh` `tier_b_body`) and its *existing* pinning test (`test_house_style_hook.py` `TIER_B_HEADINGS`) five→six. That is enumeration-sync of pre-existing checks (the same slug-list flip every agent/prompt gets), not the addition of any new mechanical check, so DR2's intent stands: the rule itself is judgment-enforced; the only test touched is the anchor-existence pin that already guards the slug list.
  - **Why:** Plain-language quality is a judgment call (a short sentence can still be unclear; a long one can be clear); a regex would produce false positives and a maintenance burden the em-dash counter already shows is touchy.
  - **Alternatives rejected:** Wire measurable triggers into `design-mechanical-checks.py` + new tests (largest blast radius, false-positive-prone, contradicts DR1's no-measurement stance). Adding a new `dsc-` rule for `## Plain language` (rejected for the same reason — the section is judgment-enforced, like Voice and tone, which carries no `dsc-` rule).
- 2026-06-13T15:00Z [ctx=safe] **DR3 — The new rule joins the always-on AI-tell subset (five→six flip) with natural reach.** It applies to chat, durable Markdown, AND `*.java`/`*.kt` code-comment rationale — the same reach `## Orientation` already has. No carve-out excluding code comments.
  - **Why:** The aim names conversation, which only the subset reaches; consistency with the Orientation precedent avoids a special-case carve-out in the §1.5 tier table.
  - **Alternatives rejected:** Confine to chat + docs + issues and exclude code comments (needs a one-section carve-out in the §1.5 Tier-B column — an exception with no principled reason, since plain rationale comments help every reader).
  - **Tier-B restatement is required, not skipped (resolves adversarial A2).** "Same reach as Orientation" does not mean "no per-surface text." Orientation joins the subset yet still carries a `*.java`/`*.kt` restatement at `conventions.md:574-581` because its literal test ("too terse to follow without opening the code") does not transfer to a reader who has the file open. Plain language has the same transfer problem, so the §1.5 Tier-B prose gains a parallel plain-language paragraph (a new planned edit site): at code-comment scale the **common-word preference, acronym expansion, and idiom-avoidance** moves apply; the **short-sentence / clause-nesting** move is near-meaningless for a one-line `// why` fragment and is not enforced there. The new `## Plain language` section itself states this Tier-B carve in one line, mirroring the Orientation floor restatement at `conventions.md:574-581`.
- 2026-06-13T15:14Z [ctx=safe] **DR4 — Branch takes the §1.7(k) prose-rule self-application opt-out; it does not stage.** It edits `.claude/workflow/**`, `.claude/skills/**`, and `.claude/agents/**` live and is held to its own new rule while it runs. Carries the opt-out marker in `### Constraints`, not the workflow-modifying marker.
  - **Why:** Both opt-out criteria hold — (1) no `_workflow/**` artifact schema moves; (2) §1.7(k) criterion 2 (`conventions.md:1229-1237`) keys on *what part of the file* an edit touches, not the file's identity: a gate-sequence or schema change must stage; a prose citation need not. The two execution-procedure files in the inventory (`step-implementation.md:1038-1041`, `implementer-rules.md:1100-1105`) receive only the AI-tell-subset *enumeration* sentence ("slugs to apply…"), which is prose, not a parsed control structure, so no schema a running phase parses moves. The #1142 Orientation flip confirms by construction: it edited those same two files live under the same opt-out and shipped.
  - **Alternatives rejected:** Full staging via the workflow-modifying marker (staging buys isolation a prose-only branch never needs and forfeits self-application — the whole point of authoring the rule on a branch that can exercise it); a hybrid that stages only the two execution-procedure files (the two markers are mutually exclusive per §1.7(k), and the precedent shows the enumeration edit is judgment-layer).
  - **Consequence (per §1.7(k) Mandatory stamp-advance):** live edits to `.claude/workflow`, `.claude/skills`, `.claude/agents` advance HEAD past the artifacts' stamp base, so the drift gate fires every later session on the branch's own authoring. Suppress per session; run `/migrate-workflow` once after the branch-final pathspec-touching commit to advance every stamp to HEAD. (l) re-points the Phase-3A review trio onto the prose lenses via the opt-out marker.
- 2026-06-13T15:14Z [ctx=safe] **DR5 — New section is `## Plain language`, placed right after `## Orientation`, with a hard anti-conflation boundary.** Proposed plain-language moves (judgment-only, no counts): (a) prefer the common general-English word where it means the same (use/utilize, about/regarding, help/facilitate); (b) short sentences, one idea each — prefer a period over a stacked subordinate clause; (c) avoid idioms, metaphors, and ambiguous phrasal verbs (ship it, low-hanging fruit, spin up, knock out) — use the literal verb; (d) expand a non-floor acronym/abbreviation on first use; (e) keep grammar explicit (the active subject-verb-object the Passive-voice rule already favors). **Boundary clause (load-bearing):** plain language governs *general* English only; it never simplifies technical content or re-teaches the mid-level Java/DB floor — domain terms (MVCC, idempotent, latch, RID, WAL) are not "hard words" to replace. This is the rule-level guard against conflating the two "mid-level"s.
  - **Why:** A dedicated section parallels `## Orientation` (structural clarity) as its lexical/syntactic complement, so neither duplicates the other; placement right after Orientation keeps the two cold-read clarity rules adjacent. The name "Plain language" is itself plain and avoids colliding with the "mid-level Java/DB developer" reader phrase.
  - **Alternatives rejected:** Fold the moves into `## Voice and tone` and `## Banned vocabulary` (scatters one idea across two sections, muddies the Banned-vocabulary AI-tell focus, and gives nothing for the subset enumeration to name); name the section "Mid-level English" (collides with the technical-knowledge "mid-level" already in `:6`/`:42`).
  - **Reconciliation with `## Voice and tone` "bias toward less text":** no conflict — plain language reduces word count (common short words, short sentences); the section states it shares Orientation's anti-padding stance and is never a license to add tutorial text.
  - **Reconciliation with `## Banned vocabulary` (resolves adversarial A4).** Move (a) (prefer the common word) overlaps the banned-word tiers, so the section states the precedence in one line: `## Banned vocabulary` owns the closed AI-tell word list (`leverage→use`, `utilize` is not even on it); `## Plain language` owns general-English word choice *outside* that list and never re-bans a word a tier already covers. The two have different scopes — Banned vocabulary targets AI-anomalous frequency (`house-style.md:80`), plain language targets reader proficiency — so move (a) is not a back-door extension of the banned list toward general word-frequency (which DR1's no-measurement stance would resist).
- 2026-06-13T15:24Z [ctx=safe] **DR6 — Tier = `lite` (user-confirmed).** Gate 1 = no design. **Refinement (per adversarial A5):** the flip does touch a hook (`house-style-write-reminder.sh`) and a test (`test_house_style_hook.py`), but the edit is the hook's *reminder prose / slug list* and the test's *existing anchor-pin*, not hook *control-flow logic* or a new check — so no Workflow-machinery HIGH sub-criterion fires (no control-flow / schema / new-hook-logic / always-loaded-logic change; only judgment-layer prose plus enumeration sync). Gate 1 stays no, tier stays `lite`. Gate 2 = multi-track (~52-file footprint exceeds the ~20-25 track ceiling). Centrally-matched HIGH categories at the design-needing bar: none. User added a **Workflow-machinery prose lens** to the Phase-0→1 adversarial gate (D16). Adversarial gate model = `opus` (D14, lite). PR title carries no issue prefix (user: no issue number).
- 2026-06-13T15:42Z [ctx=safe] **DR7 — De-enumerate `CLAUDE.md:104` instead of growing its count (resolves adversarial A1).** Replace the four-item parenthetical at `CLAUDE.md:104` with a pointer to the canonical subset list (`house-conversation.md` / `conventions.md §1.5`), rather than enumerating five or six section names there.
  - **Why:** The parenthetical already drifted (it omits Orientation after the four→five flip). Re-enumerating it to six just re-arms the same drift for the next flip. A pointer removes `CLAUDE.md` from every future flip's blast radius and fixes the current lag in one move — the single-source-of-truth discipline the rules already follow (`house-style.md:20` calls itself "the single declarative source").
  - **Alternatives rejected:** Leave it at four (a growing, visible inconsistency on an always-loaded surface); grow it to six (re-arms the drift; adds a sixth maintenance site for nothing). Treat `CLAUDE.md` as out of scope entirely (leaves the Orientation lag unfixed and the drift live).
  - **Scope note:** `CLAUDE.md` is a live prose edit outside the stamp pathspec; it is a de-enumeration, not a slug-flip site.

## Surprises & Discoveries

- 2026-06-13T14:57Z [ctx=safe] **"Mid-level" already exists in house-style.md, but on a different axis.** `house-style.md:6` and `:42` define the assumed reader as a "mid-level Java and database developer" — that is a *technical-knowledge* floor (general Java, concurrency, B+-trees, WAL, MVCC, page caches as given; YouTrackDB specifics always glossed). It is explicitly "not a verbosity dial". The aim introduces an orthogonal axis: mid-level *English-language proficiency* (an intermediate / non-native reader). The two "mid-level"s must not be conflated in the rules.
- 2026-06-13T14:57Z [ctx=safe] **Single declarative source + pointer fan-out.** `house-style.md` is the one source of the rules. `house-conversation.md` reuses five of its sections as the always-on AI-tell subset. ~60 files under `.claude/` cite house-style by section name (pointers, not rule copies): 20 review agents in `.claude/agents/`, the workflow prompts, `conventions.md §1.5`, `CLAUDE.md`, the `ai-tells` skill, and the `dsc-ai-tell` mechanical check in `design-mechanical-checks.py`.
- 2026-06-13T14:57Z [ctx=safe] **The always-on AI-tell subset is exactly five sections** (`house-conversation.md:23-27`): Banned vocabulary, Banned sentence patterns, Banned analysis patterns, Em-dash discipline, Orientation. This subset is what applies to chat + code comments + every prose surface. The full house-style (subset + structural + document-shape) applies only to durable Markdown. To make plain-English rules cover *conversation* (the aim names it) they must join this five-section subset.
- 2026-06-13T14:57Z [ctx=safe] **Direct precedent: the explanation-style branch (YTDB-1084 + YTDB-1106).** It added `## Orientation` to the subset as a "four→five flip" and took the §1.7(k) prose-rule self-application opt-out (per session memory `explanation-style-branch.md`). A plain-English section joining the subset is the same shape: a "five→six flip" plus an opt-out branch.
- 2026-06-13T14:57Z [ctx=safe] **Some existing rules already aid an intermediate-English reader; the gaps are specific.** Already helping: Orientation (one causal link per sentence, gloss each project entity), banned roundabout negation / litotes (`not uncommon`), Mechanism traces (no run-on call chains), hedge-stacking removal. Not covered by any rule: sentence length / clause nesting, common-word preference beyond the AI-tell bans, idioms / phrasal verbs / metaphors, acronym expansion on first use.
- 2026-06-13T15:08Z [ctx=safe] **Branch base = develop tip; the five-section subset is the live baseline.** `mid-level-english` is at develop's tip (0 commits ahead). The Orientation work (`f74ef47e94`, PR #1142) is in history, so the always-on subset is already five sections. This branch's flip is genuinely five→six.
- 2026-06-13T15:08Z [ctx=safe] **Five→six blast radius ≈ 52 files (Explore sweep; corrected per adversarial A1/A5; figure approximate, the precise set is per-track).** Six carry a NUMERIC "five" count that must become "six": `house-style.md:20`, `house-conversation.md:21`, `conventions.md:570`, `commit-conventions.md:191`, `step-implementation.md:1038`, and the hook `house-style-write-reminder.sh:256`/`:262`. The rest add the sixth `## ` section slug to an enumeration list: 20 review agents in `.claude/agents/` (19 slug-flips + `review-workflow-writing-style.md`, a content edit adding a Plain-language check), **11** prompts in `.claude/workflow/prompts/` (corrected from 12; `ls` returns 11; one is `design-review.md`, a content edit to the cold-read Human-reader rules), **12** core workflow docs in `.claude/workflow/*.md` (incl. the §1.5 table at `conventions.md:567`), 6 skills, and the 2 output-styles. Plus `CLAUDE.md` (de-enumerated, DR7), the hook, and its pinning test. Same shape as the explanation-style four→five flip (#1142, 57 files) but the exact set is re-derived here, not carried by analogy. **The precise in-scope file list is enumerated exactly in each track's `## Interfaces and Dependencies` at Phase 1** via `grep -rl` over `.claude/` (a `lite`-tier requirement), not estimated.
- 2026-06-13T15:54Z [ctx=safe] **The hook reminder and its pinning test are also five→six targets (per adversarial A5).** `.claude/hooks/house-style-write-reminder.sh` carries the subset twice: a comment "names the five Tier-B" (`:256`) and the `tier_b_body` reminder string (`:262`) that enumerates all five `§ ` slugs plus the numeric "five" and the Orientation code-comment carve. Its pinning test `.claude/scripts/tests/test_house_style_hook.py` holds `TIER_B_HEADINGS` (the five `## ` slugs) with a docstring "Update the hook script and this test together." The flip must (a) add `§ Plain language` to the hook `tier_b_body` and change "five"→"six" at `:256`/`:262`, with a one-line code-comment carve mirroring the Orientation one (DR3 Tier-B carve); (b) add `## Plain language` to `TIER_B_HEADINGS`. Both files sit **outside** the `.claude/workflow|skills|agents` stamp pathspec, so editing them does not advance the stamp base (same status as `house-style.md` / `CLAUDE.md` under §1.7(k)). The test edit is an enumeration-sync update to an existing pin, **not** new mechanical enforcement — DR2 still holds (see refinement on DR2/DR6).
- 2026-06-13T15:42Z [ctx=safe] **CLAUDE.md:104 is illustrative, not a canonical subset enumeration — and currently lags (per adversarial A1).** `CLAUDE.md:104` names the house-conversation subset as a four-item parenthetical (banned vocabulary, banned sentence patterns, banned analysis patterns, em-dash discipline) and still omits Orientation; `#1142` did not touch `CLAUDE.md` (`git show --stat f74ef47e94` has no `CLAUDE.md` row). The canonical subset enumeration lives in `house-conversation.md:21-27` and `conventions.md §1.5`. See DR7 for the resolution. (CLAUDE.md sits at repo root, outside the `.claude/workflow|skills|agents` stamp pathspec, so editing it does not advance the stamp base — same status as `house-style.md` under §1.7(k).)
- 2026-06-13T15:08Z [ctx=safe] **Tier-shaping observation (not yet the tier decision).** The change writes no Java production logic and touches no `_workflow/**` artifact schema, so Gate 1 (needs a `design.md`?) leans *no*. The ~52-file footprint sits far above the ~20-25 in-scope-file track ceiling, so the propagation alone is *multi-track*. Together that points at `lite` (no design + multi-track), but the tier is confirmed with the user at Step 4. The new-section content + reconciliation with `## Voice and tone` ("bias toward less text") and `## Orientation` is the only design-ish work, and it is small — a track Decision Record can carry it without a `design.md`.

## Open Questions

- 2026-06-13T14:57Z [ctx=safe] **Q1 — What is the concrete target for "mid-level English"?** Candidates: CEFR B1–B2 (intermediate), the plain-language movement (short common words, short sentences, active voice), or a reading-grade band. This sets how prescriptive the new rules are and whether they carry measurable triggers (e.g., a sentence-length soft cap like the em-dash count).
- 2026-06-13T14:57Z [ctx=safe] **Q2 — New dedicated section vs. fold into existing sections?** A dedicated `## Plain language` section parallels `## Orientation`; folding spreads the rules into Voice and tone / Banned vocabulary. Affects how the §1.5 mapping and the review-agent enumerations change.
- 2026-06-13T14:57Z [ctx=safe] **Q3 — Confirm the new rules join the always-on five-section subset.** The aim names conversation, so the rules must apply to chat and code comments, not only durable Markdown — implying a five→six subset flip. Confirm.
- 2026-06-13T14:57Z [ctx=safe] **Q4 — §1.7(k) prose-rule self-application opt-out?** The edits look judgment-layer (style rules, the §1.5 table, review-agent prose) and touch no `_workflow/**` schema, so the branch likely takes the opt-out and edits live, self-applying the new rules. Confirm and set the workflow-modifying / opt-out marker accordingly.

- 2026-06-13T15:16Z [ctx=safe] **Resolved.** Q1 → DR1 (plain-language, no band/grade). Q2 → DR5 (dedicated `## Plain language` section). Q3 → DR3 (joins the five→six subset, natural code-comment reach). Q4 → DR4 (§1.7(k) opt-out, precedent-grounded by #1142). No open questions remain blocking the tier classification.

## Baseline and re-validation

This is a §1.7(k) prose-rule opt-out branch (DR4): it edits `.claude/workflow/**`, `.claude/skills/**`, and `.claude/agents/**` live, so it carries the rebase-drift anchor an opt-out branch needs.

- **Branch fork point / base:** `develop` tip `5ec2b01a44` (HEAD == develop at session start; 0 commits ahead).
- **Workflow stamp base (HEAD path-scoped log over `.claude/workflow .claude/skills .claude/agents`):** `f74ef47e94` — the Orientation flip (PR #1142). Phase-1 artifacts created on this branch stamp with this SHA.
- **Drift-gate handling (per §1.7(k) Mandatory stamp-advance):** every live commit to the three prefixes advances HEAD past the stamp base, so the drift gate fires each later session on the branch's own authoring. Suppress per session; run `/migrate-workflow` once after the branch-final pathspec-touching commit to advance every artifact stamp to HEAD and re-arm the gate for real develop-side drift.
- **Re-validation on rebase:** if `develop` lands new workflow-format commits while this branch runs, rebase onto develop and re-read the §1.5 table, the §1.7(k)/(l) sections, and the subset-enumeration sites end-to-end (the prose-file rebase recheck in `CLAUDE.md` § Rebase Conflict Resolution) before continuing.

## Adversarial gate record

<!-- The Phase-0→1 adversarial gate appends one verdict heading per iteration here. -->

### Adversarial review of this log (2026-06-13T15:40Z) — NEEDS REVISION: 0 blocker, 2 should-fix, 2 suggestion
Review file: `reviews/research-log-adversarial-iter1.md` (manifest `findings: 4`; count grep = 4, validated). A1 (blast-radius miscount: 11 prompts not 12; CLAUDE.md not a #1142 flip target and lags at four subset sections) and A2 (DR3 under-argues — the Orientation precedent itself needed a Tier-B restatement, so plain-language needs the parallel code-comment-scale text) both `should-fix`; A3 (DR4 opt-out survives) and A4 (DR5 move (a) ↔ Banned vocabulary precedence undocumented) both `suggestion`. All four addressed below before iteration 2.

### Adversarial review of this log (2026-06-13T15:52Z) — NEEDS REVISION: 0 blocker, 1 should-fix, 1 suggestion
Review file: `reviews/research-log-adversarial-iter2.md` (verdict-producer variant; manifest `findings: 2`; count grep = 2, validated). All four iteration-1 findings VERIFIED; DR7 challenged as a fresh decision and survives. New: A5 (`should-fix`) — the blast radius still omits `.claude/hooks/house-style-write-reminder.sh:256,262` (numeric "five" + the 5-slug `tier_b_body` Tier-B reminder) and its pinning test `test_house_style_hook.py` `TIER_B_HEADINGS` (docstring: "update the hook and this test together"); both are five→six targets, contradicting DR2 "no test fixtures" and DR6 "no hook-logic edit". A6 (`suggestion`) — DR3's Tier-B bullet cites `house-style.md:574-581` but the restatement lives at `conventions.md:574-581`. Both addressed below before iteration 3.

### Adversarial review of this log (2026-06-13T16:02Z) — PASS
Review file: `reviews/research-log-adversarial-iter3.md` (verdict-producer variant; manifest `findings: 1`; count grep = 1, validated). Both iteration-2 findings VERIFIED (A5 hook/test blast-radius + DR2/DR6 refinement; A6 citation fix); the DR2/DR6 refinements were challenged as fresh decisions and survive (a reminder-string edit plus an anchor-pin append is prose/low per `risk-tagging.md` §Workflow machinery, so `lite` + the §1.7(k) opt-out both hold; only `test_16_section_name_guard` keys on the slug list, the Tier-B path tests match on a prefix, so no new test logic). One remaining `suggestion` A7 (three drifting approximate totals ~50/~51/~52) — addressed by unifying the figure to ~52. **Gate cleared at iteration 3 of the iteration_budget of 3.** No blocker or should-fix remains; the log's load-bearing decisions are sound and Phase-1 derivation may proceed.
