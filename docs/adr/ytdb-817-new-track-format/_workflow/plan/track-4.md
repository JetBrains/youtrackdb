# Track 4: Update readers (workflow docs, sub-agent prompts, remaining references)

## Purpose / Big Picture

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Update every code path that reads a per-track file by section heading: Phase A (`track-review.md`), Phase B readers (`step-implementation.md`, `implementer-rules.md`, `step-implementation-recovery.md`), Phase C (`track-code-review.md`), all sub-agent prompts, plus `workflow.md` startup and the remaining workflow docs. Readers that today grep a step's inline blockquote (for risk tag, for episode fields like What-was-done / Key files) now perform a **section-join**: read `## Concrete Steps` for the plan fields (risk tag, description, status); read `## Episodes ### Step N — commit <SHA>` for the episode fields per D11 (`## Artifacts and Notes` is reserved for cross-step content only). **Join key is step number primary, commit SHA disambiguates** — readers match the Episodes block by step number per D5/D9 (`step-implementation-recovery.md` shows retries get monotonically-increasing step numbers, e.g. step 1 `[!]` followed by step 2 retry, so step numbers remain unique within a track file); when the roster line carries `commit: <SHA>`, readers cross-check the Episodes block header SHA as a tiebreaker against any future reshape where step-number-primary alone is insufficient. Ends with a writer-plus-reader smoke test (`/create-plan` then `/review-plan` against a synthetic sentinel) and an expanded grep verification across `.claude/` for both heading-form and prose-form legacy references.

## Progress
- [x] 2026-05-17T06:00Z [ctx=info] Review + decomposition complete
- [x] 2026-05-17T07:53Z [ctx=safe] Step 1 complete (commit f54daa7476)
- [x] 2026-05-17T08:01Z [ctx=info] Step 2 complete (commit 2953500922)
- [x] 2026-05-17T08:06Z [ctx=info] Step 3 complete (commit 5539c59629)
- [x] 2026-05-17T08:12Z [ctx=info] Step 4 complete (commit 60e07ff0e2)
- [x] 2026-05-17T08:30Z [ctx=info] Step 5 complete (commit 1faaad999f)
- [ ] Step implementation
- [ ] Track-level code review
- [ ] Track completion

## Surprises & Discoveries
- 2026-05-17T07:53Z: C&O-as-current-state carve-out idiom established in `consistency-review.md`'s intent-axis pre-screen — when a classification rule splits `[ ]` track sections into intent vs. current-state, `## Context and Orientation` belongs in the current-state set, not the intent / target-state set. Steps 2 and 3 of this track touch analogous rules in `track-review.md` and `implementer-rules.md`; apply the same idiom. See Episodes §Step 1.
- 2026-05-17T08:01Z: Step 6 grep-verification allow-list needs three additions for intentional retired-section references: `track-review.md:474` (legacy `## Reviews completed` retired-section explanation), `episode-format-reference.md:7` (legacy `## Steps` retired-section explanation), `review-mode.md:637` (enumeration of the ten non-Phase-1 sections of the new 14-section shape). See Episodes §Step 2.
- 2026-05-17T08:06Z: Step 6 grep-verification allow-list needs three more additions: `implementer-rules.md:139`, `implementer-rules.md:143`, `implementer-rules.md:152` — all retired-section explanations of the legacy `**Risk:**` blockquote line / `## Description` section, matching the idiom Step 2 introduced. The "legacy NAME blockquote/section no longer exists" idiom is now a recurring documentation pattern across reader-doc explanations. See Episodes §Step 3.
- 2026-05-17T08:12Z: Step 5 advisory — when sweeping `step-implementation.md` and `track-code-review.md`, (a) grep for any remaining `tracks/` path tokens as a residue check (Track 2's atomic switch caught most but prose-context occurrences can slip through, evidenced by `implementation-review.md:509`); (b) prefer forward-looking rewrite over historical-framing preservation for any "pre-Track-2 plans" or "`**What/How/Constraints/Interactions**` subsections" prose, matching the precedent set by Track 3 inline-replanning cases 2–6 and Track 4 Step 4 case 1. See Episodes §Step 4.

## Decision Log
<!-- Continuous-log. Empty at Phase 1. -->

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective
- [x] Technical: PASS at iteration 2 (11 findings — T1+T2 in-scope list expansion + lifecycle-row sync applied; T3 [blocker] smoke-test two-invocation chain + two-pattern grep verification with allow-list applied; T4 risk-tagging.md §Track-file format end-to-end rewrite scoped in decomposition; T5+T11 11 sub-agent prompts coordinated step scoped in decomposition; T6 implementer-rules.md Preconditions one-focused-edit scoped in decomposition; T7+T8 REJECTED as already file-level in-scope; T9 prompts-vs-workflow-doc disambiguation applied; T10 human-eye sweep added)
- [x] Adversarial: PASS at iteration 3 (8 iter-1 + 2 iter-2 regression findings — A1+A2+A3+A5+A6+A7 applied; A4 [should-fix] join-key reframe at iter-2 introduced A9 regression contradicting D5/D9 ("step number + commit SHA" had been inverted to "SHA primary") and citing a non-existent renumber-edge case, reframed at iter-3 to "step number primary, commit SHA disambiguates" matching the Decision Records and `step-implementation-recovery.md` L888-889 monotonic-numbering evidence; A8 [should-fix] D11 immutable mid-execution per planning.md:308 — REJECTED with Phase 4 distillation cost noted as future-reader hazard; A10 [suggestion] L55 invariant parenthetical added at iter-3 for the §2.1 row-sync carve-out exception; iter-3 gate clear with no new regressions)

## Context and Orientation

Reader code paths across `.claude/workflow/` and `.claude/workflow/prompts/` still name the legacy section headings (`## Description`, `## Steps`, `## Reviews completed`) and grep per-step blockquotes for episode content. Track 2 (atomic shape switch per D13) has migrated this branch's own track files to the new 14-section shape and rewired the episode-writer; Track 3 has updated the writer SKILLs. The remaining gap is the reader half — every reader that today expects `## Description` finds nothing under the new shape and either falls back to a whole-file read or returns vacuous output. The transient-reader-staleness window from Track 2 step 6's HEAD to Track 4's HEAD is documented in Track 2's Constraints; Track 4 closes that window.

## Plan of Work

Update each reader in turn. Phase A reader (`track-review.md`) is the largest single file — every `## Description` reference splits across the four new Phase 1 track-level sections (`## Purpose / Big Picture` + `## Context and Orientation` + `## Plan of Work` + `## Interfaces and Dependencies`). Phase B readers (`step-implementation.md` reader half, `implementer-rules.md`, `step-implementation-recovery.md`) batch update via `steroid_apply_patch`; `implementer-rules.md`'s Preconditions block (`level=step` reads the `**Risk:**` line; `level=track` reads `## Description` plus per-step `**Risk:**` lines) is one focused edit that rewrites the join semantics to roster-line `risk:` + `## Episodes` blocks. Phase C reader (`track-code-review.md`) gets the section-join pattern for per-step episode reads. **`risk-tagging.md` §Track-file format example is an end-to-end rewrite** — lines ~219–242 currently show the legacy step-blockquote shape with nested `**Risk:**` lines and an "episode appends below it as today" claim contradicting D9/D11; rewrites to the new roster shape (`1. <description> — risk: <level> [ ]`) plus per-step content in `## Episodes`. Lines 184–205 (Phase-B-upgrade prose referencing `**Risk:**` line rewrite) also need `**Risk:**` → inline roster `risk:` updates. **The 11 sub-agent prompts** under `.claude/workflow/prompts/` (`adversarial-review.md`, `consistency-gate-verification.md`, `consistency-review.md`, `create-final-design.md`, `design-review.md`, `dimensional-review-gate-check.md`, `review-gate-verification.md`, `risk-review.md`, `structural-gate-verification.md`, `structural-review.md`, `technical-review.md`) share an identical template block and are decomposed as **one coordinated step** applied via `steroid_apply_patch` — choose ONE replacement strategy (drop `## Description` reference and name the four 14-section homes explicitly OR instruct whole-file read) and apply uniformly so every sub-agent converges on the same section-name guidance. `prompts/consistency-review.md` and `prompts/structural-review.md` carry the largest content edits within that coordinated step; the workflow-doc-level `.claude/workflow/structural-review.md` (distinct from the prompt with the same basename) also has a §Goal `**What/How/Constraints/Interactions**` + `## Description` reference at L9–L10 that lands in a different step (the workflow-doc sweep). `workflow.md` startup updates path references. **Step ordering for self-modification safety** (per A7): the two most-read self-modifying readers — `step-implementation.md` and `track-code-review.md` — land in the **last** implementation step so earlier Track-4 steps run under stable orchestrator rules. **Verification expansion**: end with (a) an **expanded grep verification** across `.claude/{workflow,workflow/prompts,skills,agents,scripts,docs}/` for both heading-form (`^## Description$`, `^## Reviews completed$`, `^## Steps$`) and prose-form (` `## Description` `, ` `## Reviews completed` `, ` `**What/How/Constraints/Interactions**` `, ` `**Risk:**` ` for the per-step blockquote risk-line distinct from inline roster `risk:`) legacy references, with an explicit allow-list for known-legitimate matches (see Inter-track dependencies below); (b) a **writer-plus-reader smoke test** (`/create-plan` against a sentinel directory then `/review-plan` against the resulting plan) — `/create-plan` alone validates only writers; the reader code paths Track 4 modifies require sub-agent dispatch (Phase 2 `prompts/consistency-review.md` + `prompts/structural-review.md`); (c) a **focused human-eye sweep** on `git diff origin/develop...HEAD .claude/` as a backstop for prose residue the regex patterns miss.

## Concrete Steps

1. Sweep all 11 `.claude/workflow/prompts/*.md` files via one `steroid_apply_patch` call replacing the shared legacy `## Description` + `**What/How/Constraints/Interactions**` template block with the chosen replacement strategy — drop the `## Description` reference and name the four 14-section homes explicitly (`## Purpose / Big Picture` for the intro + `## Context and Orientation` + `## Plan of Work` + `## Interfaces and Dependencies` for the detail subsections), applied uniformly so every sub-agent converges on the same section-name guidance. — risk: high (architecture — coordinated multi-file write across the sub-agent prompts that drive every Phase 2/A/B/C review; inconsistent landing would fragment dispatch semantics across the workflow)  [x] commit: f54daa7476
2. Update Phase A reader (`track-review.md` — every `## Description` reference splits across the four track-level sections + every per-step blockquote read becomes a roster + `## Episodes` section-join), `episode-format-reference.md` (writer reference also read at episode-read time), `risk-tagging.md` (§Track-file format example L219–242 end-to-end rewrite to the new roster shape `1. <description> — risk: <level> [ ]` plus per-step content moved to `## Episodes`; L184–205 Phase-B-upgrade prose `**Risk:**` → inline roster `risk:` updates), and `review-mode.md` (legacy section refs in Approval-panel + Flow examples). — risk: medium (multi-file workflow-doc sweep including the risk-tagging.md structural example rewrite that every Phase A decomposer reads)  [x] commit: 2953500922
3. Update `implementer-rules.md` Preconditions block (one focused edit: `level=step` reads `**Risk:**` line → reads roster-line `risk:` field; `level=track` reads `## Description` → reads the four track-level sections, per-step `**Risk:**` lines → roster `risk:` tags, plus `## Episodes` blocks) and `step-implementation-recovery.md` (resume-from-handoff reader paths, `[!]` failure-row handler, retry-row writer, and any legacy section-name references). — risk: medium (Phase B reader-half rewrite — affects every implementer spawn's precondition read but the orchestration loop itself is untouched)  [x] commit: 5539c59629
4. Cross-cutting workflow-doc sweep — targeted line-level edits across: `workflow.md` startup (path references for the new `_workflow/plan/` shape); `mid-phase-handoff.md:257` (`**Reviews completed**` → `**Outcomes & Retrospective**`); `plan-slim-rendering.md:68,114` (two prose lines naming `## Description` → name the four 14-section homes); `implementation-review.md` (L173/L386/L509 legacy `**What/How/Constraints/Interactions**` + `tracks/` refs); workflow-doc `.claude/workflow/structural-review.md:L9–L10` (§Goal `**What/How/Constraints/Interactions**` + `## Description`); `.claude/skills/edit-design/SKILL.md:L56,L175` (`## Description` refs — sweep verify-or-update only, not a rewrite); `.claude/workflow/inline-replanning.md:225–231` orphan `**What/How/Constraints/Interactions**` reference in the §Updating plan and track files preamble (forward-looking rewrite to name the four 14-section homes); `.claude/workflow/conventions-execution.md` §2.1 *Section lifecycle* table `## Progress` row only (sync the stale `(empty)` Phase-1-writer cell to the four pre-seeded checkpoints `/create-plan` actually writes — preserve §2.1 mapping prose at L84/L89/L93/L165 as intentional historical migration notes per the in-scope carve-out). — risk: low (default — targeted line-level edits across 8 files; each individual edit is mechanical with well-localized failure mode; no HIGH/MEDIUM trigger fires individually)  [x] commit: 60e07ff0e2
5. Update the two most-read self-modifying reader docs in the **last** implementation step (per A7 step-ordering directive — keeps earlier Track-4 steps running under stable orchestrator rules): `step-implementation.md` reader half (every `## Description` reference + per-step blockquote read becomes the roster + `## Episodes` section-join with step-number-primary + commit-SHA-disambiguating; risk-tag reads from inline roster `risk:` field) and `track-code-review.md` (same section-join pattern for per-step episode reads + the multi-step track summary + the workflow-doc `consistency-review.md`/`structural-review.md` triage prose). — risk: high (architecture + self-modification hazard — modifies the very readers the orchestrator uses to spawn Phase B/C sub-agents; landing earlier would break later Track-4 steps' sub-agent dispatch, which the explicit last-step ordering avoids)  [x] commit: 1faaad999f
6. Run the writer+reader smoke chain: (a) `/create-plan` against a fresh sentinel directory `docs/adr/_smoke-test-ytdb-817/` to write a new-shape plan + per-track files; (b) `/review-plan` against the resulting plan so the Phase 2 sub-agent prompts (`prompts/consistency-review.md`, `prompts/structural-review.md`, gate-verifications) read the sentinel track files end-to-end. Then run the two-pattern grep verification across `.claude/{workflow,workflow/prompts,skills,agents,scripts,docs}/` for both heading-form patterns (`^## Description$`, `^## Reviews completed$`, `^## Steps$`, `tracks/track-N`) and prose-form patterns (` `## Description` `, ` `## Reviews completed` `, ` `**What/How/Constraints/Interactions**` `, ` `**Risk:**` `), accepting only the allow-list at §Interfaces and Dependencies (`.claude/skills/analyze-changes/SKILL.md:17`; `.claude/workflow/conventions-execution.md:L84,L89,L93,L165`; fenced Markdown blocks). Verify `ephemeral-identifier-rule.md` + `design-document-rules.md` carry zero legacy refs (verification-only per the in-scope list). Run a focused human-eye sweep on `git diff origin/develop...HEAD .claude/` as a backstop for prose residue. Delete the sentinel directory; fold deletion into the same commit. — risk: medium (verification gate quality — false-negative would let regressions slip through downstream plans; no production-code risk but workflow-tooling-wide impact)  [ ]

## Episodes

### Step 1 — commit f54daa7476, 2026-05-17T07:53Z [ctx=safe]
**What was done:** Swept all 11 `.claude/workflow/prompts/*.md` files for the shared legacy template block (`## Description` heading reference plus the `**What/How/Constraints/Interactions**` subsection label) and replaced every occurrence with explicit names of the four 14-section homes (`## Purpose / Big Picture`, `## Context and Orientation`, `## Plan of Work`, `## Interfaces and Dependencies`). 10 files needed edits (1 prompt — `dimensional-review-gate-check.md` — was already clean); replacement applied uniformly via 5 atomic `steroid_apply_patch` calls. Dimensional review (5 workflow-review agents — consistency, prompt-design, instruction-completeness, context-budget, writing-style) ran in 2 iterations. Iter-1 applied 3 should-fix findings: M1 fixed a lifecycle-attribution drift across 3 prompts (the four track-level sections are seeded at Phase 1 by `/create-plan`, not at Phase A); M2 added a `## Context and Orientation`-is-current-state carve-out to `consistency-review.md`'s intent-axis pre-screen; M3 expanded the dangling `W/H/C/I subsections` shorthand in `structural-review.md`. Iter-2 surfaced one regression (WI3): the upstream target-state-claim definition still listed `## Context and Orientation` alongside Purpose/Plan/Interfaces, contradicting M2's carve-out — removed that section from the upstream list so both passages reinforce each other. Iter-2 gate-check PASS on the affected dimension (instruction-completeness). Iter-1 deferred suggestions on enumeration repetition (WP1/WP3/WP4) — future cleanup could introduce a shorthand alias.

**What was discovered:** The C&O-as-current-state carve-out idiom now established in `consistency-review.md` (iter-1 M2 + iter-2 WI3 fix) is a reusable pattern for any classification rule that splits a `[ ]` track's sections into intent vs. current-state. Steps 2 and 3 of this track touch analogous rules in `track-review.md` (Phase A reader) and `implementer-rules.md` (level=track reader); both should apply the same idiom — treat `## Context and Orientation` as the current-state carve-out, not as part of the intent / target-state set. The plan-Checklist already named "11 sub-agent prompts" but only 10 actually carried legacy refs; `dimensional-review-gate-check.md` was inspected and skipped intentionally (verified via grep). Three prompts (`adversarial-review.md`, `risk-review.md`, `technical-review.md`) carry byte-identical "Where things live" / "Start by reading…" paragraphs — likely copy-pasted at prompt creation; any future drift between these three would fragment dispatch semantics. The iter-2 gate-check assigned the ID `WI3` to its new finding via "next-available integer" logic, colliding with the iter-1 deferred `WI3` (the 10-of-11 file count note) — minor audit-trail wrinkle; both are documented here.

**What changed from the plan:** none.

**Key files:**
- `.claude/workflow/prompts/adversarial-review.md` (modified)
- `.claude/workflow/prompts/consistency-gate-verification.md` (modified)
- `.claude/workflow/prompts/consistency-review.md` (modified)
- `.claude/workflow/prompts/create-final-design.md` (modified)
- `.claude/workflow/prompts/design-review.md` (modified)
- `.claude/workflow/prompts/review-gate-verification.md` (modified)
- `.claude/workflow/prompts/risk-review.md` (modified)
- `.claude/workflow/prompts/structural-gate-verification.md` (modified)
- `.claude/workflow/prompts/structural-review.md` (modified)
- `.claude/workflow/prompts/technical-review.md` (modified)

**Critical context:** none.

### Step 2 — commit 2953500922, 2026-05-17T08:01Z [ctx=info]
**What was done:** Swept legacy section-name and per-step-blockquote references in four Phase A reader docs to match the new 14-section per-track shape. (1) `track-review.md` — every `## Description` reference replaced with explicit naming of the four Phase 1 track-level sections (`## Purpose / Big Picture`, `## Context and Orientation`, `## Plan of Work`, `## Interfaces and Dependencies`); legacy `**What/How/Constraints/Interactions**` subsection labels dropped from prose; `### Clarifications` write-target moved from `## Description` to `## Context and Orientation` per the Step-1 C&O-as-current-state idiom. (2) `episode-format-reference.md` — legacy `## Steps` mention reframed as the retired section name; `## Concrete Steps` is canonical. (3) `risk-tagging.md` — end-to-end rewrite of §Track-file format example (L219-242) from the legacy step-blockquote shape with nested `**Risk:**` lines to the new `## Concrete Steps` roster shape (`N. <description> — risk: <level> [ ]`); Phase-B-upgrade prose (L184-205) and §"Where this file is loaded" updated so `**Risk:**` line references become inline roster `risk:` field references. (4) `review-mode.md` — anchor-section gate's protected-section list inverted (the four Phase 1 sections are now the light-amendment zone, the other ten 14-section sections are protected); `### Clarifications` carve-out moves under `## Context and Orientation`; `EDIT_STEP_DESC` payload description and ESCALATE-on-new-track description name the four sections explicitly. Applied via `steroid_apply_patch` (multi-site literal-text route per project rule).

**What was discovered:** Three intentional historical references survived the sweep and are NOT regressions — Step 6's two-pattern grep verification allow-list needs to cover them: `track-review.md:474` ("the legacy `## Reviews completed` section no longer exists in the 14-section per-track shape" — explicit retired-section explanation), `episode-format-reference.md:7` ("no longer a nested blockquote under a legacy `## Steps` roster item" — retired-section explanation), and `review-mode.md:637` (enumeration of the ten non-Phase-1 sections inside the anchor-section gate's protected-section list — `## Artifacts and Notes` is a legitimate section in the new shape, not a legacy reference). The C&O-as-current-state idiom from Step 1 generalised cleanly to the `review-mode.md` anchor-section gate: the `### Clarifications` subsection (user-supplied current-state notes) naturally belongs under `## Context and Orientation` rather than the original `## Description`, reinforcing the cross-step idiom. The initial `steroid_apply_patch` invocation on `track-review.md` failed atomically because one hunk's old_string carried a 2-space indent that didn't match disk — re-issued with the corrected anchor and the 12-hunk patch landed in one shot; consistent with "atomic, all-or-nothing pre-flight" semantics of the tool.

**What changed from the plan:** none.

**Key files:**
- `.claude/workflow/track-review.md` (modified)
- `.claude/workflow/episode-format-reference.md` (modified)
- `.claude/workflow/risk-tagging.md` (modified)
- `.claude/workflow/review-mode.md` (modified)

**Critical context:** none.

### Step 3 — commit 5539c59629, 2026-05-17T08:06Z [ctx=info]
**What was done:** Rewired the Phase B reader-half rulebook (`implementer-rules.md` Preconditions block + §"Risk upgrade required" risk-line writeback reference) and the Phase B Resume recovery dispatch table (`step-implementation-recovery.md` `late-risk-upgrade` row) to name the new 14-section per-track-file vocabulary. The Preconditions `level=step` bullet now reads the inline `risk: <tag>` token on the `## Concrete Steps` roster line as the per-step risk source; the `level=track` bullet expands `## Description` into the four track-level sections per D10 and names the `## Episodes` blocks as the per-step content source per D9/D11. The §"Risk upgrade required" writeback prose now points at the inline roster `risk:` token rewrite, matching the orchestrator-side path already documented in `apply_upgrade_then_decide`. The recovery-doc table cell "Verify Risk line" → "Verify roster-line `risk:` token". Applied via one `steroid_apply_patch` call (3 hunks across 2 files, atomic).

**What was discovered:** `step-implementation-recovery.md` was already substantively on the new shape from Track 2 step 6's atomic switch — only one stale table cell ("Verify Risk line") carried legacy phrasing. The rest of the file already names `## Concrete Steps` roster lines, inline `risk: <tag>` tokens, `## Episodes` blocks, and `## Progress` entries throughout the resume-from-handoff reader paths, `[!]` failure-row handler, and retry-row writer; the step prompt's mention of "any legacy section-name references" turned up empty beyond that single line. Three additional retired-section explanations introduced in this commit need Step 6's allow-list: `implementer-rules.md:139` (level=step bullet — "legacy `**Risk:**` blockquote line no longer exists"), `implementer-rules.md:143` (level=track bullet — "no `## Description` section exists; track-level intent now spans four sections per D10"), and `implementer-rules.md:152` (level=track bullet — "legacy per-step `**Risk:**` blockquote line no longer exists"). The "legacy NAME blockquote/section no longer exists" idiom is now established across six reader-doc explanations spanning Steps 1–3.

**What changed from the plan:** none. Step 3 scope as planned — implementer-rules.md Preconditions block (one focused edit) plus step-implementation-recovery.md legacy references. The recovery-doc scope turned out narrower than anticipated (single table-cell edit rather than broader rewrite), but the step's contract was met (resume paths verified clean; `[!]` handler and retry-row writer already named `## Concrete Steps` / `## Episodes`).

**Key files:**
- `.claude/workflow/implementer-rules.md` (modified)
- `.claude/workflow/step-implementation-recovery.md` (modified)

**Critical context:** none.

### Step 4 — commit 60e07ff0e2, 2026-05-17T08:12Z [ctx=info]
**What was done:** Swept legacy section-name and path references across 8 workflow/SKILL files in one atomic `steroid_apply_patch` call (12 hunks). `workflow.md` (L234 / L421) and `implementation-review.md` (L173) replaced "track file's `## Description`" / "`**What/How/Constraints/Interactions**` subsections" prose with explicit enumeration of the four track-level sections. `plan-slim-rendering.md` (L68 / L114) and `structural-review.md` (L9-10) got the same treatment in their pending-track-detail prose. `mid-phase-handoff.md:257` Phase A "Do NOT redo" default updated from `**Reviews completed**` to `**Outcomes & Retrospective**`. `implementation-review.md:509` Completion staging guard updated from "`tracks/` directory" to "`plan/` directory". `edit-design/SKILL.md` (L56 / L175) track-file `## Description` references updated to `## Decision Log` (the new home for per-track inlined Decision Records and `**Full design**` refs). `inline-replanning.md` case 1 (L226-228) dropped the legacy "`**What/How/Constraints/Interactions**` subsections" historical framing in favour of a forward-looking description of the three track-level sections that absorb the prose. `conventions-execution.md` §2.1 *Section lifecycle* table `## Progress` row Phase 1 writer cell synced from `(empty)` to the four pre-seeded phase checkpoints `/create-plan` actually writes. §2.1 mapping prose at L84/L89/L93/L165 preserved verbatim per the in-scope carve-out.

**What was discovered:** The retired-section-explanation idiom does NOT recur in any of this step's 8 files — no new allow-list entries for Step 6 beyond those Steps 1–3 already documented (six entries total). The single remaining `tracks/` match in the 8 in-scope files (`conventions-execution.md:123`) sits inside a fenced example-episode code block intentionally preserving historical context — legitimate, no action needed. Track 3 already swept inline-replanning cases 2–6 on a forward-looking basis (no historical framing); Step 4 has now matched case 1 to that style — precedent for Step 5 if step-implementation.md / track-code-review.md carry analogous "pre-Track-2 plans carried…" historical prose. The `tracks/`-token path-glob residue in `implementation-review.md:509` is evidence that prose-context occurrences can slip through atomic switches even after a directory rename — Step 5 should grep step-implementation.md / track-code-review.md for any remaining "tracks/" path tokens as a residue check.

**What changed from the plan:** none.

**Key files:**
- `.claude/workflow/workflow.md` (modified)
- `.claude/workflow/mid-phase-handoff.md` (modified)
- `.claude/workflow/plan-slim-rendering.md` (modified)
- `.claude/workflow/implementation-review.md` (modified)
- `.claude/workflow/structural-review.md` (modified)
- `.claude/workflow/inline-replanning.md` (modified)
- `.claude/workflow/conventions-execution.md` (modified)
- `.claude/skills/edit-design/SKILL.md` (modified)

**Critical context:** none.

### Step 5 — commit 1faaad999f, 2026-05-17T08:30Z [ctx=info]
**What was done:** Rewrote the reader-half of the two most-read self-modifying orchestrator docs (`step-implementation.md` and `track-code-review.md`) to the new 14-section per-track-file vocabulary. The sub-agent context blocks for both step-level and track-level review now name the four Phase 1 track-level sections (`## Purpose / Big Picture`, `## Context and Orientation`, `## Plan of Work`, `## Interfaces and Dependencies`), describe the `## Concrete Steps` roster shape (numbered line + inline `risk: <tag>` token + checkbox + optional `commit: <SHA>`), and define the `## Episodes` section-join (step-number-primary, commit-SHA-disambiguating). Track-completion description-collapse prose in `track-code-review.md` rewritten forward-looking to drop the legacy `**What/How/Constraints/Interactions**` framing (matching the precedent across Track 3 inline-replanning cases 2-6 and Track 4 Step 4 case 1). Dimensional review (4 workflow-review agents — consistency, instruction-completeness, context-budget, writing-style) ran in 1 iteration. Iter-1 applied 3 should-fix findings: M1 added the missing fifth episode field (`**Critical context:**`) to both context-block enumerations matching the canonical episode shape; M2 + M3 demoted prose em-dashes to colons / parentheses / sentence breaks in both files' Track File paragraphs, eliminating the banned "X — Y — Z" triple-clause cadence (concise-doc.md L49); the checkbox-state-alphabet annotation (`[x]` complete, `[!]` failed-and-retried, `[ ]` pending) folded naturally into the rewrite since it touched the same roster-syntax line. Iter-2 gate-check PASS on both affected dimensions (consistency, writing-style). One suggestion (WC2 roster code-span typography drift) deferred as out-of-scope polish. Applied via two `steroid_apply_patch` calls (initial commit: 2 hunks step-impl + 4 hunks track-review = 6 hunks; review-fix commit: 4 hunks across 2 files); both atomic.

**What was discovered:** Step 4's tactical advisories were followed and both matched the discovered state: (a) `tracks/` path-token residue check ran on both files at the start — neither carried a remaining reference, so Track 2's atomic switch was comprehensive on these two readers; (b) the forward-looking-vs-historical-framing choice was decided forward-looking, matching Track 3 cases 2-6 and Track 4 Step 4 — the prose at `track-code-review.md:1090` ("track file's `## Description` section from Phase 1 onward") was the only on-disk passage actively contradicting the new 14-section shape, so a historical-preservation rewrite would have left an active misstatement on disk. The dim-review iter-1 surfaced two writer-side completeness gaps the implementer hadn't caught: (1) field-enumeration drift (the sub-agent context block listed 4 of 5 canonical episode fields); (2) em-dash overload + banned triple-clause cadence (4 dashes in step-implementation.md, 5 in track-code-review.md). Both are write-time hazards that load-bearing on-every-review-spawn prose silently propagated; the gate-check pattern caught them before they shipped.

**What changed from the plan:** none.

**Key files:**
- `.claude/workflow/step-implementation.md` (modified)
- `.claude/workflow/track-code-review.md` (modified)

**Critical context:** none.

## Validation and Acceptance
<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery
<!-- Phase A placeholder — Phase A names per-step idempotence and recovery paths once steps are decomposed. -->

## Artifacts and Notes
<!-- Cross-step artifacts only. Empty until cross-step content surfaces. -->

## Interfaces and Dependencies

**In-scope files**: every workflow doc and prompt not already updated by Tracks 1, 2, or 3. Specifically: `track-review.md`, `step-implementation.md` (reader half), `implementer-rules.md`, `step-implementation-recovery.md`, `track-code-review.md`, every `prompts/*.md`, `workflow.md`, `episode-format-reference.md`, `risk-tagging.md`, `mid-phase-handoff.md`, `plan-slim-rendering.md`, `review-mode.md`, `ephemeral-identifier-rule.md` (verification only), `design-document-rules.md` (verification only), `.claude/workflow/implementation-review.md` (legacy `**What/How/Constraints/Interactions**` + `tracks/` references at L173/L386/L509), `.claude/workflow/structural-review.md` (workflow-doc, distinct from `prompts/structural-review.md`; §Goal legacy references at L9–L10), `.claude/skills/edit-design/SKILL.md` (`## Description` references at L56/L175 — sweep verify-or-update only, not a rewrite), `.claude/workflow/conventions-execution.md` §2.1 *Section lifecycle* table `## Progress` row only (sync the stale `(empty)` Phase-1-writer cell to the four pre-seeded checkpoints `/create-plan` actually writes — preserve §2.1 mapping prose at L84/L89/L93/L165 as intentional historical migration notes), `.claude/workflow/inline-replanning.md:225–231` orphan `**What/How/Constraints/Interactions**` reference in the §Updating plan and track files preamble (Track 3 swept cases 1–6; this orphan sits between cases and outside the per-case enumeration — decide at decomposition between forward-looking rewrite vs explicit historical framing).

**Out-of-scope**: workflow-review triage update (Track 1), spec files (Track 2) **except** the `conventions-execution.md` §2.1 *Section lifecycle* `## Progress` row sync explicitly carved into scope above (this is a Track-3 Phase-C plan-correction routed here per plan-file L261; Track 2 is `[x]` so the row sync cannot route back), writer SKILLs (Track 3), `step-implementation.md` sub-step 7 writer half (Track 2 step 6 per D13).

**Coordinated with Track 2 step 6 on `step-implementation.md`**: Track 2 step 6 (atomic shape switch per D13) lands sub-step 7's writer-half edit and the on-disk shape migration; Track 4 picks up Track 2's HEAD as base. Verify Track 2 step 6's writer-half edit is intact after Track 4's reader-half edits land.

**No section-content semantics changes**: only section names change; the logic each reader implements stays identical. This is a renaming sweep, not a behavioral rewrite. Resist scope-creep into rewriting the readers' semantics. (Exception: the `conventions-execution.md` §2.1 *Section lifecycle* `## Progress` row sync per the L51 carve-out is a lifecycle-table content correction, not a section-name change; preserved as a scope-creep guard for all other in-scope files.)

**Smoke test discipline**: the smoke test is a **two-invocation chain** — (a) `/create-plan` against a sentinel directory (e.g., `_smoke-test-ytdb-817`) to write a new-shape plan + per-track files; (b) `/review-plan` against the resulting plan so the Phase 2 sub-agent prompts (`prompts/consistency-review.md`, `prompts/structural-review.md`, gate-verifications) read the sentinel track files. The (a)+(b) pair is the writer+reader integration test; `/create-plan` alone covers only writer code paths Track 3 already validated. The leading-underscore convention keeps future hooks / CI from pre-empting the sentinel; do NOT run `/execute-tracks` against the sentinel (would attempt real implementation). Delete the sentinel directory after both invocations pass; deletion is the final commit in Track 4 (or a fold into the final implementation step's commit if no other content remains).

**Markdown-only changes**: no Java, no Maven, no tests.

**Inter-track dependencies**:
- **Depends on Track 1** (workflow-review triage) for correct Phase C dispatch on this track's diff, **Track 2** (spec + atomic shape switch per D13 — by Track 4 start, this branch's track files are already on the new shape and the episode writer already follows the multi-section convention), and **Track 3** (writer-SKILL changes).
- Shares `step-implementation.md` with Track 2 step 6 (writer half) — Track 4 owns the reader half; see In-scope files above.
- The final grep verification gates Phase C of this track. Verification is a **two-pattern set** with an explicit allow-list: heading-form patterns (`^## Description$`, `^## Reviews completed$`, `^## Steps$`, `tracks/track-N`) and prose-form patterns (` `## Description` `, ` `## Reviews completed` `, ` `**What/How/Constraints/Interactions**` `, ` `**Risk:**` `). Allow-list — known-legitimate matches that are NOT regressions: `.claude/skills/analyze-changes/SKILL.md:17` (legitimate `## Steps` SKILL heading unrelated to track-file vocabulary); `.claude/workflow/conventions-execution.md:L84,L89,L93,L165` (intentional historical migration-mapping prose preserved per the In-scope carve-out above); fenced Markdown blocks marked as legacy examples per the §192 invariant. Any match outside the allow-list is a blocker — implies a missed reference somewhere across the four tracks.
- A **focused human-eye sweep** on `git diff origin/develop...HEAD .claude/` runs in Track 4's final implementation step as a backstop for prose residue the regex patterns miss. ~10 minutes of operator attention on a focused diff; catches references no pattern enumeration would have caught.
- The smoke test exercises the writer-plus-reader chain via `/create-plan` then `/review-plan` (Phase 2 sub-agent dispatch against the sentinel) — the closest thing this plan has to an integration test. If it fails, the failure is almost certainly a missed section-name reference in a `prompts/*.md` file or a Phase A/B/C reader doc; rerun the grep verification narrowed to the surfaced section name.

## Base commit
c59f2c2cab4765d11b460a20b9be78b047bf6364
