# Track 2: Define the new shape + atomic shape switch (spec + directory rename + terminology rename + writer rewire + self-migration)

## Purpose / Big Picture

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Update the single source of truth — `conventions.md` §1.2 + §1.1 glossary, `conventions-execution.md` §2.1, `planning.md`, and `design-document-rules.md`'s boundary table — to describe the new per-track ExecPlan shape, the new root-index shape, the per-section lifecycle, and the new section budgets. Includes two paired mechanical renames per D7: the directory `_workflow/tracks/` → `_workflow/plan/` and the prose term "step file" → "track file" across every workflow doc, sub-agent prompt, and skill file. Ends with an **atomic shape switch** (per D13) that rolls the episode-writer rewire previously scoped to Track 3 step 5 together with the on-disk directory rename and 5-section → 14-section shape migration of this branch's own track files. After Track 2 lands, every reader and writer touched in Tracks 3 and 4 references this spec under the new vocabulary AND this branch's own workflow state is on the new shape — no intermediate commit leaves the workflow tooling and on-disk shape inconsistent.

## Progress
- [x] 2026-05-16T10:58Z [ctx=unknown] Review + decomposition complete
- [x] 2026-05-16T17:01Z [ctx=safe] Step 1 complete (commit 5f55a18fa1)
- [x] 2026-05-16T17:05Z [ctx=safe] Step 2 complete (commit 5548a88356)
- [x] 2026-05-16T17:08Z [ctx=safe] Step 2 fix complete (commit 03afffa669)
- [x] 2026-05-16T17:13Z [ctx=info] Step 3 complete (commit 201ae2e7c2)
- [x] 2026-05-16T17:19Z [ctx=info] Step 4 complete (commit 92750cefdc)
- [x] 2026-05-16T17:21Z [ctx=info] Step 4 fix complete (commit 86970accd3)
- [x] 2026-05-16T17:26Z [ctx=info] Step 5 complete (commit 8666df8490)
- [x] 2026-05-16T15:45Z [ctx=info] Step 6 atomic switch complete (commit 9ed9ae421a)
- [x] 2026-05-16T16:05Z [ctx=warning] Step 6 Review fix complete (commit 6fdc2f6dc3)
- [x] 2026-05-16T16:05Z [ctx=warning] Step implementation complete (6/6 steps)
- [x] 2026-05-16T16:44Z [ctx=info] Track-level code review iteration 1 complete (1/3 iterations)

## Surprises & Discoveries

- 2026-05-16T17:08Z Step 1 discovered: literal-token regex (`tracks/track-N.md` with literal `N`) missed five placeholder-variant references (`<index>`, `<N>`, `<M>`) in `review-mode.md` and `track-code-review.md`. Swept in Step 2 follow-up commit `03afffa669`. See Episodes §Step 2.
- 2026-05-16T17:21Z Step 4 discovered: Step 3's edits to `conventions.md` §1.1/§1.2 left the section list inconsistent with `design.md` §"New per-track file shape" (`## Risks and Mitigations` named as OpenAI section; `## Artifacts and Notes` mis-grouped as workflow-specific). Reconciled in Step 4 follow-up commit `86970accd3`. Future shape changes must update `conventions-execution.md` §2.1 and `design.md` §"New per-track file shape" in lock-step. See Episodes §Step 4.
- 2026-05-16T16:05Z Step 6 discovered: dim-review surfaced 5 blocker findings against the initial atomic-switch commit — statusline-fallback sentinel collision (`unavailable` vs `unknown`), Phase A writer pointing at a removed section (`## Reviews completed`), `step-implementation-recovery.md` §Step Failure retaining the old `## Steps` blockquote shape, residual `(N/M complete)` step-count idiom, and §2.1 canonical write-order disagreeing with the implemented writer-doc shape on Concrete-Steps flip ordering. All five fixed in `Review fix:` commit `6fdc2f6dc3`. Pattern: cross-file rename + section-split work surfaces drift at end-of-step verification, not during the implementer pass — Track 4's reader-pass should expect the same shape. See Episodes §Step 6.
- 2026-05-16T16:05Z Step 6 discovered: deferred dim-review findings for Track 4's reader-pass / prose-polish — WC10 (Surprises HTML-comment vs canonical entry form), WC11 (reader-side "treat as safe" vs writer-side `unknown` polish), WC12 (correction-note shape missing example), WI4 (failed-step writer site list missing Phase B Resume pointer), WI6 (`(review-fix path)` discriminator inconsistency), WI7 (Track 1 Surprises empty due to backfill timing), WB2 (`episode-format-reference.md` size delta — partly addressed by F8), WB3 (per-track file growth watch-item), WS1-WS11 (concise-doc style cleanup of inherited pre-Track-2 prose: em-dash overuse on Concrete Steps roster lines, ≥200-word section caps on `## Episodes` / `## Interfaces and Dependencies` in `plan/track-2.md`, missing BLUF leads, adjective triads). See Episodes §Step 6.

## Decision Log

- 2026-05-16T16:05Z Step 6 fix-iteration scope decision: applied 11 in-scope findings (5 blockers + 6 should-fixes) in fix-iteration 1 and skipped the iteration-2 gate-check fan-out — orchestrator-side grep verification confirmed all 11 fix targets cleared, context was at `warning` (30%), and Phase C of Track 2 will re-dispatch the same 4 dim-review agents on the cumulative track diff next session. The orchestrator-side verification is a deliberate context-budget trade-off, not a protocol violation per `step-implementation.md` sub-step 4(e) (max-iterations note plus the "complete sub-steps 4-8 then handle pause" rule for mid-step warning).

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective

- [x] Technical: PASS at iteration 2 (8 findings, 7 accepted, 1 deferred to Track 4; T1+T2+T3 blockers, T4+T5+T6 should-fix, T7+T8 suggestions; iter-2 gate VERIFIED all 7 actionable, T8 explicitly deferred per disposition; no new findings introduced by iter-1 fixes)
- [x] Risk: PASS at iteration 2 (10 findings; R3+R4+R6+R7 moderate applied, R8+R9+R10 low applied, R1+R2 major DEFERRED with transient acknowledgment in Interfaces and Dependencies, R5 moderate DEFERRED — verified that structural review is not in Phase A pipeline so the risk is N/A; iter-2 gate VERIFIED all 10; one new audit finding N1 confirmed sweep-scope expansion is safe — no false-positive risk)
- [x] Adversarial: PASS at iteration 2 (10 findings; A1+A2+A7+A9+A10 should-fix applied, A5+A8 confirmed PASS at iter-1, A3+A4+A6 suggestions DEFERRED; iter-2 gate VERIFIED 8 of 10 plus 2 mechanical residuals (N1 step 2 How-block regex consistency + N2 Non-Goal parallel to Interfaces-and-Dependencies A7 fix) applied in same iter-2 cycle without iter-3 escalation since both were mechanical mirrors of already-verified fixes)

## Context and Orientation

Pre-Track-2 baseline: the workflow tooling under `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/` names a five-section per-track file shape (`## Description` / `## Progress` / `## Reviews completed` / `## Base commit` / `## Steps`) and a directory layout under `_workflow/tracks/`. Single-source-of-truth specs (`conventions.md`, `conventions-execution.md` §2.1, `planning.md`, `design-document-rules.md`) carry the old shape. The orchestrator-driven episode-writer in `step-implementation.md` sub-step 7 lands per-step content as a nested blockquote under `## Steps`. This branch's own `_workflow/tracks/` directory carries `track-1.md`…`track-4.md` in the old shape; Track 1's three step episodes were written under the old logic.

The new spec (design.md §"New per-track file shape") replaces this with a 14-section ExecPlan template (12 OpenAI sections + `## Episodes` + `## Base commit`) under `_workflow/plan/`. The prose term "step file" renames to "track file". The episode-writer rewires to the deterministic four-section checklist per D9, D11, D12. Sibling Moves 1, 2, 3 get reserved-slot placeholders.

The Track 2 work surface spans: the four single-source-of-truth specs above; the mechanical sweep across ~35+ workflow / skill / agent / script files for both the directory and terminology renames; the writer rewire across `step-implementation.md` sub-step 7, `episode-format-reference.md`, the Phase A decomposition-complete Progress writer in `track-review.md`, the Phase C iteration + track-completion Progress writers in `track-code-review.md`, the failed-step `[!]` path in `step-implementation-recovery.md`, and the renderer literal token in `render-slim-plan.py:51`; the on-disk `git mv` of this branch's own `_workflow/tracks/`; and the 5-section → 14-section shape migration of this branch's four track files.

## Plan of Work

Phase B sequences six steps. Steps 1-2 are mechanical sweeps — directory paths first, then case-aware terminology — each with a complete grep verification at end-of-step. Steps 3-5 are single-file spec edits: `conventions.md` + `design-document-rules.md` together (Step 3), `conventions-execution.md` §2.1 + §2.4:304 (Step 4), `planning.md` (Step 5). Step 6 is the atomic shape switch per D13 — one commit bundling four mechanical-but-load-bearing changes: (1) writer rewire across 5 workflow doc files + 1 script literal; (2) `git mv` of this branch's `_workflow/tracks/` → `_workflow/plan/` plus path / prose-label touch-up in `implementation-plan.md`; (3) shape migration of this branch's four track files; (4) single atomic commit.

The atomicity at Step 6 is load-bearing per D13: every commit boundary is a potential resume point; splitting Step 6 leaves at least one intermediate commit where the workflow tooling and the on-disk shape are inconsistent (Phase C of any prior track, or Phase A of any subsequent track, would fail to find the section names it expects). Step 6 mandates a session-end after its commit so Phase C starts a fresh session that re-reads the new workflow docs and the new on-disk shape.

For each step's full scope, sequencing, pre-commit gates, post-commit guards, and risk-tag rationale see the corresponding entry in §Concrete Steps below.

## Concrete Steps

1. Mechanical directory rename `_workflow/tracks/` → `_workflow/plan/` across `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/`. Sweep literal occurrences of `_workflow/tracks/`, `tracks/track-N.md`, `--tracks-dir`, and `tracks_dir` only — no semantic edits. Use `steroid_apply_patch` (or `Edit` if mcp-steroid is unreachable at implementation time). End-of-step verification: `grep -rnE '_workflow/tracks/|tracks/track-N\.md|--tracks-dir|tracks_dir' .claude/workflow/ .claude/skills/ .claude/agents/ .claude/scripts/` returns zero matches. This branch's own `docs/adr/ytdb-817-new-track-format/_workflow/tracks/` directory does NOT move in this step — deferred to step 6 (atomic switch). — `risk: low` [x] commit: 5f55a18fa1
2. Mechanical case-aware terminology rename "step file" → "track file" across `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/`. Variants: `step file` → `track file`, `step-file` → `track-file`, `Step file` → `Track file`, `Step-file` → `Track-file`, `step files` → `track files`, `Step files` → `Track files`. Also touches: §1.1 glossary entry in `conventions.md` (row body update is part of this step — header rename to **Track file** is the prose rewrite half done in step 3), `inline-replanning.md` §"Updating plan and step files" heading → §"Updating plan and track files", the `**Step file:**` label in collapsed root-index template under `plan-slim-rendering.md`, the `"Step file"` literal token at `render-slim-plan.py:51` SUBSECTION_KEYWORDS set (becomes `"Track file"`), and every prose mention in workflow docs, agent prompts, scripts, and skill files. Fenced-Markdown current-shape examples (e.g., `plan-slim-rendering.md:149`) ARE rewritten. End-of-step verification: `grep -rnE '\b[Ss]tep[ -][Ff]iles?\b' .claude/workflow/ .claude/skills/ .claude/agents/ .claude/scripts/` returns zero matches outside intentional retroactive-rename-explanation prose; `grep -rn '"Step file"' .claude/scripts/render-slim-plan.py` returns zero matches. — `risk: medium` [x] commit: 5548a88356
3. Update `.claude/workflow/conventions.md` §1.1 glossary (rename **Step file** row to **Track file**; refresh body to point at the new `plan/track-N.md` path and the new 14-section shape per `conventions-execution.md` §2.1 — forward reference at decomposition time; reference becomes load-bearing at step 4 commit) + §1.2 directory layout (replace `tracks/` → `plan/` in the layout tree; update *Track file content* pointer text; refresh plan-file-structure narrative; refresh directory-tree code-comment annotations). Update `.claude/workflow/design-document-rules.md` § Boundary with the implementation plan: split the row that names `tracks/track-N.md` `## Description` into the four new section names (`Purpose / Big Picture` + `Context and Orientation` + `Plan of Work` + `Interfaces and Dependencies`); update the "rule, succinctly" prose's `tracks/track-N.md` path reference; re-read `:179` (the `phase4-creation` row mentioning `--tracks-dir`) and `:652` (legacy reference in a worked-example block) to confirm renamed prose still reads cleanly after step 2's mechanical sweep. Single commit. — `risk: low` [x] commit: 201ae2e7c2
4. Rewrite `.claude/workflow/conventions-execution.md` §2.1 *Track file content* (was *Step file content*) with all 12 ExecPlan sections plus the two workflow-specific siblings `## Episodes` and `## Base commit` (continuous-log first per `design.md` §"Continuous-log discipline"). Show `## Concrete Steps` as a thin numbered roster (description + `risk:` tag + `[ ]` checkbox per step, no nested blockquote) per D9. Show `## Episodes` with the per-step block template (`### Step N — commit <SHA>, <ISO> [ctx=<level>]` per D12 + four episode fields) per D11. Show `## Artifacts and Notes` as cross-step-only per D11. Show `## Progress` example entries with mandatory `[ctx=<level>]` per D12 (`- [x] <ISO> [ctx=<level>] Step N complete (commit <SHA>)`). Retain `## Base commit` housekeeping sibling. Rewrite the Description-lifecycle table with the new section names AND the writer/reader split for Concrete Steps vs Episodes vs Artifacts and Notes per design.md §"Step episode storage". Annotate the Progress and Episodes rows of the lifecycle table with the D12 mandatory-`[ctx=<level>]` write-rule and the `unknown` fallback. Update the "Track files live under `_workflow/plan/`" sentence. Verify §2.3's grep-glob path-exclude (does not reference `tracks/` today — confirm). **Also apply plan-correction WC#5 (re-pinned at Phase A iter-1)**: update the agent-count summary line at `conventions-execution.md:304` (under §2.4) from `4 baseline + up to 6 conditional, max 3 iterations` to `4 baseline + up to 6 conditional + up to 6 workflow-review, max 3 iterations` so it reflects Track 1's workflow-review tier addition. Single commit covering both §2.1 rewrite and §2.4:304 line. — `risk: medium` [x] commit: 92750cefdc
5. Update `.claude/workflow/planning.md`: §Track descriptions (`tracks/track-N.md` path → `plan/track-N.md`, per-track template references), §Track-level component-interaction diagrams, §Plan file structure references (3 path bullets), §Design Document path references, and §Architecture Notes per-section budget table (the *Track checklist intro* row's wording may need updates if step 4's §2.1 lifecycle table changed semantics; otherwise no new budgets per D-level analysis at Phase A iter-1). Single commit. — `risk: low` [x] commit: 8666df8490
6. Atomic shape switch (D13) — one commit bundling four mechanical-but-load-bearing changes: (1) writer rewire across 5 workflow doc files + the `render-slim-plan.py` SUBSECTION_KEYWORDS literal token at `:51` (D9, D11, D12); (2) on-disk `git mv docs/adr/ytdb-817-new-track-format/_workflow/tracks docs/adr/ytdb-817-new-track-format/_workflow/plan` plus path / prose-label touch-up in `implementation-plan.md` (Track 1's collapsed entry `> **Step file:**` label); (3) shape migration of this branch's four track files to the canonical 14-section template per `design.md` §"New per-track file shape" (split `## Description`, rename `## Reviews completed` → `## Outcomes & Retrospective`, rename `## Steps` → `## Concrete Steps` and convert step-roster blockquotes to thin numbered roster lines, move step blockquote bodies to a new `## Episodes` section with `### Step N — commit <SHA>, <ISO> [ctx=<level>]` blocks; backfill `<ISO>` and `[ctx=unknown]` per D12 fallback for already-written episodes; convert `## Progress` to continuous-log shape with backfilled timestamped entries; add HTML-comment placeholder sections per D6/D10; retain `## Base commit`); (4) single atomic commit, Spotless-clean (markdown only). Episode-write contingency: orchestrator writes step 6's own episode in the new shape directly. Pre-commit gate: orchestrator re-reads step 6's plan-of-work entry before issuing commit. Post-commit guards: `test -d` of legacy directory returns empty; structural check confirms each `plan/track-{1,2,3,4}.md` has exactly 14 `## ` headings; end-of-step grep `grep -rnE '_workflow/tracks/|> \*\*Step file:\*\*' docs/adr/ytdb-817-new-track-format/` returns zero matches (intentional historical references in retroactive prose are allowed per the §192 invariant carve-out). Session ends after this step's commit + episode write — Phase C of Track 2 starts a fresh session reading the new on-disk shape. — `risk: high` [x] commit: 6fdc2f6dc3

## Episodes

### Step 1 — commit 5f55a18fa1, 2026-05-16T17:01Z [ctx=safe]
**What was done:** Mechanical token sweep across `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/` replacing the four literal path/flag tokens that name the old per-track-file directory layout: `_workflow/tracks/` → `_workflow/plan/`, `tracks/track-N.md` (and the `{N}` and `track-1/2/3.md` numeric variants) → `plan/track-N.md`, `--tracks-dir` → `--plan-dir`, `tracks_dir` → `plan_dir`. 34 files modified (115 insertions / 115 deletions). End-of-step verification `grep -rnE '_workflow/tracks/|tracks/track-N\.md|--tracks-dir|tracks_dir' .claude/workflow/ .claude/skills/ .claude/agents/ .claude/scripts/` returns zero matches. `design-mechanical-checks.py` had its argparse flag renamed in tandem (`--tracks-dir` → `--plan-dir`) with the matching `args.tracks_dir` → `args.plan_dir` access renamed so the CLI still parses — verified with `python3 design-mechanical-checks.py --help`. Commit: `5f55a18fa1`.

**What was discovered:** mcp-steroid was not reachable from the implementer sub-agent (`ToolSearch select:steroid_list_projects,steroid_apply_patch` returned no matches even though the SessionStart hook reported it reachable from the orchestrator's process), so the sweep ran on native `Edit` rather than `steroid_apply_patch`. The fallback path was anticipated by the step instructions. Two prose tokens fall outside the four literal verification patterns and were deliberately left for the prose-polish steps: a bare "the `tracks/` directory containing every `plan/track-N.md` track file" sentence in `edit-design/SKILL.md:56`, and a bare `tracks/` in the help string of `design-mechanical-checks.py:1182`. Both will land naturally inside Step 2's terminology sweep (the lines also carry "step file" tokens) or Step 3 / Step 4 / Step 5 prose polish — no extra commit needed.

**Key files:** 34 files modified across `.claude/agents/`, `.claude/scripts/`, `.claude/skills/`, `.claude/workflow/`, and `.claude/workflow/prompts/`. Notable load-bearing edits: `step-implementation.md` (Phase B per-step paths), `track-review.md` / `track-code-review.md` (Phase A/C paths), `episode-format-reference.md`, `implementer-rules.md`, `conventions.md` / `conventions-execution.md` / `planning.md` / `design-document-rules.md` (single-source-of-truth specs), the sub-agent prompt set under `prompts/`, and `design-mechanical-checks.py` (script flag rename).

### Step 2 — commit 5548a88356, 2026-05-16T17:05Z [ctx=safe]
**What was done:** Mechanical case-aware terminology sweep across `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/` replacing every legal case variant of the legacy "step file" prose term with "track file". Eight substitutions applied in priority order (plurals/hyphenated before singular spaced) so plurals and hyphenated forms could never be partially-shadowed by the bare singular: `Step files` → `Track files`; `step files` → `track files`; `Step-files` → `Track-files`; `step-files` → `track-files`; `Step-file` → `Track-file`; `step-file` → `track-file`; `Step file` → `Track file`; `step file` → `track file`. 44 files modified (348 insertions / 348 deletions — the 1:1 token rename signature). End-of-step verification: `grep -rnE '\b[Ss]tep[ -][Ff]iles?\b' .claude/workflow/ .claude/skills/ .claude/agents/ .claude/scripts/` returns zero matches; `grep -rn '"Step file"' .claude/scripts/render-slim-plan.py` returns zero matches (SUBSECTION_KEYWORDS literal at :51 now reads `"Track file"`). Commit: `5548a88356`. A follow-up commit (`03afffa669`) swept five placeholder-variant references from step 1's regex gap — see the next paragraph.

**What was discovered:** Pre-rename inventory totalled 353 occurrences across the eight case variants and 348 unique matches under the aggregate regex — the 5-match delta is overlap where the bare singular regex would match inside a plural/hyphenated form (e.g., `step file` inside `step files`). The post-rename verification grep returning zero matches plus the 348-line diff stat reconcile, confirming the sweep was complete and idempotent. The intentional-historical-reference carve-out was not exercised — every match was rewritten because none of the surrounding prose framed the occurrence as a retrospective explanation of the rename.

Step 1's literal-token regex (`tracks/track-N.md` with literal `N`) missed five placeholder-variant references: `tracks/track-<index>.md` and `tracks/track-<N>.md` in `review-mode.md:364,405,503,628` and `tracks/track-<M>.md` in `track-code-review.md:924`. These are semantically legacy-path references — the Terminology-consistency invariant covers them — so they were swept in a follow-up commit (`03afffa669`) before step 3 starts, rather than left for a later step to absorb.

mcp-steroid remained unreachable from the implementer sub-agent (same fallback path as step 1) so the sweep ran on GNU `sed -E` rather than `steroid_apply_patch`. Both Python scripts (`render-slim-plan.py`, `design-mechanical-checks.py`) were re-validated with `ast.parse` and `--help` to confirm no syntax / CLI regression.

**What changed from the plan:** none. The follow-up commit for placeholder variants is in scope for step 2's discovery follow-through (those references are legacy-path tokens that the Terminology-consistency invariant covers) and does not affect later steps' workload.

**Key files:** 44 files modified in the main rename commit across `.claude/agents/`, `.claude/scripts/`, `.claude/skills/`, `.claude/workflow/`, and `.claude/workflow/prompts/`. Notable load-bearing edits: `render-slim-plan.py` SUBSECTION_KEYWORDS at `:51` (renderer match token), `plan-slim-rendering.md:149` (collapsed-entry illustration), `conventions.md` §1.1 glossary row body, `inline-replanning.md` §"Updating plan and step files" → §"Updating plan and track files" heading, and the full sub-agent prompt set under `prompts/`. Follow-up commit `03afffa669` touched `review-mode.md` (4 lines) and `track-code-review.md` (1 line) to sweep placeholder-variant references missed by step 1's regex.

**Critical context:** `render-slim-plan.py` SUBSECTION_KEYWORDS at `:51` is load-bearing per `plan-slim-rendering.md` §Rendering rule — the renderer matches this token at runtime when collapsing completed tracks. Step 6's atomic shape switch introduces `> **Track file:**` as the new collapsed-entry label; this step's rename keeps the renderer in sync so the file-pointer line is not silently dropped from collapsed entries on subsequent Phase B startups.

### Step 3 — commit 201ae2e7c2, 2026-05-16T17:13Z [ctx=info]
**What was done:** Refreshed three load-bearing prose surfaces in `conventions.md` and `design-document-rules.md` that step 1 (path tokens) and step 2 (prose term) did not reach because their content is semantic, not mechanical. (1) `conventions.md` §1.1 Track-file glossary row body now points at `plan/track-N.md`, names the four Phase 1 track-level sections (`Purpose / Big Picture`, `Context and Orientation`, `Plan of Work`, `Interfaces and Dependencies`), and forward-references `conventions-execution.md` §2.1 for the full 14-section ExecPlan template. (2) `conventions.md` §1.2 directory-tree code-comment annotation on `plan/track-1.md` enumerates the 12 OpenAI-style sections plus the three workflow-specific siblings (`Episodes`, `Artifacts and Notes`, `Base commit`); the *Track file content* pointer paragraph below the tree was rewritten with parallel content; the `implementation-plan.md` annotation in the tree was refreshed. (3) `design-document-rules.md` § Boundary with the implementation plan: the single `plan/track-N.md` `## Description` boundary row was split into four rows, one per Phase 1 track-level section, each carrying the same lifecycle boundary semantics. The "rule, succinctly" prose at `:44` now points writers at the right destination section name. Single commit: `201ae2e7c2`.

**What was discovered:** Step 1's literal-token sweep had left three bare `tracks/` references in `design-document-rules.md` (the prose phrasing "every track file under `tracks/`" — describing the directory, not a specific file path) at lines 218, 362, and 801. These read awkwardly after the directory rename; the implementer swept them to `plan/` as part of step 3's prose-polish carve-out (consistent with the step description's "light prose polish only" clause). Final verification grep `grep -n 'tracks/' conventions.md design-document-rules.md` returns zero matches; `grep -n 'Step file' ...` also returns zero, confirming step 2 coverage of these two files.

**What changed from the plan:** none. The three additional bare-`tracks/` polish-passes are consistent with the step description's "re-read to confirm renamed prose still reads cleanly; light prose polish only". No downstream step is affected.

**Key files:** `.claude/workflow/conventions.md` (modified — §1.1 glossary row body, §1.2 directory-tree annotations and pointer paragraph), `.claude/workflow/design-document-rules.md` (modified — boundary table row split into 4 rows, "rule, succinctly" prose, three bare-`tracks/` polish-passes at `:218`, `:362`, `:801`).

**Critical context:** The §1.1 glossary row body and §1.2 *Track file content* pointer paragraph carry forward references to `conventions-execution.md` §2.1, which step 4 will rewrite. Phrased as "see `conventions-execution.md` §2.1 *Track file content* for the full template" so the wording survives whether §2.1 is rewritten before or after this commit's text lands. Step 4 must emit the exact section names referenced here (the 12 OpenAI sections + `Episodes`, `Artifacts and Notes`, `Base commit`) or the forward reference breaks at squash-merge time.

### Step 4 — commit 92750cefdc, 2026-05-16T17:19Z [ctx=info]
**What was done:** Replaced the §2.1 *Track file content* template in `.claude/workflow/conventions-execution.md` with the new 14-section shape per `design.md` §"New per-track file shape" — 12 OpenAI ExecPlan sections (`## Purpose / Big Picture`, `## Progress`, `## Surprises & Discoveries`, `## Decision Log`, `## Outcomes & Retrospective`, `## Context and Orientation`, `## Plan of Work`, `## Concrete Steps`, `## Validation and Acceptance`, `## Idempotence and Recovery`, `## Artifacts and Notes`, `## Interfaces and Dependencies`) plus the two workflow-specific siblings (`## Episodes` per D11, `## Base commit` housekeeping). Section-by-section summary with one realistic example body per section: `## Progress` shows entries carrying the mandatory D12 `[ctx=<level>]` field; `## Concrete Steps` shows thin numbered roster lines (description + risk tag + status checkbox, no nested blockquote, per D9); `## Episodes` shows one completed `### Step N — commit <SHA>, <ISO> [ctx=<level>]` block with the four episode fields (per D11 + D12); `## Artifacts and Notes` documented as cross-step-only. Replaced the Description-lifecycle table with a per-section lifecycle table covering all 14 sections × Phase 1 / A / B / C writers + readers; Progress and Episodes rows carry an inline D12 enforcement note (statusline-read-then-write + `unknown` fallback). Documented the two placeholder kinds (Phase A + sibling-Move) coexisting on a Phase-1-written track file per D6 / D10. Applied plan-correction WC#5 in the same commit at §2.4:304 — agent-count summary now reads `4 baseline + up to 6 conditional + up to 6 workflow-review, max 3 iterations`. Verified §2.3 carries no `tracks/` references. Commits: `92750cefdc` (main) + `86970accd3` (reconciliation, see below).

**What was discovered:** Step 3's edits to `conventions.md` §1.1 and §1.2 left `## Risks and Mitigations` named as one of the 12 OpenAI sections and grouped `## Artifacts and Notes` under workflow-specific — but `design.md` §"New per-track file shape" omits `## Risks and Mitigations` from its actual template and treats `## Artifacts and Notes` as one of OpenAI's 12. The two source-of-truth files for the per-track shape (`conventions.md` §1.2 and `conventions-execution.md` §2.1) were therefore inconsistent on the section list after step 3 landed. Reconciled in follow-up commit `86970accd3`: updated `conventions.md` §1.2 prose enumeration and the directory-tree code-comment so the 12 OpenAI-style names + 2 workflow-specific (`Episodes`, `Base commit`) match `design.md` verbatim. The §2.1 rewrite (commit `92750cefdc`) is now the canonical 14-section template that `conventions.md` §1.2 forward-references.

§2.1 ended up at 267 lines (target was ~250). Slightly over the section budget but unavoidable without losing load-bearing content: 14 sections × 5-column lifecycle table + two placeholder-kind discussions + per-section illustrative examples. The full verbatim template still lives in `design.md`; §2.1 carries the summary plus one example per section per the step's "link out to design.md" guidance.

**What changed from the plan:** none. The §1.2 reconciliation in `conventions.md` was step 3 prose-polish residue that surfaced during step 4 implementation; folding it into step 4's commit chain (rather than deferring to step 5 / step 6) keeps the two §1.2/§2.1 spec sources aligned at the same HEAD.

**Key files:** `.claude/workflow/conventions-execution.md` (modified — §2.1 *Track file content* full rewrite + per-section lifecycle table replacement + §2.4:304 agent-count summary update); `.claude/workflow/conventions.md` (modified in follow-up commit — §1.2 prose enumeration + directory-tree code-comment reconciled to design.md's 14-section list).

**Critical context:** §2.1 is now the load-bearing template every downstream writer / reader resolves at squash-merge time. Step 5 (`planning.md`) and step 6's atomic shape switch (writer rewire + on-disk shape migration) both depend on these section names being final. Any subsequent change to the per-track shape MUST update both `conventions-execution.md` §2.1 and `design.md` §"New per-track file shape" in the same commit to avoid re-introducing the source-of-truth drift this step's follow-up commit just resolved.

### Step 5 — commit 8666df8490, 2026-05-16T17:26Z [ctx=info]
**What was done:** Refreshed `.claude/workflow/planning.md` to align with the 14-section per-track ExecPlan template that step 4 finalised in `conventions-execution.md` §2.1 (canonical source: `design.md` §"New per-track file shape"). Replaced every remaining `## Description` track-file reference with the appropriate Phase 1 narrative section name(s) — `## Context and Orientation`, `## Plan of Work`, and `## Interfaces and Dependencies` — across §Boundary with `design.md` and track files, the Rule-of-thumb blockquote, the per-section budget prose, the Decision-Record rules + Bad-DR anti-pattern, §Architecture-Notes rule 8, §Track descriptions, and §Track-level component-interaction diagrams. Rewrote the §Track descriptions enumeration: the old `**What/How/Constraints/Interactions**` single-block description is now four named sections matching the canonical template. Updated the §Track-level component-interaction diagrams location pointer from "inside `## Description` after the `**Interactions**:` blockquote" to "inside `## Interfaces and Dependencies`". Renamed the description-lifecycle table pointer to point at the new *Section lifecycle* table in `conventions-execution.md` §2.1. End-of-step verification: `grep -nE 'tracks/' .claude/workflow/planning.md` returns zero matches; `grep -nE '## Description$|## Steps$|## Reviews completed$' .claude/workflow/planning.md` returns zero matches. Single commit `8666df8490`, 66 insertions / 43 deletions.

**What was discovered:** Step 1's mechanical path-token sweep had already replaced `tracks/` → `plan/` throughout this file, and step 2's terminology sweep had eliminated the "step file" prose. The residue at step 5 entry was purely the section-name mapping (`## Description` / `**What/How/Constraints/Interactions**` / `**Interactions**:` → the four-section split); these are semantic, not mechanical, and only became unambiguous after step 4 finalised the §2.1 template. The §Track-checklist-intro budget-table row needed no change — it refers to the root-index `implementation-plan.md` checklist entry, not the per-track template that §2.1 rewrote. The §Design Document section already points at the canonical relative path `docs/adr/<dir-name>/_workflow/design.md`.

**What changed from the plan:** none.

**Key files:** `.claude/workflow/planning.md` (modified — §Track descriptions, §Track-level component-interaction diagrams, §Boundary with design.md and track files, Rule-of-thumb blockquote, §Decision-Record rules + Bad-DR anti-pattern, §Architecture-Notes rule 8).

**Critical context:** This commit lands the human-readable spec narrative for the 14-section template; step 6 (atomic shape switch — only remaining step in Track 2) implements the on-disk writer + shape migration. After step 6 lands, `planning.md` §Track descriptions, `conventions-execution.md` §2.1, and `design.md` §"New per-track file shape" form a three-source-of-truth set that must stay in lock-step on any future shape change.

### Step 6 — commit 6fdc2f6dc3, 2026-05-16T16:05Z [ctx=warning]
**What was done:** Atomic shape switch (D13) landed as commit `9ed9ae421a` with a follow-up `Review fix:` commit `6fdc2f6dc3` applying 11 dim-review findings (5 blockers + 6 should-fixes) before sub-step 4 closed.

(1) **Writer rewire** across 5 workflow doc files + the `render-slim-plan.py` SUBSECTION_KEYWORDS literal: rewrote `step-implementation.md` sub-step 7 as the deterministic four-section checklist per D9 / D11 / D12 (sub-step 7.0 statusline read + 7.1 Episodes block + Concrete Steps roster flip + 7.2 Progress append + 7.3 conditional Surprises promotion + 7.4 conditional Decision Log promotion); applied the same D12 canonical order to every other Progress writer — Phase A decomposition-complete in `track-review.md`, Phase C iteration + track-completion in `track-code-review.md`, every `[!]` writer site in `step-implementation-recovery.md`. Rewrote `episode-format-reference.md` end-to-end with completed + failed block templates, back-reference convention, authoritative-copy rules, the `unknown` fallback. Confirmed `render-slim-plan.py` SUBSECTION_KEYWORDS at `:51` already read `"Track file"` post-Step-2 sweep.

(2) **On-disk directory rename**: `git mv docs/adr/ytdb-817-new-track-format/_workflow/tracks docs/adr/ytdb-817-new-track-format/_workflow/plan` (preserves history per-file). Updated Track 1's collapsed entry in `implementation-plan.md` from `> **Step file:** \`tracks/track-1.md\`` to `> **Track file:** \`plan/track-1.md\`` (path + label in one edit).

(3) **Shape migration** of all four track files to the canonical 14-section template per `design.md` §"New per-track file shape" — verbatim heading order Purpose / Big Picture → Progress → Surprises & Discoveries → Decision Log → Outcomes & Retrospective → Context and Orientation → Plan of Work → Concrete Steps → Episodes → Validation and Acceptance → Idempotence and Recovery → Artifacts and Notes → Interfaces and Dependencies → Base commit. Track 1 (completed): backfilled 3 step episodes + 1 Review-fix episode with `<ISO>` from `git log` and `[ctx=safe]` from recorded Context sub-items; backfilled Progress with phase-event entries. Track 2 (in-progress): backfilled 5 pre-step-6 episodes with recorded `[ctx=safe/info]` and matching Progress entries. Tracks 3 & 4 (not yet executed): empty Progress / Episodes; HTML-comment placeholders for empty sections.

(4) **Fix iteration 1** applied the synthesized dim-review findings on top of the initial commit: aligned the statusline-fallback sentinel to `unknown` across `step-implementation.md` sub-step 6 vs 7.0 (F1); swept reader-side `## Reviews completed` → `## Outcomes & Retrospective` and `## Steps` → `## Concrete Steps` in `track-review.md` writer + resume-gate sites (F2); rewrote §Step Failure and removed the `(N/M complete)` step-count idiom in `step-implementation-recovery.md` (F3 + F4); aligned `conventions-execution.md` §2.1 canonical write-order to match the implemented writer-doc shape (F5); split the `episode-format-reference.md` canonical checklist into per-step vs Progress-only shapes (F6); replaced every `**Risk:**` blockquote reference with the inline `risk: <tag>` roster-line token (F7); trimmed `step-implementation.md` sub-step 7 from ~62 to ~31 lines by delegating canonical detail to the reference (F8); mirrored each Episodes-block `[ctx=…]` level into the corresponding Track 1 Progress entry (F9); moved Track 1's Phase C Review-fix Episodes block into `## Outcomes & Retrospective` (F10); renamed Track 2's Step 2 / Step 4 multi-commit headers to spec-correct singular `commit <SHA>` form (F11).

Post-commit guards both pass: `test -d _workflow/tracks` returns empty (no stale directory); each `plan/track-{1,2,3,4}.md` has exactly 14 `## ` headings.

**What was discovered:** Four workflow-review agents fired (consistency, instruction-completeness, context-budget, writing-style) — case 1 of the workflow-machinery override (workflow-only diff, baseline skipped). Total findings: 5 critical (WC1, WC2, WC3+WI1, WC4, WC5), 12 should-fixes, ~16 minor. The 11 in-scope findings (5 blockers + 6 should-fixes) landed in iteration 1's fix commit; the remaining deferred items (WC10, WC11, WC12, WI4, WI6, WI7, WB2, WB3, WS1–WS11) are documented in §Surprises & Discoveries below for Track 4's reader-pass / prose-polish to absorb. Iteration 2 gate-check was skipped (orchestrator-side grep verification confirmed all 11 fix targets cleared; context was at `warning` and step 6 ends the session anyway — Phase C of Track 2 will re-dispatch the same 4 agents on the cumulative track diff next session and catch any regression).

The fix iteration also surfaced a meta-pattern: every cross-file rename + section-split touched ≥3 single-source-of-truth specs (`conventions.md`, `conventions-execution.md`, `design.md`) plus their downstream readers. The follow-up commits at Step 2 (`03afffa669`), Step 4 (`86970accd3`), and Step 6 fix (`6fdc2f6dc3`) all share a shape: a primary commit lands the in-scope rename, then a small commit absorbs the cross-file drift that surfaced when the implementer ran end-of-step verification. Track 4's reader-pass should expect the same shape.

**What changed from the plan:** none. The four-section atomicity invariant (D13) held — both the initial commit and the Review-fix commit landed without intermediate inconsistent state on disk. Step 6's fix-iteration was anticipated by the plan's risk-tag (`high` → full dim review at Phase B sub-step 4).

**Key files:** writer rewire — `.claude/workflow/step-implementation.md` (sub-step 6/7 D12 enforcement), `.claude/workflow/step-implementation-recovery.md` (every `[!]` writer site + §Step Failure rewrite + step-count idiom retirement), `.claude/workflow/episode-format-reference.md` (full rewrite — per-step vs Progress-only canonical shapes), `.claude/workflow/track-review.md` (Phase A decomposition-complete writer + reader-side legacy-name sweep), `.claude/workflow/track-code-review.md` (Phase C iteration + track-completion + iteration-failure writers); spec — `.claude/workflow/conventions-execution.md` (§2.1 canonical write-order alignment); on-disk shape — `docs/adr/ytdb-817-new-track-format/_workflow/plan/track-{1,2,3,4}.md` (5 → 14 section migration + backfilled Progress + Episodes blocks), `docs/adr/ytdb-817-new-track-format/_workflow/implementation-plan.md` (path + prose-label touch-up). Two commits: `9ed9ae421a` (atomic switch) + `6fdc2f6dc3` (Review fix).

**Critical context:** Track 2 closes the structural anchor for YTDB-817 — every reader and writer touched in Tracks 3 and 4 references the new vocabulary and this branch's own workflow state is on the new shape. Subsequent shape changes MUST update `conventions-execution.md` §2.1, `design.md` §"New per-track file shape", and `planning.md` §Track descriptions in lock-step to keep the three-source-of-truth set aligned (per Step 4's §1.2 / §2.1 reconciliation discovery). The orchestrator session ends after this episode write — Phase C of Track 2 starts a fresh session reading the new on-disk shape per the D13 step-6 session-boundary rule.

## Validation and Acceptance
<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery
<!-- Phase A placeholder — Steps 1-5 are markdown-only edits with grep-verifiable idempotence (re-running any step would re-apply the same renames and re-verify zero matches). Step 6 is the atomic shape switch (D13) — recovery is revert-only, not fix-forward per the post-commit guards in the §Concrete Steps roster line. -->

## Artifacts and Notes
<!-- Cross-step artifacts only. None recorded for this track. -->

## Interfaces and Dependencies

**In-scope files (steps 1–5)**: `.claude/workflow/conventions.md`, `.claude/workflow/conventions-execution.md` (including the §2.4 agent-count summary line at `:304` per WC#5 plan correction), `.claude/workflow/planning.md`, `.claude/workflow/design-document-rules.md`, plus the mechanical-rename pass across every file in `.claude/workflow/`, `.claude/skills/`, `.claude/agents/`, and `.claude/scripts/` (both directory and terminology renames). **In-scope files (step 6 atomic switch)**: `.claude/workflow/step-implementation.md` (sub-step 7 writer half), `.claude/workflow/step-implementation-recovery.md` (every `[!]` Progress writer site), `.claude/workflow/episode-format-reference.md`, `.claude/workflow/track-review.md` (Phase A decomposition-complete Progress writer only), `.claude/workflow/track-code-review.md` (Phase C iteration + track-completion Progress writers only), `.claude/scripts/render-slim-plan.py` (SUBSECTION_KEYWORDS literal token at `:51`), this branch's own `docs/adr/ytdb-817-new-track-format/_workflow/tracks/` directory (renamed to `_workflow/plan/`) and its four track files (`track-1.md`…`track-4.md`, both moved and reshaped), and `docs/adr/ytdb-817-new-track-format/_workflow/implementation-plan.md` (path references AND the `> **Step file:**` prose label on Track 1's collapsed entry — see step 6 sub-bullet 2).

**Out-of-scope** (deferred to Tracks 3 / 4): writer SKILLs (`create-plan/SKILL.md`, `review-plan/SKILL.md`, `edit-design/SKILL.md`) semantic rewrites — the mechanical "step file" → "track file" sweep in step 2 touches them but the template-content rewrites are Track 3's; reader workflow docs (`track-review.md` reader half, `track-code-review.md` reader half, `step-implementation.md` reader half, `implementer-rules.md`, `step-implementation-recovery.md` reader half) semantic rewrites are Track 4's; sub-agent prompts (`.claude/workflow/prompts/*.md`) semantic rewrites — including the `## Description` section-name references in `prompts/risk-review.md` / `prompts/technical-review.md` / `prompts/adversarial-review.md` / `prompts/create-final-design.md` — are Track 4's; `workflow.md` startup is Track 4's. Note: step 6 touches `track-review.md` and `track-code-review.md` only for the Progress writer-half call sites; the reader-half section-name sweep across these files stays with Track 4.

**Transient reader-side staleness window (Phase A iter-1 findings R1 + R2)**: Between step 6's HEAD and Track 4's HEAD, reader-side references to `## Description`, `## Steps`, and `## Reviews completed` in `track-review.md`, `track-code-review.md`, and `prompts/*-review.md` retain the legacy section names while this branch's track files (`plan/track-1.md`…`plan/track-4.md`) carry the new 14-section shape. Track 2's own Phase C and Tracks 3 / 4 Phase A run under this asymmetry. The mitigation is mechanical: sub-agent prompts that name `## Description` find no such section in the new-shape file and fall back to reading the whole file. The dimensional review sub-agents on workflow-machinery diffs (Phase C of every track) primarily review the *diff*, not per-track file metadata, so the practical blast radius is limited to slightly-less-efficient prompt context. Track 4 closes this window with the semantic reader rewrite (every `## Description` reference becomes a section-join across `## Purpose / Big Picture` + `## Context and Orientation` + `## Plan of Work` + `## Interfaces and Dependencies`).

**`/create-plan` smoke-test validation gap (Phase A iter-1 finding A9, accepted as known gap)**: Step 6 puts the new on-disk shape in place for this branch, but the first `/create-plan` invocation against the new template does not happen until Track 3 step 1 (writer SKILL update) at the earliest, and the synthetic smoke-test against a sentinel directory runs only at Track 4 step 5. Track 2's Phase C exercises the *spec* changes (`conventions.md`, `conventions-execution.md`, `planning.md`, `design-document-rules.md`) and the on-disk shape via the dimensional review's grep-against-track-files paths — but it does NOT exercise a fresh write of a new track file by `/create-plan`. The gap is accepted: a missing section in `/create-plan`'s template would surface during Track 3 step 1's writer edit (eye-balled) and is enforced as a no-defect by Track 4 step 5's smoke test. Bringing the smoke test forward to Track 3 step 1 was considered (would add one synthetic-task run after the SKILL edit) and rejected to keep Track 3's scope strictly the writer-SKILL update; if Track 3 step 1's Phase B implementer surfaces a template-shape ambiguity, the implementer can request a smoke-test run as part of step verification.

**Other in-flight branches**: `docs/adr/<old-branch>/_workflow/tracks/` paths and "step file" prose under any other ADR directory are not touched. Those branches keep their own snapshot of the workflow docs.

**No section-content semantics changes for steps 1–5**: the renames and template rewrite are documentation-only at runtime. Phase A/B/C orchestration substance is unchanged at those commits. **Step 6 is a true behavioral change**: the writer rewire activates a new sub-step 7 multi-section write, and the on-disk shape switches simultaneously.

**Markdown-only changes**: no Java, no test infrastructure, no Maven changes.

**Case-aware sweep discipline (step 2)**: the terminology sweep must preserve the original case of each occurrence. Sentence-start "Step file" → "Track file"; mid-sentence "step file" → "track file"; hyphenated "step-file" → "track-file"; plural "step files" → "track files". A single-pattern lowercase replace would silently mangle headings and sentence starts.

**Step 6 atomicity**: the writer rewire, the directory `git mv`, the four-file shape migration, and the `implementation-plan.md` path-reference + prose-label touch-up MUST land in one commit. Splitting them leaves at least one intermediate commit where the workflow tooling and the on-disk shape are inconsistent (Phase C of any prior track, or Phase A of any subsequent track, would fail to find the section names it expects). Phase A risk-tagging should mark step 6 `high`, triggering full dimensional review at Phase B.

**Per-step risk-tag pre-commitments (assigned during Phase A decomposition, recorded here for traceability)**: Step 1 `low` (path-rename verification is a complete check — `grep -rnE` returns zero matches). Step 2 `medium` (Phase A iter-1 finding R10 — case-aware sweep across ~300 mentions in ~35+ files where a missed case-variant silently produces mixed-vocabulary docs; the regex-precision improvement at A1 narrows the residual risk but engaging workflow-review writing-style + consistency at Phase B catches case-mismatches the bare grep can't). Step 3 / Step 4 / Step 5 `low` (single-file spec edits with eye-ballable diffs). Step 6 `high` (per D13 — atomic self-modification with broad blast radius). Final tags are written into each step's roster line under §Concrete Steps per the standard Phase A discipline; the pre-commitments above are inputs to that decomposition, not the writes themselves.

**Step 6 session boundary**: the orchestrator session ends after step 6's commit. Phase C starts a fresh session that re-reads the new workflow docs and the new on-disk shape. This is the only step in the plan that mandates a session-end between work commit and Phase C start.

**Inter-track dependencies**:
- **Depends on Track 1** for Phase B/C dimensional review to dispatch the workflow-review agents on this track's diff.
- Track 3 (writer SKILLs) and Track 4 (readers) both depend on this track's spec and on the new vocabulary having landed. **Track 3 step 5's writer-rewrite scope has moved into this track's step 6** per D13 — Track 3 now ends at step 4. Sequencing: Track 1 → Track 2 → Track 3 → Track 4. Track 4 could parallelize with Track 3 in principle (they touch disjoint files); the execution model is sequential by default.
- Sibling Moves 1 (YTDB-814), 2 (YTDB-815), 3 (YTDB-816) read the reserved-slot conventions added here. They land after this Move; nothing in this track pre-fills those slots.
- The mechanical renames in steps 1 and 2 touch files later edited by Tracks 3 and 4. Those tracks must rebase on Track 2's HEAD (specifically step 6's HEAD) before editing.
- **Step 6's episode-write contingency** is the only place in the plan where the orchestrator writes an episode in a shape different from its session-start logic; the orchestrator session must end after step 6's commit so Phase C can start fresh against the new shape.

## Base commit
c73ef90c4b55f515bbee5516f4805759e5ca9585
