<!-- workflow-sha: 3c57672e9b12b504d5feb5134ca96be891b3ffbc -->
# Track 1: Style-machinery rework — disguise-rule removal, technical-writer voice, persona readers, Summary rename, BLUF hardening

## Purpose / Big Picture
After this track, YouTrackDB's design and plan documents are authored and reviewed under a readability-first style regime — staged now under `_workflow/` and promoted to the live tree only at Phase 4. The regime bundles five moves:

- the six disguise-only style rules are removed;
- the design and track writer adopts the technical-writer voice of the YouTrackDB internals book — the reader-praised teaching book whose voice rules this change borrows;
- the two cold review agents run on Sonnet as named reader personas;
- the per-section `TL;DR` block becomes a `### Summary` sub-heading;
- the opening sentence of each section (its BLUF lead) must stand as a self-contained plain claim, per YTDB-1163.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

The workflow's authored documents are governed by one declarative style file, `.claude/output-styles/house-style.md`, plus a handful of surfaces that name or mirror individual rules. Today that machinery carries a class of rules whose only job is to make text read as human-authored; those rules fight model training and add no comprehension value, yet the plan documents stay hard to read and expensive to review. This track stops fighting training and spends the freed effort on readability: it deletes the disguise-only rules at `house-style.md` and at every surface that names or mirrors them, and reworks the author and the two review readers around teaching and reading each document rather than disguising it. Every edit is staged under `_workflow/staged-workflow/.claude/**` and promotes to the live tree only at Phase 4, so the new rules do not take effect while the branch is open. Three of the central files sit outside the standard §1.7 staging surface — `house-style.md` and `house-conversation.md` under `.claude/output-styles/`, and root `CLAUDE.md` — so the track also extends §1.7 to reach them and bootstraps those three by hand for its own promotion (D12).

> **Scope:** ~21 files covering the `house-style.md` source of truth and its consumer surfaces, the five-agent review roster, the `dsc-ai-tell` mechanical checker with its test and fixture, the workflow prose that hard-codes the `TL;DR` shape, the §1.7 staging-surface extension (D12: `conventions.md`, `implementer-rules.md`, `create-final-design.md`), and the `create-plan` Step-4b consumer of the D2 hybrid shape (A4)
> **Complexity (predicted):** high — the Workflow-machinery HIGH trigger fires centrally on the whole planned work (house-style rules, review-agent model pins and personas, and the `dsc` mechanical checker with its tests)

## Progress
- [x] Review + decomposition
- [ ] Step implementation
- [ ] Track-level code review
- [ ] Track completion

**REPLANNED 2026-07-03 (inline replan, option 1).** The §1.7 staging-surface blocker (A1/A2) is resolved by extending §1.7 to `.claude/output-styles/**` and root `CLAUDE.md` (D12), folding the iter-1 should-fixes into the sections below. The phase ledger is reset to `phase=0`, so the next `/execute-tracks` session re-runs State 0 (plan review) against this revised track, then re-enters Phase A from iteration 1 — the superseded iter-1 reviews under `plan/track-1/reviews/` are re-run there.

- [x] 2026-07-03T10:24Z [ctx=info] Review + decomposition complete
- [x] 2026-07-03T11:03Z [ctx=safe] Step 1 complete (commit eae8c1c25c90c9998aa5f9ae42cb27a2f75fdd9c)
- [x] 2026-07-03T11:26Z [ctx=safe] Step 2 complete (commit a21861936ae902f775d9fbe64bb1b59959e33028)
- [x] 2026-07-03T11:48Z [ctx=safe] Step 3 complete (commit 6ddd58a305bac15e861ede2fd767d9d70493c18e)
- [x] 2026-07-03T12:09Z [ctx=safe] Step 4 complete (commit c1c54eef694947c66dd07ecca74ff9e949f880f4)

## Surprises & Discoveries
<!-- Continuous-log. Promoted by the orchestrator from per-step "What was
discovered" when the finding affects future steps or other tracks. Empty
at Phase 1. -->

- The `Write` tool refuses to create new `.md` files under `_workflow/` (it classifies them as report files and blocks them). Steps that create staged `.md` files must write them with a Bash heredoc instead. This affects step 3, which hand-mirrors the three out-of-surface files, two of them `.md` (`house-style.md`, `house-conversation.md`) plus root `CLAUDE.md` — the first copy-then-edit of each is a new file under the staged mirror. See Episodes §Step 1.
- After step 2, a staged `implementer-rules.md` exists whose write-routing rule and pre-commit gate already cover `.claude/output-styles/**` and root `CLAUDE.md`. Per §1.7(d) reads-precedence, a later implementer reading the rulebook resolves to this staged copy, so step 3's out-of-surface handling and the staged rulebook agree on the destination: writes go to `_workflow/staged-workflow/.claude/output-styles/**` and `_workflow/staged-workflow/CLAUDE.md`. Step 3's prompt still carries the explicit manual copy-then-edit-into-mirror instruction and the per-commit `git diff --cached --name-only -- .claude/output-styles CLAUDE.md` guard so the outcome is correct regardless of which rulebook the implementer reads. See Episodes §Step 2.
- Staged/promote-together dependency for step 4: the staged `comprehension-review.md` (step 3) describes the D2 hybrid output and forward-references `prompts/design-review.md § Output format` as the producer contract. Step 4 must land the `design-review.md` hybrid producer (item 13) and the `edit-design`/`create-plan` gate-return parse extensions (items 14/21) so the reference resolves before Phase-4 promotion. Both sides are staged and promote in the same commit. Resolved in step 4 (item 13 landed). See Episodes §Step 3.
- Phase-4 promotion note (tooling): `workflow-reindex.py --write --files <path>` operates on staged copies under `_workflow/staged-workflow/`, and it regenerates each TOC row's summary from that section's own HTML-comment `summary="…"` field (a hand-edited TOC row is overwritten). A changed summary must be edited in the section comment, and the summary is capped at 120 chars. Relevant when Phase 4 promotes staged files with TOC regions or when a fix iteration re-touches one. See Episodes §Step 4.

## Decision Log
<!-- The track-canonical live decision carrier (D7). Phase 1 seeds the full
inline Decision Records this track owns; the section then continues as the
execution-time continuous log. Seeded from the frozen design.md D-records. -->

#### D1: Remove the six disguise-only style rules at the source plus every mirrored consumer
- **Alternatives considered:** Chat-register-only removal — rejected because the review cost lives in the durable documents, so leaving the disguise rules in `house-style.md` keeps fighting training on the surface that matters most.
- **Rationale:** The disguise-only rules make text look human-authored while adding no comprehension value and carry enforcement cost. Removal happens at the source (`house-style.md`): the design-review cold-read prompt cites `house-style.md` by section and inherits the deletion for free, while every other surface that names or mirrors a removed rule takes an explicit same-change edit. The six removed rules are the negative-parallelism ban, the roundabout-negation ban, the closing-phrases ban, the hyphenated-word-pair rule, the curly-quotes rule, and the sentence-case heading mandate (Title Case allowed again). The mirrored consumers touched in the same change, enumerated by grep over the removed pattern names: the `dsc-ai-tell` regexes in `design-mechanical-checks.py` with their test and fixture, the always-loaded `ai-tells/SKILL.md` `description:` frontmatter plus its catalogue, `review-workflow-writing-style.md`, `house-conversation.md`, the `design-document-rules.md` `dsc-ai-tell` catalogue row, and the `CLAUDE.md` § Writing Style parenthetical.
- **Risks / Caveats:** A removed regex, its assertion, and its fixture line are one unit — deleting the regex alone breaks the build at test time. The `ai-tells/SKILL.md` description is loaded into every session, so three of the six removals keep costing context until it is edited. Removal is not a backfill: legacy committed designs under `docs/adr/**` were authored under the removed rules and are not re-reviewed.
- **Implemented in:** this track.
- **Full design**: design.md §"Removing the disguise-only style rules".

#### D2: Hybrid narrative-plus-findings on the whole-doc gate; persona-voiced structured findings only on the sliced auditor
- **Alternatives considered:** Pure narrative plus a synthesis step (the book model verbatim) — larger loop rework, and the orchestrator must parse prose into revision items. Manifest-only with a persona identity — loses the experiential signal on the whole-doc surface where it is real. Uniform hybrid on both roles — the auditor-path narrative has no consumer and only costs context.
- **Rationale:** The reading-experience narrative is a whole-read property, so it attaches only to the whole-doc comprehension gate (the time-constrained reviewer), whose narrative is meaningful because it reads the full document in order and whose consumer is real — the orchestrator's gate verdict plus the author's rework seed. The range-sliced target reader returns persona-voiced structured findings only: at a ~200-line slice a "stumble" is exactly what a structured finding already records (verbatim quote, location, why it breaks), and a per-slice narrative would flow into the orchestrator with no consumer, multiplied by up to six slices across three rounds.
- **Risks / Caveats:** The structured findings keep the dual-clean convergence keyed on discrete findings in both roles.
- **Implemented in:** this track.
- **Full design**: design.md §"Persona readers: target reader and time-constrained reviewer".

#### D3: Move both reader agents to Sonnet as a mid-level proxy; pin the author to Opus, never Fable
- **Alternatives considered:** Keep Opus readers — defeats the reader-proxy calibration and costs more. Keep one Opus backstop on the outer gate only — the gate's cited comprehension answers already make its failures visible, so the backstop buys nothing there, and splitting the pins re-introduces the calibration mismatch on the gate that most needs the proxy. Fable for the author — unnecessary given the resolved-log input.
- **Rationale:** The reader roles judge reading experience, not design quality, so a mid-level proxy is the right model: if Sonnet can follow the document, a mid-level developer can. The author stays Opus because it drafts from an already-resolved research log, so Fable adds nothing. Three mitigations target the false-clean risk (a weaker reader glossing over what it failed to follow): the persona contract targets that failure mode directly ("you will not fill in gaps"), Opus readers carry the opposite error at similar cost (following prose a mid-level human cannot), and the `dsc-ai-tell` pre-flight plus the Phase-C style pass remain as backstops.
- **Risks / Caveats:** The proxy claim is one-directional (it covers "if Sonnet follows it, a human will"); the load-bearing direction is whether Sonnet reports what it could not follow. Residual risk is named and accepted: if dual-clean documents keep drawing human readability complaints, the model pin is the first suspect to revisit.
- **Implemented in:** this track.
- **Full design**: design.md §"Reader-proxy model pins and the false-clean risk".

#### D4: Rename `TL;DR` to a `### Summary` sub-heading with details under their own sub-headings; keep legacy spellings accepted
- **Alternatives considered:** A `**Summary.**` bold-prefix rename alone — the summary still runs into the details, which is the separation failure at issue. A hard rename without legacy acceptance — breaks every committed legacy design under `docs/adr/**` on re-review.
- **Rationale:** A sub-heading gives structural separation of summary from detail — skimmable end-to-end and regex-enforceable — where "Summary" is also self-explanatory to a reader for whom "TL;DR" is jargon. Backward compatibility follows the D11 References→Decisions & invariants precedent: both spellings are accepted in the shape regexes. The complete rename-site list is enumerated by grep over `.claude/`; the subtle one is `MANDATORY_OR_FORM_SUBHEADINGS`, which must gain `"summary"` alongside `"tl;dr"` or the same-shape sibling similarity check counts the shared `### Summary` heading in every section and false-positives on every well-formed design.
- **Risks / Caveats:** This design is authored under the current live rules, so it still uses `**TL;DR.**`; the `### Summary` form applies to designs authored after promotion.
- **Implemented in:** this track.
- **Full design**: design.md §"Renaming TL;DR to Summary".

#### D5: Technical-writer voice governs design and track prose only; issue, PR, and commit bodies stay terse BLUF
- **Alternatives considered:** Apply the voice everywhere — worsens the act-oriented surfaces (a narrative issue body adds length without comprehension gain). Apply it over whole track files without the carve-out — an author agent could then legitimately narrativize rosters and episode fields, inflating every implementer read.
- **Rationale:** The teaching-narrative voice suits documents read to build a mental model. In a track file the voice governs the prose sections only — `## Purpose / Big Picture`, `## Context and Orientation`, and `## Plan of Work`. The structured ExecPlan surfaces stay registry-terse: the `## Concrete Steps` roster, `## Episodes` fields, `## Decision Log` records, `## Invariants & Constraints` bullets, and `## Interfaces and Dependencies` boundary lists. The prose trio is exhaustive.
- **Risks / Caveats:** `## Validation and Acceptance` and `## Idempotence and Recovery` are author-written but stay registry-terse — they are not part of the prose trio.
- **Implemented in:** this track.
- **Full design**: design.md §"The technical-writer voice".

#### D6: Recast the two existing cold readers as personas; add no new spawns; mechanical checks stay personless
- **Alternatives considered:** A third veteran spawn — extra per-round cost for a signal the comprehension verdict already carries. New persona agents alongside the old roles — duplicates the review surface. A persona without the checklist — silently drops the judgment half of the axis, leaving the kept rules with no per-round enforcer.
- **Rationale:** `readability-auditor` becomes the target reader (a mid-level developer who "will not fill in gaps", persona-voiced structured findings). `comprehension-review` becomes the time-constrained reviewer (thirty minutes to build a working mental model, hybrid output); the skeptical veteran's depth-skepticism folds into its mental-model verdict rather than a third spawn. The persona governs register and stance, not the checklist: the target reader keeps sole ownership of the prose AI-tell axis (invariant S4) and retains the house-style `§`-citation obligation for the kept judgment rules the regexes cannot reach.
- **Risks / Caveats:** `absorption-check` and `fidelity-check` stay personless coverage cross-checks; coverage matching needs no reader stance.
- **Implemented in:** this track.
- **Full design**: design.md §"Persona readers: target reader and time-constrained reviewer".

#### D7: Per-rule remove/keep disposition under one criterion, with a closure default
- **Alternatives considered:** Keeping the heading-case rule — marginal navigation value, and textbook fighting-training for cosmetics (flagged to the user, not pulled back). Removing padding rules as tells — they cut review time, so they contribute to the aim and stay.
- **Rationale:** One criterion, applied rule by rule. Remove when the rule's only benefit is disguise and it carries enforcement cost; keep when deleting the flagged text shortens the document or when the rule forces the writer to supply substance. Six rules are removed (listed in D1). Orientation and Plain language stay wholesale; passive voice, nominalization, broken grammar around identifiers, elegant variation, and the boldface cap stay; throat-clearing, prompt-restating, and superficial `-ing` clauses stay as additive padding; hedge stacking, filler hedges, trailing hedges, vague attribution, false ranges, generic positive conclusions, and persuasive authority tropes stay because each forces a claim to carry its number, source, or named trade-off. The consumer-only "adjective triads" rule stays under the same criterion (it forces each vague adjective to become a named quality) and gains no new house-style section. Closure default: any rule not on the remove list keeps its current disposition.
- **Risks / Caveats:** The line between remove and keep is illustrated by negative parallelism (removed) versus throat-clearing (kept): throat-clearing is additive filler whose deletion shortens the text, whereas negative parallelism reframes real content at near-zero length cost, so banning it buys only disguise, and its trailing-negation variant is the most false-positive-prone regex in the checker.
- **Implemented in:** this track.
- **Full design**: design.md §"Removing the disguise-only style rules".

#### D8: Dual-register design document — the frozen `design.md` stays the full-tier track seed; the machine-facing skeleton is exempt from the narrative voice
- **Alternatives considered:** The research log as the full-tier track seed — forces mechanism re-derivation from code, risks divergence from the frozen reviewed design, and loses the integration pass. Applying the narrative voice uniformly, including records and summaries — dissolves operative content into the story arc, which is the extraction hazard the user's doubt named.
- **Rationale:** The research log is an append-only ledger with superseded entries and no integrated mechanism, so it cannot supply the mechanism content that tracks need; the frozen design is reviewed, integrated, and user-accepted. Its seed consumer, the track-derivation spawn, is itself a cold agent, and narrative orientation helps that reader. The guard is a register boundary inside the document: the technical-writer voice governs the mechanism prose, while decision records keep the four-bullet form, `### Summary` blocks stay plain-claim and self-contained, decisions-and-invariants footers stay lists, and diagrams stay.
- **Risks / Caveats:** When `design_gate=yes`, the frozen design is the seed, not the research log; the log is a Phase-2 cross-check input, never a Step-4b seeding input in that case.
- **Implemented in:** this track.
- **Full design**: design.md §"Dual-register design document".

#### D9: Transfer the eight internals-book voice rules; rank the two colliding rules; add the link-staleness re-read reminder
- **Alternatives considered:** A whole-document story arc — design docs are consulted, not read cover to cover. Dropping the forward/backward links entirely — the book's dip-in readers showed the links carry navigation. Leaving the two rule sets unranked — two enforcement agents with contradictory targets thrash the authoring loop.
- **Rationale:** Of the eight `BOOK_BRIEF.md` voice rules, five transfer verbatim (concrete before abstract, one concept per section, earn every name, diagrams must teach, no bullet-point fact dumps), two are adapted from chapter scope to section scope (narrative not reference; connect forward and backward as light prose links), and one is already present (precise `file:line` citations). Two precedence rankings resolve collisions with kept house-style rules: a worked-example opener beats the section-length heuristics (the anti-padding clause still applies inside the example), and one-concept-per-section beats same-shape sibling consolidation when the siblings teach distinct concepts. The adapted links cite the neighbor's heading verbatim so they are greppable.
- **Risks / Caveats:** A section rename that skips the neighbor re-read leaves a dangling prose link no mechanical check catches — the `edit-design` mutation-discipline reminder (re-read neighbor openers and closers on section-move/remove/rename) is the guard.
- **Implemented in:** this track.
- **Full design**: design.md §"Transferring the internals-book voice rules".

#### D10: Adopt YTDB-1163's two BLUF-hardening rules; compose the plain claim ahead of the D9 backward-link opener; three acceptance sites
- **Alternatives considered:** Deferring YTDB-1163 to its own branch — the site overlap is with D1 and D7 (the removals rewrite `house-style.md` § Banned sentence patterns and renumber the self-check, and the disposition rework rewrites `review-workflow-writing-style.md`), so a second branch pays a second review round over the same regions. Treating the issue as advisory input only — the initial request explicitly folds YTDB-1163 into scope.
- **Rationale:** Today's section-level BLUF rule permits an opaque forward-reference lead and a body that reads only as its continuation. Two rules close the gap: the lead must be a self-contained plain claim parseable before the body (gloss any domain term, or fall back to why-before-what), and the body must stand as complete prose with the lead deleted (an anaphor opener resolving into the lead is a finding). The two rules share the section-opening slot with D9's connect-backward opener; the plain claim comes first and the backward link rides as a subordinate clause or the following sentence, so every enforcement agent reads one ranked rule. Acceptance names three sites: `house-style.md` § BLUF lead and § Orientation with an exemplar pair beside the existing orientation exemplar, the `review-workflow-writing-style.md` `### BLUF lead` enforcement criteria (develop-state lines 78-80, not only its § Project-context summary bullet), and the by-name house-style self-check BLUF item. The two rules bind every section lead across the workflow-markdown surfaces and the issue/PR BLUF, including surfaces with no `### Summary` block.
- **Risks / Caveats:** The self-check item is anchored by name rather than ordinal because this change's D1 and D7 removals renumber the self-check list.
- **Implemented in:** this track.
- **Full design**: design.md §"Hardening the section BLUF lead".

#### D12: Extend the §1.7 staging surface to `.claude/output-styles/**` and root `CLAUDE.md`; stage the mechanism edits and bootstrap this branch's three files by hand (revised after Track 1 Phase A)
<!-- Numbered D12, not D11: this track's D4 and Plan of Work cite an external "D11" (the References→Decisions & invariants both-spellings precedent from a prior merged plan, also cited in the frozen design.md), so a track-local D11 would collide. The "D11" in D4 and Plan of Work is that external precedent, not this record. -->
- **Original decision:** The frozen design's §"Staging and promotion under §1.7" classified the branch as §1.7 staged mode and assumed every edit accumulates under `_workflow/staged-workflow/.claude/**` and promotes at Phase 4, treating the standard four-prefix §1.7 surface (`.claude/{workflow,skills,agents,scripts}/**`) as sufficient. It recorded no D-record.
- **What changed:** Phase-A review (A1 blocker plus A2) found three of the track's central files fall outside that surface: `.claude/output-styles/house-style.md` (the source of truth, item 1), `.claude/output-styles/house-conversation.md` (item 2), and root `CLAUDE.md` (item 19, not under `.claude/` at all). §1.7(a) is explicit: "No other prefixes participate." The live Phase-4 promotion (`create-final-design.md` Step 4, `../conventions.md` §1.7(e)) runs `cp -r "$STAGED_DIR/.claude/." .claude/`, which copies staged output-styles into the working tree, but `git add`s only the four prefixes, so a staged `house-style.md` edit is copied yet never committed and drops silently from the PR; root `CLAUDE.md` is never cp'd. The divergence guard and the `implementer-rules.md` enforcement gate likewise never watch these paths. State 0's consistency review checked file existence, not stageability, so it missed this.
- **Revised decision:** Extend the §1.7 mechanism to cover `.claude/output-styles/**` and repo-root `CLAUDE.md`, and stage those mechanism edits like every other workflow-machinery change so I6 holds. The extension touches §1.7(a) mirror layout (adds `.claude/output-styles/`; adds a repo-root mapping so `CLAUDE.md` mirrors to `_workflow/staged-workflow/CLAUDE.md`), the §1.7(b) marker enumeration (descriptive tail only, since the stable-prefix property already keeps develop-era markers matching while the enumeration grows), the §1.7(c)/(d)/(e)/(f) path lists, the `implementer-rules.md` write-routing plus the pre-commit `git diff --cached --name-only` gate, and the `create-final-design.md` Step-4 divergence check, `cp`, and `git add`. The live mechanism during this branch still knows only the four prefixes, so this branch bootstraps its own three files by hand: implementers copy-then-edit them into the mirror manually, and Phase 4 runs an augmented promotion (details in `## Idempotence and Recovery`).
- **Alternatives considered:** Edit the three files live — rejected, because `house-style.md` is the source of truth, so a live edit lets this branch's own later authoring and the concurrent branches (transactional-schema, ir-filter-step) run under the new rules mid-flight, the cross-branch contamination the staging discipline exists to prevent. Full staging-approach redesign folding R1/A3 plus A4 — rejected as heavier than the surface gap requires; R1/A3 and A4 are folded as should-fix track edits below without reopening the approach.
- **Rationale:** Staging the mechanism edits keeps I6 intact and makes the extended surface live for every future workflow-modifying branch at promotion. The stable-prefix marker property (§1.7(b)) was designed for exactly this enumeration growth — its own text names "the bootstrap that lets a workflow-modifying plan keep its develop-state marker verbatim while the staged definition adds prefixes" — so the develop-era marker keeps the gate active through the branch. The manual bootstrap is bounded and one-time: three files, a manual mirror-write, and two extra promotion lines.
- **Risks / Caveats:** The live pre-commit gate does not watch output-styles or root `CLAUDE.md` this branch, so a stray live edit to them is caught only by the extended acceptance check (`git diff --name-only`), not the automated gate; the manual discipline is the guard until promotion. The frozen `design.md` §"Staging and promotion under §1.7" now diverges from this record; Phase 4's `design-final.md` reconciles the as-built surface.
- **Implemented in:** this track (revised).
- **Full design**: superseded — the frozen design.md § Staging premise is corrected here per inline replanning; design.md stays frozen, and Phase 4 reconciles it. Mechanism detail lives in this record, `## Plan of Work`, and `## Idempotence and Recovery`.

## Outcomes & Retrospective
<!-- Continuous-log. Review iteration outcomes and the track-completion
summary at Phase C. -->

**Phase A reviews** (high-tier panel: Technical + Risk + Adversarial; adversarial pinned Fable 5 per D14, `design_gate=yes`; Track-1 exception drops only the cross-track-episode challenge). Iter-1 findings and iter-2 gate verdicts live under `plan/track-1/reviews/`.

- [x] Technical: PASS at iteration 2 (3 findings, 3 accepted; +1 gate suggestion T4 accepted — fixture-only-mode edit folded into item 8).
- [x] Risk: PASS at iteration 2 (6 findings, 6 accepted; +1 gate should-fix R7 accepted — see the finding-ID provenance note below).
- [x] Adversarial: PASS at iteration 2 (10 findings, 10 accepted; the mandated single-track sizing challenge A8 survived — both split candidates fall below the ~12 floor and overlap Track 1).

**Finding-ID provenance note (R7).** Inline finding-ID tags in this track's prose (`(T2)`, `(R3)`, `(R4/A3)`, `(R5/A5)`, …) are provenance breadcrumbs to two Phase-A review generations, both under `plan/track-1/reviews/`: the replan's folded iter-1 should-fixes and this session's re-run iter-1 plus iter-2 gate. The same prefix can label distinct concerns across the two generations, so a bare tag is not a unique lookup key — the surrounding prose is authoritative on each fix's content, and the `## Concrete Steps` roster below names each concern in words rather than by bare cross-review ID.

## Context and Orientation
The workflow authors and reviews its durable documents under one declarative style file, `.claude/output-styles/house-style.md`, which is the single source of truth for authored-document style. Four other surfaces are named as its consumers, but only one inherits a rule deletion for free: the cold-read prompt in `.claude/workflow/prompts/design-review.md` cites `house-style.md` by section heading and restates no rule, so deleting a rule at the source removes it from that prompt's reach at once. The other three name or mirror specific rules and take an explicit same-change edit: the `dsc-ai-tell` regex checker inside `.claude/scripts/design-mechanical-checks.py` mirrors a subset of the rules as its own hard-coded regexes; the `ai-tells` skill (`.claude/skills/ai-tells/SKILL.md`) names three of the removed rules in its always-loaded `description:` frontmatter and its body catalogue; and the chat-register file `.claude/output-styles/house-conversation.md` lists the banned patterns by name in its AI-tell subset. Three further surfaces hold their own copies and must be edited directly: `.claude/agents/review-workflow-writing-style.md` (the Phase-C style reviewer, which carries its own enforce-these-rules checklist), `.claude/workflow/design-document-rules.md` (section shapes plus the `dsc-ai-tell` catalogue row), and the project `CLAUDE.md` § Writing Style.

A few terms recur. A **disguise-only rule** is a style rule whose only benefit is making text read as human-authored, with no gain in comprehension and no reduction in length; these are what D1/D7 remove. **`dsc-ai-tell`** is the mechanical regex checker in `design-mechanical-checks.py` that flags a subset of the house-style patterns whose textual fingerprint is reliable enough to match; its exact fire/no-fire behavior is pinned by `.claude/scripts/tests/test_dsc_ai_tell.py` against `.claude/scripts/tests/fixtures/dsc-ai-tell-fixture.md`, so a regex, its assertion, and its fixture line form one unit. The **ExecPlan** is the fifteen-section per-track execution-plan template (defined in `conventions-execution.md §2.1`) that this very file follows. A **persona reader** is a cold review agent given a named reader identity and stance instead of a neutral checklist.

The review side today is a roster of five agents. `design-author` (Opus), `readability-auditor` (Opus), and `comprehension-review` (Opus) are the three that change here; `absorption-check` and `fidelity-check` (both already Sonnet) are unchanged and listed only so the loop's full membership is visible. `readability-auditor` and `comprehension-review` are the two review readers driven by the `phase1-creation` dual-clean authoring-review loop in `.claude/skills/edit-design/SKILL.md`. A reader's warmth says how much authoring context it carries. A warm agent reads the research log and the planning history, so it checks the draft against what the author already knew. The two review readers instead judge the finished document on its own, the way the eventual reader meets it, and differ only in why they lack that context: a cold reader never carried the authoring context, while a de-warmed reader would normally run warm but has that context deliberately withheld for the read. Each round the author drafts; a range-sliced auditor (cold) and a warm coverage-matcher run in parallel until both return clean, and a de-warmed comprehension gate reads the finished document once at the end.

The concrete deliverables of this track are the staged edits to the ~21 files listed under `## Interfaces and Dependencies`; each file's entry there names what changes in it and which decision records drive the change.

Three process constraints govern how those edits land. First, the branch runs in `conventions.md` §1.7 staged mode — not the §1.7(k) prose-only opt-out — because the `dsc-ai-tell` regexes and the two agent frontmatters are behavior-bearing, and behavior-bearing changes disqualify the opt-out. Every implementation edit therefore accumulates under `_workflow/staged-workflow/.claude/**`, mirroring the live path it will replace (copy-then-edit on first touch), and the live `.claude/**` tree is untouched until the Phase-4 promotion commit. Second, because the new rules are staged, they are not live while the branch is open: this track file and the frozen `design.md` are both authored and reviewed under the current live rules — bold-prefix `**TL;DR.**` summaries, sentence-case headings, and the current banned-pattern set — and the new machinery reviews only documents authored after promotion.

Third, the standard §1.7 surface stages only four prefixes (`.claude/{workflow,skills,agents,scripts}/**`), and three central files sit outside it: `house-style.md` and `house-conversation.md` under `.claude/output-styles/`, and root `CLAUDE.md`. D12 extends §1.7, the `implementer-rules.md` enforcement gate, and the `create-final-design.md` promotion to reach these paths, and stages those mechanism edits like every other workflow-machinery change. The extended surface goes live for future branches at promotion; for this branch the live mechanism still knows only the four prefixes, so the three out-of-surface files are bootstrapped by hand. An implementer editing one of them copies the live file into the mirror (`_workflow/staged-workflow/.claude/output-styles/**` for the two output-styles files, `_workflow/staged-workflow/CLAUDE.md` for root `CLAUDE.md`) on first touch and edits the mirror copy, because the live gate does not auto-route these paths. Reads stay on the live copy, which is correct here: the branch authors under the old rules, and the old (live) `house-style.md` is exactly what those reads should see. Phase 4 promotes the three by hand per `## Idempotence and Recovery`.

## Plan of Work
The work is sequenced so that each edit is complete before its dependents rely on it, and so the build never sees a half-removed rule.

Start with the removal (D1/D7), because the Summary rename, the BLUF hardening, and the book-rule transfer all rewrite regions of the same files and want a stable base. Remove each of the six rules at its `house-style.md` site, then propagate to the mirrored consumers. Three of the six removals (negative parallelism, hyphenated pairs, Title Case) have a `dsc-ai-tell` regex, so each of those deletes the regex in `design-mechanical-checks.py`, its assertions in `test_dsc_ai_tell.py`, and its fixture lines in `dsc-ai-tell-fixture.md` in one change — deleting the regex while leaving the fixture and assertion breaks the test suite at build time. Negative parallelism is two regex objects, `NEGATIVE_PARALLELISM_RE` and `NEGATIVE_PARALLELISM_TRAILING_RE`, so its removal deletes both, plus each one's fixture line and assertion; a partial delete leaves the trailing regex enforcing a rule house-style no longer bans (T2). Deleting a removed pattern's positive fixture blocks shifts the line numbers of every surviving line-anchored assertion in the shared fixture (the `ANCHORED_REGRESSION_CASES` and `NEGATIVE_RANGES` line lists), so each removal renumbers the survivors in the same change or the kept-rule assertions fail at build time (R3). The other three removals (roundabout negation, closing phrases, curly quotes) touch prose consumers only. Edit the always-loaded `ai-tells/SKILL.md` `description:` frontmatter in the same pass, since it hard-codes three of the removals by name and costs context every session until it changes.

Rename `TL;DR` to a `### Summary` sub-heading across every site that hard-codes the spelling (D4). Add `"summary"` to `MANDATORY_OR_FORM_SUBHEADINGS` in the same change as the shape regex, or the same-shape sibling check false-positives on every well-formed `### Summary` design. Add the Summary entry to `SHAPE_EXEMPT_SECTION_NAMES` in display case (`"Summary"`): that set compares raw section titles, while `MANDATORY_OR_FORM_SUBHEADINGS` compares lowercased headings, so a lowercase `"summary"` in the display-case set would leave a Part-level `## Summary` unexempted (T1/R2/A5). Keep both spellings accepted in the shape regexes, following the References→Decisions & invariants both-spellings precedent (the external D11, distinct from this track's D12), and pin the acceptance with new shape tests modeled on `test_design_mechanical_checks_d11.py`.

Edit the agent roster (D3/D6): move `readability-auditor` and `comprehension-review` to Sonnet and give each its persona (target reader; time-constrained reviewer with hybrid output), add the Opus-pin note and the technical-writer voice mandate to `design-author`, and drop the removed-rule criteria from `review-workflow-writing-style.md` while adding its D10 BLUF criteria. The definitive drop-site set is whatever a case-insensitive grep of the six removed-rule names **and their canonical exemplar phrases** (`It's not X`, `In conclusion`, the curly-quote glyphs) returns across the file — the enumeration is grep-derived, not a hand-maintained line list (three plan-review iterations each found the hand-list incomplete). The exemplar clause is load-bearing: line 185 names the negative-parallelism rule only through its exemplar `"It's not X — it's Y"`, never by the rule name, so a names-only grep skips it and leaves a removed rule enforced at the reviewer's most severe bucket (T2/R3/A1). Treat the develop-state snapshot {29, 34, 38, 71, 89, 185, 188, 200} as an authoritative floor — the grep-derived set must be a superset. The snapshot maps to: the § Project-context key-rules bullets (29, 34), the § Tooling `Read`/`Grep`-target list (38, "negative-parallelism markers"), the § Banned sentence patterns sweep formulaic-phrasings bullet (71), the § Heading style bullet (89), the `#### Critical` / `#### Recommended` finding-format severity buckets (185, 188 — the Critical bucket at 185 is the exemplar-only site), and the `**WS<N>**` finding-format template's axis token plus its Cost examples (200). Each edit is surgical: remove only the removed-rule names and keep the kept-rule mentions (throat-clearing, trailing hedges, prompt-restating, missing-BLUF, section length, and the "banned sentence patterns" category label, under which the kept patterns still fall). The drop must reach every hit, or the Phase-C reviewer keeps enforcing a rule house-style no longer bans (R4). Scope `comprehension-review`'s hybrid narrative to mental-model and comprehension only: a "stumble" reported as a prose-quality verdict is a prose-AI-tell judgment, which S4 reserves for the target reader, so the persona contract keeps the comprehension gate's narrative off that axis (R5). The persona recast must preserve S4: exactly one prose-AI-tell-axis owner (the target reader). The phrase "prose AI-tell axis" occurs at several sites across the two agent files, so the S4 acceptance grep is scoped and ownership-distinguishing — it confirms exactly one agent *claims* the axis, not merely that the phrase appears once (R6).

Fold the book-rule transfer and the dual-register guard into `design-document-rules.md` (D8/D9): the eight voice rules, the two precedence rankings, and the register boundary that keeps records, summaries, footers, and diagrams registry-terse. Add the link-staleness re-read reminder to the `edit-design` mutation discipline. Harden the section BLUF lead (D10) at its three named sites, placing the exemplar before/after pair beside the existing § Orientation worked exemplar, and anchor the self-check BLUF item by name because the removals renumber the list. In `review-workflow-writing-style.md` the D10 rules land in the `### BLUF lead` enforcement-criteria section (develop-state lines 78-80), not only the § Project-context summary bullet (28): an add that reaches the summary but not the enforcer produces a Phase-C reviewer that does not actually enforce D10 (T3).

Extend the §1.7 staging surface (D12) so the branch's out-of-surface files can promote. In the staged `conventions.md`, add `.claude/output-styles/` plus a repo-root mapping for `CLAUDE.md` to the §1.7(a) mirror layout, the §1.7(b) marker enumeration, and the §1.7(c)/(d)/(e)/(f) path lists. In the staged `implementer-rules.md`, add both to the write-routing rule and the pre-commit `git diff --cached --name-only` gate. In the staged `create-final-design.md`, add both to the Step-4 divergence check and the `git add`, and add an explicit `cp` for root `CLAUDE.md`, which the live `cp -r "$STAGED_DIR/.claude/." .claude/` does not reach. These mechanism edits stage like the rest; the branch's own three files are bootstrapped by hand per `## Idempotence and Recovery`.

Give the D2 hybrid contract consumer-side edits alongside its producer edit. Item 13 adds the hybrid narrative-plus-findings output to the `design-review.md` comprehension gate (the producer). Two consumers then take matching edits so the narrative is not delivered to a parser that drops it: `create-plan` Step-4b pins a bounded inline gate return that the hybrid output would contradict (A4, item 21), and `edit-design`'s own gate-return parse contract (develop-state lines ~664-676) parses only a Verdict line plus a structural-findings list with no narrative slot, so item 14 extends it to accept the hybrid on both output-path branches and routes the narrative into the Step-6 author rework input (A2).

Handle the staged `dsc-ai-tell` tests' corpus reach and add a fixture-only run mode. `test_dsc_ai_tell.py` resolves `REPO_ROOT` as `Path(__file__).resolve().parents[3]`, and both the fixture paths (`SCRIPT`, `FIXTURE`) and the corpus paths (`CALIBRATION_ADRS`) resolve *through* `REPO_ROOT` — the same base, not one relative to `__file__`. From the staged path `_workflow/staged-workflow/.claude/scripts/tests/`, `parents[3]` is `staged-workflow/`: the `.claude/**` mirror (edited script + edited fixture) exists under it, so the fixture fire/no-fire assertions run against the staged edits, while `docs/adr/**` is not mirrored there, so the corpus-calibration assertions cannot resolve (T1). But `main()` runs both groups and `assert_calibration_adrs()` appends a hard failure — not a skip — for each missing ADR, so the staged file as-written always exits non-zero, and "green while staged" is unsatisfiable without a way to run the fixture assertions alone (R1). So a decomposition step gives the staged `test_dsc_ai_tell.py` a fixture-only mode (treat a missing calibration ADR as a skip, or gate `assert_calibration_adrs()` behind a CLI flag / env var); this edit is behavior-bearing, so it stages and promotes like the rest. Phase B then runs the fixture-only assertions green in place — which is also what validates the R3 line-anchor renumbering (`ANCHORED_REGRESSION_CASES`, `NEGATIVE_RANGES`) — and the corpus-calibration assertions run at Phase-4 promotion against the live tree (R1/A3). The new D4 shape tests (item 10) use only staged `tests/fixtures/` inputs and carry no `docs/adr/**` back-reference: the `test_design_mechanical_checks_d11.py` precedent hard-codes `FROZEN_DESIGN = REPO_ROOT/docs/adr/plan-slimization/...` and FATAL-aborts if it is absent, so copying that block verbatim would dangle from the staged path (R2). `## Validation and Acceptance` and `## Idempotence and Recovery` record which assertions run when.

Throughout, every edit lands under `_workflow/staged-workflow/.claude/**` (and, for root `CLAUDE.md`, under `_workflow/staged-workflow/CLAUDE.md`); the branch diff must touch no live `.claude/**` path, no live `.claude/output-styles/**`, and no live root `CLAUDE.md`. The invariants to preserve are listed under `## Invariants & Constraints`.

## Concrete Steps
<!-- Thin roster (D9): one entry per step with description, `risk:` tag, an
optional `size:` clause, and a `[ ]` status checkbox. Immutable after Phase A
except the status flip and an optional `commit:` annotation. Full per-step
mechanism lives in `## Plan of Work` and `## Interfaces and Dependencies`.
Concerns are named in words (not by bare cross-review finding ID) per the R7
provenance note in `## Outcomes & Retrospective`; trailing `(D.../…)` refs are
provenance breadcrumbs only. Reconciliation: max(step tags) = high = predicted
tag; no divergence, no missed reviewers owed. -->

1. Rework the `dsc-ai-tell` mechanical checker and its tests: delete the four disguise-rule regexes (both negative-parallelism regexes together, hyphenated-pair, Title-Case) — each removal an atomic unit of regex + its `PATTERN_SIGNATURES` entry + fixture block + assertion — then renumber the surviving line-anchored assertions (`ANCHORED_REGRESSION_CASES`, `NEGATIVE_RANGES`); add `### Summary` shape support (`section_has_tldr`, display-case `"Summary"` in `SHAPE_EXEMPT_SECTION_NAMES`, lowercase `"summary"` in `MANDATORY_OR_FORM_SUBHEADINGS`) keeping both spellings accepted; add a fixture-only run mode so the fixture assertions run green while staged; and add both-spellings + case-correctness shape tests on staged fixtures only (a new test file, no `docs/adr/**` back-reference). Files: `design-mechanical-checks.py`, `test_dsc_ai_tell.py`, `dsc-ai-tell-fixture.md`, new shape-test file (D1/D4/D7). — risk: high (workflow machinery — behavior-bearing checker; build-break and false-positive-on-every-design exposure)  [x] commit: eae8c1c25c90c9998aa5f9ae42cb27a2f75fdd9c
2. Extend the §1.7 staging surface (D12) to `.claude/output-styles/**` and repo-root `CLAUDE.md` so the branch's own out-of-surface files can promote: in `conventions.md` add both path classes to §1.7(a)–(f), refresh the §1.5 tier pointers, and rename its `TL;DR`; in `implementer-rules.md` add both to the write-routing rule and the pre-commit `git diff --cached --name-only` gate; in `create-final-design.md` extend the Step-4 divergence check and `git add`, add an explicit root-`CLAUDE.md` `cp` interleaved *before* the promote commit, and rename its `TL;DR`. Files: `conventions.md`, `implementer-rules.md`, `create-final-design.md` (D12/D4). — risk: high (workflow machinery — behavior-bearing staging/promotion/gate mechanism)  [x] commit: a21861936ae902f775d9fbe64bb1b59959e33028
3. Rework the style source of truth and the review agents. In `house-style.md` (out-of-surface): remove the six disguise-only rules with surgical clause-level self-check sub-edits (not whole-item deletion; includes the mid-list "sentence case on H2+" clause), swap the writer persona to a technical writer, rename `TL;DR`→`### Summary`, add the two D10 BLUF-hardening rules plus the Orientation exemplar pair and the self-check BLUF item, and renumber the self-check list. Mirror the disguise-rule removals and the reader change into `house-conversation.md` (out-of-surface); drop the negative-parallelism parenthetical in root `CLAUDE.md` (out-of-surface). Move `readability-auditor` and `comprehension-review` to Sonnet with their personas (target reader; time-constrained reviewer with hybrid output kept off the prose-AI-tell axis so a single agent owns it), pin `design-author` to Opus with the technical-writer voice mandate, and in `review-workflow-writing-style.md` drop the removed-rule criteria at every names-plus-exemplar grep hit (authoritative floor {29,34,38,71,89,185,188,200}; 185 is the exemplar-only site), retarget the `#### Critical`/`#### Recommended` finding-format buckets and the axis token, and add the D10 BLUF criteria at the `### BLUF lead` enforcement section (dev lines 78-80). Edit the always-loaded `ai-tells/SKILL.md` `description:` frontmatter and its body catalogue. The three out-of-surface files carry the per-step live-isolation guard (copy-then-edit into the mirror; `git diff --cached --name-only -- .claude/output-styles CLAUDE.md` empty before each commit). Files: `house-style.md`, `house-conversation.md`, `CLAUDE.md`, `readability-auditor.md`, `comprehension-review.md`, `design-author.md`, `review-workflow-writing-style.md`, `ai-tells/SKILL.md` (D1/D3/D4/D5/D6/D7/D10). — risk: medium (bounded behavioral workflow edits — declarative rules and agent config; no build surface) — size: ~8 files; merging step 4 would reach ~14 files and trip the overblown line (under-fill reason a)  [x] commit: 6ddd58a305bac15e861ede2fd767d9d70493c18e
4. Propagate the Summary rename, the D2 hybrid contract, and the internals-book voice rules to the authoring and planning consumers. In `design-document-rules.md`: the eight book voice rules plus the two precedence rankings (D9), the dual-register boundary (D8), the `### Summary` shapes/templates (D4), and the `dsc-ai-tell` catalogue row (D1). In `design-review.md`: the `### Summary` structural finding and TOC row, every grep-returned `TL;DR` site, and the hybrid narrative producer contract scoped to mental-model and comprehension (D2). In `edit-design/SKILL.md`: every grep-returned `TL;DR` site, the D9 link-staleness re-read reminder, and the gate-return parse-contract extension that accepts the hybrid and routes the narrative into the Step-6 author rework input. In `create-plan/SKILL.md`: the Step-4b inline-gate contract accepts the hybrid return shape. Rename the `TL;DR` sites in `planning.md` and `review-workflow-pr/SKILL.md`. Files: `design-document-rules.md`, `design-review.md`, `edit-design/SKILL.md`, `create-plan/SKILL.md`, `planning.md`, `review-workflow-pr/SKILL.md` (D2/D4/D8/D9). — risk: medium (bounded behavioral workflow prose and prompt edits) — size: ~6 files; the remaining authoring-consumer unit, which merging into step 3 would push to ~14 files (under-fill reason a)  [x] commit: c1c54eef694947c66dd07ecca74ff9e949f880f4

## Episodes
<!-- Continuous-log. Phase B sub-step 7 appends one block per completed
step, identified by step number + commit SHA. Empty at Phase 1. -->

### Step 1 — commit eae8c1c25c90c9998aa5f9ae42cb27a2f75fdd9c, 2026-07-03T11:03Z [ctx=safe]
**What was done:** Reworked the `dsc-ai-tell` checker and its tests in the §1.7 staged mirror. Deleted the four disguise-rule regexes as atomic units — `NEGATIVE_PARALLELISM_RE` and `NEGATIVE_PARALLELISM_TRAILING_RE` together, `HYPHENATED_PAIR_CLUSTER_RE` with its threshold, and the Title-Case check — each with its `PATTERN_SIGNATURES` entry, fixture block, and assertion. Renumbered the surviving line-anchored assertions (`ANCHORED_REGRESSION_CASES`, `NEGATIVE_RANGES`) against the shortened fixture. Added `### Summary` shape support: `section_has_tldr` accepts all three spellings, `SHAPE_EXEMPT_SECTION_NAMES` gains display-case `"Summary"`, `MANDATORY_OR_FORM_SUBHEADINGS` gains lowercase `"summary"`. Added a `--fixture-only` CLI flag that gates `assert_calibration_adrs()` so the staged runner exits zero in place. Added `test_dsc_summary_shape.py` with four fixtures pinning both-spellings acceptance, the still-fires case, the sibling-similarity exclusion, and the Part-level exemption.
**What was discovered:** The A10 no-surprise check passed: the staged checker yields zero `dsc-ai-tell` findings on all three live calibration ADRs, which retires the Phase-4 corpus-assertion promotion-blocker risk named in `## Idempotence and Recovery`. A dead paragraph-walker helper (`iter_paragraphs` plus `_BULLET_START_RE`, ~118 lines) served only the three deleted paragraph scans and was removed (grep-confirmed single caller). The `Write` tool refuses to create new `.md` files under `_workflow/` (it treats them as report files); the new staged `.md` fixtures were written via Bash heredoc. See Surprises & Discoveries — step 3 hits the same guard when it hand-mirrors the out-of-surface `.md` files.
**What changed from the plan:** No material deviation. Two prescribed-choice points resolved within plan bounds: the fixture-only mode is a `--fixture-only` CLI flag (one of the two options the track names), and the shape tests live in a new file `test_dsc_summary_shape.py` (the roster line specified a new file).
**Key files:** staged mirror under `_workflow/staged-workflow/.claude/scripts/` — `design-mechanical-checks.py`, `tests/test_dsc_ai_tell.py`, `tests/fixtures/dsc-ai-tell-fixture.md`, `tests/test_dsc_summary_shape.py`, and four `tests/fixtures/summary-shape-*.md` fixtures.
**Critical context:** The implementer's RESULT reported commit SHA `…cd1a…827a`, which does not resolve; the actual commit is `eae8c1c25c90c9998aa5f9ae42cb27a2f75fdd9c` (same short prefix, verified as HEAD). The staged corpus-calibration assertions defer to Phase-4 promotion — run `test_dsc_ai_tell.py` with no flag from the live tree after promotion; fixture-only green while staged is by design.

### Step 2 — commit a21861936ae902f775d9fbe64bb1b59959e33028, 2026-07-03T11:26Z [ctx=safe]
**What was done:** Extended the §1.7 staging surface to `.claude/output-styles/**` and repo-root `CLAUDE.md` across three staged files. In staged `conventions.md`, §1.7 intro and (a)–(f) now cover both path classes: (a) adds the mirror-layout lines and the repo-root mapping to `_workflow/staged-workflow/CLAUDE.md`, (b) grows the canonical marker's descriptive tail and the older-spelling recognition list, (c) recasts the promotion guard as staged-content presence, (d)/(e)/(f) extend the read-precedence, write-routing, copy-then-edit, additive-promotion, gate, and divergence path lists; §1.5 gains a staged-read pointer for `house-style.md`. In staged `implementer-rules.md`, both path classes join the Sub-step-1 write-routing rule and the pre-commit gate pathspec (`.claude/output-styles/ CLAUDE.md`). In staged `create-final-design.md`, the Step-4 guard, divergence check, and `git add` are extended, and an explicit `cp` of root `CLAUDE.md` plus its own `git add CLAUDE.md` sit inside the guarded block after the prefix `git add` and before the `git diff --cached --quiet || git commit` line. Renamed the two `TL;DR` pointer sites to `### Summary` (D4).
**What was discovered:** A bare `CLAUDE.md` git pathspec anchors at the repo root and matches only the root file, so the extended pre-commit gate and divergence check need no pathspec magic to avoid tripping on nested `CLAUDE.md` files. The prompt-design dim review confirmed the Step-4 cp/gate/divergence mechanism is correct (0 findings).
**What changed from the plan:** Two within-bounds completions beyond the literally named edit sites. The Step-4 outer guard became `[ -d "$STAGED_DIR/.claude" ] || [ -f "$STAGED_DIR/CLAUDE.md" ]` with the recursive cp guarded by `-d`, so a future branch that stages only root `CLAUDE.md` still promotes it (the old directory-only guard would skip it silently — the D12 failure class). One consistency touch outside §1.7: §1.6(h)'s staged-subtree enumeration now names the extended surface so the staged document does not contradict its own (a).
**Key files:** staged mirror under `_workflow/staged-workflow/.claude/workflow/` — `conventions.md`, `implementer-rules.md`, `prompts/create-final-design.md`.
**Critical context:** This branch's own Phase 4 still runs the LIVE four-prefix `create-final-design.md`; the augmented promotion of the three hand-bootstrapped out-of-surface files remains the manual procedure in `## Idempotence and Recovery`. The staged copies carry no `workflow-sha` stamps by design (§1.6(h) excludes `staged-workflow/` from the stamp walk).

### Step 3 — commit 6ddd58a305bac15e861ede2fd767d9d70493c18e, 2026-07-03T11:48Z [ctx=safe]
**What was done:** Reworked the style source of truth and the review agents in the §1.7 staged mirror. In `house-style.md`: removed the six disguise-only rules (negative parallelism, roundabout negation, closing phrases, hyphenated word-pairs, curly quotes, the sentence-case heading mandate) with surgical clause-level self-check sub-edits — including the mid-list "sentence case on H2+" clause — swapped the writer persona to a technical writer, renamed the per-section `TL;DR` block to `### Summary`, added the two D10 BLUF-hardening rules plus an Orientation before/after exemplar pair and the self-check BLUF item, and renumbered the self-check from 10 items to 9. Mirrored the removals and the mid-level-reader change into `house-conversation.md`; dropped the negative-parallelism parenthetical from root `CLAUDE.md`. Moved `readability-auditor` and `comprehension-review` to `model: sonnet` with their personas (target reader with the will-not-fill-in-gaps contract; time-constrained reviewer with the cited-comprehension-answers obligation and a hybrid narrative scoped off the prose AI-tell axis). Pinned `design-author` to Opus (never Fable) with a technical-writer voice mandate. Dropped every removed-rule name and exemplar from `review-workflow-writing-style.md` at the grep-derived sites, retargeted the Critical/Recommended buckets and the WS Cost examples to kept rules, and added the two D10 BLUF criteria at the `### BLUF lead` enforcement section plus the § Project-context summary bullet. Edited the always-loaded `ai-tells` `description:` (three removed rules gone, "adjective triads" kept) and its body catalogue.
**What was discovered:** The grep-derived `review-workflow-writing-style.md` drop set matched the develop-state floor exactly ({29,34,38,71,89,185,188,200}) — line 38 uses the hyphenated `negative-parallelism` spelling and line 185 is the exemplar-only Critical bucket, both caught. The four review-agent files carry no real HTML-comment TOC region (their `Document index` match is only the backtick-wrapped instructional reference inside the TOC-protocol block), so the heading changes needed no TOC reindex.
**What changed from the plan:** No material deviation. Two prescribed-choice points resolved within bounds: the § Self-check "Punctuation" item was renamed to "Boldface" (only the excessive-boldface clause survived after the hyphenated-pair and curly-quote removals); `comprehension-review.md`'s hybrid-output description forward-references `prompts/design-review.md § Output format`, which step 4 (item 13) wires as the producer contract — both are staged and promote together.
**Key files:** staged mirror — `.claude/output-styles/house-style.md`, `.claude/output-styles/house-conversation.md`, root `CLAUDE.md`; `.claude/agents/{readability-auditor,comprehension-review,design-author,review-workflow-writing-style}.md`; `.claude/skills/ai-tells/SKILL.md`.
**Critical context:** During the branch, agent reads of `house-style.md` still resolve to the live (old-rules) copy by design — the branch authors under the old rules. Orchestrator spot-check confirmed the load-bearing invariants: both readers `model: sonnet`; exactly one agent (`readability-auditor`) claims the prose AI-tell axis while `comprehension-review` explicitly disclaims it (S4); the removed-rule names and exemplars are absent from the `review-workflow-writing-style` drop sites with the "banned sentence patterns" category label kept.

### Step 4 — commit c1c54eef694947c66dd07ecca74ff9e949f880f4, 2026-07-03T12:09Z [ctx=safe]
**What was done:** Propagated the Summary rename (D4), the D2 hybrid comprehension-gate contract, and the internals-book voice rules (D8/D9) to the six authoring and planning consumers in the §1.7 staged mirror. `design-document-rules.md` gained the eight book voice rules, the two precedence rankings, the dual-register boundary, the `### Summary` shape/template with a both-spellings note, and a three-pattern `dsc-ai-tell` catalogue row (down from seven, reflecting step 1's regex removals). `design-review.md` gained the hybrid reading-narrative producer contract in § Output format, scoped to comprehension and the mental model with the prose AI-tell axis reserved to the readability auditor (S4/R5), plus the `### Summary` structural finding and TOC row. `edit-design/SKILL.md` extended the comprehension-gate return-parse contract to accept the hybrid on both output-path branches and route the narrative into the Step-6 author rework input, and gained the D9 link-staleness re-read reminder in the Step-1 mutation discipline. `create-plan/SKILL.md`'s Step-4b inline-gate contract now accepts the hybrid return shape. `planning.md` and `review-workflow-pr/SKILL.md` had their `TL;DR` shape references renamed. Every grep-derived `TL;DR` shape site was renamed; only D4 legacy-acceptance mentions remain.
**What was discovered:** `workflow-reindex.py` processes staged copies under `_workflow/staged-workflow/`, not just live `.claude/**`, and it regenerates each TOC row's summary from the section's own HTML-comment `summary="…"` field — a hand-edited TOC row is overwritten on `--write`, so a changed summary must be edited in the section comment. It also enforces a 120-char cap on the summary field, which the new voice / dual-register / Output-format section comments initially exceeded and had to be shortened.
**What changed from the plan:** No material deviation. Item 16's `create-final-design.md` `TL;DR` half was out of scope here (already renamed in step 2).
**Key files:** staged mirror — `.claude/workflow/design-document-rules.md`, `.claude/workflow/prompts/design-review.md`, `.claude/skills/edit-design/SKILL.md`, `.claude/skills/create-plan/SKILL.md`, `.claude/workflow/planning.md`, `.claude/skills/review-workflow-pr/SKILL.md`.
**Critical context:** Item 13 resolves the staged/promote-together dependency from step 3: the staged `comprehension-review.md` forward-reference to `prompts/design-review.md § Output format` now resolves — the producer contract matches its wording (short reading narrative + cited comprehension answers + mental-model verdict + structural findings + verdict, minus the prose-AI-tell rows). Both sides are staged and promote in the same Phase-4 commit. Orchestrator spot-check confirmed the staged TOC `--check` is green over all six files and the residual `TL;DR` mentions are D4 legacy-acceptance prose only.

## Validation and Acceptance
Track-level behavioral acceptance criteria:

- The `dsc-ai-tell` fixture-based fire/no-fire assertions in `test_dsc_ai_tell.py`, run in **fixture-only mode** (item 8 — a missing calibration ADR skips rather than fails), are green while staged, with no assertion or fixture line still referencing a deleted regex, and the surviving line-anchored assertions (`ANCHORED_REGRESSION_CASES`, `NEGATIVE_RANGES`) renumbered to the post-removal fixture (R3). The corpus-calibration assertions cannot resolve `REPO_ROOT` from the staged path, so they are verified at Phase-4 promotion against the live tree, not in place (R1/A3).
- New shape tests pin that `**TL;DR.**`, `### TL;DR`, and `### Summary` all pass the section-shape regexes, and that a well-formed `### Summary` design does not trip the same-shape sibling similarity check (the `MANDATORY_OR_FORM_SUBHEADINGS` guard). A Part-level `## Summary` heading is shape-exempted, confirming `SHAPE_EXEMPT_SECTION_NAMES` carries the display-case `"Summary"` its raw-title compare needs (T1).
- A scoped, ownership-distinguishing grep over the staged agent definitions shows exactly one agent claims the prose AI-tell axis (the target reader / `readability-auditor`), even though the phrase "prose AI-tell axis" occurs at several sites across the two files (R6).
- The staged `readability-auditor.md` carries the will-not-fill-in-gaps target-reader persona contract, the staged `comprehension-review.md` carries the cited-comprehension-answers obligation, and both agent frontmatters read `model: sonnet` (D3 false-clean mitigations pinned, A7).
- The three removed rules named in the always-loaded `ai-tells/SKILL.md` `description:` (negative parallelism, Title Case headings, closing phrases) are gone, while "adjective triads" remains.
- The two D10 BLUF-hardening rules are present at their three acceptance sites (staged `house-style.md` § BLUF lead / § Orientation with the exemplar pair, the `review-workflow-writing-style.md` `### BLUF lead` enforcement-criteria section at develop-state lines 78-80 — not only its § Project-context summary bullet — and the by-name self-check BLUF item).
- The staged §1.7 surface names the two new path classes: staged `conventions.md` §1.7(a)/(b)/(c)/(d)/(e)/(f), the staged `implementer-rules.md` write-routing plus pre-commit gate, and the staged `create-final-design.md` Step-4 divergence check, `cp`, and `git add` all cover `.claude/output-styles/**` and root `CLAUDE.md` (D12).
- The staged `create-plan` Step-4b inline-gate contract (A4) and the staged `edit-design` gate-return parse contract (A2) both accept the D2 hybrid narrative-plus-findings return shape, and `edit-design` routes the narrative into its Step-6 author rework input.
- `git diff --name-only` against the branch point shows no live `.claude/**` path, no live `.claude/output-styles/**` path, and no live root `CLAUDE.md` changed; every edit lands under `_workflow/staged-workflow/` (the `.claude/` mirror for the four prefixes plus output-styles, and `_workflow/staged-workflow/CLAUDE.md` for root `CLAUDE.md`). At Phase-4 the promote commit is additionally verified to *contain* `.claude/output-styles/**` and root `CLAUDE.md`, not merely that no live path changed pre-promotion (R4).

<!-- Phase A placeholder for per-step EARS/Gherkin lines. -->

<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used
verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery
<!-- Phase A placeholder — names per-step idempotence and recovery
paths once steps are decomposed. Two track-level items below are recorded
now because the replan (D12) depends on them. -->

**Phase-4 manual promotion of the three out-of-surface files.** The live `create-final-design.md` Step 4 that runs during this branch's Phase 4 knows only the four §1.7 prefixes: its `git add` (`create-final-design.md:548`) omits `.claude/output-styles`, and its `cp -r "$STAGED_DIR/.claude/." .claude/` (`:547`) copies the staged output-styles into the working tree but never reaches root `CLAUDE.md`. The standard block *ends* with `git diff --cached --quiet || git commit` (`:549`), so the augmented `cp`/`git add` must run **inside** the guarded block, immediately before that commit line — not after it. Running them after the block would stage the two path classes into a fresh index the promote commit already passed, and they would drop silently from the PR (the exact failure D12 exists to prevent, R4/A3):

```bash
# ... inside the create-final-design.md Step-4 guarded block, after the four-prefix
# `git add` (:548) and BEFORE the `git diff --cached --quiet || git commit` (:549):
# output-styles is already copied by the live `cp -r "$STAGED_DIR/.claude/." .claude/`;
# root CLAUDE.md is not, so copy it explicitly:
cp "$STAGED_DIR/CLAUDE.md" ./CLAUDE.md
# the live `git add` omits both new path classes — add them so they ride the promote commit:
git add .claude/output-styles CLAUDE.md
```

If the standard block was already run verbatim and the promote commit exists, fold the two path classes in with `git add .claude/output-styles CLAUDE.md && git commit --amend --no-edit` before `git push`. Extend the Step-4 divergence check the same way (add `.claude/output-styles` and `CLAUDE.md` to its `git log ... --` path list) so a develop-side change to either path halts promotion rather than being silently overwritten.

**Post-rebase mirror refresh (A4).** The divergence-halt recovery is a rebase (§1.7(f)); after the rebase the check passes, but the hand-bootstrapped mirrors are full-file copies taken at branch time and still hold pre-rebase content, so a straight re-promote would `cp` a stale mirror over develop's rebased changes and silently revert them. Before re-running promotion, for each hand-bootstrapped mirror (`_workflow/staged-workflow/CLAUDE.md` and the two `_workflow/staged-workflow/.claude/output-styles/**` files) re-copy the rebased live file into the mirror and re-apply this branch's edits, then promote. This is this track's recovery procedure, not a §1.7 redesign — the four-prefix surface shares the latent gap but is out of this track's scope.

The step is idempotent: a re-run copies identical content and `git add` produces an empty index for already-committed paths, so the Step-4 `git diff --cached --quiet || git commit` short-circuit yields no second commit. The staged extended `create-final-design.md` goes live only at this promotion, so it governs future branches; this branch runs the augmented block and the mirror refresh by hand.

**Staged `dsc-ai-tell` corpus assertions.** The corpus-calibration assertions in `test_dsc_ai_tell.py` resolve their corpus through `REPO_ROOT` (`parents[3]`), which points at `staged-workflow/` while staged and cannot reach `docs/adr/**` (R1/A3). Recovery: run the fixture-only mode (item 8) during Phase B — the fixture paths resolve under the staged `.claude/**` mirror and pass in place — and run the corpus-calibration assertions once at Phase-4 promotion against the promoted live tree. A red corpus assertion at promotion is a promotion blocker, not a Phase-B one. That risk is near-nil — `assert_calibration_adrs` enforces a zero-false-positive contract over `dsc-ai-tell` findings only, and this track only removes regexes (a removal cannot raise a zero count) while the `### Summary` additions touch shape checks the corpus filter never sees — so an optional Phase-B no-surprise check retires it: run the staged `design-mechanical-checks.py` by hand over the three live calibration ADRs (`docs/adr/{persist-visible-count,index-gc,non-durable-wow}/adr.md`) and confirm zero `dsc-ai-tell` findings (A10).

## Artifacts and Notes
<!-- Continuous-log (rare). Cross-step artifact references that don't
belong to one specific step. Often empty. -->

## Interfaces and Dependencies
This is a single-track change: the whole planned work is Track 1, so there are no inter-track dependencies. Most implementation edits land as staged copies under `_workflow/staged-workflow/.claude/**` mirroring the live paths below (copy-then-edit on first touch, §1.7(e)). The three out-of-surface files — the two `.claude/output-styles/**` files and root `CLAUDE.md` — mirror to `_workflow/staged-workflow/.claude/output-styles/**` and `_workflow/staged-workflow/CLAUDE.md`; the live gate does not auto-route them this branch, so the implementer writes those mirror copies by hand (D12).

**In-scope files, with what changes in each:**

1. `.claude/output-styles/house-style.md` — remove the six disguise-only rules (D1/D7); swap the § Voice and tone writer persona to a technical writer (D5); rename the § Navigability `TL;DR` shape to `### Summary` (D4); add the § BLUF lead / § Orientation hardening rules plus the exemplar pair and the self-check BLUF item (D10); renumber the self-check list after the removals. The self-check surgical sub-edits are enumerated by the same name+exemplar grep over house-style.md's own body (A9): items mixing kept and removed clauses take clause-level edits, not whole-item deletion — e.g. the mid-list "sentence case on H2+" clause is removed while the rest of its item stays. Out-of-surface file — mirrored to the staged subtree by hand (D12).
2. `.claude/output-styles/house-conversation.md` — remove the mirrored disguise-rule lines (D1); change the chat reader from senior engineer to mid-level developer. Out-of-surface file — mirrored to the staged subtree by hand (D12).
3. `.claude/agents/readability-auditor.md` — Sonnet pin; target-reader persona; persona-voiced structured findings (D3/D6/D2).
4. `.claude/agents/comprehension-review.md` — Sonnet pin; time-constrained-reviewer persona; hybrid narrative-plus-findings output, scoped to mental-model and comprehension, off the prose AI-tell axis S4 reserves for the target reader; its `TL;DR` structural-finding site (D3/D6/D2/D4/R5).
5. `.claude/agents/design-author.md` — Opus pin note (never Fable); technical-writer voice mandate (D3/D5).
6. `.claude/agents/review-workflow-writing-style.md` — drop the removed-rule criteria at every hit of a case-insensitive grep of the six removed-rule names **and their exemplar phrases** (develop-state snapshot floor: lines 29, 34, 38, 71, 89, 185, 188, 200 — the set is grep-derived and a superset of this floor, not a hand-maintained list; line 185 is the exemplar-only site a names-only grep misses, T2/R3/A1), surgically keeping the kept-rule mentions and the "banned sentence patterns" category label (D1/D7/R4); retarget the `**WS<N>**` finding-format template axis token (line 200) and the `#### Critical` / `#### Recommended` finding-format buckets (185, 188) so their "negative parallelism" / "closing-phrase" Cost examples no longer name removed rules (the surviving heading rule is one-H1-per-file; swap the Cost examples to kept-rule ones); add the two D10 BLUF criteria to the `### BLUF lead` enforcement-criteria section (develop-state lines 78-80) as well as the § Project-context summary bullet (28), so the reviewer's detailed criteria actually enforce D10 (T3); the adjective-triads rule stays.
7. `.claude/scripts/design-mechanical-checks.py` — delete `NEGATIVE_PARALLELISM_RE`, `NEGATIVE_PARALLELISM_TRAILING_RE`, `HYPHENATED_PAIR_CLUSTER_RE`, and the Title-Case check (D1); `section_has_tldr` gains `### Summary`; `SHAPE_EXEMPT_SECTION_NAMES` gains display-case `"Summary"` and `MANDATORY_OR_FORM_SUBHEADINGS` gains lowercase `"summary"`, each matching its set's compare case (D4/T1).
8. `.claude/scripts/tests/test_dsc_ai_tell.py` — remove the assertions of the deleted regexes and their `PATTERN_SIGNATURES` entries (D1); the regex, its `PATTERN_SIGNATURES` entry, test, and fixture are one unit; renumber the surviving line-anchored assertions (`ANCHORED_REGRESSION_CASES`, `NEGATIVE_RANGES`) after the fixture removals shift line numbers (R3); add a **fixture-only run mode** — treat a missing calibration ADR as a skip, or gate `assert_calibration_adrs()` behind a CLI flag / env var — so Phase B runs the fixture assertions green while staged (this edit is behavior-bearing and stages/promotes like the rest, T4/R1); the corpus-calibration assertions cannot resolve `REPO_ROOT` from the staged path, so they run at Phase-4 promotion, not in place (R1/A3).
9. `.claude/scripts/tests/fixtures/dsc-ai-tell-fixture.md` — remove the fixture lines that fed the deleted regexes (D1); the removal shifts the line numbers the survivors' anchored assertions target, renumbered in `test_dsc_ai_tell.py` (R3).
10. dsc shape tests — add both-spellings acceptance cases for the Summary rename, following the `test_design_mechanical_checks_d11.py` both-spellings precedent (new file or an extension; Phase A decides placement) (D4).
11. `.claude/skills/ai-tells/SKILL.md` — edit the always-loaded `description:` frontmatter that names three removed rules; update the body catalogue entries (D1).
12. `.claude/workflow/design-document-rules.md` — `### Summary` shapes and templates (D4); the book-rule transfer and the two precedence rankings (D9); the dual-register boundary (D8); the `dsc-ai-tell` catalogue row (D1).
13. `.claude/workflow/prompts/design-review.md` — rename every `TL;DR` site a grep over the file returns (D4; the structural-findings cluster and TOC row are orientation, not the full edit set — the grep also reaches the design-sync accuracy clause and comprehension question 4, which instruct the gate to read/verify the block, A6); the hybrid output contract for the comprehension gate, scoped to mental-model and comprehension rather than prose AI-tells (D2/R5).
14. `.claude/skills/edit-design/SKILL.md` — every `TL;DR` site a grep over the file returns (D4; grep-derived, not a hand-count, A6); the D9 link-staleness re-read reminder on section-move/remove/rename; extend the comprehension-gate return-shape parse contract (develop-state lines ~664-676) to accept the D2 hybrid (narrative + Verdict + structural findings) on both output-path branches and route the narrative into the Step-6 author rework input (A2).
15. `.claude/workflow/planning.md` — its `TL;DR` site (D4).
16. `.claude/workflow/prompts/create-final-design.md` — its `TL;DR` site (D4); extend the Step-4 promotion — the divergence check, the `git add`, and an explicit `cp` for root `CLAUDE.md` (which the live `cp -r "$STAGED_DIR/.claude/." .claude/` does not reach) — to cover `.claude/output-styles/**` and root `CLAUDE.md` (D12).
17. `.claude/skills/review-workflow-pr/SKILL.md` — its `TL;DR` site (D4).
18. `.claude/workflow/conventions.md` — the §1.5 tier pointers and its `TL;DR` site (D4); extend §1.7(a)/(b)/(c)/(d)/(e)/(f) to add `.claude/output-styles/` and a repo-root `CLAUDE.md` mapping to the staging surface (D12).
19. `CLAUDE.md` — the § Writing Style negative-parallelism parenthetical (D1). Out-of-surface file (repo root) — mirrored to `_workflow/staged-workflow/CLAUDE.md` by hand (D12).
20. `.claude/workflow/implementer-rules.md` — add `.claude/output-styles/**` and root `CLAUDE.md` to the write-routing rule and the pre-commit `git diff --cached --name-only` live-path gate (D12).
21. `.claude/skills/create-plan/SKILL.md` — the Step-4b inline-gate consumer contract accepts the D2 hybrid narrative-plus-findings shape (A4).

**Out of scope:** legacy committed designs under `docs/adr/**` (no backfill); `absorption-check.md` and `fidelity-check.md` (unchanged, personless); the live `.claude/**` tree, the live `.claude/output-styles/**`, and the live root `CLAUDE.md` until Phase-4 promotion; this branch's own `_workflow/` authoring artifacts (written under the current live rules).

**Sizing:** ~21 in-scope files — the replan adds `implementer-rules.md` and `create-plan/SKILL.md` to the earlier ~19 — within the ~20–25 ceiling and above the ~12 floor. The change stays cohesive and single-track: the §1.7 extension is the enabling mechanism for promoting the branch's own style files, not a separable unit of work, so it belongs in Track 1. No further autonomous unit exists to pack, so the single-track argumentation gate is satisfied by "this is the whole change".

## Invariants & Constraints
<!-- Plan-at-start, combined section (D9): per-track testable constraints
and testable invariants; each invariant becomes a test assertion in the
relevant step. Process-only constraints go to Context and Orientation or
the Decision Log instead. -->

- **Removal completeness.** After the removals, no removed-pattern regex — including both `NEGATIVE_PARALLELISM_RE` and `NEGATIVE_PARALLELISM_TRAILING_RE` (T2) — and no fixture line or assertion for a removed pattern remains in the `dsc-ai-tell` surfaces, and the surviving line-anchored assertions are renumbered (R3). (Assert: the fixture-based assertions run green in place in fixture-only mode (item 8); grep the three files for the removed regex names returns nothing. Corpus-calibration assertions verified at Phase-4 promotion, R1/A3.)
- **Both-spellings acceptance.** `**TL;DR.**`, `### TL;DR`, and `### Summary` all pass the section-shape regexes. (Assert: new shape tests, modeled on `test_design_mechanical_checks_d11.py`.)
- **Summary added case-correctly to both sets.** `MANDATORY_OR_FORM_SUBHEADINGS` carries lowercase `"summary"` (compared lowercased) and `SHAPE_EXEMPT_SECTION_NAMES` carries display-case `"Summary"` (compared against raw titles), so a well-formed `### Summary` design does not false-positive on the same-shape sibling check and a Part-level `## Summary` is shape-exempted (T1). (Assert: a shape test feeding a multi-`### Summary` design expects no consolidation finding, and a `## Summary` Part heading is exempted.)
- **Single prose-axis owner (S4).** Exactly one staged agent definition claims the prose AI-tell axis (the target reader / `readability-auditor`), and the comprehension gate's hybrid narrative stays off that axis (R5). (Assert: a scoped, ownership-distinguishing grep over the staged agent definitions — matching the claim, not every occurrence of the phrase, R6.)
- **Live-tree isolation (§1.7 / I6).** The branch diff touches no live `.claude/**` path, no live `.claude/output-styles/**` path, and no live root `CLAUDE.md`; every edit is under `_workflow/staged-workflow/` (the `.claude/` mirror plus `_workflow/staged-workflow/CLAUDE.md`). (Assert: `git diff --name-only` scope check.) **Per-step guard (R5/A5):** the live pre-commit gate does not watch `.claude/output-styles/**` or root `CLAUDE.md` this branch, so every step touching one of the three out-of-surface files carries an inline copy-then-edit-into-mirror instruction and a per-commit assertion — `git diff --cached --name-only -- .claude/output-styles CLAUDE.md` must be empty before the commit — shrinking the detection window from track-level to per-commit.
- **§1.7 surface extension (D12).** The staged §1.7, the staged `implementer-rules.md` gate, and the staged `create-final-design.md` promotion all name `.claude/output-styles/**` and root `CLAUDE.md`. (Assert: grep the staged copies for both path classes at each of the three surfaces.)
- **D10 rules present at three sites.** The self-contained-lead and body-stands-without-the-lead rules are greppable in the staged `house-style.md`, `review-workflow-writing-style.md`, and the self-check BLUF item.

## Base commit
Phase B base commit (HEAD at Phase B startup, before the first implementer spawn): `76ba1a4ed2509e1f8f5acceb44441f7ac34823a1`.
