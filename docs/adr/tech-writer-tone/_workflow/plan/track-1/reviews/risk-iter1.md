<!-- MANIFEST
findings: 6   severity: {blocker: 0, should-fix: 5, suggestion: 1}
index:
  - {id: R1, sev: should-fix, loc: ".claude/scripts/tests/test_dsc_ai_tell.py:288", anchor: "### R1 ", cert: C1, basis: "assert_calibration_adrs hard-fails on the 3 missing staged docs/adr ADRs and main() returns 1 if any failure; no exists-skip or flag separates the deferred corpus assertions from the in-place fixture assertions, so the staged suite cannot exit 0 and the plan's 'fixture assertions green while staged' acceptance is unsatisfiable — which also blocks in-place validation of the R3 renumbering"}
  - {id: R2, sev: should-fix, loc: ".claude/scripts/tests/test_design_mechanical_checks_d11.py:183", anchor: "### R2 ", cert: C2, basis: "the new D4 shape tests (item 10) follow the d11 precedent, which FATAL-aborts on a missing FROZEN_DESIGN=docs/adr/plan-slimization frozen design and asserts on it; a copy-modeled test staged under staged-workflow resolves that corpus path to a non-existent staged-workflow/docs/adr and cannot run green staged, contradicting the item-10/line-169 acceptance"}
  - {id: R3, sev: should-fix, loc: "docs/adr/tech-writer-tone/_workflow/plan/track-1.md:143", anchor: "### R3 ", cert: C3, basis: "the stated drop-site method ('case-insensitive grep of the six removed-rule NAMES') does not reproduce the plan's own snapshot including 185: line 185 carries the negative-parallelism EXEMPLAR string with no rule name, so a name-grep misses it; following the method literally leaves the Phase-C reviewer's Critical bucket naming a removed rule (R4-class contradiction)"}
  - {id: R4, sev: should-fix, loc: "docs/adr/tech-writer-tone/_workflow/plan/track-1.md:187", anchor: "### R4 ", cert: C6, basis: "the Phase-4 augmented-promotion recovery contradicts itself on ordering — prose says run cp+git add 'After the standard Step-4 block' (which ends in git commit) while the inline comment says 'before the commit'; run as prose states, the 3 out-of-surface files are staged-but-uncommitted and drop from the promote commit, the exact silent-drop D12 exists to prevent"}
  - {id: R5, sev: should-fix, loc: "docs/adr/tech-writer-tone/_workflow/plan/track-1.md:115", anchor: "### R5 ", cert: C7, basis: "no automated pre-commit gate watches .claude/output-styles/** or root CLAUDE.md this branch (D12's own account); the sole guard is manual discipline + a git diff --name-only at track acceptance, which catches a stray live edit before merge but not the mid-branch window in which concurrent active branches read the contaminated live source-of-truth house-style.md"}
  - {id: R6, sev: suggestion, loc: "docs/adr/tech-writer-tone/_workflow/plan/track-1.md:139", anchor: "### R6 ", cert: C8, basis: "each regex removal is a five-artifact 'one unit' (regex + fixture block + PATTERN_SIGNATURES + anchored/negative renumbering); the plan says 'in one change' but sets no decomposition-time atomicity constraint, so a step split that lands the regex delete without its fixture/assertion red-builds at the intermediate commit"}
evidence_base: {section: "## Evidence base", certs: 8, matches: 6}
cert_index:
  - {id: C1, verdict: DIFFICULT, anchor: "#### C1 "}
  - {id: C2, verdict: DIFFICULT, anchor: "#### C2 "}
  - {id: C3, verdict: CONTRADICTED, anchor: "#### C3 "}
  - {id: C4, verdict: VALIDATED, anchor: "#### C4 "}
  - {id: C5, verdict: VALIDATED, anchor: "#### C5 "}
  - {id: C6, verdict: RESIDUAL-MEDIUM, anchor: "#### C6 "}
  - {id: C7, verdict: RESIDUAL-MEDIUM, anchor: "#### C7 "}
  - {id: C8, verdict: RESIDUAL-LOW, anchor: "#### C8 "}
flags: [CONTRACT_OK]
-->

# Risk review — Track 1 (Style-machinery rework), iteration 1

This track is safe to implement, but four staging-and-verification gaps should be closed at decomposition before Phase B relies on them. The change is single-track, workflow-modifying (`s17=staged` on the phase ledger), Markdown + Python under `.claude/**` plus repo-root `CLAUDE.md`; no Java, storage, WAL, or transaction path is in scope. Per the branch context, named references were verified as workflow file paths, `§`-anchors, and Python identifiers via grep + Read against the live tree; `_workflow/staged-workflow/` does not exist yet, so every `.claude/**` read resolved to the live file per §1.7(d) — correct, because the branch authors under the current live rules. mcp-steroid is not reachable this session; grep is the right tool here, since the in-scope symbols are Python identifiers and Markdown anchors, which PSI does not index for reference search. No reference-accuracy caveat attaches to any symbol-based claim, because no Java symbol audit was needed.

The five prose criteria (rule coherence and non-contradiction, instruction completeness, prompt-design soundness, context-budget impact, breakage of dependent prompts or agents) supersede the Java-oriented WAL/crash/migration/hot-caller criteria for the workflow prose here. Critical-path exposure is re-pointed to the workflow machinery's own critical paths: the `dsc-ai-tell` build coupling, the §1.7 staging surface, and the Phase-4 promotion path.

The replan (D12) resolved several prior iter-1 findings. The `SHAPE_EXEMPT_SECTION_NAMES` / `MANDATORY_OR_FORM_SUBHEADINGS` case-correctness is now specified per-set (prior R2, now C4 VALIDATED), the fixture line-anchor renumbering is handled at the edit level (prior R3, now covered by the plan's R3 handling), the writing-style multi-site drop is reframed as grep-derived (prior R4), and the S4 single-owner grep is scoped and the comprehension-gate narrative is bounded off the prose AI-tell axis (prior R5/R6). The findings below are what the replan did not fully mechanize.

## Findings

### R1 [should-fix]
**Certificate**: C1 (Testability — staged fixture-assertion run).
**Location**: Track acceptance "the `dsc-ai-tell` fixture-based fire/no-fire assertions in `test_dsc_ai_tell.py` are green while staged" (`track-1.md:168`) and the Removal-completeness invariant "the fixture-based assertions run green in place" (`track-1.md:242`); source `test_dsc_ai_tell.py:288-292`, `:321`, `:323-328`.
**Issue**: The plan splits verification into "run the fixture-based assertions in place during Phase B; run corpus-calibration at Phase-4 promotion" (`track-1.md:151`, `:199`), but the test as written cannot express that split. `main()` runs the four fixture assertions and then calls `assert_calibration_adrs()` unconditionally (`:321`); that function appends a hard failure — not a skip — for each of the three calibration ADRs missing on disk (`if not adr.exists(): failures.append(...)`, `:288-292`), and `main()` returns 1 whenever any failure accumulates (`:323-328`). From the staged path `_workflow/staged-workflow/.claude/scripts/tests/test_dsc_ai_tell.py`, `REPO_ROOT = parents[3]` (`:59`) resolves to `staged-workflow/`; `SCRIPT` and `FIXTURE` (`:60-62`) resolve under that root and do exist (both are staged), so the fixture assertions execute against the edited checker and edited fixture — but `CALIBRATION_ADRS` (`:64-68`) resolve to `staged-workflow/docs/adr/**`, which do not exist, so the suite always exits 1. There is no CLI flag, env var, or exists-skip to run the fixture assertions alone. So the "green while staged" acceptance is not satisfiable by running the staged test, and — the sharper consequence — the R3 line-anchor renumbering (the survivors' anchors in `ANCHORED_REGRESSION_CASES` and `NEGATIVE_RANGES`) is validated only by these same fixture assertions, so a mis-renumbered survivor would surface only at Phase-4 promotion, a late and expensive discovery. Likelihood high (the implementer hits it the first time they try to prove the fixture green); impact is a blocked Phase-B validation loop plus deferred detection of a renumber slip.
**Proposed fix**: Add a decomposition step giving the staged `test_dsc_ai_tell.py` a way to run the fixture assertions without the corpus assertions — either turn the missing-ADR case into a skip (treat `not adr.exists()` as skip, not failure) or gate `assert_calibration_adrs()` behind a CLI flag / env var, so Phase B runs fixture-only green and Phase 4 runs the full suite against the promoted live tree. This edit is itself behavior-bearing, so it stages and promotes like the rest. Reword `track-1.md:168`/`:242` to state that the fixture-only mode is what "green while staged" means.

### R2 [should-fix]
**Certificate**: C2 (Testability — new shape tests inherit the staged-path corpus dangle).
**Location**: Track item 10 "add both-spellings acceptance cases … following the `test_design_mechanical_checks_d11.py` both-spellings precedent" (`track-1.md:219`) and acceptance "New shape tests pin that … all pass … while staged" (`track-1.md:169`); source `test_design_mechanical_checks_d11.py:69-70`, `:183-185`, `:215-217`.
**Issue**: The named precedent references a live-corpus input the new tests would inherit if copied structurally. `test_design_mechanical_checks_d11.py` sets `FROZEN_DESIGN = REPO_ROOT / "docs" / "adr" / "plan-slimization" / "_workflow" / "design.md"` (`:69-70`), aborts with "FATAL: required input missing" if it is absent (`:183-185`), and asserts on it (`:215-217`). A shape test copy-modeled on it and staged under `staged-workflow/.claude/scripts/tests/` resolves `FROZEN_DESIGN` to `staged-workflow/docs/adr/…`, which does not exist, so it FATAL-aborts before it ever tests the `### Summary` shapes — the same class of staged-path dangle as R1, and it defeats the item-10 / line-169 acceptance. The fixture inputs (`FIXTURE_DIR`, `:63`) do stage correctly, so the tests can be written to avoid the corpus reference; the risk is an implementer copying d11's `FROZEN_DESIGN` block verbatim. Likelihood medium (the plan tells the implementer to model on d11 without flagging the corpus line); impact is a shape test that cannot run staged.
**Proposed fix**: In decomposition, direct the new shape tests to use only fixture inputs under the staged `tests/fixtures/` (both-spellings pass fixtures plus a multi-`### Summary` design and a Part-level `## Summary` design), with no `docs/adr/**` back-reference. If a corpus-calibration case is wanted, defer it to Phase 4 the same way R1 defers the `dsc-ai-tell` corpus assertions.

### R3 [should-fix]
**Certificate**: C3 (Assumption — the grep-derived drop-site method reproduces the plan's snapshot).
**Location**: "the definitive drop-site set is whatever a case-insensitive grep of the six removed-rule names returns across the file — the enumeration is grep-derived, not a hand-maintained line list" (`track-1.md:143`, `:215`), with develop-state snapshot {29, 34, 38, 71, 89, 185, 188, 200}; source `review-workflow-writing-style.md:185`.
**Issue**: The stated method and the stated result disagree at line 185. A case-insensitive grep of the six removed-rule names (negative parallelism, roundabout negation, closing phrases, hyphenated word-pair, curly quotes, Title/sentence case) over the live file returns {29, 34, 38, 71, 89, 188, 200}, not the plan's {29, 34, 38, 71, 89, 185, 188, 200}. Line 185 (`[Hard violations — "It's not X — it's Y" anti-pattern in load-bearing position, …]`) carries the negative-parallelism *exemplar string*, not any rule name, so a name-grep cannot see it. The plan's own snapshot includes 185, which means the snapshot was produced by a broader grep that also matched exemplar strings — a method the plan does not describe. Because the plan actively steers the implementer to re-run the grep rather than trust the snapshot ("not a hand-maintained line list", "three plan-review iterations each found the hand-list incomplete"), an implementer following the literal method gets seven lines and leaves line 185 un-retargeted, so the Phase-C writing-style reviewer's Critical bucket keeps naming "It's not X — it's Y" as a hard violation of a rule house-style no longer bans (the R4-class rule contradiction the drop is meant to remove). Likelihood medium-high; impact is a surviving enforcement of a removed rule at the most load-bearing severity of the Phase-C reviewer.
**Proposed fix**: Specify the drop-site grep to include the removed rules' exemplar strings, not only their names — at minimum `It'?s not .* it'?s`, `In conclusion`, and the curly-quote glyphs — so it reproduces the {…185…} snapshot; or state that the drop set is the union of the name-grep and the recorded snapshot, and that line 185's Critical-bucket exemplar is retargeted to a kept banned pattern. Add line 185 explicitly to item 6's edit list either way.

### R4 [should-fix]
**Certificate**: C6 (Exposure — Phase-4 augmented-promotion sequencing).
**Location**: `## Idempotence and Recovery` "Phase-4 manual promotion of the three out-of-surface files" (`track-1.md:187-197`); source `create-final-design.md:547-549`.
**Issue**: The manual bootstrap that D12 relies on can silently drop its three files if run as its prose reads. The live Step-4 block is `cp -r "$STAGED_DIR/.claude/." .claude/` (`create-final-design.md:547`), then `git add .claude/workflow .claude/skills .claude/agents .claude/scripts` (`:548`), then `git diff --cached --quiet || git commit …` (`:549`) — so the block ends by committing. The recovery text says to run `cp "$STAGED_DIR/CLAUDE.md" ./CLAUDE.md` + `git add .claude/output-styles CLAUDE.md` "After the standard Step-4 block" (`track-1.md:187`), while the inline comment inside the same snippet says "add them before the commit" (`track-1.md:193`). The two orderings contradict. Run as the prose states (after the block), the `git add` stages the three out-of-surface paths into a fresh index that the block's `git commit` already passed, and the snippet carries no follow-up commit, so `.claude/output-styles/**` and root `CLAUDE.md` land staged-but-uncommitted and drop from the promote commit — the exact silent-drop failure D12 was written to prevent. (`cp -r "$STAGED_DIR/.claude/."` does copy the staged output-styles into the working tree at `:547`, which makes the drop silent rather than an obvious missing-file error; root `CLAUDE.md` is not under `.claude/` and is not copied at all until the explicit `cp`.) Likelihood medium (it depends on which of the two contradictory instructions the implementer follows); impact is the promotion of this branch's own style rules quietly missing from the PR.
**Proposed fix**: Rewrite the recovery so the `cp "$STAGED_DIR/CLAUDE.md"` and the `git add .claude/output-styles CLAUDE.md` run *before* the block's `git diff --cached --quiet || git commit`, i.e. interleave them into the standard block rather than after it; or append an explicit `git add … && git commit --amend --no-edit` step. Remove the contradictory "After the standard Step-4 block" phrasing. Confirm the acceptance `git diff --name-only` (`track-1.md:175`) also checks that the promote commit *contains* the two path classes, not only that no live path changed pre-promotion.

### R5 [should-fix]
**Certificate**: C7 (Exposure — live-tree isolation of the out-of-surface files).
**Location**: D12 risk/caveat "the live pre-commit gate does not watch output-styles or root `CLAUDE.md` this branch" (`track-1.md:115`), the Live-tree-isolation invariant (`track-1.md:246`), and the acceptance `git diff --name-only` check (`track-1.md:175`); enforcement gate `implementer-rules.md` (develop-era, four-prefix per D12's own account at `track-1.md:112`, `:229`).
**Issue**: For the two highest-stakes files in the change there is no automated commit-time guard this branch, only manual discipline. D12 itself records that the develop-era `implementer-rules.md` pre-commit `git diff --cached --name-only` gate keys on the four §1.7 prefixes and must be *extended* to reach `.claude/output-styles/**` and repo-root `CLAUDE.md` (`track-1.md:112`, `:229`); that extension is staged and goes live only at promotion. So during Phase B a stray *live* edit to `.claude/output-styles/house-style.md` — the source of truth every authoring loop reads — is not blocked at commit. The `git diff --name-only` at track acceptance (`track-1.md:175`) catches such an edit before merge, which protects PR hygiene, but it does not protect the window between the stray edit and the acceptance check: concurrent active branches (transactional-schema, ir-filter-step per the branch index) run their `edit-design` loops against the live `house-style.md`, so a mid-branch live edit contaminates them immediately — the cross-branch contamination the staging discipline exists to prevent. Likelihood low (it requires an implementer to bypass the manual mirror-routing); impact high (a source-of-truth edit changes the rules other in-flight branches author under).
**Proposed fix**: Make the isolation check per-step rather than only at track acceptance: add to each step's validation a cheap scoped `git diff --name-only -- .claude/output-styles CLAUDE.md` that must be empty before the step commits, so a stray live edit is caught within one step instead of at end-of-track. Note in the step files that these two paths have no automated gate this branch and the scoped diff is their stand-in.

### R6 [suggestion]
**Certificate**: C8 (Exposure — removal "one unit" atomicity across decomposition).
**Location**: `## Plan of Work` "deleting the regex while leaving the fixture and assertion breaks the test suite at build time" and "a partial delete leaves the trailing regex enforcing a rule house-style no longer bans (T2)" (`track-1.md:139`); Removal-completeness invariant (`track-1.md:242`); source `design-mechanical-checks.py:103`, `:127`, `:191`, `:1734`.
**Issue**: The build-breakage exposure is real but well-guarded by the plan's own sequencing; the residual risk is that decomposition splits the unit. Each of the three regex removals is a five-artifact edit — the regex in `design-mechanical-checks.py`, its positive block in `dsc-ai-tell-fixture.md`, its `PATTERN_SIGNATURES` entry and any anchored assertion in `test_dsc_ai_tell.py`, and the renumbering of every surviving line-anchored assertion the block shift moves (R3) — and for negative parallelism the unit spans two regex objects (`NEGATIVE_PARALLELISM_RE` at `:103` and `NEGATIVE_PARALLELISM_TRAILING_RE` at `:127`, T2). The plan says "in one change" repeatedly but sets no explicit decomposition-time constraint that these five artifacts stay in one step/commit, so a decomposer that splits (say) the regex delete from the fixture delete red-builds the staged test at the intermediate commit. Likelihood low (the plan's guidance is clear and decomposition immediately follows this review), impact bounded (a red intermediate commit, caught locally).
**Proposed fix**: In decomposition, make "one removal = one atomic step touching all of {regex, fixture block, `PATTERN_SIGNATURES`, anchored/negative renumbering}" an explicit per-step constraint in `## Concrete Steps`, and keep both negative-parallelism regexes in the same step per T2. State it as a testable step invariant so the code review checks it.

## Evidence base

#### C1 Testability: run the `dsc-ai-tell` fixture assertions from the staged path during Phase B
- **Coverage target**: the track's green-suite acceptance for the fixture assertions (fire/no-fire + anchored + negative), not a line/branch percentage.
- **Difficulty assessment**: `test_dsc_ai_tell.py` computes `REPO_ROOT = Path(__file__).resolve().parents[3]` (`:59`). From `_workflow/staged-workflow/.claude/scripts/tests/test_dsc_ai_tell.py`, `parents[3]` = `staged-workflow/`. `SCRIPT` (`:60`) and `FIXTURE` (`:61-62`) resolve under that root and exist (both staged), so the fixture assertions execute against the edited checker and edited fixture. `CALIBRATION_ADRS` (`:64-68`) resolve to `staged-workflow/docs/adr/**`, which do not exist. `assert_calibration_adrs()` appends a hard failure per missing ADR (`if not adr.exists(): failures.append(...)`, `:288-292`) — no skip — and `main()` calls it unconditionally (`:321`) and returns 1 if `failures` is non-empty (`:323-328`). No CLI flag or env var separates the two assertion groups.
- **Existing test infrastructure**: the assert functions (`assert_fixture_positive` `:200`, `assert_fixture_anchored` `:214`, `assert_fixture_negative` `:231`, `assert_calibration_adrs` `:278`) are separable in code but exposed only through the single `main()` (`:307`).
- **Feasibility**: DIFFICULT.
- **Detail**: The staged suite always exits 1, so "fixture assertions green while staged" (`track-1.md:168`, `:242`) is unsatisfiable as written, and the R3 renumbering that only these assertions validate is deferred to Phase-4 promotion.

#### C2 Testability: new D4 both-spellings shape tests run green from the staged path
- **Coverage target**: item-10 acceptance that `**TL;DR.**`, `### TL;DR`, and `### Summary` all pass the shape regexes and a well-formed multi-`### Summary` design does not trip the sibling check, while staged (`track-1.md:169`).
- **Difficulty assessment**: the named precedent `test_design_mechanical_checks_d11.py` uses `REPO_ROOT = parents[3]` (`:61`) and `FROZEN_DESIGN = REPO_ROOT/docs/adr/plan-slimization/_workflow/design.md` (`:69-70`), which it treats as a required input — FATAL abort if missing (`:183-185`) — and asserts on (`:215-217`). Copy-modeled and staged, `FROZEN_DESIGN` dangles the same way `CALIBRATION_ADRS` does in C1. `FIXTURE_DIR` (`:63`) stages correctly, so the tests can be fixture-only.
- **Existing test infrastructure**: the d11 fixtures (`NEWNAME_PASS`, `BARE_FAIL`, `LEGACY_PASS`, `NESTED_PASS`, `:65-68`) are the reusable, staged-safe pattern to copy; the `FROZEN_DESIGN` corpus case is the part that must not be copied.
- **Feasibility**: DIFFICULT (avoidable — fixture-only inputs make it ACHIEVABLE).
- **Detail**: The risk is structural imitation of d11 that carries the corpus reference; the plan's item-10 wording invites it without flagging the line.

#### C3 Assumption: the "grep of the six removed-rule names" method reproduces the recorded drop-site snapshot
- **Track claim**: the drop set "is whatever a case-insensitive grep of the six removed-rule names returns across the file — the enumeration is grep-derived, not a hand-maintained line list", develop-state snapshot {29, 34, 38, 71, 89, 185, 188, 200} (`track-1.md:143`, `:215`).
- **Evidence search**: grep (case-insensitive) over `review-workflow-writing-style.md` (live = develop state per §1.7(d)) for the six removed-rule names: `negative[ -]?parallel|roundabout|closing[ -]?phrase|hyphenat|curly|title[ -]?case|sentence[ -]?case`.
- **Code evidence**: the name-grep returns {29, 34, 38, 71, 89, 188, 200}. Line 185 is `[Hard violations — "It's not X — it's Y" anti-pattern in load-bearing position, a banned sentence or analysis pattern in a CLAUDE.md or skill description]` — the negative-parallelism exemplar string with no rule name, invisible to a name-grep. The plan's snapshot nonetheless lists 185.
- **Verdict**: CONTRADICTED.
- **Detail**: The snapshot was produced by a wider grep than the stated method; following the literal method drops line 185, which leaves the Phase-C reviewer's Critical bucket enforcing the removed negative-parallelism rule (R3).

#### C4 Assumption: the two `### Summary` set additions match each set's compare case
- **Track claim**: add display-case `"Summary"` to `SHAPE_EXEMPT_SECTION_NAMES` (raw-title compare) and lowercase `"summary"` to `MANDATORY_OR_FORM_SUBHEADINGS` (lowercased compare) (`track-1.md:216`, `:244`).
- **Evidence search**: Read of `design-mechanical-checks.py` around the two sets and their compare sites.
- **Code evidence**: `is_shape_exempt` compares `section["title"] in SHAPE_EXEMPT_SECTION_NAMES` (raw title, `:737`); the current set holds display-case `"TL;DR"` (`:54`). The sibling check compares `sub.lower() not in MANDATORY_OR_FORM_SUBHEADINGS` (lowercased, `:1335`); the current set holds lowercase `"tl;dr"` (`:63`). The plan's case assignment matches both.
- **Verdict**: VALIDATED.
- **Detail**: No finding; this closes prior iter-1 R2. The T1 invariant (`track-1.md:244`) is testable via a multi-`### Summary` fixture plus a `## Summary` Part-heading fixture.

#### C5 Assumption: `.claude/output-styles/**` and root `CLAUDE.md` fall outside the §1.7 surface, and the marker's stable-prefix property lets D12 grow the enumeration
- **Track claim**: D12 — the three central files are outside the four-prefix §1.7 surface, and the develop-era marker stays verbatim while the staged definition adds prefixes (`track-1.md:110-114`).
- **Evidence search**: grep + Read of `conventions.md` §1.7(a) and §1.7(b).
- **Code evidence**: §1.7(a) mirror layout enumerates only `workflow/skills/agents/scripts` (`conventions.md:975-978`) and states "No other prefixes participate" (`:991`). §1.7(b) records the stable-prefix property: a plan can "keep its develop-state marker verbatim while the staged definition adds prefixes" (`:1039-1041`). Step-4 promotion `git add`s exactly those four prefixes (`create-final-design.md:548`).
- **Verdict**: VALIDATED.
- **Detail**: D12's premise (the surface gap and the marker-growth mechanism) holds; no finding on the premise. Its promotion-path consequence is C6.

#### C6 Exposure: Phase-4 augmented promotion of the three out-of-surface files
- **Track claim**: Phase 4 promotes `.claude/output-styles/**` and root `CLAUDE.md` by a manual augmented block in the same promote commit (`track-1.md:187-197`).
- **Critical path trace**:
  1. `create-final-design.md` Step 4 runs `cp -r "$STAGED_DIR/.claude/." .claude/` (`:547`) — copies the staged output-styles into the live working tree, but not root `CLAUDE.md` (outside `.claude/`).
  2. `git add .claude/workflow .claude/skills .claude/agents .claude/scripts` (`:548`) — omits `.claude/output-styles` and `CLAUDE.md`.
  3. `git diff --cached --quiet || git commit …` (`:549`) — the block ends by committing.
  4. The recovery's `git add .claude/output-styles CLAUDE.md` runs "After the standard Step-4 block" per prose (`track-1.md:187`) yet "before the commit" per the inline comment (`track-1.md:193`) — a contradiction.
- **Blast radius**: run as the prose reads, the two path classes are staged after the block's commit with no follow-up commit in the snippet, so they drop from the promote commit; the copied-but-uncommitted output-styles change makes the drop silent.
- **Existing safeguards**: the augmented divergence check (`track-1.md:197`) and the idempotence short-circuit — neither fixes ordering.
- **Residual risk**: MEDIUM — this branch's own style-rule promotion can quietly miss the PR (R4).

#### C7 Exposure: live-tree isolation of `.claude/output-styles/**` and root `CLAUDE.md` this branch
- **Track claim**: the live gate does not watch these paths; manual discipline plus a `git diff --name-only` at acceptance is the guard (`track-1.md:115`, `:175`, `:246`).
- **Critical path trace**:
  1. Per D12's own account, the develop-era `implementer-rules.md` pre-commit `git diff --cached --name-only` gate keys on the four §1.7 prefixes and must be extended to reach the two out-of-surface path classes (`track-1.md:112`, `:229`); the extension is staged, live only at promotion.
  2. A live edit to `.claude/output-styles/house-style.md` therefore commits without a block.
  3. Concurrent active branches read the live `house-style.md` in their `edit-design` loops.
- **Blast radius**: a mid-branch stray live edit to the source of truth contaminates concurrent branches (transactional-schema, ir-filter-step) immediately; the acceptance `git diff --name-only` catches it before merge but not within the contamination window.
- **Existing safeguards**: manual copy-then-edit mirror routing; acceptance-time `git diff --name-only`.
- **Residual risk**: MEDIUM — low-likelihood, high-impact cross-branch rule contamination (R5).

#### C8 Exposure: removal "one unit" atomicity across decomposition
- **Track claim**: each regex removal deletes the regex, its fixture lines, and its assertions in one change; both negative-parallelism regexes go together (T2) (`track-1.md:139`, `:242`).
- **Critical path trace**:
  1. Regex objects live in `design-mechanical-checks.py` (`NEGATIVE_PARALLELISM_RE:103`, `NEGATIVE_PARALLELISM_TRAILING_RE:127`, `HYPHENATED_PAIR_CLUSTER_RE:191`, `_title_case_violation:1734`).
  2. Each has a positive block in `dsc-ai-tell-fixture.md` (negative parallelism 39-44, Title Case 46-51, hyphenated pair 60-65, trailing 67-75) and a `PATTERN_SIGNATURES` entry plus, for two, an `ANCHORED_REGRESSION_CASES` entry (line 69 removed) in `test_dsc_ai_tell.py`.
  3. Removing a positive block shifts the survivors' line anchors (`ANCHORED_REGRESSION_CASES` lines 79/130, `NEGATIVE_RANGES` 115-126), which must renumber in the same change (R3).
- **Blast radius**: an intermediate commit landing part of the five-artifact unit red-builds the staged test at that commit.
- **Existing safeguards**: the plan states "in one change" repeatedly; decomposition follows this review.
- **Residual risk**: LOW — bounded to a red intermediate commit caught locally; the guard is a decomposition-time atomicity constraint the plan does not yet state (R6).
