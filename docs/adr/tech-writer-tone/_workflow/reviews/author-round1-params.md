# design-author params — phase1-creation, round 1

- target: design
- output_path: /home/andrii0lomakin/Projects/ytdb/tech-writer-tone/docs/adr/tech-writer-tone/_workflow/design.md
- research_log_path: /home/andrii0lomakin/Projects/ytdb/tech-writer-tone/docs/adr/tech-writer-tone/_workflow/research-log.md
- round: 1

## Notes for this spawn

- Single-file design: no `design-mechanics.md` companion exists or is planned. Do not create one.
- Structure per `.claude/workflow/design-document-rules.md` (TOC-read it; §Structure, §Overview, §Core Concepts, §Per-section mandatory shape, §Required content, §D/S code discipline): concept-first `## Overview`; `## Core Concepts` (the doc introduces ≥3 new domain terms — persona reader, dual-register design, disguise-only rule, reader-proxy model pin); `## Class Design` recast for a workflow-machinery change as the artifact/agent architecture (which `.claude/**` files change and how they relate — Mermaid diagram + prose); `## Workflow` (the changed authoring-review loop at runtime — Mermaid sequence/flowchart + prose); one `##` section per complex topic with the mandatory per-section shape (TL;DR + mechanism overview + edge cases + `### Decisions & invariants` footer).
- This change is workflow-machinery prose/config/script, no Java. Ground in: `.claude/output-styles/house-style.md` + `house-conversation.md`, `.claude/agents/readability-auditor.md` / `comprehension-review.md` / `absorption-check.md` / `design-author.md` / `review-workflow-writing-style.md`, `.claude/workflow/design-document-rules.md`, `.claude/workflow/prompts/design-review.md`, `.claude/skills/edit-design/SKILL.md`, `.claude/scripts/design-mechanical-checks.py` + `.claude/scripts/tests/test_dsc_ai_tell.py` + `.claude/scripts/tests/fixtures/dsc-ai-tell-fixture.md`, `.claude/skills/ai-tells/SKILL.md`, `.claude/workflow/conventions.md` §1.5/§1.7, project `CLAUDE.md` § Writing Style, and the book at `/home/andrii0lomakin/Projects/ytdb/docs-ytdb-internals-book/docs/ytdb-internals-book/` (`BOOK_BRIEF.md`, `README.md`, `beta-feedback/*.md` for the persona-report shape). Verify every `file:line` you cite against the live tree. PSI/mcp-steroid is not required for this prose-shaped change — Read/Grep suffice; skip the `steroid_list_projects` preflight.
- Absorb every research-log `## Decision Log` entry (D1-D9) as a seed D-record — the warm absorption check will two-way match the log against your D-records, including the gate-strengthened clauses (D1's full consumer enumeration, D2's per-role hybrid split, D3's false-clean mitigation, D4's complete rename-site list incl. `MANDATORY_OR_FORM_SUBHEADINGS`, D5's track-file anchor carve + closure, D6's axis-ownership clause, D7's closure default + keep/remove criteria, D8's dual-register skeleton guard, D9's precedence + link-staleness hardening). The log's `## Surprises & Discoveries` carries the YTDB-1163 rules (self-contained lead; body-stands-without-lead) and the book-pipeline findings you will need.
- §1.7 staging: this branch is workflow-modifying in staged mode (behavior-bearing script + agent-frontmatter changes) — the design must state that edits accumulate under `_workflow/staged-workflow/.claude/**` until Phase-4 promotion, and that this design itself is authored and reviewed under the CURRENT live rules (the new voice applies only after promotion).
- The design document you write follows the CURRENT live house-style and design-document-rules (TL;DR spelling, current section shapes) — it documents the new rules; it is not yet written under them.
