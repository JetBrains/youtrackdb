# Track 4: buildInitialHistogram mixed-mode encoding

## Purpose / Big Picture
After this track, the in-memory recalibration-rollback divergence is structurally impossible on the `buildHistogramAfterFill` path, which is the only production caller of `buildInitialHistogram`. The in-memory `AtomicLong`s advance through the new in-mem-only `IndexCountDelta.accumulateInMemRecalibration` accumulator instead of direct writes; on rollback they stay at their prior approximate values. The persisted EP-page side keeps today's inline `setApproximateEntriesCount(op, target)` writes, which are WAL-tracked, revert on rollback, and heal pre-existing in-mem-vs-persisted drift on every successful recalibration.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Convert `BTreeMultiValueIndexEngine.buildInitialHistogram()` and `BTreeSingleValueIndexEngine.buildInitialHistogram()` to mixed-mode encoding. Keep today's inline `setApproximateEntriesCount(op, target)` writes on the persisted EP pages, and route the in-memory `AtomicLong` writes through a new `IndexCountDelta.accumulateInMemRecalibration(op, id, totalDelta, nullDelta)` accumulator consumed by Hook B. After this track, the in-memory recalibration-rollback divergence is structurally impossible on the `buildHistogramAfterFill` path; the persisted-side drift-healing behavior already in production is preserved.

## Progress
- [x] 2026-05-25T13:37Z [ctx=safe] Review + decomposition complete
- [x] 2026-05-25T14:11Z [ctx=safe] Step 1 complete (commit 2a83268cc3)
- [x] 2026-05-25T14:49Z [ctx=warning] Step 2 complete (commit 9148ccc71e, review-fix 46be37d853)
- [x] 2026-05-25T16:56Z [ctx=info] Step 3 complete (commit 823486d21d)
- [x] 2026-05-25T17:53Z [ctx=info] Step 4 complete (commit 9bbf0204fc)
- [x] 2026-05-25T17:53Z [ctx=info] Step implementation
- [x] 2026-05-25T18:39Z [ctx=warning] Track-level code review iteration 1 complete (1/3 iterations)
- [ ] Track-level code review
- [ ] Track completion

## Surprises & Discoveries

- 2026-05-25T14:49Z: Step 2 inheritance for Step 3 and Step 4. See `## Episodes §Step 2`. Step 3 (SV `buildInitialHistogram`) must mirror Step 2 verbatim: assert message format `"buildInitialHistogram() snapshot invariant violated on engine=NAME id=N: currentTotal=… currentNull=…"` (Track 3 `clear()` precedent), bifurcated-lock-posture comment, CHM-cache scope disclaimer, holder-inspection assertions on `getInMemAdjustTotal` / `getInMemAdjustNull` plus `getTotalDelta()==0` / `getNullDelta()==0` pins, and a divergent pre-state on the SV `_delegatesToManager` test. Step 4 picks up six review-deferred test-coverage items: MV one-tree-empty drift shape pin, Q3 sign-opposed counter-example pin (worked example `currentTotal=100, currentNull=10, scannedNonNull=80, exactNullCount=15 → totalDelta=-5, nullDelta=+5`), zero-delta no-op holder-row pin, `assertsSnapshotInvariant_currentNullExceedsTotal` mirror for `buildInitialHistogram`, crash-recovery WAL-replay test (disk-mode crash before checkpoint, reopen, persisted EP page lands at recalibrated target), and reload-via-`load()` convergence test (crash after commit but before Hook B applies; on reopen `load()` reseeds in-mem AtomicLongs from persisted target).
- 2026-05-25T14:49Z: Pre-existing risk inherited (not introduced by Step 2): `BTree.setApproximateEntriesCount` asserts `count >= 0` under `-ea` but lacks production-side clamp. If `mgr.buildHistogram` returns `nonNullCount = approxTotal - approxNull < 0` from prior in-mem skew (Track-1 clamp event or rolled-back put), the inline `setApproximateEntriesCount(op, scannedNonNull)` write at the MV recalibration site can persist a negative value under `-ea` off. Same exposure as pre-rewrite. Out of Track 4 scope; flagged for the bug C / YTDB-953 backlog or a separate follow-up.
- 2026-05-25T14:49Z: Plan-file edit deferred — `track-4.md §Clarifications A4` describes safety on the `buildHistogramAfterFill` path as "additively composable" semantics. The actual safety story is per-tree-lock-till-op-complete on `setApproximateEntriesCount`'s `executeInsideComponentOperation` window plus single-threaded holder per atomic op. Pure additive composition alone would not converge two concurrent recalibrations on the same engine. Tighten on the Track 4 retro or in `design-final.md` at Phase 4.
- 2026-05-25T16:56Z: SV delegation-pin parameter shape diverges from MV — Step 4 must apply when writing SV mirrors of any MV holder-migration. See `## Episodes §Step 3 — Critical context`. The SV `buildInitialHistogram` body passes `(approxTotal, exactNullCount, ...)` to `mgr.buildHistogram` where `exactNullCount` is the live null-count scan from SV:625; MV passes `(approxTotal, approxNull, ...)`. A reviewer copying the MV verify shape verbatim onto the SV side writes a failing pin.
- 2026-05-25T16:56Z: Dimensional review surfaced three concurrency/handler items whose disposition routes to already-deferred follow-up work, not to in-step fixes. See `## Episodes §Step 3 — What was discovered`. (a) Snapshot reads at SV:674–675 are non-atomic and can spuriously trip the new `currentNull <= currentTotal` assert under concurrent same-engine Hook B; applies symmetrically to merged MV Step 2; routes to the Q5 single follow-up YouTrack issue scope. (b) "Additively composable" wording in the in-mem-side comment block at SV:654–655 (and the MV mirror) overclaims; routes to `design-final.md` at Phase 4 per the row above. (c) `IndexAbstract.buildHistogramAfterFill:401–408`'s `IOException`-only catch lets a rewrapped `StorageException` from the new `-ea` assert escape the "must not fail rebuild" contract; pre-existing handler narrowness, widened in exposure by the new assert site; Phase C or follow-up consideration.
- 2026-05-25T17:53Z: `db.close` is not sufficient for persisted-side close-and-reopen verification. See `## Episodes §Step 4 — What was discovered`. `db.close` on `DatabaseSessionEmbedded` only decrements `sessionCount`; the storage and engine instances stay alive in the `YouTrackDB` manager, and a subsequent `youTrackDB.open` returns the same engine with its in-mem `AtomicLong`s intact. `forceDatabaseClose` drops the storage entirely so the next open runs `load()` and reseeds from disk. Reference pattern lives at `IndexAbstractBuildHistogramRollbackTest` lines 219–230.
- 2026-05-25T17:53Z: `QUERY_STATS_HISTOGRAM_MIN_SIZE` early-exit in `IndexHistogramManager.buildHistogram` short-circuits the key scan when `totalCount - nullCount < threshold` and returns `nonNullCount` derived from the in-mem total. Any test that artificially diverges the in-mem counters above the actual leaf count must pin the threshold low (the class pins `1`) and be marked `@Category(SequentialTest.class)` because `GlobalConfiguration` is a JVM-wide singleton. See `## Episodes §Step 4 — What was discovered`.
- 2026-05-25T17:53Z: Cross-branch audit candidate. Other branches that use `db.close + youTrackDB.open` to verify persistence are checking live in-mem state, not disk state. `BTreeEnginePersistedCountIT` already uses `forceDatabaseClose`. Recipe candidate for the project's Recipes section, scoped to future work. See `## Episodes §Step 4 — What was discovered`.

## Decision Log

- 2026-05-25T14:49Z: Step 2 scope expansion — merged Step 4's MV holder-migration work into Step 2. The dimensional review surfaced that Step 2 left the in-mem-side recalibration arithmetic with zero assertion coverage (three disabled `assertEquals` blocks tagged `TODO: migrate to holder inspection`), and the test fixture `BTreeEngineTestFixtures.MultiValueFixture` already installs a real `IndexCountDeltaHolder` so holder-inspection works inline today. Landed the migration in the Step 2 review-fix commit (5 holder-inspection assertion blocks plus a divergent pre-state on `_delegatesToManager` so its assertions stop passing by coincidence). Step 4's roster narrows to: new `IndexAbstractBuildHistogramRollbackTest` + SV equivalents of the MV holder migrations (after Step 3 reproduces the same MV pattern on the SV side). See `## Episodes §Step 2`.

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective

- [x] Technical: PASS at iteration 2 (6 findings, all VERIFIED)
- [x] Adversarial: PASS at iteration 2 (8 findings, all VERIFIED)

## Context and Orientation

The two buildInitialHistogram bodies:

- `BTreeMultiValueIndexEngine.buildInitialHistogram(AtomicOperation)` at `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeMultiValueIndexEngine.java:604–681` (post-Step-2). Computes `approxTotal` and `approxNull` from current in-memory counters at lines 613–614 (used for histogram bucket sizing in `mgr.buildHistogram`). Scans for exact `scannedNonNull` and `exactNullCount`. Pre-Step-2 the recalibration block held both persisted-side writes and two `AtomicLong.set` calls; post-Step-2 the recalibration site is split: the persisted-side absolute writes stay at lines 670–671, the snapshot-invariant assert + `accumulateInMemRecalibration` call lands at lines 672–680, and the comment block extends to line 669.

- `BTreeSingleValueIndexEngine.buildInitialHistogram(AtomicOperation)` at `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeSingleValueIndexEngine.java:615–682` (post-Step-3). Same shape with one tree; post-Step-3 the persisted-side write stays at line 673 (`sbTree.setApproximateEntriesCount`), the snapshot-invariant assert + `accumulateInMemRecalibration` call lands at lines 674–681, and the comment block extends to line 671.

Only one production caller: `IndexAbstract.buildHistogramAfterFill` at `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/IndexAbstract.java:390–415` (PSI-verified at the Pre-Flight gate, post-Track-3). The call site wraps `buildInitialHistogram` in `executeInsideAtomicOperation` at lines 397–400, then swallows `IOException` with a warn at lines 401–408 ("Histogram build failure must not fail the index rebuild. The histogram will be built lazily on the next rebalance.").

The recalibration arithmetic for the mixed-mode conversion (per the design doc's mechanism block):
- **Persisted side (both engines)**: keep today's inline writes. MV calls `svTree.setApproximateEntriesCount(op, scannedNonNull)` and `nullTree.setApproximateEntriesCount(op, exactNullCount)`; SV calls `sbTree.setApproximateEntriesCount(op, exactTotal)`. Each write is WAL-tracked through the AOM lifecycle, reverts on rollback, and lands at the absolute target regardless of the pre-rebuild persisted value. The drift-healing property already present in today's code is preserved.
- **In-mem side (both engines)**: route the writes through the new in-mem-only accumulator with `totalDelta = target - currentInMemTotal` and `nullDelta = exactNullCount - currentInMemNull`. MV: `target = scannedNonNull + exactNullCount`. SV: `target = exactTotal = scannedNonNull + exactNullCount`. The deltas are signed and may oppose each other; the new accumulator has no sign-alignment precondition, unlike `accumulateClearOrRecalibrate`.

Hook A's `persistCountDelta` reads `delta.getTotalDelta()` and `delta.getNullDelta()` only. The new fields `inMemAdjustTotal` / `inMemAdjustNull` are not consumed by Hook A; the persisted side is fed by the inline `setApproximateEntriesCount` calls above, not by Hook A on the recalibration path. Hook B's `applyIndexCountDeltas` (at `AbstractStorage.java:2496`, engine-mutator calls at lines 2538–2541) sums `getTotalDelta() + getInMemAdjustTotal()` and `getNullDelta() + getInMemAdjustNull()` and calls `addToApproximate{Entries,Null}Count`. The in-mem `AtomicLong`s therefore advance only post-commit, exactly when the persisted side has landed at the absolute target.

The `approxTotal` / `approxNull` reads at MV:613–614 and the `approxTotal` read at SV:624 (paired with the `exactNullCount = countNulls(atomicOperation)` scan at SV:625) used for histogram bucket sizing are unaffected; they consume the in-memory counter *as-of* the start of `buildInitialHistogram`, before the recalibration delta is recorded.

The deliverable: a new `IndexCountDelta.accumulateInMemRecalibration` static method with two new package-private fields and a sum update in `AbstractStorage.applyIndexCountDeltas`; mixed-mode rewrites of `buildInitialHistogram` on both engines (persisted-side inline writes preserved, in-mem-side writes routed through the new accumulator); a recalibration-rollback regression test; and an existing-test rewrite migrating the in-mem-side assertions in `BTreeEngineHistogramBuildTest` to the holder-inspection pattern.

### Clarifications

Carried forward from the Track Pre-Flight gate (post-Track-3, 2026-05-25) plus the Phase A iteration-1 design call. Captured here so reviewers see them as current-state context.

- **A1 design call resolved: (β) Mixed-mode encoding chosen.** Pure-delta encoding on both sides would transfer pre-existing in-mem-vs-persisted drift to the persisted side instead of healing it. Worked arithmetic: pre-rebuild `currentInMem = persisted + d` makes Hook A's `Δ = -d` land persisted at `target - d` instead of `target`. Today's `setApproximateEntriesCount(op, target)` writes absolute via WAL — persisted lands at `target` and drift heals on every successful recalibration. Track 4 keeps the inline absolute writes on the persisted side and routes only the in-mem `AtomicLong` writes through a new accumulator. Track 3 (`clear()`) stays pure-delta because `Δ = -current` converges both sides to zero regardless of drift.
- **New in-mem-only accumulator added; existing overload untouched.** Track 4 Step 1 adds `IndexCountDelta.accumulateInMemRecalibration(AtomicOperation, int, long, long)` plus two new package-private long fields `inMemAdjustTotal` / `inMemAdjustNull` on the per-engine `IndexCountDelta` record (PSI-verified absent at session start). `accumulateClearOrRecalibrate` (Track 3, commit `5f7f882830`, body at `IndexCountDelta.java:140–154` post-Step-1) stays unchanged with its `|nullDelta| <= |totalDelta|` + sign-alignment precondition; only `clear()` callers consume it. The original "preflight-add" branch is removed: Track 4 adds a *different* method, not a re-add of the Track 3 overload.
- **Q3 supersession.** The Q3 question (runtime assert relaxation on `accumulateClearOrRecalibrate`) is moot under (β). The new `accumulateInMemRecalibration` method has no sign-alignment or magnitude precondition by design — in-mem-only recalibration deltas are arbitrarily signed because organic drift between `approximateIndexEntriesCount` and `approximateNullCount` (from Track-1 `reportAndClampUnderflow` events, or any pre-existing in-mem skew) can produce sign-opposed deltas. Worked counter-example that would have tripped option (a)'s in-place relaxation: `currentTotal=100, currentNull=10, scannedNonNull=80, exactNullCount=15 → totalDelta=−5, nullDelta=+5` (magnitude-and-sign violation). The new method accepts it without an assert.
- **Mockito recipe option (1) accepted.** The regression test wraps `executeInsideAtomicOperation` test-side so `engine.buildInitialHistogram(op)` runs first (records the in-mem delta and lands the persisted-side `setApproximateEntriesCount` write), then the lambda re-throws `IOException` to trigger rollback. The thrown `IOException` is rewrapped as a `StorageException` (RuntimeException via BaseException) by `AtomicOperationsManager.java:147` / `:174`, so the test catches `StorageException`, not `IOException`. No production-side change is required; the IOException catch at `IndexAbstract.buildHistogramAfterFill:401–408` does not apply because the test invokes AOM directly and bypasses `buildHistogramAfterFill`.
- **Lock posture option (a) accepted.** `buildInitialHistogram` does not transitively hold a per-tree exclusive lock when reading `approximateIndexEntriesCount.get()` / `approximateNullCount.get()` for the recalibration snapshot. PSI confirms `IndexAbstract.buildHistogramAfterFill:400` is the only production caller, invoked post-`fillIndex` post-rebuild, so no concurrent `clearIndex` API path is active under the narrow call graph. Track 4 inherits the Q5-style multi-thread contention test deferral; one combined follow-up YouTrack issue under `YTDB` with the `dev-workflow` tag covers both engines and both `clear()` + `buildInitialHistogram()` seams (no separate ticket).
- **Histogram CHM cache divergence (A3) is out of scope.** `IndexHistogramManager.cache.put` at lines 763, 806, 831 mutates eagerly inside the atomic op and is NOT reverted on rollback. A rolled-back `buildInitialHistogram` therefore leaves the CHM cache at the recalibrated snapshot while the persisted `.ixs` page reverts via WAL. The structural-impossibility claim is scope-bounded to in-mem count counters. Follow-up deferred — separate work, not Track 4 scope.
- **Concurrent-surface review (A4).** The new in-mem-only accumulator is additively composable (`+=` semantics on the per-engine `IndexCountDelta` record) so concurrent recalibrations on the same engine converge to truth on the in-mem side. The persisted side uses inline `setApproximateEntriesCount(target)` which is WAL-tracked and serialized through `executeInsideComponentOperation`'s per-tree lock. `ProductionAllocatorConcurrencyMTTest.java:660–661` already exercises concurrent `buildInitialHistogram` on the same MV engine across worker threads; Track 4 Step 4 reviews this test against the new arithmetic before committing.
- **Snapshot-invariant assert (T5).** Both `buildInitialHistogram` rewrites add `assert currentTotal >= 0 && currentNull >= 0 && currentNull <= currentTotal` (with the engine `name` + `id` in the message) before computing deltas, matching Track 3's `clear()` precedent. This catches structural in-mem skew at the read-snapshot site before it propagates into the delta arithmetic.
- **Bifurcated lock posture acknowledged.** Track 2's Invariant 3 holds on the main-commit path (Hook B runs under the per-index lock acquired at `lockIndexes` (AbstractStorage:2255)). On the `clearIndex` API and `buildHistogramAfterFill` paths, no per-index lock is held during Hook B's apply; the in-mem `AtomicLong`'s `addAndGet` additive semantics make ordering harmless. The `implementation-plan.md` Invariant 3 wording is updated in this Phase A round to acknowledge both paths.
- **Q5 multi-thread real-tree contention test gap inherited.** Track 3 Decision Log Q5 deferred to a single follow-up YouTrack issue under `YTDB` with the `dev-workflow` tag, covering both engines and both `clear()` + `buildInitialHistogram()` seams together. Track 4 picks up the second half of the same deferral; no separate ticket should be filed.
- **Line citations refreshed (post-Step-4).** All MV/SV `buildInitialHistogram` and `IndexAbstract.buildHistogramAfterFill` line numbers are PSI-verified at Phase C: MV body 604–681, MV persisted-side recalibration calls 670–671, MV bucket-sizing reads 613–614, MV null-tree scan 631–641, MV comment block 643–669, MV in-mem-side assert + `accumulateInMemRecalibration` at lines 672–680; SV body 615–682, SV persisted-side recalibration write 673 (`sbTree.setApproximateEntriesCount`), SV bucket-sizing read 624 + `countNulls` at 625, SV comment block 639–671, SV in-mem-side assert + `accumulateInMemRecalibration` at lines 674–681; `IndexAbstract.buildHistogramAfterFill` 390–415 (`executeInsideAtomicOperation` call at 397–400, `IOException` catch at 401–408). `IndexCountDelta.java` post-Step-1 reference: per-put `accumulate` at lines 88–96, `accumulateClearOrRecalibrate` body at lines 140–154, its Javadoc at lines 98–139 (precondition justification at lines 128–139), and new `accumulateInMemRecalibration` at lines 191–196 with its Javadoc at lines 156–190.

## Plan of Work

Four logical edits:

1. **Add mixed-mode plumbing to `IndexCountDelta`.** Land a new `accumulateInMemRecalibration(AtomicOperation, int, long, long)` static method alongside the existing `accumulate` (per-put/remove, `IndexCountDelta.java:88–96` post-Step-1) and `accumulateClearOrRecalibrate` (clear long-form, body at lines 140–154 post-Step-1). Add two new package-private long fields on the per-engine `IndexCountDelta` record: `inMemAdjustTotal` and `inMemAdjustNull` (default 0), plus two public getters `getInMemAdjustTotal()` / `getInMemAdjustNull()`. The new method has no sign-alignment or magnitude precondition (recalibration deltas are arbitrarily signed by drift nature). Update `AbstractStorage.applyIndexCountDeltas` (method at line 2496; engine-mutator calls at lines 2538–2541) to sum both accumulators when calling the engine mutators:
   ```java
   btreeEngine.addToApproximateEntriesCount(
       delta.getTotalDelta() + delta.getInMemAdjustTotal());
   btreeEngine.addToApproximateNullCount(
       delta.getNullDelta() + delta.getInMemAdjustNull());
   ```
   Hook A's `persistCountDelta` reads `delta.getTotalDelta()` and `delta.getNullDelta()` only and is unchanged; the in-mem-only adjustments do not feed the persisted side. Trim the existing `accumulateClearOrRecalibrate` Javadoc at `IndexCountDelta.java:98–139` post-Step-1 to drop the two `buildInitialHistogram` caller bullets and tighten the precondition justification at lines 128–139 to mention only `clear()` callers; both `buildInitialHistogram` paths route through the new `accumulateInMemRecalibration` method post-Track-4. Unit tests cover the additive composition of per-put deltas with a recalibration delta in the same atomic operation, the rollback discard contract, and the in-mem-only contract (Hook A's `persistCountDelta` is NOT called for in-mem-only deltas).

2. **Rewrite `BTreeMultiValueIndexEngine.buildInitialHistogram` lines 650–653 (pre-Step-2)** to mixed-mode encoding. Keep the two `setApproximateEntriesCount` calls on `svTree` and `nullTree` (WAL-tracked, drift-healing; land at lines 670–671 post-Step-2). Replace the two `AtomicLong.set` calls with a snapshot-invariant assert and the new in-mem-only accumulator call (these land at lines 672–680 post-Step-2):
   ```java
   // Persisted-side absolute writes: WAL-tracked, revert on rollback,
   // heal pre-existing in-mem-vs-persisted drift on every successful
   // recalibration.
   svTree.setApproximateEntriesCount(atomicOperation, scannedNonNull);
   nullTree.setApproximateEntriesCount(atomicOperation, exactNullCount);
   // In-mem-side delta: advances only post-commit via Hook B.
   long currentTotal = approximateIndexEntriesCount.get();
   long currentNull  = approximateNullCount.get();
   long targetTotal  = scannedNonNull + exactNullCount;
   assert currentTotal >= 0 && currentNull >= 0 && currentNull <= currentTotal
       : "buildInitialHistogram() snapshot invariant violated on engine="
           + name + " id=" + id
           + ": currentTotal=" + currentTotal + " currentNull=" + currentNull;
   IndexCountDelta.accumulateInMemRecalibration(
       atomicOperation, id, targetTotal - currentTotal, exactNullCount - currentNull);
   ```
   Update the comment block to describe the mixed-mode design (persisted-side absolute writes plus in-mem-side delta); the comment extends to line 669 post-Step-2. The histogram-build code at lines 612–625 and the null-tree scan at lines 631–641 are unchanged. Assert message format aligned with the Track 3 `clear()` precedent during the Step 2 review-fix iteration so `assertsSnapshotInvariant_currentNullExceedsTotal`-style substring tests work uniformly across `clear()` and `buildInitialHistogram`.

3. **Rewrite `BTreeSingleValueIndexEngine.buildInitialHistogram` lines 647–650 (pre-Step-3)** with the structurally identical change for the single-tree case. Keep the `exactTotal` local and the `sbTree.setApproximateEntriesCount` call (the absolute write lands at line 673 post-Step-3). Replace the two `AtomicLong.set` calls with the same snapshot-invariant assert and `accumulateInMemRecalibration` call shape (lines 674–681 post-Step-3):
   ```java
   long exactTotal = scannedNonNull + exactNullCount;
   sbTree.setApproximateEntriesCount(atomicOperation, exactTotal);
   long currentTotal = approximateIndexEntriesCount.get();
   long currentNull  = approximateNullCount.get();
   assert currentTotal >= 0 && currentNull >= 0 && currentNull <= currentTotal
       : "buildInitialHistogram() snapshot invariant violated on engine="
           + name + " id=" + id
           + ": currentTotal=" + currentTotal + " currentNull=" + currentNull;
   IndexCountDelta.accumulateInMemRecalibration(
       atomicOperation, id, exactTotal - currentTotal, exactNullCount - currentNull);
   ```
   Update the parallel comment block to point at the mixed-mode design (the SV comment extends to line 671 post-Step-3, mirroring Step 2's MV comment update). Assert message format matches the Track 3 `clear()` precedent that Step 2's review-fix iteration locked in.

4. **Add a recalibration-rollback regression test and rewrite the existing positive assertions.** New test `IndexAbstractBuildHistogramRollbackTest` under `core/src/test/.../index/`:
   - Build an index with known counters (a few thousand entries with a mix of null and non-null keys).
   - Wrap `executeInsideAtomicOperation` test-side so the lambda runs `engine.buildInitialHistogram(op)` (records the in-mem delta and lands the persisted-side `setApproximateEntriesCount` write), then re-throws `IOException`. Catch the rewrapped `StorageException` per `AtomicOperationsManager.java:147` / `:174`.
   - Assertions pin: (a) the in-mem `AtomicLong`s retain pre-recalibration values after the throw, (b) the persisted EP page reverts via WAL on rollback, (c) on a separate successful run both sides land on the post-recalibration target, (d) Hook B's `setApplied()` latch is true on success / false on rollback.
   - Javadoc scopes the assertions to count counters only; CHM cache divergence is named as out-of-scope.

   Rewrite the existing positive assertions in `BTreeEngineHistogramBuildTest.java` that read `assertEquals(target, f.engine.getTotalCount(f.op))` to the holder-inspection pattern matching Track 3's clear-tests (`f.op.getOrCreateIndexCountDeltas().getDeltas().get(f.engine.getId())`). Under (β), the in-mem getters return the *pre-recalibration* value during the atomic op because Hook B has not yet applied; the persisted-side `verify(...setApproximateEntriesCount(target))` Mockito assertions stay valid because the persisted write still happens inline. Only the in-mem-side `assertEquals(target, f.engine.getTotalCount(f.op))` assertions migrate.

Ordering constraint: this track depends on Track 2's Hook B, which consumes the new `inMemAdjustTotal` / `inMemAdjustNull` fields via `applyIndexCountDeltas`. Track 3's `accumulateClearOrRecalibrate` overload is independently consumed by `clear()` and is unaffected; Track 4 adds a separate method (`accumulateInMemRecalibration`) rather than reusing the Track 3 overload, because the two methods carry different invariants (clear has sign-alignment + magnitude bound; recalibration has neither).

Invariants to preserve: the `approxTotal` read at MV:613 / SV:624, the `approxNull` read at MV:614, and the `exactNullCount = countNulls(atomicOperation)` scan at SV:625 still feed the histogram-bucket sizing in `IndexHistogramManager.buildHistogram`. The null-tree scan logic (MV lines 631–641) is unchanged. Hook A's `persistCountDelta` is not invoked for the recalibration deltas — the persisted side is fed by the inline `setApproximateEntriesCount` writes, not by Hook A on this path.

## Concrete Steps

1. Add mixed-mode plumbing to `IndexCountDelta`: new `accumulateInMemRecalibration(AtomicOperation, int, long, long)` static method (no precondition), two new package-private fields `inMemAdjustTotal` / `inMemAdjustNull` with public getters, and Hook B sum update at `AbstractStorage.applyIndexCountDeltas` (method line 2496; engine-mutator calls lines 2538–2541 post-Step-1). Trim the `accumulateClearOrRecalibrate` Javadoc (lines 98–139 post-Step-1) to drop the two `buildInitialHistogram` caller bullets and tighten the precondition justification (lines 128–139 post-Step-1) to mention only `clear()` callers. Unit tests cover additive composition, rollback discard, and in-mem-only contract.  — risk: medium (multi-file logic spanning new SPI method, holder field additions, hook wiring; touches Track 2's `applyIndexCountDeltas` consumer)  [x] commit: 2a83268cc3
2. Rewrite `BTreeMultiValueIndexEngine.buildInitialHistogram` lines 650–653 (pre-Step-2) to mixed-mode encoding: keep the two `setApproximateEntriesCount` calls on `svTree` and `nullTree` (lines 670–671 post-Step-2, WAL-tracked drift-healing), replace the two `AtomicLong.set` calls with a snapshot-invariant assert plus `IndexCountDelta.accumulateInMemRecalibration(op, id, targetTotal - currentTotal, exactNullCount - currentNull)` (lines 672–680 post-Step-2). Update the comment block to describe the mixed-mode design (extends to line 669 post-Step-2).  — risk: high (concurrency: changes the persisted-vs-in-mem semantics on the recalibration path; structurally-impossible-divergence claim depends on this rewrite landing correctly; under the bifurcated lock posture the in-mem advance happens post-commit under Track 2's Hook B)  [x] commit: 9148ccc71e ; review-fix: 46be37d853
3. Rewrite `BTreeSingleValueIndexEngine.buildInitialHistogram` lines 647–650 (pre-Step-3) with the structurally identical change for the single-tree case: keep the `exactTotal` local and the `sbTree.setApproximateEntriesCount` call (the absolute write lands at line 673 post-Step-3), replace the two `AtomicLong.set` calls with the same snapshot-invariant assert plus `accumulateInMemRecalibration` call (lines 674–681 post-Step-3). Update the parallel comment block (extends to line 671 post-Step-3).  — risk: high (concurrency: same surface as Step 2 for the single-value engine; SV `persistCountDelta` already ignores `nullDelta` per Track 1's cross-track signal, so the in-mem-only accumulator is the sole `nullDelta` carrier on this path)  [x] commit: 823486d21d
4. Add `IndexAbstractBuildHistogramRollbackTest` under `core/src/test/.../index/` and rewrite the existing positive in-mem assertions in `BTreeEngineHistogramBuildTest.java`. New test builds an index with known counters, wraps `executeInsideAtomicOperation` test-side to run `engine.buildInitialHistogram(op)` then re-throw `IOException` (caught as the rewrapped `StorageException` per `AtomicOperationsManager.java:147` / `:174`), and pins: (a) in-mem `AtomicLong`s retain pre-recalibration values after the throw, (b) the persisted EP page reverts via WAL on rollback, (c) on a separate successful run both sides land on the post-recalibration target, (d) Hook B's `setApplied()` latch is true on success / false on rollback. Javadoc scopes the assertions to count counters only; CHM cache divergence is out-of-scope. Migrate the in-mem-side `assertEquals(target, f.engine.getTotalCount(f.op))` assertions in `BTreeEngineHistogramBuildTest` to the holder-inspection pattern (`f.op.getOrCreateIndexCountDeltas().getDeltas().get(f.engine.getId())`); persisted-side `verify(...setApproximateEntriesCount(target))` Mockito assertions stay.  — risk: medium (test-infrastructure changes plus a new rollback regression test; touches 10+ assertion sites in BTreeEngineHistogramBuildTest; rollback recipe inherited from Track 3 with a fresh Mockito stub seam)  [x] commit: 9bbf0204fc

## Episodes

### Step 4 — commit 9bbf0204fc, 2026-05-25T17:53Z [ctx=info]
**What was done:** Step 4 lands the per-engine end-to-end rollback regression test and the remaining in-mem-side migration items deferred from earlier reviews. The new `IndexAbstractBuildHistogramRollbackTest` drives the AOM lifecycle directly: each test bumps the engine's in-mem `AtomicLong` counters above the persisted EP page, runs `buildInitialHistogram` inside `executeInsideAtomicOperation`, and pins both the rollback contract (in-mem stays at the bumped pre-state, `holder.isApplied()` is false, persisted reverts via WAL) and the success contract (Hook B applies the recalibration delta, `holder.isApplied()` is true, persisted lands at the absolute target). Two crash-recovery shapes round out the class: `forceDatabaseClose` between commit and reopen verifies WAL replay re-applies both the `svTree` and `nullTree` EP-page absolute writes; a separate test pins that `load()` on reopen reseeds the in-mem `AtomicLong`s from the persisted EP page even when Hook B's pre-crash apply was discarded with the JVM. Five additional `BTreeEngineHistogramBuildTest` tests migrate to the holder-inspection pattern (zero-delta no-op holder-row pin on both engines, sign-opposed counter-example pin, snapshot-invariant assert message pins on both engines, MV one-tree-empty drift shape pin) and the `completelyEmptyTree` test moves from in-mem getter assertions to holder-row inspection.

**What was discovered:** The persisted-side close-and-reopen verification needs `forceDatabaseClose`, not `db.close`. `db.close` on `DatabaseSessionEmbedded` only calls `storage.close(session)`, which decrements `sessionCount`; the storage and engine instances stay alive in the `YouTrackDB` manager. A subsequent `youTrackDB.open` returns the same engine instance with its in-mem `AtomicLong`s intact, so the post-restart read echoes the live counter rather than the persisted EP page. `forceDatabaseClose` drops the storage entirely so the next open runs `load()` and reseeds from disk. The earlier `BTreeSingleValueIndexEngineClearRollbackTest` happens to pass on plain `db.close` because `clear()` in that test never executes (a queued failure fires before `commitIndexes`), so the engine's in-mem counters stay at the preparatory value by coincidence. The contract there is "no in-mem write happened"; the contract here is "the in-mem write was discarded by rollback, and the persisted side reverted via WAL", which requires the full reload.

The histogram manager's `buildHistogram` early-exits when `totalCount - nullCount < QUERY_STATS_HISTOGRAM_MIN_SIZE` and returns `nonNullCount` derived from the diverged in-mem total without scanning the key stream. With the default threshold of 1000 and the few-row fixtures, `scannedNonNull` would echo the bumped in-mem total and the recalibration delta would collapse to zero, hiding routing regressions. The class now pins `QUERY_STATS_HISTOGRAM_MIN_SIZE=1` and is marked `SequentialTest` because `GlobalConfiguration` is a JVM-wide singleton.

Future-session note: any test that uses plain `db.close + youTrackDB.open` to verify persistence is checking live in-mem state, not disk state. `forceDatabaseClose` is the supported pattern (`BTreeEnginePersistedCountIT` already uses it). The `QUERY_STATS_HISTOGRAM_MIN_SIZE` early-exit at `IndexHistogramManager.buildHistogram` is relevant to any future test that artificially diverges the in-mem total above the actual leaf count and expects the histogram scan to recover the true value; the threshold must be pinned for the scan to run.

**What changed from the plan:** Step 4's scope absorbed the six review-deferred test-coverage items enumerated in `## Surprises & Discoveries` row 1 (MV one-tree-empty drift shape pin, sign-opposed counter-example, zero-delta no-op holder-row, snapshot-invariant assert mirror, crash-recovery WAL-replay test, reload-via-`load()` convergence test) plus the `completelyEmptyTree` migration. The plan's Step 4 roster is unchanged; the count is just larger than the plan-of-work text suggested.

**Key files:**
- `core/src/test/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/IndexAbstractBuildHistogramRollbackTest.java` (new)
- `core/src/test/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeEngineHistogramBuildTest.java` (modified)

**Critical context:** Future branches picking up this rollback-test recipe should use `forceDatabaseClose` for persisted-side checks; `db.close` alone reuses the engine and reads echo live counters. The pattern at lines 219–230 of `IndexAbstractBuildHistogramRollbackTest` is the reference shape (call `db.activateOnCurrentThread` before `forceDatabaseClose`, cast the reopen return to `DatabaseSessionEmbedded`). When seeding artificial drift via `engine.addToApproximateEntriesCount`, pin `QUERY_STATS_HISTOGRAM_MIN_SIZE` low and mark the class `@Category(SequentialTest.class)` so the global singleton write does not race with parallel test classes.

### Step 3 — commit 823486d21d, 2026-05-25T16:56Z [ctx=info]
**What was done:** SV `buildInitialHistogram` lands the mixed-mode encoding, mirroring Step 2's MV rewrite. The persisted-side `sbTree.setApproximateEntriesCount(op, exactTotal)` call stays inline (WAL-tracked, drift-healing); the two `AtomicLong.set` calls are replaced with a snapshot-invariant assert in Track 3 `clear()`-precedent message format plus `IndexCountDelta.accumulateInMemRecalibration(op, id, exactTotal - currentTotal, exactNullCount - currentNull)`. The in-mem-side comment block names the bifurcated lock posture (`IndexAbstract.buildHistogramAfterFill` is the only production caller and does not hold the per-index lock during Hook B's apply), the count-counter scope of the structural-impossibility claim (CHM-cache divergence flagged as out-of-scope), and an SV-specific paragraph noting that `persistCountDelta` ignores `nullDelta` on this engine so the new in-mem-only accumulator is the sole `nullDelta` carrier on the SV recalibration path. Six `BTreeEngineHistogramBuildTest` SV-side tests migrate to the holder-inspection pattern: `_delegatesToManager` (with divergent pre-state `(7, 2)` and delegation pin updated to `(7L, 1L, 1)` since the SV body passes `exactNullCount`, not `approxNull`, to `mgr.buildHistogram`), `_approxDiverged_recalibrates`, `_emptyNonNull_recalibratesToNullOnly`, `_filtersTombstoneRID`, plus the two SV-only tests `_countsSnapshotMarkerRID` and `_allTombstones_returnsZero`. Each migrated test reads `op.getOrCreateIndexCountDeltas().getDeltas().get(engineId)` and asserts `getInMemAdjustTotal` / `getInMemAdjustNull` against pre-state-derived deltas plus `getTotalDelta()==0` / `getNullDelta()==0` pins that guard against accumulator-method routing regressions. Step-level review fan-out ran 8 baseline + conditional agents in parallel; iteration 1 closed with no respawn (5 PASS, 3 advisory dimensions surfaced findings whose disposition lands on already-deferred work).

**What was discovered:** Two SV-only tests outside the Step-specific guidance's migration roster carried `assertEquals(target, f.engine.getTotalCount(f.op))` assertions: `singleValue_buildInitialHistogram_countsSnapshotMarkerRID` and `singleValue_buildInitialHistogram_allTombstones_returnsZero`. The MV side has no counterparts (they exercise SV-specific tombstone / snapshot-marker / null-tree-folded behaviors). Step 2's review-fix migration list did not include them because the migration was scoped to tests with MV cousins; on the SV side the first focused test run surfaced them as failures (expected vs actual mismatched on pre-state values vs post-recalibration targets). Both were migrated to the holder-inspection pattern with persisted-side `verify(sbTree).setApproximateEntriesCount(op, target)` pins added. The SV `_delegatesToManager` delegation-pin verify shape diverges from MV: the SV body passes `(approxTotal, exactNullCount)` to `mgr.buildHistogram`, where `exactNullCount` is the live null-count scan, not the pre-state `approxNull`. The MV body passes `(approxTotal, approxNull)`. The Step 3 verify therefore reads `eq(7L), eq(1L), eq(1)` not the symmetric `(7L, 2L, 1)` an author copying the MV shape verbatim would write.

Five dimensional-review findings are folded forward without in-step fix; each routes to existing deferred work or pre-existing concerns out of Step 3 scope, not to a respawn. (a) `review-bugs-concurrency` flagged the snapshot reads at `BTreeSingleValueIndexEngine.java:674–675` as non-atomic — a concurrent same-engine `addToApproximateNullCount(+k)` landing between the two reads can spuriously trip the new `currentNull <= currentTotal` assert under `-ea`. The reviewer named this as applying symmetrically to the merged MV Step 2 commit; the production call graph (one caller, post-`fillIndex`) is narrow and the multi-thread real-tree contention surface is already carved out to the Q5 single follow-up YouTrack issue per `## Surprises & Discoveries` row 1; the proposed mitigations (swap read order, or relax the cross-counter clause) belong to the same scope. (b) `review-bugs-concurrency` and `review-test-concurrency` both flagged the "additively composable" wording in the in-mem-side comment block at `BTreeSingleValueIndexEngine.java:654–655` as overclaiming — same finding already on record in `## Surprises & Discoveries` row 3 and `## Decision Log` 2026-05-25T14:49Z as deferred to `design-final.md` at Phase 4. (c) `review-bugs-concurrency` flagged the `IndexAbstract.buildHistogramAfterFill:401–408` catch as `IOException`-only — the rewrapped `StorageException` from the new assert escapes the handler's "histogram build failure must not fail the index rebuild" contract under `-ea`. Pre-existing; widened in exposure by the new assert site; not a Step 3 regression. (d) `review-test-completeness` named the missing zero-delta SV holder-row pin — already a Step 4 deferral per `## Surprises & Discoveries` row 1 ("zero-delta no-op holder-row pin"). (e) `review-test-concurrency` named the SV-side absence of a `ProductionAllocatorConcurrencyMTTest`-equivalent multi-thread real-tree contention test — already a Q5 single follow-up YouTrack issue carry-forward covering both engines and both `clear()` + `buildInitialHistogram()` seams.

**What changed from the plan:** Step 3's migration roster expanded from four to six SV tests. The plan text and the Step-specific guidance both name only the four SV cousins of the MV migrations; the two extra SV-only tests are not in the Step 2 episode's `Critical context`. Step 4's roster is unaffected — the new `IndexAbstractBuildHistogramRollbackTest` and the six review-deferred test-coverage items still stand.

**Key files:**
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeSingleValueIndexEngine.java` (modified)
- `core/src/test/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeEngineHistogramBuildTest.java` (modified)

**Critical context:** The SV `_delegatesToManager` delegation-pin verify reads `eq(7L), eq(1L), eq(1)`, not the symmetric `(7L, 2L, 1)` that MV's mirror used. The SV body passes the live `exactNullCount` (1) to `mgr.buildHistogram`, not the pre-state `approxNull` (2). A reviewer or Step 4 author copying the MV verify shape verbatim onto the SV side will write a failing pin. The asymmetry follows from SV:625 reading the live null-count scan before the delegation call; MV does not.

### Step 2 — commit 9148ccc71e (+ review-fix 46be37d853), 2026-05-25T14:49Z [ctx=warning]
**What was done:** MV `buildInitialHistogram` lands the mixed-mode encoding. The two persisted-side `setApproximateEntriesCount` calls on `svTree` and `nullTree` stay inline (WAL-tracked, drift-healing); the two `AtomicLong.set` calls are replaced with a snapshot-invariant assert plus `IndexCountDelta.accumulateInMemRecalibration(op, id, targetTotal - currentTotal, exactNullCount - currentNull)`. The in-mem-side comment block names the bifurcated lock posture explicitly — `IndexAbstract.buildHistogramAfterFill` is the only production caller and does not hold the per-index lock during Hook B's apply; correctness on this path comes from `AtomicLong.addAndGet`'s additive semantics. A scope-disclaimer paragraph names the count counters as the structural-impossibility surface and flags `IndexHistogramManager.cache` snapshots as out-of-scope follow-up. The review-fix iteration absorbed Step 4's MV holder-migration work: five `BTreeEngineHistogramBuildTest` test bodies now read `op.getOrCreateIndexCountDeltas().getDeltas().get(engineId)` and assert `getInMemAdjustTotal` / `getInMemAdjustNull` against pre-state-derived deltas, plus `getTotalDelta()==0` / `getNullDelta()==0` pins that guard against argument-swap regressions; the `_delegatesToManager` test pre-state diverged from `(3, 1)` to `(7, 2)` so its assertions stop passing by coincidence. Step-level review fan-out ran 8 baseline + conditional agents (skipped review-test-structure as too borderline); iteration 1 surfaced 5 in-scope findings (M1–M5) and 6 deferred items; iteration 2 gate-check returned PASS on all 7 dimensions with zero new findings.

**What was discovered:** The `_delegatesToManager` test's `verify(buildHistogram(..., 3L, 1L, 1))` was keyed on the pre-recalibration approximate counters as observed by the histogram-build call; diverging the pre-state to `(7, 2)` required updating that verify to `(7L, 2L, 1)`. Delegation contract correctly preserved — the histogram build is called with whatever the pre-recalibration in-mem counters report, not with the post-recalibration target. The snapshot-invariant assert message format on Step 2's first commit diverged from Track 3's `clear()` precedent (`name + "[" + id + "]:"` vs `"engine=" + name + " id=" + id`); the review-fix aligned to the Track 3 format so `assertsSnapshotInvariant_currentNullExceedsTotal`-style substring tests work uniformly. Dimensional review surfaced six items that belong with Step 4's roster amendment: MV one-tree-empty drift shape pin, Q3 sign-opposed counter-example pin, zero-delta no-op holder-row pin, snapshot-invariant assert pin, crash-recovery WAL-replay test, reload-via-`load()` convergence test. One plan-file edit deferred: `track-4.md §Clarifications A4` "additively composable" wording is misleading — actual safety on this path is per-tree-lock-till-op-complete. One pre-existing risk inherited (not introduced by Step 2): negative `scannedNonNull` can persist under `-ea` off if prior in-mem skew makes `nonNullCount = approxTotal - approxNull < 0`.

**What changed from the plan:** Step 4's MV holder-migration work absorbed into Step 2's review-fix iteration (see `## Decision Log`). Step 4 roster narrows to: new `IndexAbstractBuildHistogramRollbackTest` + SV equivalents of the MV holder migrations once Step 3 reproduces the MV pattern on the SV side. Six review-deferred test-coverage items extend Step 4's scope: see `## Surprises & Discoveries`.

**Key files:**
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeMultiValueIndexEngine.java` (modified, both commits)
- `core/src/test/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeEngineHistogramBuildTest.java` (modified, both commits)

**Critical context:** Step 3 (SV `buildInitialHistogram`) must mirror Step 2's M1–M5 amendments verbatim on the single-tree shape: assert message format identical to Track 3 `clear()`, bifurcated-lock-posture comment, CHM-cache scope disclaimer, holder-inspection assertions in `BTreeEngineHistogramBuildTest` (the SV cousins of `_approxDiverged_recalibrates` / `_emptyNonNull_recalibratesToNullOnly` / `_filtersTombstoneRID`), and a divergent pre-state on the SV `_delegatesToManager` test. The fixture `BTreeEngineTestFixtures.SingleValueFixture` already installs a real `IndexCountDeltaHolder` (parallel to the MV fixture). For the SV engine: `persistCountDelta` ignores `nullDelta` per Track 1's cross-track signal, so the new `accumulateInMemRecalibration` accumulator is the sole `nullDelta` carrier on the SV recalibration path — verify the SV mirror records `inMemAdjustNull` correctly when `exactNullCount != currentNull`.

### Step 1 — commit 2a83268cc3, 2026-05-25T14:11Z [ctx=safe]
**What was done:** Mixed-mode plumbing landed in the core module. `IndexCountDelta` gained two package-private long fields (`inMemAdjustTotal`, `inMemAdjustNull`) with public getters and a new `accumulateInMemRecalibration(AtomicOperation, int, long, long)` static method carrying no precondition (organic in-mem drift can produce sign-opposed deltas where null-magnitude exceeds total-magnitude; recalibration arithmetic must accept this shape). `AbstractStorage.applyIndexCountDeltas` (Hook B) now sums `getTotalDelta() + getInMemAdjustTotal()` and `getNullDelta() + getInMemAdjustNull()` when calling the BTree mutators. `accumulateClearOrRecalibrate` Javadoc trimmed: caller list shortened to the two `clear()` sites with a forward pointer to the new method, and the precondition justification rewritten to mention only `clear()`. Seven new unit tests in `IndexCountDeltaHolderTest` cover the no-precondition contract (sign-opposed and skewed deltas accepted), additive composition with per-put and long-form accumulators, per-engine isolation, rollback discard at the holder boundary, and default-zero invariant on the new fields.

**What was discovered:** PSI lookups before editing confirmed plan citations were still accurate post-Track-3 base: `accumulateClearOrRecalibrate` at line 111, `applyIndexCountDeltas` at line 2496 (mutator calls at 2529–2530). No drift to fix. A pre-existing "rather than" phrasing inherited from the original `accumulateClearOrRecalibrate` Javadoc surfaced through the AI-tell scan after the Javadoc trim; rewrote inline to a parenthetical "(not per-key)" to keep the trimmed paragraph clean.

**What changed from the plan:** none.

**Key files:**
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/IndexCountDelta.java` (modified)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/storage/impl/local/AbstractStorage.java` (modified)
- `core/src/test/java/com/jetbrains/youtrackdb/internal/core/index/engine/IndexCountDeltaHolderTest.java` (modified)

**Critical context:** Step 2 and Step 3 callers must use the new `accumulateInMemRecalibration`, not `accumulateClearOrRecalibrate`, because recalibration deltas can be sign-opposed and skewed in ways that trip the latter's runtime assert under `-ea`. The new accumulator plus Hook B's sum is the sole in-mem path for `buildInitialHistogram` recalibration; the persisted-side write stays inline (`setApproximateEntriesCount(op, target)`) per the mixed-mode design. Step 4's rollback regression test should assert that rollback leaves `inMemAdjustTotal` / `inMemAdjustNull` discarded (holder is dropped with the rolled-back AtomicOperation), matching the test added here at the holder boundary.

## Validation and Acceptance

After Track 4 lands:

- `buildInitialHistogram` on both engines does not mutate the in-memory `AtomicLong` directly. The in-mem side advances through Hook B's `applyIndexCountDeltas` after `commitChanges` returns, which sums `getTotalDelta() + getInMemAdjustTotal()` and `getNullDelta() + getInMemAdjustNull()` and calls `addToApproximate{Entries,Null}Count`.
- The persisted EP-page side keeps today's inline `setApproximateEntriesCount(op, target)` writes; on every successful recalibration the persisted side lands at the absolute target, healing any pre-existing in-mem-vs-persisted drift. On rollback the persisted side reverts via WAL.
- The recalibration-rollback regression test passes: on a forced rollback after `buildInitialHistogram` returns (in-mem delta recorded but not yet applied), the in-mem counters retain pre-recalibration values and the persisted EP page reverts via WAL. On a separate successful run, both sides land on the post-recalibration target.
- The structural-impossibility claim is scope-bounded to the count counters. `IndexHistogramManager.cache.put` at lines 763, 806, 831 mutates eagerly inside the atomic op and is NOT reverted on rollback; histogram CHM cache divergence is named as out-of-scope (deferred follow-up).
- The histogram-build itself (bucket creation, key scanning) is unchanged.
- `IndexAbstract.buildHistogramAfterFill`'s outer `IOException` handler still swallows builder failures at lines 401–408 (PSI-verified at the Pre-Flight gate, post-Track-3).

## Idempotence and Recovery

## Artifacts and Notes

## Interfaces and Dependencies

**In-scope files**:
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/IndexCountDelta.java` — new `accumulateInMemRecalibration` static method, two new package-private fields `inMemAdjustTotal` / `inMemAdjustNull`, two new public getters.
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/storage/impl/local/AbstractStorage.java` — `applyIndexCountDeltas` (method at line 2496) reads both accumulator pairs when calling the engine mutators at lines 2538–2541 post-Step-1.
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeMultiValueIndexEngine.java` — `buildInitialHistogram` body, mixed-mode rewrite of lines 650–653 (pre-Step-2) landing at lines 670–680 post-Step-2 with comment block extended to line 669.
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/index/engine/v1/BTreeSingleValueIndexEngine.java` — `buildInitialHistogram` body, mixed-mode rewrite of lines 647–650 (pre-Step-3) landing at lines 673–681 post-Step-3 with comment block extended to line 671.
- New regression test `IndexAbstractBuildHistogramRollbackTest` under `core/src/test/.../index/`.
- Existing-test rewrite in `core/src/test/.../index/BTreeEngineHistogramBuildTest.java` (in-mem-side assertions migrate to the holder-inspection pattern; persisted-side `verify(...setApproximateEntriesCount...)` assertions stay).

**Out of scope**:
- `IndexHistogramManager.buildHistogram` itself (the histogram-bucket-creation logic).
- The `IOException`-swallow at `IndexAbstract.buildHistogramAfterFill:401–408` (PSI-verified at the Pre-Flight gate, post-Track-3).
- `clear()` conversion (Track 3).
- `IndexCountDelta.accumulateClearOrRecalibrate` is untouched (Track 3 contract preserved); Track 4 adds a separate method rather than re-tuning the existing one.
- Histogram CHM cache divergence at `IndexHistogramManager.java:763, 806, 831` (eager mutation inside the atomic op, not reverted on rollback). Deferred follow-up; named in Validation and Acceptance as out-of-scope.
- Hook A's `persistCountDelta` — unchanged. The in-mem-only adjustments do not feed the persisted side.

**Inter-track dependencies**:
- **Depends on Track 2**: Hook B's `applyIndexCountDeltas` consumes the new `inMemAdjustTotal` / `inMemAdjustNull` fields. Without Track 2's hook wiring, the in-mem delta is dropped at the end of the atomic op and the in-mem side never advances — strictly worse than today.
- **Independent of Track 3**: Track 3 added `accumulateClearOrRecalibrate` (commit `5f7f882830`) for the `clear()` long-form path; Track 4 adds `accumulateInMemRecalibration` as a separate method. The previously-anticipated overload reuse is dropped under (β) because the two methods carry different invariants (clear has sign-alignment + magnitude bound; recalibration has neither).

**Library/function signatures relevant to this track**:
- New: `IndexCountDelta.accumulateInMemRecalibration(AtomicOperation atomicOperation, int engineId, long totalDelta, long nullDelta)` — in-mem-only recalibration accumulator, no precondition.
- New fields: `IndexCountDelta.inMemAdjustTotal` / `inMemAdjustNull` (package-private long, default 0) with public getters `getInMemAdjustTotal()` / `getInMemAdjustNull()`.
- Modified: `AbstractStorage.applyIndexCountDeltas` body (method at line 2496; engine-mutator calls at lines 2538–2541 post-Step-1) sums both accumulator pairs when calling `addToApproximate{Entries,Null}Count`.
- `BTreeMultiValueIndexEngine.buildInitialHistogram(AtomicOperation)` — rewritten lines 650–653 (pre-Step-2) landing at lines 670–680 post-Step-2 (mixed-mode).
- `BTreeSingleValueIndexEngine.buildInitialHistogram(AtomicOperation)` — rewritten lines 647–650 (pre-Step-3) landing at lines 673–681 post-Step-3 (mixed-mode).
- `IndexAbstract.buildHistogramAfterFill` — unchanged caller.
- Existing untouched: `IndexCountDelta.accumulateClearOrRecalibrate(AtomicOperation, int, long, long)` (Track 3, commit `5f7f882830`, body at `IndexCountDelta.java:140–154` post-Step-1).

## Base commit

1ebde1e462765720b99c1d6ec445b7a59132e972
