<!-- workflow-sha: 5db61a37462f0b28965113f39a81b6fcb1ed1340 -->
# Track 1: Foundation extension in `core` for OTel-readiness

## Purpose / Big Picture

After this track lands, `core` carries the SPI surfaces the OTel module needs: a process-global listener registry exposed as static methods on `YourTracks`, ten new default accessors on `QueryMetricsListener.QueryDetails` (operation / collection / namespace / errorType / querySource plus the five structural fields from D19), one new accessor on `TransactionMetricsListener.TransactionDetails` (namespace), a `Classification` value record plus two static-utility classifiers (`GremlinBytecodeClassifier`, `SqlSyntaxClassifier`) called directly from the existing fire sites, a `GremlinSpanLifecycleHook` interface plus a `setLifecycleHook(...)` setter on `YTDBQueryMetricsStep` that the OTel module's Track 3 strategy uses to install its impl, and a `beginInternal()` snapshot of the global listener registry the per-TX listener fields layer on top of. No new lifecycle methods are added to `TransactionMetricsListener` (D3 / D10 reversed); the only TX-level listener fires remain the pre-existing `writeTransactionCommitted` / `writeTransactionFailed` whose try/catch wrapper Track 1 widens from `Exception` to the union `Exception | LinkageError | AssertionError` (a deliberately narrower set than `Throwable`, so OTel-typical failures are isolated while `VirtualMachineError` and `ThreadDeath` still propagate per JLS guidance). No behavior changes for transactions without registered listeners.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Extend the existing listener SPI with the hooks the OTel module needs: a process-global listener registry exposed as static methods on `YourTracks`, accessors on the nested `QueryDetails` and `TransactionDetails` interfaces for the classifier-derived operation/collection plus the database name, the new `querySource` accessor that lets the OTel listener route between enrich-existing-span and create-new-span paths, two static-utility classifier classes (`GremlinBytecodeClassifier`, `SqlSyntaxClassifier`) and a `Classification` value record living in `core` next to the existing parsing code, a `GremlinSpanLifecycleHook` interface plus a `setLifecycleHook(...)` setter on `YTDBQueryMetricsStep`, and a strategy-gate update so globally-registered listeners actually receive callbacks. No behavior change yet; every existing call path stays green.

## Progress
- [ ] Review + decomposition
- [ ] Step implementation
- [ ] Track-level code review
- [ ] Track completion

## Surprises & Discoveries

## Decision Log

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective

## Context and Orientation

The relevant code lives entirely under `core/src/main/java/com/jetbrains/youtrackdb/`:

- `internal/common/profiler/monitoring/QueryMetricsListener.java` carries the existing nested interface `QueryMetricsListener.QueryDetails` and the single `queryFinished` callback. Today `queryFinished` fires only when `ytdbTx.getQueryMetricsListener()` is non-NO_OP — a single per-TX listener.
- `internal/common/profiler/monitoring/TransactionMetricsListener.java` carries the existing nested `TransactionMetricsListener.TransactionDetails` interface and two existing default no-op methods (`writeTransactionCommitted`, `writeTransactionFailed`).
- `internal/common/profiler/monitoring/YTDBQueryMetricsStep.java` is the TinkerPop step injected by `YTDBQueryMetricsStrategy`. Its `close()` method (around line 116) builds an anonymous `QueryDetails` and fires the listener inside a `try { ... } catch (Exception e) { ... }` (line 148). The step itself is injected only when `YTDBQueryMetricsStrategy.apply()` sees `ytdbTx.isQueryMetricsEnabled()` return true — and that method returns true only when a per-TX `withQueryListener` was set, so a globally-registered listener with no per-TX wiring never sees the step at all.
- `internal/core/gremlin/YTDBTransaction.java` is the Gremlin-side TX wrapper (a `final` subclass of TinkerPop's `AbstractTransaction`). Its `doOpen()` (line 154) calls `activeSession.begin()`; `doRollback()` (line 186) calls `activeSession.rollback()`. Both delegate to the underlying `FrontendTransactionImpl`. This track does not touch `YTDBTransaction` — the new TX-lifecycle fires live in `FrontendTransactionImpl` so both Gremlin and native-SQL paths emit them.
- `internal/core/tx/FrontendTransactionImpl.java` is the underlying TX manager. `beginInternal()` (line 164) and `rollbackInternal()` (line 356) are the single chokepoints for every TX begin and rollback in the codebase (Gremlin delegates here; `DatabaseSessionEmbedded` calls them directly from six call sites). The existing private `notifyMetricsListener()` (line 712) carries an `Exception`-only try/catch wrapper for commit success/failure fires.
- `api/YourTracks.java` is a `final` utility class with a private constructor and only static factory methods. The public `YouTrackDB` interface is the factory product. The process-global registry exposes static methods on `YourTracks` only; the `YouTrackDB` interface gets no new methods (so `YouTrackDBRemote` and any other implementors keep compiling unchanged).

The track touches none of the actual emission code (that lives in Track 3). It adds API surfaces, captures the global listener registry snapshot at `FrontendTransactionImpl.beginInternal()`, refactors the existing single-listener call sites to iterate the per-TX snapshot, and widens the existing exception-isolation wrappers from `Exception` to `Exception | LinkageError | AssertionError`. Concrete deliverables:

1. New default accessors on `QueryMetricsListener.QueryDetails` covering the OTel-emission and routing slots — `Optional<String> getOperationName()`, `Optional<String> getCollectionName()`, `Optional<String> getDatabaseName()`, `Optional<String> getErrorType()`, `Optional<QuerySource> getQuerySource()` (where `QuerySource` is a new public enum with `GREMLIN` / `SQL` values, used by `OTelQueryMetricsListener.queryFinished` to route between the enrich-existing-span and create-new-span paths per design.md §"Context propagation in embedded"), `OptionalLong getResultCount()`, `Optional<String> getInvokedVia()` (D40 — `"gremlin_dsl"` when the SQL fire site detects an outer Gremlin span via `Span.fromContext(capturedContext).getAttribute("db.youtrackdb.gremlin.dsl_method") != null`, `"native"` otherwise; populated by `InstrumentedSqlResultSet.close()` at fire time), `Map<String, Object> getCustomAttrs()` (D40 — defaults to `Map.of()`; populated by `YTDBQueryMetricsStep.close()` from `Classification.customAttrs()` for the GREMLIN path; carries `db.youtrackdb.gremlin.*` entries the OTel listener emits as span attributes with appropriate type coercion), plus the structural-attribute accessors from D19 (`getWherePresent`, `getOrderPresent`, `getLimitPresent`, `getFromClassCount`, `getStepCount`, `getHasSubtraversal`), plus the three advisor-foundation accessors from D42 (`getAdvisorFullScan(): Optional<Boolean>`, `getAdvisorCrossClassCount(): Optional<Integer>`, `getAdvisorLargeResult(): Optional<Boolean>` — all default `Optional.empty()`; populated by the classifiers when index-availability and result-size heuristics resolve a value), plus `getExplicitTrackingId(): Optional<String>` from D17. All default to `Optional.empty()` / `OptionalLong.empty()` / `Map.of()` so existing inline implementations keep working; `YTDBQueryMetricsStep` populates them only when the classifier resolves a value and from `session.getDatabaseName()` for the namespace.
2. One new default accessor on `TransactionMetricsListener.TransactionDetails`: `Optional<String> getDatabaseName()`. Populated at the fire site in `FrontendTransactionImpl` from `session.getDatabaseName()`.
3. A `Classification` value record plus two static-utility classifier classes (`GremlinBytecodeClassifier`, `SqlSyntaxClassifier`) in `core/.../profiler/monitoring/`. Each classifier exposes one public static method (`Classification classify(Traversal)` / `Classification classify(SQLStatement)`); the fire sites call them directly. `GremlinBytecodeClassifier.classify(Traversal)` dispatches internally across three branches: **(A) Path A** — when the traversal's step list contains a `YTDBMatchPlanStep` (PR #1038's translator boundary step), it reads `getClassification()` and returns the precomputed `Classification(operation, collection)` value stored on the step at translation time. **(B) Pure passthrough** (D40) — when the bytecode contains only `CallStep[YTDBCommandService.NAME]` or `CallStep[YTDBCommandService.YQL_NAME]` instances (optionally with terminal aggregates like `toList` / `count`), the classifier extracts `CallStep.parameters["command"]` as raw SQL, parses via `SQLEngine.parse(...)` (cached through `YqlStatementCache`), picks the first non-administrative statement (skipping BEGIN / COMMIT / ROLLBACK), and runs `SqlSyntaxClassifier.classify(...)` on the result. The returned `Classification` carries operation + collection from the SQL classifier plus YTDB-specific custom attributes via the new `customAttrs: Map<String, Object>` field on the `Classification` record (`db.youtrackdb.gremlin.dsl_method`, `statement_count`, `has_transaction_control`, `statements_summary` when `statement_count > 1`). **(C) Path B fallback** — otherwise walks the bytecode the same way `produceScript()` already does; if the bytecode contains BOTH graph steps AND `CallStep[YTDBCommandService]` (mixed shape), the classifier runs the normal Path B walk for operation + collection and adds `db.youtrackdb.gremlin.has_graph_steps = true`, `statement_count`, and `embedded_sql.N` (N = 0..2 capped, three-entry bound; sanitized SQL is medium-high cardinality) for the embedded SQL strings. Empty-command edge case sets `db.youtrackdb.gremlin.no_op = true` and falls back to `youtrackdb` catch-all classification. No SPI, no `ServiceLoader`, no `META-INF/services` manifest — the parsing logic the classifiers contain is already happening at the fire sites, so piggybacking on existing parsing costs almost nothing and avoids a plugin layer for code that has exactly one impl per input type. The `Classification` record gains a `customAttrs: Map<String, Object>` field (default empty) carrying any `db.youtrackdb.gremlin.*` attributes derived by Path B branches (B) and (C); Track 3's `OTelQueryMetricsListener` reads the map and sets each entry as a span attribute.
4. A process-global listener registry: a package-private static holder under `internal/common/profiler/monitoring/` (e.g., `GlobalListenerRegistry`) with `CopyOnWriteArrayList` per listener type, exposed via static methods on `YourTracks`: `registerGlobalQueryListener(QueryMetricsListener)`, `unregisterGlobalQueryListener(QueryMetricsListener)`, plus the transaction-listener pair.
5. Snapshot wiring at TX begin: `FrontendTransactionImpl.beginInternal()` captures the global listener registry snapshots (`List<QueryMetricsListener>` and `List<TransactionMetricsListener>`) into new fields before incrementing `txStartCounter` so nested begins reuse the outermost snapshot. The fields clear on outermost commit / rollback. No new `transactionStarted` / `transactionRolledBack` fires are added (D3 / D10 reversed); the only TX-level listener fires remain the pre-existing `writeTransactionCommitted` / `writeTransactionFailed` in `notifyMetricsListener` (line 712). **No `QueryMonitoringMode` snapshot:** mode is resolved per-query via D15's resolver — see deliverable 6 below. The per-TX `QueryMonitoringMode` default set via `YTDBTransaction.withQueryMonitoringMode(...)` is exposed unchanged through `FrontendTransaction.getDefaultQueryMonitoringMode()` (no snapshotting; the commit fire site reads the live value at the moment it fires). **`FrontendTransactionNoTx` symmetry**: `db.query(...)` called before `tx.begin()` runs against `FrontendTransactionNoTx` as `currentTx`. Track 1 implements `FrontendTransactionNoTx.iterateAllQueryListeners()` to return the current global snapshot (the same `GlobalListenerRegistry.snapshotQueryListeners()` value `FrontendTransactionImpl.beginInternal()` captures, read live at call time since NoTx has no begin-time hook). Without this, a globally-registered OTel listener would silently miss every SQL query issued outside an explicit transaction. The accessor returns an empty iterable only when the global registry is empty. Same treatment for `iterateAllTransactionListeners()` even though NoTx never fires commit listeners (returning the global snapshot keeps the contract uniform).
6. Per-query mode resolution infrastructure: new utility `QueryMonitoringModeResolver` in `core/.../profiler/monitoring/`, new sealed interface `TagRule<T>` with three records (`Exact<T>`, `Prefix<T>`, `Regex<T>`) in the same package, and config parsing for `OPENTELEMETRY_QUERY_MODE_TAG_RULES` consumed at the resolver's startup. Resolver holds an immutable `List<TagRule<QueryMonitoringMode>>` parsed once plus a bounded-LRU cache (`Collections.synchronizedMap` over `LinkedHashMap(1024, 0.75f, true)` with access-order eviction, capacity 1024; INFO once per 60 s window on eviction) for resolved `(tag → mode)` pairs, so unique-tag-per-request misuse cannot exhaust the heap. Static `global()` singleton accessor. Two new methods on `FrontendTransaction` interface: `getDefaultQueryMonitoringMode(): QueryMonitoringMode` (renamed from old `getQueryMonitoringMode()` for clarity) and `resolveQueryMonitoringMode(Optional<String> tag): QueryMonitoringMode` (default implementation delegates to `QueryMonitoringModeResolver.global().resolve(tag, getDefaultQueryMonitoringMode())`). Parallel `SlowQueryThresholdResolver` ships in the same package, consuming `TagRule<Long>`; matching `FrontendTransaction.resolveSlowQueryThresholdNanos(Optional<String>)` accessor delegates to it. Config-parser format mirrors the mode rules. The heartbeat gate (D18) has no resolver: the listener reads a single global `defaultHeartbeatNanos` field captured from `OPENTELEMETRY_QUERY_HEARTBEAT_SAMPLE_MILLIS` at SDK init.
7. `GremlinSpanLifecycleHook` interface in `core/.../profiler/monitoring/` next to `YTDBQueryMetricsStep`, with two methods — `onFirstHasNext(QueryDetails details, long startEpochNanos, io.opentelemetry.context.Context parent)` and `onClose(QueryDetails details, long endEpochNanos)`. `YTDBQueryMetricsStep` gains a single-slot `volatile GremlinSpanLifecycleHook lifecycleHook` field plus a `setLifecycleHook(GremlinSpanLifecycleHook)` setter (package-default access, called by the OTel-module strategy from Track 3 that runs as a `FinalizationStrategy` after `YTDBQueryMetricsStrategy`). The step's first `hasNext()` invokes `lifecycleHook.onFirstHasNext(...)` if set; the step's `close()` invokes `lifecycleHook.onClose(...)` AFTER the listener iteration completes. The interface depends only on `io.opentelemetry.context.Context` from the OTel API package the project's listener SPI already references via `capturedContext` on `InstrumentedSqlResultSet`; no new transitive dependency on the OTel SDK is introduced in `core`.
8. Strategy gate update: `YTDBQueryMetricsStrategy.apply()` (declared at line 23; the gate check at line 36) and `YTDBTransaction.isQueryMetricsEnabled()` widen to also return true when the global query snapshot is non-empty, so a globally-registered listener actually causes the step to inject.
9. Existing single-listener call sites refactored to iterate the per-TX snapshot: `YTDBQueryMetricsStep.close()` (fires `queryFinished` against every registered query listener instead of a single per-TX one), `FrontendTransactionImpl.notifyMetricsListener()` (same for commit success/failure). Per-TX `withQueryListener` continues to work by inserting one entry into the same snapshot.
10. Exception-isolation widening: the existing `} catch (Exception e) {` blocks in `FrontendTransactionImpl.notifyMetricsListener:730` and `YTDBQueryMetricsStep:148` widen to `} catch (Exception | LinkageError | AssertionError t) {`. The union is deliberately narrower than `Throwable`: it isolates OTel-typical failure modes (misconfigured SDK throwing `IllegalStateException`, missing exporter classes throwing `NoClassDefFoundError`, listener assertions) while letting `VirtualMachineError` and `ThreadDeath` propagate per JLS guidance. **Test-suite migration**: the widening changes observable behaviour for tests that assert on `NoClassDefFoundError` or `AssertionError` propagating through the listener-fire path. Before the widening lands, grep `core` and `server` test trees for `assertThrows(LinkageError.class, ...)`, `assertThrows(AssertionError.class, ...)`, and `assertThrows(NoClassDefFoundError.class, ...)` whose method-under-test name contains `Listener`, `queryFinished`, `commit`, or `notifyMetricsListener`. Migrate matched tests to assert on the WARN log output via a captured log appender (the widening's documented contract: the throw is swallowed and logged with the listener class name) rather than on the exception propagating. No production-code test in the current tree is expected to match this pattern, but the grep is the safeguard.
11. `YTDBQueryMetricsStep` reads the query's `QueryMonitoringMode` via `txStateAccessor.resolveQueryMonitoringMode(traversal.getConfig(YTDBQueryConfigParam.querySummary))` — per-query resolution from the traversal's tag using the resolver from deliverable 6. The step caches the resolved `isLightweight` at construction time (it stays once-per-step rather than once-per-method-call to keep `hasNext()` / `next()` / `close()` consistent within one step iteration). Required for the per-query Timing-mode uniformity invariant: both the Gremlin step and the SQL hook resolve from the same query tag and therefore reach the same mode value, while a different traversal with a different tag in the same TX can pick a different mode.

12. **PR #1038 boundary-step extension for Path A classification**. `YTDBMatchPlanStep` (in the gremlin/translator/step package, merged via PR #1038) gains a `private final Classification classification` field (2 String references), a fifth constructor parameter, and a public `getClassification(): Classification` accessor. The single caller, `GremlinToMatchStrategy`, computes the precomputed value once at translation time before constructing the boundary step: `operation = "SELECT"` hardcoded (Phase 1 only translates read patterns); `collection = pattern.firstNode().aliasClasses.get(boundaryAlias)` when present, else `Optional.empty()`. For `MultiPlanMatchStep` (PR #1038 Track 10 union variant subclass), the strategy populates the value from the first child plan's first-node class at translation time, mirroring the single-plan rule. The accessor's only consumer is `GremlinBytecodeClassifier.classifyFromBoundaryStep(YTDBMatchPlanStep)` in deliverable 3. The change is additive (existing constructors stay; only the call site in `GremlinToMatchStrategy` updates). Memory cost is 2 String references per boundary-step instance — the minimum the step can pay while still supporting Path A dispatch, vs. retaining the full ~19-field `MatchPlanInputs` IR. The IR record itself is discarded by the strategy after `MatchExecutionPlanner` consumes it, as in pre-extension PR #1038. Carried inside YTDB-496 rather than as a follow-up commit on `develop` because the accessor exists exclusively for OTel telemetry classification. Track 1 tests assert that the accessor returns the value the strategy computed, that `MultiPlanMatchStep` instances carry their own precomputed `Classification`, and that the strategy never holds a reference to `MatchPlanInputs` after the boundary step is constructed.

## Plan of Work

The track lands in nine edits, sequenced so each commit leaves the test suite green.

The first edit adds the `getDatabaseName()` default accessor on the nested `TransactionMetricsListener.TransactionDetails`. Because the accessor has a default implementation returning `Optional.empty()`, every existing call site and every existing test class compiles unchanged.

The second edit extends `QueryMetricsListener.QueryDetails` with the routing and emission slots: `getOperationName`, `getCollectionName`, `getDatabaseName`, `getErrorType`, `getQuerySource` (returning `Optional<QuerySource>` where `QuerySource` is the new public enum with `GREMLIN` / `SQL` values), `getResultCount`, plus the six structural-attribute accessors from D19 (`getWherePresent`, `getOrderPresent`, `getLimitPresent`, `getFromClassCount`, `getStepCount`, `getHasSubtraversal`). All as default methods returning `Optional.empty()` / `OptionalLong.empty()`; the inline impl in `YTDBQueryMetricsStep` does not need to override yet.

The third edit defines the `Classification` record plus the two static-utility classifier classes under `internal/common/profiler/monitoring/`. The record carries the operation/collection pair from D9, the six structural fields from D19 (`wherePresent`, `orderPresent`, `limitPresent`, `fromClassCount`, `stepCount`, `hasSubtraversal`), three advisor flags from D42 (`advisorFullScan`, `advisorCrossClassCount`, `advisorLargeResult` — all default to `Optional.empty()`; populated by the classifiers using index-availability and result-size heuristics), and a `customAttrs: Map<String, Object>` map from D40 (defaults to `Map.of()`) carrying any `db.youtrackdb.gremlin.*` attributes derived during Path B classification. A `Classification.EMPTY` constant covers the unrecognized-shape case. `GremlinBytecodeClassifier.classify(Traversal)` dispatches across three branches: **Path A** reads `YTDBMatchPlanStep.getClassification()` when the step list contains the PR #1038 boundary step. **Pure passthrough (D40)** detects when the bytecode contains only `CallStep[YTDBCommandService.NAME]` or `CallStep[YTDBCommandService.YQL_NAME]` instances (optionally with terminal aggregates `toList` / `count`), extracts `CallStep.parameters["command"]` as raw SQL, parses via `SQLEngine.parse(...)` (cached through `YqlStatementCache`), picks the first non-administrative statement (skipping BEGIN / COMMIT / ROLLBACK), runs `SqlSyntaxClassifier.classify(...)` for operation + collection, sanitizes via `SqlSanitizer` for `db.query.text`, and populates `customAttrs` with `db.youtrackdb.gremlin.dsl_method`, `statement_count`, `has_transaction_control`, and `statements_summary` (joined string, capped at 5 statements then `"; +N more"`). **Path B fallback** walks the bytecode the same way `produceScript()` already does; on mixed shapes (graph steps AND `CallStep[YTDBCommandService]`), it adds `db.youtrackdb.gremlin.has_graph_steps = true`, `statement_count`, and `embedded_sql.N` (N = 0..2 capped, three-entry bound; sanitized SQL is medium-high cardinality) to `customAttrs`. Empty-command edge case sets `db.youtrackdb.gremlin.no_op = true` and falls back to `youtrackdb` catch-all classification. `SqlSyntaxClassifier` exposes `static Classification classify(SQLStatement statement)`, dispatching on the AST subclass. Both helpers are pure functions, fail-safe (any unexpected input returns `Classification.EMPTY`), and unit-testable in isolation without spinning up a transaction. Track 3 wires the Gremlin classifier into `YTDBQueryMetricsStep.close()` (and reads `Classification.customAttrs` to set the `db.youtrackdb.gremlin.*` attributes on the outer Gremlin span); Track 4 wires the SQL classifier into the new `InstrumentedSqlResultSet` session-boundary wrapper installed at the five `DatabaseSessionEmbedded` wrap sites.

The fourth edit creates the process-global registry: a package-private static holder (e.g., `GlobalListenerRegistry`) under `internal/common/profiler/monitoring/` with a `CopyOnWriteArrayList` per listener type, plus `snapshotQueryListeners()` / `snapshotTransactionListeners()` returning immutable `List.copyOf(...)` snapshots. `YourTracks` gains four public static methods (`registerGlobalQueryListener` / `unregisterGlobalQueryListener` plus the transaction-listener pair) that delegate to the holder.

The fifth edit wires `FrontendTransactionImpl.beginInternal()` to capture the global query and transaction listener snapshots into two new fields before `txStartCounter` increments. No new lifecycle fires are added in `beginInternal()` or `rollbackInternal()` (D3 / D10 reversed); the only TX-level listener fires remain the pre-existing `writeTransactionCommitted` / `writeTransactionFailed` in `notifyMetricsListener` (line 712), which the seventh edit refactors to iterate the captured transaction snapshot plus the per-TX list. No `QueryMonitoringMode` snapshot is captured — mode resolves per-query via the resolver added in the sixth edit. Two accessors on `FrontendTransaction` expose mode: `getDefaultQueryMonitoringMode()` returns the live per-TX default (set via `YTDBTransaction.withQueryMonitoringMode(...)`, read from the YTDBTransaction field with no snapshotting because per-query resolution provides finer-grained control); `resolveQueryMonitoringMode(Optional<String> tag)` delegates to the resolver. Track 4's SQL fire site at `InstrumentedSqlResultSet.wrapIfListening(...)` calls `currentTx.resolveQueryMonitoringMode(Optional.empty())` (SQL has no tag source; resolver short-circuits to per-TX default); the Gremlin step calls the same accessor with `traversal.getConfig(YTDBQueryConfigParam.querySummary)`; the commit fire site uses `currentTx.getDefaultQueryMonitoringMode()` directly. On outermost commit / rollback the listener snapshots clear.

The sixth edit adds `QueryMonitoringModeResolver` + `TagRule<T>` sealed interface in `core/.../profiler/monitoring/` and the config-parsing entry point that consumes `OPENTELEMETRY_QUERY_MODE_TAG_RULES` from `GlobalConfiguration` at startup. `QueryMonitoringModeResolver` holds an immutable `List<TagRule<QueryMonitoringMode>>` parsed once (Track 5 adds the config entry; the resolver reads it on first `global()` access) and a bounded-LRU cache (`Collections.synchronizedMap` over `LinkedHashMap(1024, 0.75f, true)` with access-order eviction, capacity 1024). `resolve(Optional<String> tag, QueryMonitoringMode txDefault)` returns `txDefault` for empty tags; otherwise walks the rule list first-wins, caching results. `TagRule<T>` is a sealed interface with three records: `Exact<T>(String tag, T value)`, `Prefix<T>(String prefix, T value)`, `Regex<T>(Pattern pattern, T value)`. Generic so a future per-tag slow-query threshold resolver reuses the same matcher hierarchy. Config parser format: `tag=MODE` (exact), `prefix:X=MODE` (prefix), `regex:X=MODE` (regex), comma-separated. Invalid entries log WARN at startup and are dropped; the resolver continues with valid rules.

The seventh edit adds `GremlinSpanLifecycleHook` interface in `core/.../profiler/monitoring/` next to `YTDBQueryMetricsStep`, plus the single-slot `volatile GremlinSpanLifecycleHook lifecycleHook` field and `setLifecycleHook(GremlinSpanLifecycleHook)` package-default setter on the step. The step's first `hasNext()` invokes `lifecycleHook.onFirstHasNext(details, startEpochNanos, parentContext)` if the field is non-null; the step's `close()` invokes `lifecycleHook.onClose(details, endEpochNanos)` AFTER the listener iteration completes (the lifecycle hook's `onClose` is the LAST step in `close()` so the OTel hook sees the same QueryDetails the listeners received and can read `Span.fromContext(Context.current())` to end the span the listeners enriched). No core-side impl ships here; Track 3 installs the OTel impl via a `FinalizationStrategy` that runs after `YTDBQueryMetricsStrategy`. The interface depends only on `io.opentelemetry.context.Context` from the OTel API package the project already references via `InstrumentedSqlResultSet`'s `capturedContext` field; no new transitive dependency on the OTel SDK is introduced in `core`.

The eighth edit updates the strategy gate: `YTDBQueryMetricsStrategy.apply()` (declared at line 23; the gate check at line 36) and `YTDBTransaction.isQueryMetricsEnabled()` widen the gate to also return true when the global query snapshot is non-empty. Without this edit a globally-registered listener never sees `queryFinished` because the step is never injected. `isTransactionMetricsEnabled()` widens symmetrically so the commit-side fire path injects when the global transaction-listener snapshot is non-empty.

The ninth edit refactors the single-listener call sites: `YTDBQueryMetricsStep.close()` now iterates every query listener in the per-TX snapshot (which includes any per-TX `withQueryListener` plus the global snapshot taken at begin), and `FrontendTransactionImpl.notifyMetricsListener()` iterates the transaction snapshot for commit success/failure. Each per-iteration call sits inside the existing try/catch wrapper, widened to `Exception | LinkageError | AssertionError`. The same edit switches `YTDBQueryMetricsStep`'s timing-mode read source from `ytdbTx.getQueryMonitoringMode()` (mutable YTDBTransaction field) to `txStateAccessor.resolveQueryMonitoringMode(traversal.getConfig(YTDBQueryConfigParam.querySummary))` — per-query resolution from the traversal's tag via the resolver. The construction-time `isLightweight` cache (line 68) stays; it now caches the resolved value for this step's lifetime. Cache consistency across `hasNext()` / `next()` / `close()` is per-step (the cached value is captured once at step construction); each query in the TX builds its own step instance, so different queries can use different modes when their tags resolve to different rules. The same edit removes `monitoredCommitInternal` from `FrontendTransaction` (per design.md §"Listener registration and ordering" `monitoredCommitInternal` signature change): the parameterless `commitInternal()` already on the interface (line 55) becomes the sole commit entry, `notifyMetricsListener` reads `getDefaultQueryMonitoringMode()` + `getTrackingId()` directly, and `YTDBTransaction.doCommit()` loses its `isTransactionMetricsEnabled()` branch because `commitInternal()` short-circuits on empty snapshot with zero allocation.

A final test edit (kept inside the ninth commit for ordering reasons; testing the prior edits is its own commit-sized unit) adds JUnit 4 tests under `core/src/test/java/.../profiler/monitoring/`: a test class proving that registering a listener globally causes it to receive `queryFinished` callbacks for every query and `writeTransactionCommitted` / `writeTransactionFailed` for every write transaction (covering both Gremlin and native-SQL paths through one shared fire site), a test class proving that any `Exception`, `LinkageError`, or `AssertionError` thrown from any listener method does not break the transaction (and that `VirtualMachineError` is NOT caught — a deliberately scoped test that throws a synthetic `Error` subclass outside the union and asserts the transaction unwinds), and a test class proving that listeners registered after a transaction begins are not seen by that transaction.

Ordering: edits 1, 2, 3 are independent (any order). Edit 4 must precede edits 5, 6, 8, 9. Edit 5 precedes edit 9 (snapshot field exists). Edit 6 precedes edit 9 (resolvers exist before the timing-read switch). Edit 7 is independent of 5/6 and precedes edit 9 (the hook setter must exist before Track 3's strategy can call it). Edit 8 precedes edit 9 (gate widening exposes globally-registered listeners). The test edit runs last after edit 9.

## Concrete Steps

<!-- Phase A placeholder — decomposition writes a thin numbered roster here. -->

## Episodes

## Validation and Acceptance

After Track 1, the following behaviors hold:

- Registering a `QueryMetricsListener` via `YourTracks.registerGlobalQueryListener(listener)` causes that listener to receive `queryFinished` callbacks for every subsequent query in every newly-opened transaction, on both the Gremlin path (`YTDBQueryMetricsStep.close()`) and (once Track 4 wires it in) the native-SQL path. Listeners registered after a transaction is open do not fire for that transaction.
- Registering a `TransactionMetricsListener` via `YourTracks.registerGlobalTransactionListener(listener)` causes that listener to receive `writeTransactionCommitted` for every successfully-committed write transaction and `writeTransactionFailed` for every failed write transaction, regardless of whether the transaction originated from Gremlin or native SQL. Read-only transactions emit nothing on the TX listener path (existing YTDB semantics preserved; D3 reversed dropped the TX-lifetime span).
- `QueryDetails.getQuerySource()` returns `Optional.of(QuerySource.GREMLIN)` when populated by `YTDBQueryMetricsStep.close()` and `Optional.of(QuerySource.SQL)` when populated by `InstrumentedSqlResultSet.close()` (Track 4). The default returns `Optional.empty()` so custom inline impls are unaffected.
- `YTDBQueryMetricsStep` exposes `setLifecycleHook(GremlinSpanLifecycleHook)`. When set, the step invokes `onFirstHasNext(...)` on the first `hasNext()` call and `onClose(...)` on `close()` after the listener iteration completes. When unset (the default), the step's behavior is identical to today's.
- Any `Exception`, `LinkageError`, or `AssertionError` thrown from any listener callback is caught and logged at WARN; the transaction continues; subsequent listeners in the snapshot still fire. `VirtualMachineError` and `ThreadDeath` are NOT caught and propagate per JLS guidance.
- Per-TX `withQueryListener` / `withTransactionListener` semantics change from single-slot (overwrite) to additive list-append. `tx.withQueryListener(l1).withQueryListener(l2)` previously yielded only `l2`; now both fire in insertion order. `clearMonitoringState()` clears the list instead of resetting a single slot. This is a deliberate backward-incompatible change in an internal SPI; the single-slot model was the source of the inert-flag bug the global registry exists to fix. `getQueryMetricsListener()` is replaced by `getQueryMetricsListeners(): Iterable<QueryMetricsListener>`. `isQueryMetricsEnabled()` returns true when either the per-TX list is non-empty OR the registry snapshot captured at `beginInternal()` is non-empty (this is the widening `YTDBQueryMetricsStrategy.apply()` depends on for Gremlin step injection).
- `monitoredCommitInternal(listener, mode, trackingId)` is removed from `FrontendTransaction`. The parameterless `commitInternal()` (already in the interface) handles listener iteration internally: `FrontendTransactionImpl.notifyMetricsListener` reads the transaction-listener snapshot field plus the per-TX `transactionMetricsListeners` list and iterates both inside the existing try/catch wrapper. Mode and trackingId previously passed as parameters become reads of `currentTx.getDefaultQueryMonitoringMode()` and `currentTx.getTrackingId()` at the fire site. `YTDBTransaction.doCommit()` loses its `isTransactionMetricsEnabled()` branch — `commitInternal()` short-circuits with zero allocation when no listeners are registered.
- `QueryMetricsListener.QueryDetails.getOperationName()`, `getCollectionName()`, and `getDatabaseName()` are reachable; the first two return `Optional.empty()` until Track 3 / Track 4 wire classifiers; `getDatabaseName()` returns the session's database name when the fire site populates it, else `Optional.empty()`.
- `YTDBQueryMetricsStrategy.apply()` injects `YTDBQueryMetricsStep` when the global query snapshot is non-empty even without a per-TX `withQueryListener` call.

<!-- Phase A placeholder for per-step EARS/Gherkin lines. -->

<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery

<!-- Phase A placeholder — names per-step idempotence and recovery paths once steps are decomposed. -->

## Artifacts and Notes

## Interfaces and Dependencies

In scope:
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/QueryMetricsListener.java` (extend nested `QueryDetails`)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/TransactionMetricsListener.java` (extend nested `TransactionDetails` only — no new methods on the outer interface; D3 / D10 reversed)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/YTDBQueryMetricsStep.java` (slot enrichment, iterate per-TX snapshot at `close()`, widen catch to `Exception | LinkageError | AssertionError`, switch `QueryMonitoringMode` read source from `ytdbTx.getQueryMonitoringMode()` to `txStateAccessor.resolveQueryMonitoringMode(traversal.getConfig(YTDBQueryConfigParam.querySummary))` — per-query resolution; `isLightweight` cache at ctor stays, now caches the resolved value for this step's lifetime)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/YTDBQueryMetricsStrategy.java` (gate widening so the step injects when the global query snapshot is non-empty)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/gremlin/translator/step/YTDBMatchPlanStep.java` (PR #1038 boundary step; adds a `private final Classification classification` field, a constructor parameter, and a public `getClassification(): Classification` accessor so `GremlinBytecodeClassifier` Path A can read the precomputed value. The field holds a lightweight 2-String value `GremlinToMatchStrategy` computes once at translation time; the IR `MatchPlanInputs` stays discarded as in pre-extension PR #1038. Lifetime cost is 2 String references per boundary-step instance. Additive change; no behavior impact on PR #1038 functionality.)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/gremlin/translator/step/MultiPlanMatchStep.java` (PR #1038 Track 10 union variant; inherits the new `classification` field + accessor from the `YTDBMatchPlanStep` base class. `GremlinToMatchStrategy` computes the value from the first child plan's first-node class at translation time, mirroring the single-plan rule. No file-local edits beyond the base-class extension itself.)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/gremlin/translator/strategy/GremlinToMatchStrategy.java` (PR #1038 strategy; updates the `new YTDBMatchPlanStep(...)` and `new MultiPlanMatchStep(...)` construction sites to compute the `Classification` once at translation time and pass it to the constructor. The IR `MatchPlanInputs` record is still discarded after `MatchExecutionPlanner` consumes it. Additive call-site changes, ~10 lines total.)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/tx/FrontendTransactionImpl.java` (listener snapshot capture in `beginInternal`, lifecycle fires in `beginInternal` + `rollbackInternal`, iterate transaction snapshot in `notifyMetricsListener`, widen wrappers to `Exception | LinkageError | AssertionError`; `getDefaultQueryMonitoringMode()` implementation reads the YTDBTransaction field live, no snapshotting)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/tx/FrontendTransactionNoTx.java` (implement `iterateAllQueryListeners()` and `iterateAllTransactionListeners()` to return the live global registry snapshot; `monitoredCommitInternal` removal mirrors `FrontendTransactionImpl`)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/tx/FrontendTransaction.java` (two new accessors: `getDefaultQueryMonitoringMode(): QueryMonitoringMode` returning the per-TX fallback mode and `resolveQueryMonitoringMode(Optional<String> tag): QueryMonitoringMode` with default implementation delegating to `QueryMonitoringModeResolver.global().resolve(tag, getDefaultQueryMonitoringMode())`)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/QueryMonitoringModeResolver.java` (new utility — immutable rule list parsed from `OPENTELEMETRY_QUERY_MODE_TAG_RULES` at startup, bounded-LRU cache via `Collections.synchronizedMap` over `LinkedHashMap(1024, 0.75f, true)` access-order, static `global()` accessor, `resolve(Optional<String>, QueryMonitoringMode)` walking rules first-wins)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/TagRule.java` (new sealed interface with three records `Exact<T>`, `Prefix<T>`, `Regex<T>`; generic to support future per-tag threshold resolver)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/gremlin/YTDBTransaction.java` (untouched — existing `withQueryMonitoringMode(...)` and `getQueryMonitoringMode()` accessors continue to work; no setter wiring needed because mode is no longer snapshotted)
- `core/src/main/java/com/jetbrains/youtrackdb/api/YourTracks.java` (four new static methods delegating to the new registry holder)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/GlobalListenerRegistry.java` (package-private holder; `CopyOnWriteArrayList` per listener type plus snapshot methods)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/Classification.java` (small value record + `EMPTY` constant)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/GremlinBytecodeClassifier.java` (static utility — `Classification classify(Traversal)` dispatching Path A via `YTDBMatchPlanStep.getClassification()` (precomputed by the strategy at translation time) and Path B via bytecode walk)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/SqlSyntaxClassifier.java` (static utility — `Classification classify(SQLStatement)`)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/QuerySource.java` (public enum with `GREMLIN` / `SQL` values; consumed by `QueryDetails.getQuerySource()` to route the OTel listener)
- New `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/GremlinSpanLifecycleHook.java` (interface with `onFirstHasNext(QueryDetails, long, Context)` / `onClose(QueryDetails, long)` methods; impl ships in Track 3 inside the OTel module)
- `core/src/main/java/com/jetbrains/youtrackdb/internal/common/profiler/monitoring/YTDBQueryMetricsStep.java` (gains the `volatile GremlinSpanLifecycleHook lifecycleHook` field + `setLifecycleHook(GremlinSpanLifecycleHook)` package-default setter; `processNextStart()` invokes `lifecycleHook.onFirstHasNext(...)` on first call; `close()` invokes `lifecycleHook.onClose(...)` after listener iteration)

Out of scope:
- `core/src/main/java/com/jetbrains/youtrackdb/api/YouTrackDB.java` — no new methods; the registry is process-global on `YourTracks` only (CR9 resolution).
- `core/src/main/java/com/jetbrains/youtrackdb/internal/core/gremlin/YTDBTransaction.java` `doOpen()` / `doRollback()` — untouched; the new TX-lifecycle fires live in `FrontendTransactionImpl` so both Gremlin and native-SQL paths emit them. `withQueryMonitoringMode(...)` and `getQueryMonitoringMode()` accessors stay unchanged — mode is no longer snapshotted, so no setter wiring into the impl is needed.
- Every file in `server/`, `embedded/`, `tests/`. Foundation does not need to know about server lifecycle or embedded host.
- The OTel module (does not exist yet, lands in Track 2).
- The fire-site wiring that calls the classifiers (Track 3 wires `GremlinBytecodeClassifier` into `YTDBQueryMetricsStep.close()`; Track 4 wires `SqlSyntaxClassifier` into the new `InstrumentedSqlResultSet` wrapper at the three `DatabaseSessionEmbedded` SQL entry points). Track 1 ships the classifier helpers themselves; the consumer tracks wire them at the fire sites.

Inter-track dependencies:
- Provides for Track 3: the listener SPI extensions, the `QueryDetails` / `TransactionDetails` accessor slots, the `GremlinBytecodeClassifier` static helper called by `YTDBQueryMetricsStep.close()` to populate operation/collection on the inline `QueryDetails`, the iteration shape in `YTDBQueryMetricsStep.close()`.
- Provides for Track 4: the `SqlSyntaxClassifier` static helper called by `InstrumentedSqlResultSet`'s lazy `QueryDetails.getOperationName()` / `getCollectionName()` accessors to populate operation/collection on the SQL `QueryDetails`; the `QueryDetails.getResultCount(): OptionalLong` default accessor signature that the wrapper's row counter feeds; the global query snapshot accessible from `currentTx`; the `currentTx.resolveQueryMonitoringMode(Optional<String>)` accessor returning the per-query `LIGHTWEIGHT` / `EXACT` value the SQL fire site uses to route timing capture (LIGHTWEIGHT → `GranularTicker`, EXACT → `System.nanoTime()`); plus `currentTx.getDefaultQueryMonitoringMode()` as fallback. Track 4 passes `Optional.empty()` to the resolver because SQL has no tag source (per-tag overrides apply only to Gremlin traversals); the resolver short-circuits to the per-TX default.
- Provides for Track 5: the global registry static methods used by `YouTrackDBOpenTelemetry.setOpenTelemetry()` to install the OTel listeners.

Library / function signatures introduced:

```java
// existing nested interface, new default accessors for routing + emission slots
public interface QueryMetricsListener {
  interface QueryDetails {
    // operation / collection / namespace / error / source / row count
    default Optional<String> getOperationName() { return Optional.empty(); }
    default Optional<String> getCollectionName() { return Optional.empty(); }
    default Optional<String> getDatabaseName() { return Optional.empty(); }
    default Optional<String> getErrorType() { return Optional.empty(); }
    default Optional<QuerySource> getQuerySource() { return Optional.empty(); }
    default OptionalLong getResultCount() { return OptionalLong.empty(); }
    // D17 — explicit tracking ID opt-in
    default Optional<String> getExplicitTrackingId() { return Optional.empty(); }
    // D19 — structural attributes under db.youtrackdb.*
    default Optional<Boolean> getWherePresent() { return Optional.empty(); }
    default Optional<Boolean> getOrderPresent() { return Optional.empty(); }
    default Optional<Boolean> getLimitPresent() { return Optional.empty(); }
    default Optional<Integer> getFromClassCount() { return Optional.empty(); }
    default Optional<Integer> getStepCount() { return Optional.empty(); }
    default Optional<Boolean> getHasSubtraversal() { return Optional.empty(); }
    // D40 — Gremlin DSL vs native invocation disambiguation; set by InstrumentedSqlResultSet
    // when Span.fromContext(capturedContext) carries db.youtrackdb.gremlin.dsl_method
    default Optional<String> getInvokedVia() { return Optional.empty(); }
    // D40 — db.youtrackdb.gremlin.* attributes derived by GremlinBytecodeClassifier;
    // YTDBQueryMetricsStep populates from Classification.customAttrs()
    default Map<String, Object> getCustomAttrs() { return Map.of(); }
    // existing methods unchanged
  }
  // existing method unchanged
}

// existing nested interface, one new default accessor; NO new outer methods
// (D3 / D10 reversed — transactionStarted / transactionRolledBack are NOT added)
public interface TransactionMetricsListener {
  interface TransactionDetails {
    default Optional<String> getDatabaseName() { return Optional.empty(); }
    // existing methods unchanged
  }
  // existing methods unchanged (writeTransactionCommitted, writeTransactionFailed)
}

// New public enum used by QueryDetails.getQuerySource() to route OTel listener emission.
public enum QuerySource { GREMLIN, SQL }

// New SPI extension point on YTDBQueryMetricsStep — installed by Track 3's OTel
// FinalizationStrategy after YTDBQueryMetricsStrategy injected the step.
public interface GremlinSpanLifecycleHook {
  void onFirstHasNext(QueryDetails details,
                      long startEpochNanos,
                      io.opentelemetry.context.Context parent);
  void onClose(QueryDetails details, long endEpochNanos);
}

// new value record + two static-utility classifiers in core.
// D9 carries operation + collection; D19 adds six structural fields;
// D40 adds the customAttrs map for db.youtrackdb.gremlin.* attributes
// derived during Path B classification; D42 adds three advisor flags
// for the future Phase 2 advisory-dashboard follow-up.
public record Classification(
    Optional<String> operationName,
    Optional<String> collectionName,
    Optional<Boolean> wherePresent,
    Optional<Boolean> orderPresent,
    Optional<Boolean> limitPresent,
    Optional<Integer> fromClassCount,
    Optional<Integer> stepCount,
    Optional<Boolean> hasSubtraversal,
    Optional<Boolean> advisorFullScan,
    Optional<Integer> advisorCrossClassCount,
    Optional<Boolean> advisorLargeResult,
    Map<String, Object> customAttrs) {
  public static final Classification EMPTY = new Classification(
      Optional.empty(), Optional.empty(),
      Optional.empty(), Optional.empty(), Optional.empty(),
      Optional.empty(), Optional.empty(), Optional.empty(),
      Optional.empty(), Optional.empty(), Optional.empty(),
      Map.of());
}

public final class GremlinBytecodeClassifier {
  private GremlinBytecodeClassifier() {}
  public static Classification classify(Bytecode bytecode);  // never throws; returns EMPTY for unrecognized shapes
}

public final class SqlSyntaxClassifier {
  private SqlSyntaxClassifier() {}
  public static Classification classify(SQLStatement statement);  // never throws; returns EMPTY for unrecognized shapes
}

// static methods on the existing final utility class YourTracks
public static void registerGlobalQueryListener(QueryMetricsListener listener);
public static void unregisterGlobalQueryListener(QueryMetricsListener listener);
public static void registerGlobalTransactionListener(TransactionMetricsListener listener);
public static void unregisterGlobalTransactionListener(TransactionMetricsListener listener);
```
