# design-author params — phase1-creation, round 4 (single readability fix)

- target: design
- output_path: /home/andrii0lomakin/Projects/ytdb/track-reconciliation-rules/docs/adr/track-reconciliation-rules/_workflow/design.md
- research_log_path: /home/andrii0lomakin/Projects/ytdb/track-reconciliation-rules/docs/adr/track-reconciliation-rules/_workflow/research-log.md
- round: 4

## flagged_passages (one prose fix — no re-grounding)

1. **≈line 92 (`## The new Phase-C review loop`)** — hard-to-read: replace the
   uncommon word "orthogonal" with "independent" (the document already uses
   the plain "different axis" phrasing elsewhere, e.g. near line 142). Sweep
   for any other "orthogonal" occurrences in the document and replace each
   with "independent" (or fold into the existing "different axis" phrasing)
   for consistency.

Preserve everything else byte-for-byte (line-1 stamp, flowchart, all decision
content, TL;DRs, edge cases, References footers).
