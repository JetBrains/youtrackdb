# Track 4a: Migration preflight + range computation

## Purpose / Big Picture
After this track lands, `/migrate-workflow`'s preflight and range computation run inside the branch's own worktree, scoped to the active plan's `_workflow/` directory (D13). The develop-worktree resolution is gone. The unstamped-artifact bootstrap prompt (D8) and the stamp-walking range derivation (D10) are wired in. The per-commit replay loop and final stamp-to-HEAD batch land in Track 4b.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

## Progress
- [x] Review + decomposition
- [x] Step implementation
- [ ] Track-level code review
- [ ] Track completion
- [x] 2026-05-24T05:32Z [ctx=info] Review + decomposition complete
- [x] 2026-05-24T08:53Z [ctx=safe] Step 1 complete (commit e04b5334ba)
- [x] 2026-05-24T09:05Z [ctx=safe] Step 2 complete (commit abd6d14c6b)
- [x] 2026-05-24T09:10Z [ctx=safe] Step 3 complete (commit 51ca98d078)
- [x] 2026-05-24T09:16Z [ctx=safe] Step 4 complete (commit e449dec277)
- [x] 2026-05-24T09:21Z [ctx=safe] Step 5 complete (commit 72dd4f77af)
- [x] 2026-05-24T10:05Z [ctx=info] Track-level code review iteration 1 complete (1/3 iterations)

## Surprises & Discoveries
- 2026-05-24T08:53Z — The ephemeral-identifier pre-commit gate's exclusion list covers `_workflow/**` and `.claude/workflow/**` but not `.claude/skills/**`. Plan-file Decision Record IDs cited inside SKILL bodies fail the same resolvability test the rule applies to source code and Phase 4 artifacts, since SKILL.md survives the squash but the plan-file DR identifiers do not. Inline-prose restatement of the staging convention sidesteps the issue; future tracks touching SKILL bodies should anticipate the same trap. See Episodes §Step 1.
- 2026-05-24T09:16Z — The §1.6(h) Phase 1 walk byte-copy contract between `migrate-workflow/SKILL.md` Step 2 and `workflow-drift-check.md` survives the migration-side `STAMPED_PAIRS` extension because the extension is purely additive (one init line + one assignment inside the stamped branch). A future format-change PR touching §1.6(h) can mechanically diff the two copies and confirm the pairing rows are the only legitimate divergence. The contract is now text-explicit in SKILL.md's Step 2 prose so Track 4b reviewers can grep the contract sentence when validating future edits to either copy. See Episodes §Step 4.
- 2026-05-24T09:21Z — The legacy three-check `.migration-progress` stale-detection collapsed to a single `range_start` mismatch under the new range model. The dropped branches (head-reachable-from-develop, head-older-than-develop append) had no analogue once develop ceased to anchor the range; a single `range_start` mismatch covers every shift that could make the recorded body stale (new stamps landed, a stamp was rewritten, `$USER_BOOTSTRAP_SHA` differs across sessions, a rebase moved the fold output). The track's Concrete-Step description anticipated only the stale-fork rename; the two-check drop is an internal-consistency consequence of the new range model that Track 4b implementers and Phase C reviewers need to see. The progress-file header shape is now `# range_start=<BASE_SHA> range_end=<HEAD-sha-at-skill-start>`, which Track 4b's per-commit loop must write at initialization. See Episodes §Step 5.

## Decision Log
<!-- Continuous-log. Empty at Phase 1. -->

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective
- [x] Technical: PASS at iteration 2 (4 findings, 4 accepted)
- [x] Adversarial: PASS at iteration 2 (7 findings, 5 accepted + 2 deferred — A3 D8 challenge per Decision Record immutability, A7 progress-file field rename intentional)

## Context and Orientation

`.claude/skills/migrate-workflow/SKILL.md` (~290 lines, 7 step sections) is the canonical migration skill. After Track 4a, today's Step 2 is deleted (no separate migration worktree) and the leading steps renumber down by one; today's Step 3.0 lands as new Step 2.0. The per-commit replay loop and final batch are out of scope here — they land in Track 4b. The table covers the steps Track 4a touches (Steps 0, 1, 2.0, 2, 3 in post-renumber numbering):

| Old → New | Today's purpose | Track-4a fate |
|---|---|---|
| 0 → 0 | Create progress tracker via `TaskCreate` | Keep (relabel umbrella titles; add tasks for new Step 2.0 bootstrap; Track 4b adds the task for new sub-step 4.8 final stamp-to-HEAD batch) |
| 1 → 1 | Preflight — must be on `develop`, clean tree, validate `$ARGUMENTS` | Drop the develop check; relax the argument contract since `$ARGUMENTS` becomes optional; add a refusal when any tracked file under `_workflow/**` has uncommitted changes (working tree or index) or any untracked file lives under `_workflow/**` (progress-sentinel carve-out) — D12 |
| 2 → (deleted) | Resolve migration worktree path via `git worktree list --porcelain` | Delete entirely — migration runs in cwd. Subsequent top-level steps renumber down. |
| 3.0 → 2.0 | (new) Unstamped-artifact bootstrap prompt | Add: enumerate `_workflow/**` artifacts, classify stamped vs unstamped; if any unstamped, prompt user once for a base SHA covering the unstamped set, validate via `git rev-parse --verify` + `git merge-base --is-ancestor "$SHA" HEAD`, re-prompt up to 3 times on failure, abort the session on 3 rejections |
| 3 → 2 | Compute commit range from `merge-base(develop, branch)..develop` | Replace with stamp-walking range derivation from `conventions.md` §1.6(h) — fold `STAMPED_SHAS` plus the validated user SHA (when 2.0 ran) through `git merge-base` to derive `BASE_SHA`; range is `BASE_SHA..HEAD` (no `git fetch`) — D10 |
| 4 → 3 | Load/initialize `.migration-progress`; identify target `_workflow/` | Keep, but the progress file is now a transient crash-recovery sentinel only; stamps are the durable marker |

`$ARGUMENTS` becomes optional — when called from the branch's own worktree, the argument is redundant (current branch IS the migration branch). When provided, it must match the current branch; otherwise halt with a clear error.

The stamp-walking bash block must be byte-identical to Track 3's copy in `workflow-drift-check.md`. Both files own their own copy; the canonical text lives in `conventions.md` §1.6(h).

§1.6(h) is the Phase 1 walk only (enumerate-and-classify). Phase 2 (fold + range derivation + failure recovery) is caller-specific, governed by `conventions.md` §1.6(c) and the final paragraph of §1.6(h). Track 3 implemented the drift-check-side variant of those branches; Track 4a implements the migration-side variant. The two render the same canonical contracts differently — see §Plan of Work Step 4 for the migration's specific obligations.

## Plan of Work

The rewrite proceeds in roughly five edits, in order. Step numbers below are the post-Track-4a numbering (today's Step 2 is deleted, today's Steps 3 → 4 renumber down by one, today's Step 3.0 becomes 2.0).

1. **Step 0 / Step 1 relax + dirty-`_workflow/**` refusal.** Drop the `git branch --show-current = develop` check and the `$ARGUMENTS = branch-name` validation. Replace with "active branch + clean tree + resolve active plan dir" preflight (the active plan dir is picked via today's skill ladder — enumerate `docs/adr/*/_workflow/`, apply zero/one/many, with `$ARGUMENTS` optionally pre-selecting). When `$ARGUMENTS` is supplied, the match check is a single string equality against `git branch --show-current`; the four legacy reject cases (`refs/*`, `origin/*`, hex-SHA, non-existent branch) all fail this single equality, so no separate case-by-case validation is needed. Add a refusal: enumerate `git status --porcelain` over the §1.6(h) artifact paths only (`$PLAN_DIR/_workflow/implementation-plan.md`, `$PLAN_DIR/_workflow/design.md`, `$PLAN_DIR/_workflow/design-mechanics.md`, `$PLAN_DIR/_workflow/plan/`) — NOT the full `$PLAN_DIR/_workflow/` subtree. The narrow scope is deliberate: D14's staging convention writes implementer changes to `$PLAN_DIR/_workflow/staged-workflow/.claude/{workflow,skills}/...` (literally under `$PLAN_DIR/_workflow/`), so a whole-subtree check would refuse every workflow-modifying-branch session with in-flight staged content — the dogfood path D10 explicitly accepts. The narrow scope mirrors the staged-subtree exclusion at implementation-plan.md's Non-Goals and §1.6(h)'s walk. The dropped develop-side whole-tree check incidentally guarded the live skill file (`.claude/skills/migrate-workflow/SKILL.md`); D14 staging now keeps the live skill at develop's state during workflow-modifying-branch execution, so the migration always reads the develop-state skill regardless of in-flight staged rewrites — the side-effect protection is no longer needed. If any tracked file under the narrow scope shows a non-`?` status (modified, added, deleted, renamed, etc.) or any untracked file shows `??`, halt with the offending paths printed and instructions to commit, stash, or remove them. The `.migration-progress` sentinel is the only exception — it is allowed to be untracked or modified mid-session. When the active-plan-dir ladder produces zero matches and `$ARGUMENTS` is empty, the error message substitutes "the current branch" for the legacy `$ARGUMENTS` interpolation (e.g., "no `_workflow/` directory on the current branch; nothing to migrate"). Update Step 0's umbrella task labels to drop the "migration worktree" terminology and add a task for new Step 2.0 (bootstrap).

2. **Step 2 deletion + downstream renumber.** Remove the entire "Resolve migration worktree path" section. Renumber every later top-level step down by one: today's Step 3.0 → Step 2.0, today's Step 3 → Step 2, today's Step 4 → Step 3, today's Step 5 → Step 4, today's Step 6 → Step 5. Update every cross-reference in the prose to the shifted numbers. Sub-steps inside the per-commit loop (today's 5.1-5.6) will renumber to 4.1-4.7 under Track 4b's edits — leave them alone in this track.

3. **Step 2.0 add (unstamped-artifact bootstrap).** Insert a new step before the renumbered Step 2. Walk the active plan's `_workflow/**` ephemeral artifacts (the plan dir resolved in Step 1), classify each as stamped (line-1 stamp parses) or unstamped (no stamp); collect `STAMPED_SHAS` and `UNSTAMPED_FILES`. If `UNSTAMPED_FILES` is empty, no-op and continue to Step 2. Otherwise prompt the user once with the unstamped-file list and a one-paragraph explanation of why no auto-computed reference is safe (D8). Accept short prefixes or 40-char hex; validate via `git rev-parse --verify "$SHA^{commit}"` and `git merge-base --is-ancestor "$SHA" HEAD` (the supplied SHA must be reachable from HEAD because HEAD is the comparison reference — D10). Capture the `rev-parse --verify` stdout (the canonical 40-char form) as `$USER_BOOTSTRAP_SHA`, not the user's raw input; the canonicalization is mandatory per §1.6(d) so Track 4b's lockstep advance writes 40-char stamps that satisfy I1. Re-prompt up to 3 times on validation failure; abort the session on 3 rejections (no edits applied). The 3-attempt counter is **session-bound** — a `/clear` between attempts resets it, matching §1.6(d)'s explicit "user `/clear`s the session to abandon the migration" exit shape. Step 3's progress file does not exist yet at Step 2.0 time, so no persistent counter is available; the bound is by design soft against `/clear`-based abandonment + re-entry.

4. **Step 2 rewrite (was Step 3).** Replace the old commit-range bash with the stamp-walking variant from `conventions.md` §1.6(h). The fold input set is `STAMPED_SHAS` plus `$USER_BOOTSTRAP_SHA` when Step 2.0 ran. Output is `BASE_SHA` and the `git log` of commits in `$BASE_SHA..HEAD` against workflow paths (no `git fetch`). Cross-link to `conventions.md` §1.6(h) for the Phase 1 walk byte-source and §1.6 for the surrounding stamp-format prose. The local copy of the §1.6(h) walk must additionally emit a paired array mapping each stamp to its owning artifact path (extend the walk to track `$f`+`$SHA` together, not just append to `STAMPED_SHAS`) so §1.6(c) recovery can name artifact paths in the re-prompt rather than bare SHAs. The Phase 2 fold and its failure-recovery semantics are governed by:
   - **Fold-body shape: continue-and-collect, not break-on-first-failure.** Drift-check (Track 3) uses `break` because it has no re-prompt cost to amortize. The migration's bootstrap re-prompt is user-interactive, so the fold continues past each failed pair to collect every failing stamp into a `MERGE_BASE_FAILED` set before exiting the loop. A single bootstrap re-prompt then covers the full failing set in one user interaction, matching `design.md` §"Stamp range computation"'s fold-shape. The break-shape would re-prompt the user once per failing pair encountered serially — quadratic on heavily-pruned histories.
   - **`conventions.md` §1.6(c) — merge-base failure recovery.** Each pairwise `git merge-base` call may fail (a stamp pointing at a `git gc`-pruned commit, or two stamps with no reachable common ancestor in the local repo). The migration treats every stamp in the failing call's affected set as unstamped for the rest of the session and routes the full failed set back into Step 2.0's bootstrap prompt — one user prompt covers the combined "genuinely unstamped + merge-base-failed" set per §1.6(c)'s batch semantics. Step 2 records the merge-base-failed set, then either jumps back to Step 2.0 (when no prompt has fired yet) or re-fires Step 2.0 to obtain a fresh `$USER_BOOTSTRAP_SHA` covering the **combined** unstamped + merge-base-failed file set (when 2.0 already ran but more stamps failed mid-fold). The re-prompt's input set is the combined file list; the user supplies a single new SHA that covers the combined set; the new value **replaces** the prior `$USER_BOOTSTRAP_SHA` (the variable stays singular and §1.6(d) keeps its one-SHA-per-prompt shape). Same 3-attempt validation cap as Step 2.0's initial prompt; the counter is shared with Step 2.0's initial prompt (combined session-wide bound across initial + recovery prompts).
   - **`conventions.md` §1.6(h) final paragraph — both-arrays-empty.** When the Phase 1 walk produces both `STAMPED_SHAS` and `UNSTAMPED_FILES` empty (a freshly-created `_workflow/` directory holding only a transient `handoff-*.md`, for example), the migration halts with `no artifacts to migrate` and exits with no edits applied. This is distinct from the no-drift case for the drift check; the migration has nothing to replay against.

5. **Step 3 simplification (was Step 4).** The `.migration-progress` file stays in shape but its semantic role shifts to "in-flight session crash-recovery sentinel" — the durable progress marker becomes the per-artifact stamps once Track 4b lands. The progress file's header `head=<sha>` field becomes `range_end=<sha>` (= `git rev-parse HEAD` at session start); the `fork=<sha>` field becomes `range_start=<sha>` (= `BASE_SHA`). The renames block is unchanged. Update the file-existence handling: if `range_start` in the file doesn't match the freshly-computed `BASE_SHA`, halt and ask the user to delete the file (same as today's stale-fork check, just under the new field names).

Cross-cutting: every `<migration-worktree>` placeholder in the preflight / range / progress-file sections becomes either "the current worktree" or just disappears. Every cross-reference to running "from develop" is removed. The "two worktrees" mental model is gone — readers should never wonder which worktree owns which file. Per-commit-loop and final-summary scrubs land in Track 4b.

## Concrete Steps

1. Step 0/1 preflight relax in `migrate-workflow/SKILL.md` — drop the develop-worktree check, drop the `refs/*`/`origin/*`/hex-SHA/branch-existence ladder for `$ARGUMENTS` and replace with a single equality against `git branch --show-current`, swap the active-plan-dir ladder onto the cwd, narrow the dirty check to the §1.6(h) artifact paths under `$PLAN_DIR/_workflow/` (excluding `staged-workflow/` per D14), update the zero-match error message to substitute "the current branch" when `$ARGUMENTS` is empty, and relabel Step 0's umbrella tasks to drop the "migration worktree" terminology while adding a task for new Step 2.0 — risk: medium (logic changes within one workflow component that shift external preflight behavior; multiple inter-related sub-edits in one step section; cross-references D14 staging convention)  [x] commit: e04b5334ba
2. Step 2 deletion in `migrate-workflow/SKILL.md` — remove the entire "Resolve migration worktree path" section (today's Step 2), renumber later top-level steps down by one (today's Step 3.0 → new 2.0, Step 3 → 2, Step 4 → 3, Step 5 → 4, Step 6 → 5), and update every cross-reference in the prose to the shifted numbers; sub-steps inside the per-commit loop (today's 5.1-5.6) stay untouched for Track 4b — risk: low (override: textual renumber + section delete with no surviving call sites — Step 1 already removed the develop-worktree premise this section assumed)  [x] commit: abd6d14c6b
3. Step 2.0 add in `migrate-workflow/SKILL.md` — insert the unstamped-artifact bootstrap before the renumbered Step 2: walk the active plan's `_workflow/**` per §1.6(h), classify stamped vs unstamped, prompt the user once when unstamped artifacts exist with a one-paragraph D8 rationale, validate the user-supplied SHA via `git rev-parse --verify "$SHA^{commit}"` + `git merge-base --is-ancestor "$SHA" HEAD`, capture the canonical 40-char `rev-parse` stdout as `$USER_BOOTSTRAP_SHA`, re-prompt up to 3 times with a session-bound counter that resets across `/clear` per §1.6(d) — risk: medium (new user-interactive contract; multi-step validation cascade; integrates with `conventions.md` §1.6(d) bootstrap protocol)  [x] commit: 51ca98d078
4. Step 2 rewrite (range derivation) in `migrate-workflow/SKILL.md` — replace today's `merge-base(develop, branch)..develop` bash with the §1.6(h) Phase 1 walk byte-copied from `conventions.md` (extended with a paired `(file, sha)` array so §1.6(c) recovery can name artifact paths in the re-prompt), the caller-specific Phase 2 continue-and-collect fold producing `BASE_SHA` and a `MERGE_BASE_FAILED` set, §1.6(c) recovery routing the combined failed-plus-unstamped file set back to Step 2.0 (replacing the prior `$USER_BOOTSTRAP_SHA` and sharing the 3-attempt counter), §1.6(h) both-arrays-empty halt with `no artifacts to migrate`, and a "Nothing to migrate" halt when `git log $BASE_SHA..HEAD` is empty on a fully-stamped branch — risk: medium (new range-derivation contract with caller-specific Phase 2 branches; cross-track coordination with Track 4b on the paired stamp→path array)  [x] commit: e449dec277
5. Step 3 simplification (progress file) in `migrate-workflow/SKILL.md` — rename the `.migration-progress` header fields `fork=<sha>` → `range_start=<sha>` and `head=<sha>` → `range_end=<sha>`, update the surrounding prose to reflect the new semantics (`range_start` = `BASE_SHA`, `range_end` = `git rev-parse HEAD`), and update the stale-file detection to fire when the recorded `range_start` doesn't match the freshly-computed `BASE_SHA` (same UX as today's stale-`fork=` halt, just under the new field names); the renames-tracker block stays unchanged — risk: low (override: field rename with stale-detection updated in lockstep; behavior preserved across the rename)  [x] commit: 72dd4f77af

## Episodes

### Step 1 — commit e04b5334ba, 2026-05-24T08:53Z [ctx=safe]

**What was done:** Replaced `migrate-workflow/SKILL.md`'s develop-worktree preflight with an in-branch flow. Dropped the `git branch --show-current = develop` check, the four-case `$ARGUMENTS` validation ladder (`refs/*`, `origin/*`, hex SHA, branch existence) in favour of a single equality against `git branch --show-current`, and the develop-side `pull --ff-only` dialog. Moved the active-plan-dir zero/one/many ladder from Step 4 into Step 1's preflight, capturing the result as `$PLAN_DIR` for subsequent steps. Narrowed the dirty check to `conventions.md` §1.6(h) artifact paths only, with `staged-workflow/` deliberately outside scope and the `.migration-progress` sentinel carve-out preserved. Updated Step 0's umbrella task list to the post-Track-4a shape (bootstrap task replaces worktree-resolution task) and trimmed Step 4's intro to drop the duplicate plan-dir ladder. Intro paragraphs lost the two-worktree language and point at the stamp-derived range. Two D14 plan-DR citations in the dirty-check prose were rewritten to inline prose describing the staging convention, since SKILL.md survives the squash but the plan-file DR identifiers do not.

**What was discovered:** The ephemeral-identifier pre-commit gate's exclusion list covers `_workflow/**` and `.claude/workflow/**` but not `.claude/skills/**`. SKILL bodies live durably on `develop`, so plan-file Decision Record IDs (D1, D2, …) cited in skill bodies fail the same "reader on main couldn't resolve the reference" test the rule applies to source code and Phase 4 artifacts. Inline-prose restatement of the staging convention sidesteps the issue; future tracks touching SKILL bodies should anticipate the same trap. The gate's `Step N` matches on intra-file structural headings (Step 0–Step 6 inside SKILL.md) are an inspect-then-proceed case — a reader on main resolves them trivially within the same file. Cross-track impact (sub-step 5): Track 4a Step 4 (range-derivation rewrite) must scrub residual `<migration-worktree>` and develop-relative language at SKILL.md lines 192–194 (stale-check, develop-tip-reachability, append-queue logic); Track 4a Step 5 renames `fork=`/`head=` fields, and line 192's stale-check still references `fork=`, so Step 4 must land before Step 5. The plan's Concrete-Steps order already encodes this sequence. Track 4b retains scrub scope for the remaining `<migration-worktree>` placeholders at lines 227, 265, 267, 271, 299, 323, 330–331.

**What changed from the plan:** None. Scope held to Step 1's Concrete-Steps line (preflight relax + dirty check + Step 0 relabel + Step 2.0 task add). Adjacent edits for self-consistency stayed inside the track's cross-cutting scrub scope (intro paragraph, Inputs block, Step 4 intro), per `## Plan of Work`'s closing paragraph.

**Key files:** `.claude/skills/migrate-workflow/SKILL.md` (modified).

### Step 2 — commit abd6d14c6b, 2026-05-24T09:05Z [ctx=safe]

**What was done:** Removed the `## Step 2 — Resolve migration worktree path` section from `migrate-workflow/SKILL.md`. The section's develop-worktree premise had already been dropped by Step 1. Renumbered the four remaining top-level steps down by one: `Compute commit range` became Step 2, `Load progress file` became Step 3, `Per-commit migration loop` became Step 4, `Final summary` became Step 5. Updated every cross-reference in the surrounding prose to the shifted numbers — Step 0's umbrella-task parenthetical, the `.migration-progress` carve-out line in Step 1, the per-commit-loop guard in Step 2's tail, the `Skip them in Step 4` line at the bottom of Step 3, the `On first entry to Step 4` header inside Step 4, and the two `Step 5 reads/surfaces` cross-references in the loop body. Sub-step headings (`### 5.1`–`### 5.6`) and `Step 5.x` prose references inside the per-commit loop stay untouched per the track plan; Track 4b renumbers them to `### 4.1`–`### 4.7` alongside its body rewrite.

**What was discovered:** The pre-commit ephemeral-identifier gate flags every `Step N` intra-file structural reference inside SKILL.md as a Step-label candidate. Each one resolves to a `## Step N` heading inside the same file, so a reader on `develop` opens SKILL.md and finds the section without any branch-only context. This is the same inspect-then-proceed pattern Step 1's Surprises entry recorded; Track 4b's `### 5.x → ### 4.x` sub-step renumber will trip the same matches with the same disposition. Cross-track impact (sub-step 5): Track 4b inherits the sub-step renumber (`### 5.1`–`### 5.6` → `### 4.1`–`### 4.7`, with `4.7` reserved for the final stamp-to-HEAD batch) and a uniform downward line-number shift of ~25 lines for every `<migration-worktree>` placeholder it still needs to scrub. The umbrella-task references to task index `5` (`Per-commit migration loop`) stay valid because the umbrella list was already in post-Track-4a shape after Step 1's relabel.

**What changed from the plan:** None. The 8-hunk atomic `apply_patch` ran cleanly with no anchor collisions; sub-step boundaries held.

**Key files:** `.claude/skills/migrate-workflow/SKILL.md` (modified).

### Step 3 — commit 51ca98d078, 2026-05-24T09:10Z [ctx=safe]

**What was done:** Inserted a new `## Step 2.0 — Bootstrap unstamped artifacts` section into `migrate-workflow/SKILL.md` between Step 1 and the legacy Step 2. The step walks the active plan's `_workflow/**` per `conventions.md` §1.6(h), classifies each artifact as stamped or unstamped, and when `$UNSTAMPED_FILES` is non-empty prompts the user once with the rationale for why no auto-computed default is safe. Validation runs `git rev-parse --verify "$SHA^{commit}"` then `git merge-base --is-ancestor "$SHA" HEAD`, captures the canonical 40-char `rev-parse` stdout as `$USER_BOOTSTRAP_SHA`, and caps retries at three session-bound attempts per §1.6(d). The counter resets across `/clear` because Step 3's `.migration-progress` file does not yet exist at Step 2.0 time. The step is a no-op when every artifact is stamped.

**What was discovered:** The ephemeral-identifier trap Step 1's Surprises entry recorded bit again. The initial draft cited `D10` (HEAD as range upper bound) and `Track 4b` (lockstep advance); both had to be rewritten to inline prose (the upper-bound rationale; the "Step 4's per-commit lockstep advance" phrasing) because plan-file Decision Record IDs and Track identifiers do not survive the squash, while SKILL bodies do. Intra-file `Step N` references resolve trivially within the same SKILL.md, consistent with Steps 1 and 2's prior observations. Cross-track impact (sub-step 5): Step 4 (next spawn, range-derivation rewrite) must consume `$USER_BOOTSTRAP_SHA` produced here in the fold input set, and should pre-budget one rewrite iteration for `§1.6(c)` / `§1.6(h)` recovery citations that the initial draft may carry as `D<n>` / `Track <n>` shorthand. Track 4b's lockstep advance can cite the SKILL location as a secondary reference site alongside `conventions.md` §1.6(d).

**What changed from the plan:** None. Scope held to the Concrete-Steps line; content matches `## Plan of Work` Step 3's description verbatim. The two inline rewrites (`D10` → upper-bound rationale; `Track 4b` → per-commit lockstep advance phrasing) are mandated by the ephemeral-identifier rule, not plan deviations.

**Critical context:** Legacy Step 2 (`merge-base develop` content at SKILL.md lines 221+) is still present and unchanged. Track 4a Concrete Step 4 (next spawn) replaces it with the §1.6(h) Phase 1 walk + caller-specific Phase 2 fold and must honor the `$USER_BOOTSTRAP_SHA` contract Step 2.0 establishes.

**Key files:** `.claude/skills/migrate-workflow/SKILL.md` (modified).

### Step 4 — commit e449dec277, 2026-05-24T09:16Z [ctx=safe]

**What was done:** Replaced the develop-relative `merge-base develop branch..develop` bash in `migrate-workflow/SKILL.md` Step 2 with the `conventions.md` §1.6(h) Phase 1 walk, byte-copied from the canonical source and extended with a paired `STAMPED_PAIRS` (`<path>=<sha>`) array so the recovery prompt can name failing artifact paths rather than bare SHAs. Added the §1.6(h) both-arrays-empty halt (`no artifacts to migrate`) before Phase 2 runs; the caller-specific continue-and-collect Phase 2 fold producing `BASE_SHA` and a `MERGE_BASE_FAILED` set; the §1.6(c) recovery branch that resolves failing SHAs back to artifact paths and routes the combined unstamped + merge-base-failed set through Step 2.0's bootstrap prompt (replacing `$USER_BOOTSTRAP_SHA`, sharing the 3-attempt session-wide cap); and the empty-log `Nothing to migrate` halt for fully-stamped branches whose fold collapses to HEAD. Rewrote the frontmatter description's `after the fork point` phrasing to `reachable from HEAD after the stamp base` so the skill's user-facing identity matches the new range model.

**What was discovered:** The Phase 1 walk's byte-copy contract with `workflow-drift-check.md` survives the `STAMPED_PAIRS` extension because the extension is purely additive — one new init line plus one new assignment inside the stamped branch — every byte of the drift-check copy stays in place. A future format-change PR touching §1.6(h) can mechanically diff the two copies and confirm the pairing rows are the only legitimate divergence. The new Step 2 prose names this contract text-explicitly so Track 4b reviewers can grep for the contract sentence. Cross-track impact (sub-step 5): Track 4b's per-commit lockstep advance and final stamp-to-HEAD batch consume `$BASE_SHA` produced here; the 40-char canonicalization Step 2.0 enforces on `$USER_BOOTSTRAP_SHA` carries through the fold into `$BASE_SHA` (every fold input is already 40-char form). Track 4b's lockstep-advance text should cite `conventions.md §1.6(d)` for canonicalization and §1.6(h) for the walk, never the plan's `D<n>` or `Track <n>` shorthand — the ephemeral-identifier rule continues to apply.

**What changed from the plan:** None. Scope held to the Concrete-Steps line; the frontmatter rewrite is internal consistency, not scope creep (the frontmatter described the old range model and would have been misleading after Step 2's rewrite).

**Critical context:** The new Step 2 cross-references `$PLAN_DIR` (Step 1) and `$STAMPED_SHAS` / `$UNSTAMPED_FILES` / `$USER_BOOTSTRAP_SHA` (Step 2.0). Step 3's file-existence handling further down in SKILL.md still cites `git merge-base develop $ARGUMENTS` and `$HEAD_DEVELOP`; those references are Step 5's explicit scope (progress-file field rename plus stale-check rewrite under the new range model).

**Key files:** `.claude/skills/migrate-workflow/SKILL.md` (modified).

### Step 5 — commit 72dd4f77af, 2026-05-24T09:21Z [ctx=safe]

**What was done:** Renamed `.migration-progress`'s header fields in `migrate-workflow/SKILL.md` Step 3 from `fork=<sha>` / `head=<sha>` to `range_start=<sha>` / `range_end=<sha>`. Updated the surrounding bullet to explain the new semantics: `range_start` is the `BASE_SHA` produced by Step 2's stamp-walking fold (the oldest ancestor reachable from every fold input); `range_end` is `git rev-parse HEAD` captured at skill start; together they pin the commit range Step 4 replays. Collapsed the three legacy file-existence checks (stale-fork mismatch against `git merge-base develop $ARGUMENTS`, head-reachable-from-develop, head-older-than-develop append) into a single stale-detection halt: if recorded `range_start` differs from the freshly-computed `BASE_SHA`, halt and direct the user to delete the file. The same `git stash` / commit-then-revert warning is preserved. The renames-tracker block is unchanged per the track plan. Post-edit `grep 'fork=|head=|HEAD_DEVELOP' .claude/skills/migrate-workflow/SKILL.md` returns zero matches, satisfying the Idempotence-and-Recovery field-rename consistency check.

**What was discovered:** The three legacy file-existence checks all stood on the develop-relative range model (`fork = git merge-base develop $ARGUMENTS`, head reachable from `develop`, head older than `$HEAD_DEVELOP`). The new `BASE_SHA..HEAD` model has no develop tip to chase, so the head-reachability and head-older-than-develop branches have no analogue and were dropped. A single `range_start` mismatch covers every shift that could make the recorded body stale: new stamps landed mid-session, a stamp was rewritten, `$USER_BOOTSTRAP_SHA` differs across sessions, or the branch was rebased so the fold output changed. The pre-commit ephemeral-identifier gate fired on intra-file `Step 2`, `Step 4`, `Step 5.4`, `Step 5.5` matches — all resolve to `## Step N` / `### 5.x` headings inside SKILL.md, the same inspect-then-proceed disposition Steps 1–4 recorded. Cross-track impact (sub-step 5): Track 4b's per-commit loop initializes `.migration-progress` and must emit `# range_start=$BASE_SHA range_end=$HEAD_SHA` rather than the legacy `# fork=... head=...`. Phase C reviewers can grep `'fork=|head=|HEAD_DEVELOP'` on `develop` after Tracks 4a + 4b land to confirm no legacy field names survive in the live skill.

**What changed from the plan:** None for Step 5's contract. The two-check drop (head-reachable-from-develop, head-older-than-develop append) flows mechanically from the new range model. The track's `## Plan of Work` item 5 implies the collapse via "update the file-existence handling"; the Concrete-Step line only names the stale-fork rename. Recording the broader scope here so Phase C reviewers and Track 4b implementers see what Step 3's file-existence section now is.

**Critical context:** Track 4b consumes the renamed fields when it writes the progress file at session start and reads them at session resume — the field names (`range_start`, `range_end`) are the contract Track 4b's append/read logic must honor. The header line shape `# range_start=<BASE_SHA> range_end=<HEAD-sha-at-skill-start>` is formally defined here.

**Key files:** `.claude/skills/migrate-workflow/SKILL.md` (modified).

## Validation and Acceptance

After Track 4a lands:

- Running `/migrate-workflow` from a feature branch's worktree no longer halts on the develop-worktree check.
- The `$ARGUMENTS` parameter is optional; supplying it produces a no-op match check against `git branch --show-current`.
- The preflight refuses to start if any tracked file under the §1.6(h) artifact paths (`$PLAN_DIR/_workflow/implementation-plan.md`, `$PLAN_DIR/_workflow/design.md`, `$PLAN_DIR/_workflow/design-mechanics.md`, `$PLAN_DIR/_workflow/plan/`) has uncommitted changes (working tree or index), or if any untracked file under those paths exists, except for the `.migration-progress` sentinel. The staged subtree at `$PLAN_DIR/_workflow/staged-workflow/` is deliberately outside the check (D14 dogfood). The refusal prints the offending paths and exits with no edits applied.
- On a branch with unstamped artifacts, Step 2.0 prompts the user once, validates the input via `git rev-parse --verify "$SHA^{commit}"` + `git merge-base --is-ancestor "$SHA" HEAD`, and stores the **canonical 40-char `rev-parse` stdout** (not the raw user input) as `$USER_BOOTSTRAP_SHA`. Three rejected attempts abort the session with no edits applied. The counter is session-bound; `/clear` resets it.
- On a branch with all-stamped artifacts, Step 2.0 is a no-op (no prompt fires).
- Step 2 produces a commit list whose lower bound is `BASE_SHA` (the `git merge-base` fold over `STAMPED_SHAS` plus `$USER_BOOTSTRAP_SHA` when present) and upper bound is `HEAD` — no `git fetch origin develop` runs. On a fully-stamped branch where `git log $BASE_SHA..HEAD -- workflow paths` is empty, Step 2 halts with "Nothing to migrate" without entering the per-commit loop.
- When `git merge-base` fails mid-fold (a stamp pointing at a pruned commit, or two stamps with no reachable common ancestor), Step 2's fold continues past the failing pair (continue-and-collect, not break-on-first-failure) to gather every failure into a `MERGE_BASE_FAILED` set before exiting the loop. A single re-prompt then routes back through Step 2.0's bootstrap covering the combined unstamped + merge-base-failed file set; the user-supplied SHA replaces the prior `$USER_BOOTSTRAP_SHA`. Three rejected attempts abort the session with no edits applied (counter is shared across initial + recovery prompts).
- When the Phase 1 walk produces both `STAMPED_SHAS` and `UNSTAMPED_FILES` empty (no stampable artifacts on disk), Step 2 halts with `no artifacts to migrate` and exits with no edits applied — no prompt fires.
- The progress file uses the new field names (`range_start`/`range_end`); stale-file detection still fires when the recorded range start doesn't match the freshly-computed `BASE_SHA`.

<!-- Phase A placeholder for per-step EARS/Gherkin lines. -->

<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery

Every step in this track is a single-file edit on `.claude/skills/migrate-workflow/SKILL.md` with no external state mutations (no scripts run, no Maven invocations, no IDE indexing). The recovery path is uniform: `git revert <step-commit>` rewinds the file to its pre-step state. Spotless does not apply (markdown), so no formatting catch-up is needed after a revert.

Per-step notes:

1. **Step 1 (preflight relax).** Idempotent at the file level: the edit either landed or didn't. Partial-edit recovery (Edit tool failure mid-step) reverts via `git checkout -- .claude/skills/migrate-workflow/SKILL.md`. Resume after revert is a fresh implementer spawn from the same plan; no cross-step state to reconcile.
2. **Step 2 (deletion + renumber).** Same single-file boundary. The renumber sweep is mechanical — a re-run produces the same output. If Step 2 lands but Step 3 is interrupted before insertion, the file carries a renumbered skill with no Step 2.0 yet; the next session resumes Step 3 against the renumbered baseline, no special handling.
3. **Step 2.0 add (bootstrap).** Single-file edit. The new step's bash is documentary at this point (Track 4b implements the runtime invocation in the per-commit loop, but the user-facing prompt logic lives in Step 2.0's prose now). Partial-edit recovery same as Step 1.
4. **Step 2 rewrite (range derivation).** Single-file edit. The §1.6(h) byte-copy must remain text-identical to `workflow-drift-check.md`'s copy — a Step 4 implementer that diverges fails the Phase C track-level review's byte-identity check. Partial-edit recovery same as Step 1.
5. **Step 3 simplification (field rename).** Single-file edit. Field-rename consistency check after the edit: `grep -nE 'fork=|head=' .claude/skills/migrate-workflow/SKILL.md` must return zero matches inside the migrate-workflow Step 3 section (the legacy field names persist in commit-message conventions Track 4b touches; the grep is scoped to the section). Partial-edit recovery same as Step 1.

No step depends on a runtime sentinel file, lock, or external service. The `.migration-progress` sentinel discussed in Step 5 is the migration's own runtime state, not this track's idempotence concern — Track 4a edits the skill's documentation of the sentinel, not the sentinel itself.

## Artifacts and Notes
<!-- Continuous-log (rare). Empty at Phase 1. -->

## Interfaces and Dependencies

**In-scope files:**
- `.claude/skills/migrate-workflow/SKILL.md` (Steps 0, 1, 2.0 add, 2, 3 in the post-renumber numbering — the preflight, the bootstrap prompt, the range derivation, and the progress-file rename)

**Out-of-scope files:**
- `.claude/workflow/conventions.md` (Track 1)
- `.claude/skills/create-plan/SKILL.md`, `.claude/skills/edit-design/SKILL.md` (Track 2)
- `.claude/workflow/workflow-drift-check.md` (Track 3 — shares the bash block by byte-identical copy)
- `.claude/skills/migrate-workflow/SKILL.md` Steps 4 (per-commit loop with sub-steps 4.1-4.8) and 5 (final summary) — Track 4b
- `.claude/workflow/self-improvement-reflection.md` and the new Step 6 wiring (Track 5)

**Inter-track dependencies:**
- **Depends on:** Track 1 (stamp format + SHA computation in `conventions.md` §1.6).
- **Coordinates with:** Track 3 (shared stamp-walking bash block — both files own a copy, must be byte-identical).
- **Unblocks:** Track 4b (the per-commit replay loop relies on the range Track 4a derives and the progress file Track 4a renames).

**External interfaces:**
- `git status --porcelain` over the §1.6(h) artifact paths only (`$PLAN_DIR/_workflow/implementation-plan.md`, `$PLAN_DIR/_workflow/design.md`, `$PLAN_DIR/_workflow/design-mechanics.md`, `$PLAN_DIR/_workflow/plan/`) — the narrow-scoped dirty preflight check (Step 1). Excludes `$PLAN_DIR/_workflow/staged-workflow/` per D14.
- `git rev-parse --verify "$SHA^{commit}"` and `git merge-base --is-ancestor "$SHA" HEAD` — used by Step 2.0 to validate the user-supplied bootstrap SHA before it enters the fold. HEAD is the reachability anchor because the range upper bound is HEAD (D10).
- `git merge-base <a> <b>` — pairwise fold across stamps to find the oldest reachable ancestor (`BASE_SHA`). Handles linear and divergent histories uniformly.
- `git log $BASE_SHA..HEAD -- .claude/workflow .claude/skills` — the commit range for replay.

## Base commit
e27d6478600371b35c2948d867ed47154d0b8822
