<!-- MANIFEST
findings: 9   severity: {blocker: 0, should-fix: 6, suggestion: 3}
index:
  - {id: A1, sev: should-fix, loc: "research-log.md:679 (Open Questions — Fate of design-review.md prose axes)", anchor: "### A1 ", cert: "Assumption test — prose-axis removal site count", basis: "Load-bearing open question still open; design-review.md prose-axis split is the central wiring change and the log defers it"}
  - {id: A2, sev: should-fix, loc: "research-log.md:691 (Open Questions — Loop topology, cost, iteration cap)", anchor: "### A2 ", cert: "Assumption test — iteration cap convergence", basis: "Load-bearing open question on loop topology/cap/convergence left open; cap=3 chosen with no evidence the dual-clean loop converges within it"}
  - {id: A3, sev: should-fix, loc: "research-log.md:173 (14:32Z absorption per-round)", anchor: "### A3 ", cert: "Challenge — Decision 14:32Z absorption per-round check", basis: "Dual-clean per-round loop nearly doubles the auditor fan-out cost every round; cheaper after-loop single-absorption was rejected on a thrash argument the auditor's flags-prose-not-content property already defuses"}
  - {id: A4, sev: should-fix, loc: "research-log.md:108 (13:40Z) + 116 (14:05Z) absorption de-warm", anchor: "### A4 ", cert: "Challenge — Decision 14:05Z de-warm via absorption-agent split", basis: "The de-warm rests on a premise about S2 'exactly two read sites' that the live S2 text states differently; the absorption-agent split may add a site S2 does not currently sanction"}
  - {id: A5, sev: should-fix, loc: "research-log.md:573 (17:40Z dogfood) + I6/§1.7", anchor: "### A5 ", cert: "Challenge — Decision 17:40Z dogfood-on-real-targets / self-wiring blocked", basis: "Branch authors its own design with the OLD routine, so the new loop is never exercised on a workflow-modifying design before promotion; the chosen Phase-3 dogfood target (transactional-schema) is not workflow-modifying"}
  - {id: A6, sev: should-fix, loc: "research-log.md:426 (17:25Z collapse 4a/4b boundary)", anchor: "### A6 ", cert: "Challenge — Decision 17:25Z collapse the Step-4a/4b session boundary", basis: "Collapsing the mandatory full-tier session boundary touches create-plan startup/auto-resume machinery (a schema-shaped consumer), risking §1.7 staging scope creep and a context-accumulation regression the by-reference condition only partly bounds"}
  - {id: A7, sev: suggestion, loc: "research-log.md:281 + 361 (16:00Z tool surface + cache warm-up)", anchor: "### A7 ", cert: "Assumption test — fan-out warm-up timing heuristic", basis: "The ~1-min warm-up delay and 'agent 1 warm' trigger are heuristic; survives but the orchestration-complexity cost lands on the same two skills the loop already complicates"}
  - {id: A8, sev: suggestion, loc: "research-log.md:521 (15:45Z phase4 episode-primary fidelity)", anchor: "### A8 ", cert: "Assumption test — episodes carry the divergence why", basis: "Episode-primary fidelity assumes step/track episodes reliably record divergence reasoning; survives but the staleness residual is wider than stated when a late step reworks early code without an episode note"}
  - {id: A9, sev: suggestion, loc: "research-log.md:44 (13:38Z reconstructibility stopping criterion)", anchor: "### A9 ", cert: "Assumption test — reconstructibility is a stable auditor stopping point", basis: "Reconstructibility sits between too-terse and tutorial-bloat, but the boundary is judgment; survives because § Orientation's anti-padding clause backstops the upper bound"}
flags: [CONTRACT_OK]
-->

## Findings

### A1 [should-fix]
**Certificate**: Assumption test — prose-axis removal site count
**Target**: Open Question 2026-06-16T13:55Z — "Fate of design-review.md's prose axes once the auditor owns readability"
**Challenge**: This open question is load-bearing and still open. It is the precise hinge of the whole branch: the auditor only fixes the "diluted multi-axis pass" diagnosis (Surprise 13:40Z, lines 98-106) if the § Prose AI-tell axis is actually *removed* from `design-review.md`, not duplicated. The log states the wrinkle itself (line 687: "§ Prose AI-tell also runs on the `target=tracks` Step-4b track cold-read, so its removal touches the track path too") but does not decide between "remove the prose axis entirely" and "keep the structural/comprehension half." Deriving a design or tracks over this leaves the central wiring change unspecified. The live `design-review.md` proves the stakes: § Prose AI-tell additions (lines 186-224) and § Human-reader cold-read additions (lines 169-184) currently run on `phase1-creation`, `phase4-creation`, `design-sync`, AND `target=tracks` — four invocation surfaces. If the auditor takes only part of that and the warm reviewer keeps the rest, the dilution YTDB-1130 diagnosed returns on the surface the auditor did not claim. Decision 15:30Z (line 546) *partially* resolves where the five § Human-reader items land, but it explicitly scopes itself to the Human-reader items and does not touch the § Prose AI-tell axis that this open question is about.
**Evidence**: `prompts/design-review.md` lines 186-224 (§ Prose AI-tell additions, applies-to set including `target=tracks`); line 47 of `readability-feedback/SKILL.md` (the rule-sync map already treats § Prose AI-tell additions as a `design-review.md` block that scans both targets). The 15:30Z decision (research-log.md:546) resolves the Human-reader split but not the Prose AI-tell axis.
**Proposed fix**: Resolve this into the `## Decision Log` before the gate clears: decide whether the dedicated auditor *replaces* the § Prose AI-tell additions block on all four surfaces (design kinds + tracks) or only the design kinds, and state what remains on the warm `design-review.md` reviewer. If the auditor owns prose on `target=tracks` too, say so — the 14:58Z decision already routes the loop through `create-plan` Step 4b, so the track-path auditor exists.

### A2 [should-fix]
**Certificate**: Assumption test — iteration cap convergence
**Target**: Open Question 2026-06-16T13:55Z — "Loop topology, cost, iteration cap, and phase1-vs-phase4 wiring"
**Challenge**: Sub-parts (a) topology and (c) phase1-vs-phase4 wiring were resolved by later decisions (14:50Z, 14:38Z, 14:58Z), but sub-part (b) — cost and convergence under the cap — is still open, and the 14:32Z decision raised its stakes. 14:32Z folds absorption into the inner loop as a co-equal per-round check, so a round now passes only when BOTH the readability auditor and the absorption agent are clean (line 181-182). The cap is `iteration_budget` default 3 (line 182). Two checks that can each independently re-open the loop, gated at 3 rounds, is a tighter convergence claim than the single-axis cold-read loop `edit-design` runs today, yet the log offers only a hand-wave ("converges in a round or two… the cap + escalation is the safety net," lines 194-197). No evidence is cited that a dual-clean loop converges within 3 — and the live `edit-design` Step 6 (lines 536-579) shows the cap-exhaustion path dumps unresolved findings on the user, which on a creation loop means a frozen-but-not-clean design. The cost half is also unresolved: each author round is code-grounded (PSI), each auditor round fans out ~6 range agents (line 700), and now each round adds an absorption agent — the log never bounds the worst case.
**Evidence**: research-log.md:173-200 (14:32Z dual-clean loop, cap 3, convergence hand-wave); `edit-design/SKILL.md` lines 536-579 (Step 6 iterate, budget-exhausted outcomes — "the user is the gate"); research-log.md:691-702 (the open sub-part (b) cost/cap).
**Proposed fix**: Either resolve sub-part (b) into a decision (state the expected round count, the worst-case fan-out cost, and what the cap-exhaustion escalation produces for a creation loop specifically), or explicitly waive it to planning/decomposition as out of the gate's scope. Given 14:32Z made the loop dual-clean, a one-line convergence argument for the *two-check* loop (not the single-axis one) is the minimum the rationale needs.

### A3 [should-fix]
**Certificate**: Challenge — Decision 14:32Z absorption per-round check
**Target**: Decision 2026-06-16T14:32Z — "Absorption is a co-equal per-round check inside the inner loop"
**Challenge**: The chosen approach runs absorption every round to avoid a back-edge. The rejected alternative (the 14:18Z "absorption gate before the loop" plus an optional pre-freeze re-run, lines 202-217) was rejected on a thrash argument: a post-loop absorption add re-enters the readability loop and "the loop can drop it again → thrash" (lines 174-177). But the log's own 14:18Z entry already states the property that defuses the thrash: "the auditor never requests deletions (it flags prose, not content)" (line 214). If the readability auditor structurally cannot ask for a decision to be dropped, then a decision added by a post-loop absorption check cannot be dropped by a subsequent readability round — there is no agent in the loop with the disposition to drop it. The back-edge 14:32Z fears requires a readability finding that deletes content, which the auditor by construction does not emit. So the cheaper design (single absorption check, before or after the loop) survives the thrash argument, and 14:32Z pays a real cost the log acknowledges — every round now spawns the absorption agent on top of the ~6-agent auditor fan-out (A2). The 14:32Z rationale is not wrong that per-round is *clean*; it is that the alternative was rejected for a failure mode the log elsewhere argues cannot occur.
**Evidence**: research-log.md:212-214 (14:18Z: "the auditor never requests deletions — it flags prose, not content"); research-log.md:174-177 (14:32Z thrash argument that the add "re-enters the readability loop → the loop can drop it again"); these two are in tension.
**Proposed fix**: Strengthen the 14:32Z rationale to confront its own 14:18Z premise: if the auditor cannot delete content, explain why a dropped decision still re-enters the loop (e.g., a restructure that *moves* a decision out of the audited slice, which is a different failure than a deletion). If that cross-slice-drop scenario is real, name it as the load-bearing reason per-round beats single-check; if it is not, the cheaper single-absorption design should be reconsidered. Survival test result: WEAK.

### A4 [should-fix]
**Certificate**: Challenge — Decision 14:05Z de-warm via absorption-agent split
**Target**: Decision 2026-06-16T14:05Z (absorption moves off the cold-read) refined by 14:12Z (stays at creation as a small warm agent)
**Challenge**: The de-warm decision's load-bearing premise is that S2 already sanctions exactly the read sites the new design needs, so splitting absorption into a separate warm agent "adds no third read site" (line 243: "S2 already names the Phase-2 consistency review… so absorption adds no third read site"; line 240-244: the absorption agent "reads the log at the **authoring** site… still exactly two sanctioned decision-content read sites"). Check this against the live S2 text. `research.md` lines 116-122 define S2 as: "the log is read for decision *content* in exactly two places: at Step 4a/4b artifact authoring (to seed the carriers) and by the Phase-2 consistency review." The 14:12Z claim that the absorption agent "is part of the Step-4a/4b authoring read, just spawned as a distinct agent" is a real reading of S2 — but it is a *reinterpretation*: today the absorption check is run by the cold-read reviewer (the warm reviewer that ALSO does comprehension), per the live `design-review.md` lines 54-59 and 279-284, and S2's "Step 4a/4b artifact authoring" site is the author, not a separate absorption-only spawn. Introducing a distinct warm absorption agent that reads the log is defensible under S2's spirit, but the log asserts it as a settled fact ("no third read site") when it is actually a claim that S2's "authoring site" stretches to cover a new spawn. If a reviewer or the Phase-2 consistency review later reads S2 literally as "the author seeds; the consistency review cross-checks," a third warm log-reading agent is a latent S2 violation.
**Evidence**: `research.md` lines 116-122 (S2 verbatim: "exactly two places: at Step 4a/4b artifact authoring… and by the Phase-2 consistency review"); `prompts/design-review.md` lines 54-59 + 279-284 (today the cold-read reviewer itself runs the absorption check by reading `research_log_path` — the read site is the reviewer, not a separate agent); research-log.md:240-244 (the "no third read site" assertion).
**Proposed fix**: State explicitly that this decision *extends* S2's "Step 4a/4b authoring read" to include a distinct absorption-only spawn, and that S2's prose must be updated to name the absorption agent as a sanctioned reader (or fold the absorption read back under the author's grounding). The 14:50Z files-of-change list (line 519) already names "`conventions.md` (S2/S3 prose)" as a change site — make the S2 edit a stated deliverable, not an implicit reinterpretation. Survival test result: WEAK (the decision is sound but rests on an under-stated S2 edit).

### A5 [should-fix]
**Certificate**: Challenge — Decision 17:40Z dogfood-on-real-targets / self-wiring blocked
**Target**: Decision 2026-06-16T17:40Z — "Dogfood on real existing targets, not self-referentially"
**Challenge**: The decision correctly identifies that this branch's own `design.md` is authored by the OLD routine (line 580: "This branch's Phase-1 `/create-plan` runs the OLD routine") because of §1.7 staging (I6) and the temporal bootstrap. The consequence the log under-weights: the new two-role loop is a *workflow-modifying* change, and the validation targets chosen (line 588-594) are `readability-feedback` on this design (the verify half only, available today) and the `transactional-schema` design.md in Phase 3. Neither target exercises the loop on a **workflow-modifying design** — `transactional-schema` is a database-feature design (Java/storage domain), not `.claude/**` prose. The branch's whole premise is that dense workflow-prose decisions (the very kind in THIS log — S2/S3 invariants, gate semantics, staging) need glossing for a mid-level reader. The dogfood plan never tests the author/auditor on workflow-machinery prose, which is the domain where the curse-of-knowledge is most acute and where the reconstructibility bar (D2) is hardest to hit. The "Post-promotion: the next design uses the routine naturally" point (line 593-594) defers the only real workflow-prose dogfood to after merge, when fixing a discovered gap costs a follow-up branch.
**Evidence**: research-log.md:580-594 (the three dogfood points, none workflow-prose); research-log.md:70-78 (D3 — the branch IS workflow-modifying machinery); `conventions.md` §1.7 (I6 staging — line 1241+ forward-applicable note); the matched-category for THIS gate is "Workflow machinery," confirming the target domain.
**Proposed fix**: Add a fourth validation point or amend point 2: run the implemented routine in Phase 3 against a known-dense *workflow-prose* artifact (e.g., a section of `conventions.md §1.7` staging, or the `plan-slimization` design-final.md, or this very research log), not only the `transactional-schema` storage design. Workflow prose is the branch's reason to exist; validate the loop on it before promotion. Survival test result: WEAK (the dogfood instinct is honored, but on the wrong domain).

### A6 [should-fix]
**Certificate**: Challenge — Decision 17:25Z collapse the Step-4a/4b session boundary
**Target**: Decision 2026-06-16T17:25Z — "Collapse the Step-4a/4b session boundary into one /create-plan invocation"
**Challenge**: This decision removes the mandatory full-tier session boundary between design authoring (4a) and plan derivation (4b), on the ground that sub-agent authoring provides the context isolation the boundary was a proxy for. The challenge is scope and schema-touch. The live `create-plan/SKILL.md` startup protocol (lines 178-220) and Step 4a/4b (lines 553-586) encode the boundary as load-bearing machinery: the auto-resume condition (`design.md` committed + clean → route to 4b; dirty → back to 4a), the "Step 4a ends the session… does not flow straight into Step 4b" rule (line 528), and Step 1c's resume routing. Collapsing the boundary rewrites this *resume-state routing*, which is exactly the "schema a running phase reads" class that §1.7(k) keeps OUT of the prose-only opt-out and IN staging (conventions.md lines 1365-1372: "resume-state field… stay staged"). So this is not a judgment-layer prose edit — it is an execution-procedure change, correctly staged, but the log treats it as a clean UX win without flagging that it modifies the create-plan auto-resume contract that other resume paths (drift gate, handoff) depend on. The "Retained: freeze+commit stays as a crash checkpoint, Step 1c becomes crash-recovery-only" mitigation (lines 440-444) is sound but means the auto-resume routing must now distinguish happy-path (no resume) from crash-recovery (resume into 4b) — new branching the log notes but does not fully specify. The by-reference condition (line 436-439) is stated as adopted "anyway," but if any author sub-agent returns more than a thin summary, the combined session accumulates the design + plan context the boundary existed to prevent.
**Evidence**: `create-plan/SKILL.md` lines 178-220 (startup resume routing keyed on `design.md` committed/dirty), line 528 ("Step 4a ends the session… does not flow straight into Step 4b"), lines 584-586 (commit-then-end-session); `conventions.md` §1.7(k) lines 1365-1372 (resume-state fields stay staged, not opt-out-eligible).
**Proposed fix**: Note in the decision that collapsing the boundary modifies the create-plan auto-resume contract (a staged, execution-procedure change, not prose), and that the crash-recovery resume path must be re-specified to fire only on a dirty/absent plan after a committed design. Confirm the by-reference discipline is a *hard requirement* of this decision, not an "anyway" — if the orchestrator ever pulls full artifacts, the boundary's removal regresses context isolation. Survival test result: WEAK (the decision survives but understates the machinery it touches and the hard dependency on by-reference).

### A7 [suggestion]
**Certificate**: Assumption test — fan-out warm-up timing heuristic
**Target**: Decision 2026-06-16T16:00Z — "fan-out cache warm-up + byte-identical prompt + params-in-file"
**Challenge**: The warm-up routine (spawn agent 1, wait ~1 min, fan out 2-N) rests on a heuristic delay (line 368-377) and a "is agent 1 warm yet" trigger the log itself calls heuristic (line 396: "its 'agent 1 warm yet' trigger is heuristic"). The assumption holds under the project's measured cache-sharing data (the log cites it, lines 361-362), so it survives — but two stress conditions narrow it. First, the ~1-min fixed delay is calibrated to "lands in its first turn" (line 370); a heavy code-grounded author whose first turn is slow (PSI queries, multiple file reads) could push its cold-write past 1 min, so the fan-out still races it. Second, the orchestration complexity (param files, sequencing, findings-in-file, lines 397-399) lands on `edit-design` and `create-plan` Step 4b — the same two skills A6 already complicates by collapsing the session boundary. The compounding orchestration burden on two load-bearing skills is a real maintainability cost the log accepts but does not weigh against the alternative of a simpler non-warm fan-out that just pays N cold prefixes.
**Evidence**: research-log.md:361-399 (the warm-up routine, the heuristic-trigger admission, the orchestration-complexity trade-off); research-log.md:281-293 (16:00Z tool-surface decision, the other half of the per-spawn cost).
**Proposed fix**: No decision change required — the lever is cost-only and survives. Optionally note the heavy-author-first-turn case (the delay may need to be author-vs-auditor specific) and defer the exact warm-up plumbing to implementation, flagging it as the most intricate orchestration in the branch. Survival test result: YES.

### A8 [suggestion]
**Certificate**: Assumption test — episodes carry the divergence why
**Target**: Decision 2026-06-16T15:45Z — "phase4 fidelity is primarily doc-vs-episodes (cheap), PSI only for the diagram residual"
**Challenge**: The decision makes step + track episodes the primary fidelity source because "the code shows the final state but not the *why*; the episodes carry the divergence reasoning" (lines 530-531). This assumes episodes reliably record divergence. The stated staleness residual (line 535: "a later step can rework earlier code, so later episodes supersede earlier ones") is narrower than the real risk: an inline-replan or scope-down that happens *without* a dedicated episode note (e.g., a step that silently does less than planned, or a fix applied during code review that no step episode captures) leaves design-final.md with no episode trace to check against, and the PSI residual is scoped only to "diagram/signature-level claims" (line 539), not to undocumented behavioral divergence. So the episode-primary check can pass a design-final.md that faithfully matches the episodes while both diverge from the as-built code in a way no episode recorded.
**Evidence**: research-log.md:521-544 (15:45Z episode-primary fidelity, the staleness residual scoped to "later step reworks earlier code"); `create-final-design.md` lines 64-79 (the live Phase-4 protocol mandates PSI verification of diagrams against *actual code* precisely because episodes/plan can be wrong — the decision narrows that to a "diagram residual").
**Proposed fix**: Broaden the PSI residual's trigger from "diagram/signature claims only" to "any design-final.md claim with no corresponding episode entry" — when the episode record is silent on a behavioral point design-final asserts, fall back to code/PSI rather than trusting the episode-match. Survival test result: HOLDS (the episode-primary bulk is sound; the residual definition needs widening).

### A9 [suggestion]
**Certificate**: Assumption test — reconstructibility is a stable auditor stopping point
**Target**: Decision 2026-06-16T13:38Z — the "reconstructibility" stopping criterion (lines 58-64)
**Challenge**: D2 sets the auditor's stopping bar at reconstructibility — "can a mid-level developer rebuild the mechanism from the doc alone" — positioned "between 'terse and blocked' (a finding) and tutorial bloat (banned by § Orientation)" (lines 62-64). The assumption is that this is a stable convergence point. Stress: reconstructibility is a judgment with no mechanical surface, and the two bounding rules pull unevenly. The lower bound (too-terse) has a concrete live anchor — § Orientation's "prose a reader cannot follow without opening the code is too terse, a finding the same as padding" (house-style.md:56) — but the upper bound D2 attributes to "tutorial bloat (banned by § Orientation)" is less crisply defined: § Orientation's anti-padding clause (house-style.md:64) bans "a hedge, a restatement, or a synonym cycle," and § Plain language's boundary clause (house-style.md:90) bans re-teaching the Java/DB floor — but neither names "tutorial bloat" as a banned category, so the auditor's upper stop relies on a synthesis of two clauses, not a single rule. The risk is a one-more-sentence ratchet: each round the auditor can find one more unglossed micro-step, and "reconstructible even if intricate" (line 61) is the only brake.
**Evidence**: `house-style.md` line 56 (§ Orientation too-terse floor — the lower bound, concrete), line 64 (anti-padding clause — bans hedge/restatement/synonym, the nearest upper-bound rule), line 90 (§ Plain language boundary — bans re-teaching the floor); research-log.md:58-64 (the reconstructibility bar citing "tutorial bloat banned by § Orientation").
**Proposed fix**: No decision change required — the criterion survives because § Orientation's anti-padding clause plus § Plain language's no-re-teach boundary together backstop the upper bound, and the iteration cap (A2) is the hard brake. Optionally pin the upper bound to a named clause (cite § Orientation anti-padding + § Plain language boundary explicitly) rather than the looser "tutorial bloat" phrasing so the auditor has a citable stop. Survival test result: HOLDS.

## Evidence base

#### Challenge — Decision 14:32Z absorption per-round check
- **Chosen approach**: Fold absorption into the readability inner loop as a co-equal per-round check; a round passes only when both the readability auditor and the absorption agent (warm Sonnet) are clean; bounded by `iteration_budget` (3).
- **Best rejected alternative**: The 14:18Z design — a single absorption gate before the loop (catches the author's initial omission) plus an optional cheap pre-freeze re-run as belt-and-suspenders. One absorption spawn, not one per round.
- **Counterargument trace**:
  1. In the scenario where round 2's restructure does not touch decision content (the common case), the chosen approach still spawns the absorption agent because the round runs both checks unconditionally (research-log.md:181-182). The rejected single-check approach spawns absorption zero extra times.
  2. The 14:32Z rejection of the single-check approach rests on a thrash trace (research-log.md:174-177): a post-loop absorption add "re-enters the readability loop → the loop can drop it again." But the auditor "never requests deletions (it flags prose, not content)" (research-log.md:214), so no agent in the loop has the disposition to drop the added decision.
  3. This produces: 14:32Z pays a per-round absorption-agent cost (line 191 concedes the cost, mitigated by Sonnet) to prevent a back-edge that the auditor's flags-prose-not-content property already prevents.
- **Codebase evidence**: research-log.md:214 (the auditor-never-deletes property, stated in the very entry 14:32Z supersedes) vs research-log.md:174-177 (the thrash argument 14:32Z uses to reject it). `edit-design/SKILL.md` lines 536-579 show today's single-axis loop already iterates findings without a per-round content re-check, and the auditor analogue (cold-read) only flags, never deletes.
- **Survival test**: WEAK (rationale needs to name a real cross-slice-drop scenario the single-check misses, or the cheaper design should be reconsidered).

#### Challenge — Decision 14:05Z de-warm via absorption-agent split
- **Chosen approach**: Move the absorption-completeness check off the cold-read reviewer (de-warming it) onto a separate small warm Sonnet absorption agent that reads the log at creation time; the cold readability/comprehension reviews go log-free.
- **Best rejected alternative**: Keep absorption on a single warm reviewer (the live design) but extract only the *prose* axis to a cold auditor — accept the comprehension verdict stays warm-compromised. (The log rejects this at lines 277-279 as "the YTDB-1130 anti-pattern.")
- **Counterargument trace**:
  1. In the scenario where the Phase-2 consistency review reads S2 literally, S2 sanctions exactly two decision-content read sites: "Step 4a/4b artifact authoring (to seed the carriers)" and "the Phase-2 consistency review" (research.md:117-119).
  2. A distinct warm absorption-only spawn that reads the log at creation is the author-site read "just spawned as a distinct agent" (research-log.md:241) — a defensible *extension* of S2's authoring site, but today that site is the author/cold-read reviewer, not a separate agent (design-review.md:54-59, 279-284).
  3. This produces: the "no third read site" claim (research-log.md:243) is true only under the extended reading; under a literal reading the new warm spawn is a third log-reading agent S2 does not currently name, a latent invariant drift the gate should surface now.
- **Codebase evidence**: research.md:116-122 (S2 verbatim); design-review.md:279-284 (the reviewer itself does the absorption read today); research-log.md:519 (the 14:50Z files-of-change list already names `conventions.md` S2/S3 prose as a change site — so an S2 edit is anticipated, just not bound to this decision).
- **Survival test**: WEAK (decision sound; the S2-edit it implies must be made an explicit deliverable, not an implicit reinterpretation).

#### Challenge — Decision 17:40Z dogfood-on-real-targets / self-wiring blocked
- **Chosen approach**: Do not dogfood the routine on this branch's own design (blocked by §1.7 staging + temporal bootstrap); instead validate via `readability-feedback` on this design now and the implemented routine against `transactional-schema`'s design.md in Phase 3.
- **Best rejected alternative**: Validate the implemented routine in Phase 3 against a known-dense *workflow-prose* artifact (a `conventions.md`/`§1.7` section, a prior workflow branch's design-final.md, or this research log) — the domain the branch exists to serve.
- **Counterargument trace**:
  1. In the scenario where the routine ships and the next *workflow-modifying* design is authored, the author/auditor face dense `.claude/**` prose (S2/S3 invariants, staging, gate semantics) for the first time — never tested before promotion.
  2. The rejected alternative would instead exercise the loop on workflow prose in Phase 3, where a discovered gap is fixed in-branch, because the matched category for this very gate is "Workflow machinery" and the branch's thesis is that workflow-prose density is the hardest case.
  3. This produces: the chosen plan validates only on a storage-domain design (`transactional-schema`) and the verify-half-only `readability-feedback` run; a workflow-prose density gap surfaces only post-promotion (research-log.md:593-594), costing a follow-up branch.
- **Codebase evidence**: research-log.md:70-78 (D3 confirms the branch is `.claude/**` machinery); research-log.md:580-594 (the three dogfood points, all non-workflow-prose); conventions.md §1.7 I6 (staging is why self-wiring is blocked — the constraint is real, but it does not preclude a workflow-prose *external* target).
- **Survival test**: WEAK (dogfood instinct honored on the wrong domain; add a workflow-prose Phase-3 target).

#### Challenge — Decision 17:25Z collapse the Step-4a/4b session boundary
- **Chosen approach**: Collapse full-tier Step 4a (design) + Step 4b (plan) into one `/create-plan` invocation; sub-agent authoring supplies the context isolation the session boundary proxied; freeze+commit retained as a crash checkpoint; Step 1c becomes crash-recovery-only.
- **Best rejected alternative**: Keep the session boundary (the live design). Sub-agent authoring still de-warms the plan author regardless of session, so the boundary's only remaining cost is one manual re-invoke — a small UX cost against a no-machinery-change baseline.
- **Counterargument trace**:
  1. In the scenario where a session dies mid-derivation, the live auto-resume routing (create-plan/SKILL.md:178-220) keys on `design.md` committed/clean vs dirty to route to 4b vs 4a. Collapsing the boundary changes when that routing fires (happy path no longer crosses it; only crash-recovery does), rewriting a resume-state contract.
  2. The rejected alternative leaves that routing untouched — a §1.7(k) execution-procedure file stays at its develop behavior, lower blast radius.
  3. This produces: the chosen approach is a staged execution-procedure change (correctly staged, not opt-out-eligible per conventions.md:1365-1372), but the log frames it as a clean UX win and understates the auto-resume re-specification and the hard by-reference dependency.
- **Codebase evidence**: create-plan/SKILL.md:178-220 (resume routing), :528 (Step 4a ends session, no flow-through), :584-586 (commit-then-end); conventions.md §1.7(k):1365-1372 (resume-state fields stay staged); research-log.md:436-444 (the by-reference condition stated as adopted "anyway," and the crash-recovery-only Step 1c).
- **Survival test**: WEAK (survives; understates machinery touched + the by-reference hard dependency).

#### Assumption test — prose-axis removal site count
- **Claim**: The dedicated cold auditor owning prose-readability fixes the dilution diagnosis without the log deciding what happens to `design-review.md`'s § Prose AI-tell axis (left open at research-log.md:679-689).
- **Stress scenario**: The auditor takes the design-kind prose axis but the warm reviewer keeps it on `target=tracks` (one of the four live invocation surfaces) — dilution returns on the track path, which 14:58Z explicitly routes the loop through.
- **Code evidence**: design-review.md:186-224 (§ Prose AI-tell additions runs on all three design kinds AND `target=tracks`); readability-feedback/SKILL.md:47 (rule-sync treats it as a both-target block); research-log.md:687 (the log names the track-path wrinkle but does not resolve it).
- **Verdict**: FRAGILE (the load-bearing wiring change is unspecified; an artifact derived now could re-create the dilution on the unclaimed surface).

#### Assumption test — iteration cap convergence
- **Claim**: The dual-clean inner loop (readability + absorption both clean) converges within `iteration_budget` (3) (research-log.md:194-197).
- **Stress scenario**: Two independently-re-opening checks gated at 3 rounds; a code-grounded author re-consulting PSI each round for too-terse findings (research-log.md:341-359) plus an absorption agent that can re-open on any restructure. No worst-case cost or round-count is bounded.
- **Code evidence**: edit-design/SKILL.md:536-579 (cap-exhaustion dumps unresolved findings on the user — for a creation loop, a frozen-but-not-clean design); research-log.md:691-702 (open sub-part (b) cost/cap, never resolved).
- **Verdict**: FRAGILE (holds for benign cases; no evidence the two-check loop converges within 3 in the adversarial case the cap exists for).

#### Assumption test — fan-out warm-up timing heuristic
- **Claim**: Spawning agent 1, waiting ~1 min, then fanning out 2-N takes the fan-out from N cold prefixes to ~1 cold + (N−1)×0.1× (research-log.md:368-377).
- **Stress scenario**: A heavy code-grounded author whose first turn (PSI + multi-file reads) exceeds ~1 min — the cold-write lands after the fan-out starts, so 2-N still race it; and the orchestration burden lands on the same two skills A6 complicates.
- **Code evidence**: research-log.md:370 ("lands in its first turn, so seconds-to-a-turn"); :396 ("its 'agent 1 warm yet' trigger is heuristic"); :397-399 (orchestration complexity on edit-design / create-plan Step 4b).
- **Verdict**: HOLDS (cost-only lever, backed by the project's measured cache data; first-turn-latency edge is a tuning note, not a correctness break).

#### Assumption test — episodes carry the divergence why
- **Claim**: phase4 fidelity is primarily doc-vs-episodes because episodes carry the divergence reasoning the code does not (research-log.md:530-531).
- **Stress scenario**: A silent scope-down or a code-review fix that no step episode records — design-final.md matches the (silent) episodes but diverges from as-built code, and the PSI residual is scoped only to diagram/signature claims, not undocumented behavioral divergence.
- **Code evidence**: create-final-design.md:64-79 (live Phase 4 mandates PSI verification against *actual code* precisely because the plan/episodes can be wrong); research-log.md:535 (staleness residual scoped only to "later step reworks earlier code"), :539 (PSI residual scoped to diagram/signature).
- **Verdict**: HOLDS (episode-primary bulk sound; residual trigger should widen to "any claim with no episode trace").

#### Assumption test — reconstructibility is a stable auditor stopping point
- **Claim**: Reconstructibility ("a mid-level dev rebuilds the mechanism from the doc alone") is a stable bar between too-terse and tutorial-bloat, so the loop converges rather than chasing one-more-sentence (research-log.md:58-64).
- **Stress scenario**: A one-more-sentence ratchet — each round the auditor finds one more unglossed micro-step; the upper brake "tutorial bloat (banned by § Orientation)" is not a single named rule but a synthesis of the anti-padding clause and the § Plain language no-re-teach boundary.
- **Code evidence**: house-style.md:56 (too-terse floor, concrete lower bound); :64 (anti-padding bans hedge/restatement/synonym — nearest upper-bound rule); :90 (§ Plain language bans re-teaching the floor); research-log.md:61 ("reconstructible even if intricate" — the only explicit brake).
- **Verdict**: HOLDS (the two clauses plus the iteration cap backstop the upper bound; pin the upper bound to a named clause for a citable auditor stop).
