# Design Mutation Log — ytdb-1023-workflow-toc

Append-only log of every `design.md` (and `design-mechanics.md` when present) mutation per the discipline in `.claude/workflow/design-document-rules.md § Mutation discipline § Review log`. Each entry records the diff shape, the mechanical-check verdict, the cold-read verdict, the findings list, and the iteration count.

## Mutation 1 — 2026-05-27 — structural-rewrite (design.md)

**Diff summary**: Coordinated structural rewrite that closes the TOC-protocol gap surfaced by the user during Phase 1 design refinement. Five coupled edits: (1) Overview rewritten from "two layers of metadata" to "three layers" — adds the bootstrap layer alongside the existing file-level and section-level layers, and explicitly excludes `CLAUDE.md` as a general-purpose surface; (2) Core Concepts expanded from eight to nine load-bearing ideas — adds **Bootstrap block** as a concept entry and rewrites the **Cross-reference convention** entry to drop CLAUDE.md and add SKILL.md startup read-lists; (3) New §"Bootstrap protocol for agent system prompts" inserted between §"Cross-reference convention" and §"`requires` marker discipline" — names the 38 in-scope system prompts (7 SKILL.md, 11 `.claude/workflow/prompts/*.md`, 20 `.claude/agents/*.md`), shows the bootstrap block content template, and lays out scope / placement / CI enforcement / edge cases / references; (4) §"Reindex script" rule 7 rewritten to cover SKILL.md startup read-lists and agent file refs (CLAUDE.md removed), plus new rule 8 added for bootstrap-block presence on the 38 in-scope files; (5) §"CI gate semantics" pre-commit hook regex dropped `^CLAUDE\.md$` term, edge-case bullet rewrote "in `CLAUDE.md` and agent files" to "in SKILL.md startup read-lists and agent files". Two follow-up edits applied during cold-read iteration to remove surviving CLAUDE.md mentions: §"Cross-reference convention" TL;DR and §"Migration replay semantics" change-list.

**Mechanical checks** (target=design): PASS — 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 38, 84, 242, 454, 504, 516, 522, 547 (unrelated to this mutation; carried forward as known debt).

**Cold-read** (scope: whole-doc): PASS after one iteration. First pass returned NEEDS REVISION with 2 blockers — surviving CLAUDE.md mentions in §"Cross-reference convention" TL;DR (line 143) and §"Migration replay semantics" change-list (line 512) plus the change-paths sentence (line 520). Both fixed in iteration 2. Cold-read also returned 2 ambient suggestions (Track 4 reference leaked at line 238; time-bound branch candidates at line 524–529) not addressed in this mutation — both are acceptable in the ephemeral working design and will surface as edits when `design-final.md` is composed in Phase 4.

**Findings**:
- blocker (iteration 1, resolved): §"Cross-reference convention" TL;DR (line 143) named CLAUDE.md as a carrier of the suffix — contradicted Overview line 10 and Core Concepts. Fixed: TL;DR rewritten to name SKILL.md startup read-lists + `.claude/agents/*.md` and to explicitly call out CLAUDE.md as out of scope.
- blocker (iteration 1, resolved): §"Migration replay semantics" change-list (line 512) included `CLAUDE.md — cross-reference suffix applied.` as a thing this plan changes; line 520 listed CLAUDE.md among paths the commits touch. Fixed: bullet replaced with bootstrap-block coverage on agents + prompts + SKILLs; line 520 trimmed.
- suggestion (deferred): §"Bootstrap protocol" §"Block placement and stability" mentions "Track 4 annotates" — track-number leak. Acceptable in working design; revisit when composing `design-final.md`.
- suggestion (deferred): §"Migration replay semantics" §"Verification on two branches" lists four time-bound candidate branches from memory file. Acceptable in working design.
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 38, 84, 242, 454, 504, 516, 522, 547. Not introduced by this mutation; flagged for a future ambient-style sweep.

**Iterations**: 2 of 3 (PASS).

## Mutation 2 — 2026-05-27 — structural-rewrite (design.md)

**Diff summary**: Removed the `requires` marker concept (D3) entirely. The marker was deemed fragile (subjective author classification, reflexive `requires=none`, body/marker drift, in-summary TOC clutter) while only saving a single extra round-trip Read per inter-section dependency — a cost the lazy-load model already pays cheaply. Twenty-one coupled edits to design.md: Overview roadmap (drop "the `requires` marker discipline" item), Core Concepts (drop the `**requires marker.**` blurb, recount "nine load-bearing ideas" to "eight", drop `requires=` from the Section annotation blurb, drop "`requires` presence" from the workflow-reindex.py blurb), §"Annotation idiom and TOC region" (drop `requires §1.6` from the idiom-shape example TOC row, drop the requires-cell mirroring sentence, drop the `requires=` field rules bullet, rewrite the escape-example edge case to drop "requires", drop the requires-in-summary-cell column-order edge case), §"Cross-reference convention" (remove REQ + PRELOAD nodes from the Read-decision Mermaid and simplify edges to `SECTION_MATCH -->|yes| JUMP`, drop the requires clause from the load-bearing-properties paragraph), §"Bootstrap protocol" (drop step 4 from the bootstrap block content template), §"`requires` marker discipline" (entire section removed, including References footer's D3 anchor), §"Reindex script" (drop "`requires` presence" from TL;DR, drop "optional `requires=...`" from validation rule 5, remove rule 6 entirely + renumber rules 7→6 and 8→7, drop the `requires:` example from the output-format code fence, drop "`requires` decisions and" from the output-format paragraph, drop the "Does NOT modify `requires=` fields" `--write` semantics bullet), §"CI gate semantics" (rewrite the TL;DR's hand-editing parenthetical, drop "missing `requires`" from the Author fix loop, drop the requires=none qualitative spot-check sentence from Agent-side absorption). Section-removal cross-ref cleanup per mutation discipline: D3 block removed from implementation-plan.md; Reindex script bullet, integration point, Track 2/4 scope quotes, Component Map REQ node all stripped; Track 1 scope quote and §"Schema layer" bullet de-`requires`-ed; I4 invariant (`requires` markers reference only same-file sections) removed and I5/I6 renumbered to I4/I5; non-goal naming LLM-based `requires` load-bearing classification removed. Same sweep applied to plan/track-{1,2,4}.md (Purpose, Plan of Work, Validation/Acceptance, Files-in-scope, Audit-agent extensions).

**Mechanical checks** (target=design): PASS — 0 blockers, 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 36, 81, 233, 394, 444, 455, 461, 486 (carried forward from Mutation 1 as known debt; not introduced by this mutation).

**Cold-read** (scope: whole-doc): PASS after one iteration. First pass returned NEEDS REVISION with 3 blockers — residual `requires` references in implementation-plan.md (Schema-layer Track 1 bullet, I4 invariant text, non-goal naming LLM-based classification, Track 1 scope quote + bracketed scope line) and in plan/track-1.md (Purpose / Big Picture, Plan of Work step 1 wording, glossary cross-link list step 2, Validation/Acceptance bullet), plus the dangling I4 invariant naming a removed concept. All fixed in iteration 2. Iteration 2 verified: every `**Full design**` cross-reference in implementation-plan.md resolves to an existing `## ` heading in design.md; invariants I1–I5 are sequential and self-contained; the two remaining `requires` tokens across all files (implementation-plan.md line 124 "still requires a Read" inside a D8 alternatives-considered sentence; design.md line 463 "the acceptance criterion requires verification") are pure English-verb usage in prose about a discarded alternative and a verification criterion, not marker references.

**Findings**:
- blocker (iteration 1, resolved): implementation-plan.md residues at the Schema-layer Track 1 bullet, the I4 invariant, the non-goal, and the Track 1 scope quote. Fixed: bullet de-`requires`-ed, I4 replaced with the telemetry publication-safety invariant (renumbered from I5), non-goal removed, Track 1 quote and scope line rewritten.
- blocker (iteration 1, resolved): plan/track-1.md residues at the Purpose paragraph, Plan of Work step 1 mention of the CI-enforcement rule, glossary cross-link list naming "`requires` marker", and Validation/Acceptance bullet. Fixed: all five mentions removed; the §1.8 commitments now name (role enum, phase enum, annotation idiom, TOC region format, cross-reference convention) without the marker.
- blocker (iteration 1, resolved): I4 invariant named `requires` markers while the design no longer defines the concept. Fixed: I4 deleted; I5 (telemetry percentages-only) and I6 (bootstrap block presence) renumbered to I4 and I5 respectively.
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 36, 81, 233, 394, 444, 455, 461, 486. Same ambient issues flagged in Mutation 1; not introduced by this mutation. Carried forward as known debt.

**Iterations**: 2 of 3 (PASS).

## Mutation 3 — 2026-05-27 — content-edit (design.md)

**Diff summary**: Locked `### ` annotation granularity to "every `### ` heading" — no author-judged sparseness. Five coupled edits to design.md, all carrying the same rule: (1) Core Concepts TOC region blurb (line 20) — replaced "Lists one row per `##` heading (and selected `###` where the author granularizes)" with "Lists one row per `##` and one row per `###` heading — no author-judged granularity"; (2) §"Annotation idiom and TOC region" Idiom shape (lines 47-53) — added two `### ` rows (§1.6(a) Format definition, §1.6(c) Stamp range definition) alongside the existing `## ` rows in the example TOC table; (3) §"Annotation idiom and TOC region" Edge cases / Gotchas (line 75) — extended the "single `##` heading without annotation comment is a CI blocker" bullet to cover `### ` headings with explicit "no author-judged granularity" clause; (4) §"Reindex script" Validation rule 3 (line 272) — extended the TOC-row contract from `^## ` to `^## and ^### `; (5) §"Reindex script" Validation rule 4 (line 273) — rewrote from "Annotation present after every `## ` heading. Selected `### ` headings may also have annotations when the author granularizes; the gate accepts but does not require `### ` annotation density." to "Annotation present after every `## ` and every `### ` heading. No author-judged granularity — `### ` annotations are required at the same density as `## ` annotations. A `## ` or `### ` heading without an annotation comment on the next line is a CI blocker." Overview line 8 ("after every `##`/`###` heading") and TL;DR line 38 ("after every `##`/`###` heading") were already aligned and required no edit.

**Mechanical checks** (target=design): PASS — 0 blockers, 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 36, 83, 235, 396, 446, 457, 463, 488 (same set as Mutation 1 and Mutation 2; line numbers shifted by 2 from the two new TOC rows). Cross-file ref check and reverse-direction-ref check PASS.

**Cold-read** (scope: whole-doc): PASS on first pass. The sub-agent answered all seven comprehension questions from the document alone, citing line anchors, and verified no surviving "selected", "may also", "optional", or sparse-`### ` language anywhere in the file. One should-fix surfaced: the bootstrap block's `## Reading workflow files (TOC protocol)` literal H2 (embedded in 38 in-scope agent / SKILL / prompt files per §"Bootstrap protocol") now also falls under the locked density rule and the design does not yet say whether the bootstrap heading is exempt or carries a standard annotation. This finding maps directly to the parent orchestrator's queued Task 3 ("Resolve bootstrap-heading TOC interaction") and is carried forward as known debt rather than auto-resolved here (the resolution is a deliberate design choice between exemption and standard annotation, not a mechanical fix).

**Findings**:
- should-fix (carried forward): bootstrap-heading TOC interaction surfaced by cold-read. The bootstrap block's literal `## Reading workflow files (TOC protocol)` heading appears as a real H2 in every in-scope file post-deployment and now falls under Rule 4's density contract. Design must declare whether the heading is exempt (literal-heading carve-out in the reindex script) or carries a `roles=any phases=any` annotation with a TOC row. Owned by the parent orchestrator's queued Task 3.
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 36, 83, 235, 396, 446, 457, 463, 488. Same ambient issues flagged in Mutation 1 and Mutation 2; not introduced by this mutation. Carried forward as known debt.

**Iterations**: 1 of 3 (PASS).

## Mutation 4 — 2026-05-27 — content-edit (design.md)

**Diff summary**: Resolved the bootstrap-heading TOC interaction surfaced by Mutation 3's cold-read. The bootstrap block contains a literal `## Reading workflow files (TOC protocol)` H2 that lands as a real `##` heading in every in-scope agent / SKILL / prompt file (38 files); under the locked density rule from Mutation 3 ("every `##` and `###` heading carries an annotation comment and a TOC row") the heading would have demanded its own annotation in every file. Resolution: exemption by literal-heading match — the same string the reindex script's bootstrap-block presence check (rule 7) already keys off. The carve-out surfaces in four locations: (1) §"Bootstrap protocol" → §"Block placement and stability" — new "Interaction with TOC density rules (3 and 4)" paragraph naming the exemption, the literal heading, the reuse of rule 7's match, and the rationale (byte-identical heading, structurally above H1, identical row across files would be clutter); (2) §"Reindex script" rule 3 — appended "**Exempt:** the bootstrap-block heading `## Reading workflow files (TOC protocol)` is excluded by literal-heading match (see §"Bootstrap protocol" → §"Block placement and stability")"; (3) §"Reindex script" rule 4 — appended "**Exempt:** same literal-heading match as rule 3"; (4) §"Bootstrap protocol" → §"Edge cases / Gotchas" — appended a carve-out bullet naming this as the only `##` heading in workflow scope exempt from the rule-3 / rule-4 density contract and clarifying that future bootstrap-style blocks would need analogous literal-heading carve-outs. Iteration 1 cold-read also surfaced a real blocker that traced to Mutation 2's residue: three "rule 8" references in §"Block placement and stability" (line 233), the new "Interaction with TOC density rules" paragraph (line 235), and §"CI enforcement" (line 239) cited a rule number that no longer exists — Mutation 2 had removed the old rule 6 (`requires` presence) and renumbered 7→6, 8→7, but missed three prose references in §"Bootstrap protocol". All three corrected to "rule 7" in iteration 2. Iteration 3 closed a should-fix flagged by cold-read against §"Annotation idiom and TOC region" → §"Edge cases / Gotchas" line 75: the early-doc paraphrase of the density rule didn't surface the exemption, so a cold reader landing there before reaching §"Bootstrap protocol" would read a stricter rule than the canonical rule 4. Appended a parenthetical cross-link to §"Bootstrap protocol" → §"Block placement and stability".

**Mechanical checks** (target=design): PASS — 0 blockers, 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 36, 83, 237, 399, 449, 460, 466, 491 (same set as Mutations 1-3; line numbers shifted by 2 from Mutation 3's TOC-row additions and again by carve-out paragraph insertion).

**Cold-read** (scope: whole-doc): NEEDS REVISION on iteration 1 (one blocker: stale rule-8 cross-refs from Mutation 2 residue); PASS on iteration 2 after blocker fix; iteration 3 closed the line-75 should-fix surfaced by iteration 2.

**Findings**:
- blocker (iteration 1, resolved): three stale "rule 8" cross-refs from Mutation 2's renumbering residue (lines 233, 235, 239). All three corrected to "rule 7" in iteration 2.
- should-fix (iteration 2, resolved): §"Annotation idiom and TOC region" → §"Edge cases / Gotchas" line 75 paraphrased the density rule without mentioning the bootstrap-heading exemption. Cold-read flagged this as creating early-doc / late-doc inconsistency. Iteration 3 appended a parenthetical cross-link to §"Bootstrap protocol" → §"Block placement and stability".
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 36, 83, 237, 399, 449, 460, 466, 491. Same ambient issues flagged in Mutations 1-3; not introduced by this mutation. Carried forward as known debt.

**Iterations**: 3 of 3 (PASS).

## Mutation 5 — 2026-05-27 — content-edit (design.md)

**Diff summary**: Added the in-file ref auto-stamping convention as decision D9. The convention: authors write plain `§X.Y` and `§X.Y(z)` references inside any in-scope workflow doc; `workflow-reindex.py --write` resolves each ref's target heading, reads the target's annotation comment, and stamps the matching `:roles:phases` suffix onto the ref. Three failure modes are CI blockers: unstamped plain refs (author skipped `--write`), stamped refs whose suffix drifted from the target's current annotation, and refs that don't resolve to any heading in the same file. Cross-file refs (in SKILL.md startup read-lists and `.claude/agents/*.md`) stay hand-written because the citer's slice of the target is genuinely narrower than the target's full annotation in many cases. Sites touched: (1) Core Concepts "Cross-reference convention" bullet — expanded to name both the hand-written cross-file form and the auto-stamped in-file form; (2) §"Cross-reference convention" TL;DR — rewritten to spell out both forms; (3) §"Cross-reference convention" Format example — extended with two annotated example rows showing the in-file `§1.6(c):...` and `§1.7:...` shapes alongside the cross-file rows; (4) new `### In-file reference auto-stamping` subsection between `### Read-decision flow` and `### Edge cases / Gotchas`, with four labelled blocks: "Why auto-stamp in-file but hand-write cross-file", "Drift detection", "Cross-file precision", "Read-decision flow extension"; (5) §"Cross-reference convention" Edge cases — three new bullets covering cross-file vs in-file suffix interpretation, the hand-vs-auto split being on cross-file vs in-file (not on `§X.Y(z)` presence), code-fence exclusion symmetry between `--check` and `--write`, and unresolved-ref handling; (6) §"Cross-reference convention" References — D9 added; (7) §"Reindex script" rule 6 — reworded to "Cross-file reference suffix (hand-written)" and clarified that script verifies presence but does not derive value; (8) new rule 8 — "In-file reference suffix (auto-stamped)" with three failure modes spelled out; (9) §"Reindex script" Output format example — extended with three `in-file-ref:` finding examples covering unstamped, drifted, and unresolved cases; (10) §"Reindex script" `--write` semantics — extended to include auto-stamp behaviour, code-fence skip, and halt-on-unresolved-ref; (11) §"Reindex script" Edge cases — two new bullets covering the cross-file/in-file split and drift-after-target-edit recovery; (12) §"Reindex script" References — D9 added.

Plan + track propagation: (a) implementation-plan.md — D9 record added between D8 and Invariants with cross-refs to design.md §"Cross-reference convention" → §"In-file reference auto-stamping" and §"Reindex script" rule 8; I6 invariant added for in-file ref drift detection; cleaned up three Mutation 1/2/3 residues — D8 risks "rule 8 → rule 7", I2 granularity-lock "selected `^###` where authored → every `^###` (no author-judged granularity; bootstrap-heading exception)", I5 "rule 8 enforces presence → rule 7 enforces presence"; (b) `plan/track-2.md` — Plan of Work step 2 expanded to enumerate every reindex-script check including the new rule 8 in-file ref validation; step 3 expanded to cover auto-stamp behaviour and code-fence/halt semantics; step 5 tests expanded with three new test cases (unstamped, stale-suffix, unresolved); stale CLAUDE.md residue at step 2 cleaned up (Mutation 1 residue, surfaced by this mutation's whole-doc cold-read); (c) `plan/track-4.md` — granularity prose updated at the in-scope file-count description (Mutation 3 residue); three stale CLAUDE.md residues at out-of-scope lines and the unblocks-track-5 dependency rationale cleaned up (Mutation 1 residue, surfaced by iteration 1 cold-read blocker); (d) `plan/track-5.md` — rule numbering corrected from "rule 8 + rule 7" to "rule 7 + rule 6" with new rule 8 reference added (Mutation 2 residue plus this mutation's new rule); (e) `plan/track-1.md` — stale "Cross-reference suffixes in CLAUDE.md and agent files — Track 5" out-of-scope bullet cleaned up (Mutation 1 residue, surfaced by iteration 1 cold-read blocker).

**Mechanical checks** (target=design): PASS — 0 blockers, 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 36, 83, 255, 427, 477, 488, 494, 519 (same set as Mutations 1-4; line numbers shifted by the new subsection + Format example expansion + Reindex script edits). One additional should-fix surfaced and fixed in iteration 2 of mechanical: em-dash density at the new "Why auto-stamp in-file but hand-write cross-file" paragraph (line 187, 2 em-dashes); both replaced with commas. After the em-dash fix the only remaining findings are the pre-existing ambient set.

**Cold-read** (scope: whole-doc — periodic counter triggered at 5th mutation): NEEDS REVISION on iteration 1 (one blocker: three stale CLAUDE.md references in `plan/track-4.md` at lines 65, 132, 140 contradicting the design's "out of scope" decision — a Mutation 1 residue that the original Mutation 2 cross-file ref propagation missed); PASS on iteration 2 after fixing those three sites plus an additional residue at `plan/track-1.md` line 113 surfaced by the iteration-2 verification sweep. The mutation's own auto-stamping content landed cleanly on iteration 1 — no contradictions between cross-file vs in-file split, no broken rule references, no granularity-lock contradictions.

**Findings**:
- blocker (iteration 1, resolved): four CLAUDE.md residues in plan/track-{1,4}.md contradicting the design's "out of scope" decision (track-4.md:65/132/140, track-1.md:113). All four fixed in iteration 2 to frame CLAUDE.md uniformly as out of scope.
- should-fix (mechanical iteration 1, resolved): em-dash density at the new auto-stamping subsection's "Why auto-stamp in-file but hand-write cross-file" paragraph (line 187, 2 em-dashes). Both replaced with commas; finding cleared.
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 36, 83, 255, 427, 477, 488, 494, 519. Same ambient issues flagged in Mutations 1-4; not introduced by this mutation. Carried forward as known debt.

**Iterations**: 2 of 3 (PASS).

## Mutation 6 — 2026-05-27 — section-add (design.md)

**Diff summary**: Added a new top-level `## Files and surfaces out of scope` section between Core Concepts and Annotation idiom. The section consolidates six exclusion categories into a single anchor a reader new to the design follows when asking "does the schema apply to X?" — each entry cross-references the mechanism section that owns the authoritative rule rather than duplicating it. Six exclusions covered: (1) agent files lack per-section TOC + annotations — cross-ref to §"Bootstrap protocol" → §"Scope and uniformity" + plan D6; (2) `CLAUDE.md` excluded from all three layers — cross-ref to §"Overview" + plan Non-Goals; (3) Phase 4 final artifacts (`design-final.md`, `design-mechanics-final.md`, `adr.md`) carry no annotations — cross-ref to §"Migration replay semantics"; (4) ephemeral `_workflow/**` artifacts carry no annotations — cross-ref to §"Reindex script" → §"Discovery mechanism" (the script's fixed globs are the inclusion list; `_workflow/**` paths are deliberately absent); (5) non-workflow skills (`ai-tells`, `run-jmh-benchmarks-hetzner`, `profile-jmh-regressions`, etc.) lack bootstrap blocks — cross-ref to §"Bootstrap protocol" → §"Scope and uniformity"; (6) files with no `^## ` headings carry no TOC region — cross-ref to §"Reindex script" → §"Edge cases / Gotchas". The non-Markdown `.claude/scripts/**` tree is named in a trailing paragraph as a mechanical extension exclusion (non-Markdown by extension) for completeness. The section also carries: a "Why a consolidated section" sub-paragraph explaining the single-anchor rationale; Edge cases / Gotchas with four forward-looking bullets (agent-file size growth, future-skill workflow-read expansion, new `_workflow/**` artifact types, the `.claude/scripts/**` mechanical exclusion); and References footer with D6 and D8. Structure-roadmap sentence at Overview line 14 updated to insert "the files and surfaces out of scope" between Core Concepts and "the annotation idiom and TOC region format" so a reader following the roadmap finds the new section in document order.

**Mechanical checks** (target=design): PASS — 0 blockers, 9 should-fix findings on iteration 1 including one new finding at the new section's TL;DR (line 36, "Files and surfaces out of scope" heading sharing 2/4 content words "files" and "surfaces" with the TL;DR's "Six categories of files and surfaces" sentence). Iteration 2 rewrote the TL;DR to use "Markdown" / "artifacts" / "exclusions" / "categories" without re-echoing the heading's content words; finding cleared. Final state: 8 should-fix findings, all pre-existing ambient fragmented-header issues at lines 36, 73, 120, 292, 464, 514, 525, 531, 556 (line numbers shifted from Mutation 5 by the new section's ~35-line insertion).

**Cold-read** (scope: bounded — section-add): PASS on iteration 1 with one should-fix (exclusion 4 lacked a `→ §"..."` cross-ref arrow that the other five entries carried) and one suggestion (exclusion 2's `→ §"Overview" line 10` used a brittle line-number citation). Both addressed in iteration 2: exclusion 4 gained `→ §"Reindex script" → §"Discovery mechanism"` citing the inclusion-list view (the script's fixed globs deliberately omit `_workflow/**`); exclusion 2's line-number citation was simplified to `→ §"Overview"` (stable anchor). The cold-read verified that every cross-referenced mechanism section exists and that the bootstrap-block scope match (7 named SKILL.md files) is exact between the consolidated list and §"Bootstrap protocol" → §"Scope and uniformity".

**Findings**:
- should-fix (mechanical iteration 1, resolved): TL;DR at the new section echoed the heading "Files and surfaces out of scope" with 2/4 content-word overlap (50% threshold). Rewrote the TL;DR to avoid the echo; finding cleared.
- should-fix (cold-read iteration 1, resolved): exclusion 4 (`_workflow/**` artifacts) lacked the `→ §"..."` cross-reference arrow that the other five entries carried. Added cross-ref to §"Reindex script" → §"Discovery mechanism".
- suggestion (cold-read iteration 1, resolved): exclusion 2 (`CLAUDE.md`) cited `→ §"Overview" line 10` — line-number citation is brittle to Overview edits. Simplified to `→ §"Overview"`.
- should-fix (ambient, pre-existing × 8): fragmented headers at lines 36, 73, 120, 292, 464, 514, 525, 531, 556. Same ambient issues flagged in Mutations 1-5; not introduced by this mutation. Carried forward as known debt.

**Iterations**: 2 of 3 (PASS).

## Mutation 7 — 2026-05-28 — content-edit (design.md)

**Diff summary**: Dropped the two reserved phase tokens `1a` and `1b` from the phase enum. Cardinality fell from 10 to 8 (`0, 1, 2, 3A, 3B, 3C, 4, any`). Edit sites: (1) Core Concepts entry for "Phase enum" tightened to enumerate the 8 values without the reservation prose; (2) Annotation idiom TOC example rows for §1.6 and §1.6(a) updated to drop `1a,1b` from the Phases column so the example matches the new enum; (3) `## Role and phase enums` TL;DR rewritten to lead with the lock semantics (out-of-enum tokens fail CI, future additions require a workflow-format commit) instead of paraphrasing the heading; (4) `### Phase enum (10 values)` heading became `### Phase enum (8 values)`; (5) the `1a` and `1b` rows removed from the enum table; (6) the reservation paragraph between the enum table and `### Cross-cutting flows` removed; (7) `1a/1b at rollout` qualifier removed from the edge-case bullet on zero-user tokens (the half about "future enum additions before their first author" stays); (8) D1 footer trimmed to "Lock the enum at 15 roles + 8 phases". Rationale: YTDB-975 will introduce whatever phase tokens it actually needs as part of its own workflow-format commit when it merges to develop; pre-allocating slots in this PR would pre-empt YTDB-975's design choices, and the "second workflow-format commit" cost the original D1 claimed to save is paid anyway because YTDB-975's merge trips the drift gate on every active branch regardless. Plan + track-1 follow-up edits land in the same session outside the design-mutation discipline.

**Mechanical checks** (target=design): iteration 1 surfaced one should-fix at the rewritten TL;DR (line 122) — the original "Two locked enums in conventions.md §1.8. 15 roles cover every distinct calling agent across the workflow; 8 phases cover the workflow's phase taxonomy." shared 2/3 content words with the heading "Role and phase enums" (67% overlap, threshold 50%); the original TL;DR had the same overlap but cleared the threshold by virtue of "with two slots reserved for YTDB-975 in flight" padding that this mutation legitimately removed. Iteration 2 rewrote the TL;DR to lead with the lock's semantic effect ("Both vocabularies live in `conventions.md §1.8` and are closed at rollout… Out-of-enum tokens fail CI; future additions require a workflow-format commit.") — content-word overlap dropped to 1/3 with new load-bearing information about CI failure and workflow-format commits. Final state: PASS, 0 findings.

**Cold-read** (scope: bounded — content-edit covering "Role and phase enums" + "Annotation idiom and TOC region" + "Cross-reference convention" + Overview + Core Concepts): PASS on iteration 1 with no findings. The cold-read verified that Core Concepts cardinality (8 values) matches the section, the TL;DR's lock semantics are load-bearing, the TOC example phases column agrees with the enum, the D1 footer reflects the new cardinality, and the surrounding sections (Annotation idiom, Cross-reference convention) are unaffected by the enum change. A cold reader can build a working mental model from the changed section + surroundings alone.

**Findings**:
- should-fix (mechanical iteration 1, resolved): fragmented header at line 120 — the post-cleanup TL;DR shared 2/3 content words with the heading "Role and phase enums" (67%, threshold 50%). The original TL;DR carried filler ("with two slots reserved for YTDB-975 in flight") that diluted the overlap below threshold; removing the filler tipped the section into the fragmented-header zone. Iteration 2 rewrote the TL;DR to add new load-bearing information about enum closure and CI failure, dropping overlap to 33%.

**Iterations**: 2 of 3 (PASS).

## Mutation 8 — 2026-05-28 — content-edit (design.md)

**Diff summary**: Extended cross-file ref handling from presence-only validation (rule 6 today) to presence + subset validation. The citer's `name.md:roles:phases` must now be a subset of the target heading's current annotation; equality is not required so the citer keeps its narrower-slice expressiveness. Nine hunks landed: (1) Core Concepts Cross-reference convention bullet gained "with the script subset-validating each citer's slice against the target heading's current annotation"; (2) `## Cross-reference convention` → `### In-file reference auto-stamping` gained a new "**Cross-file drift detection.**" paragraph between "Why auto-stamp..." and "Drift detection." explaining the subset check, sub-section resolution rule, file-level union rule, and the citer-too-narrow false negative (left to humans); (3) the matching edge-case bullet in the same section was extended to cite rule 6's subset check; (4) Cross-reference convention References footer gained D10; (5) `## Reindex script` rule 6 was rewritten from "presence only" to "presence + subset" with explicit sub-section and file-level resolution rules; (6) Output format gained one example line for the new `cross-file-ref:` category (`conventions.md§1.6:migrator:3A,4 — citer's phase 4 not in target's current phases 3A,3B,3C`); (7) the Output format explanation paragraph was extended to cover the new category and its fix (hand-edit, not `--write`); (8) Edge cases / Gotchas gained a bullet for subset-violation drift mirroring the in-file drift bullet; (9) Reindex script References footer gained D10. Rationale: the cross-file ref's narrower-slice contract (D9 rationale phrasing: "narrower than the target's full annotation") is already a subset relationship; the script can check it mechanically (~30 lines of Python) without giving up the hand-written narrower-slice expressiveness. False negative (citer-too-narrow valid subset) stays with humans. Plan + track-2 follow-up edits (D10 add, I7 add, D9 rationale tighten, track-2 Plan-of-Work + test list update) land outside the design-mutation discipline.

**Mechanical checks** (target=design): PASS on iteration 1 — 0 findings. The TL;DR-vs-heading overlap stayed inside threshold for both touched sections (Cross-reference convention and Reindex script) because the rewrites added new load-bearing information rather than echoing the heading content words.

**Cold-read** (scope: bounded — content-edit covering "Reindex script" + "Cross-reference convention" + "Telemetry script" + Overview + Core Concepts): PASS on iteration 1 with two suggestions. The cold-read verified Core Concepts already names subset-validation as load-bearing vocabulary, the "Cross-file drift detection" paragraph explains the why and how, rule 6 carries the mechanical contract, Output format names a concrete `cross-file-ref:` example, and Edge cases bullets close the loop on the human-fixable cases. The References footers in both touched sections list D10 alongside the related D codes.

**Findings**:
- suggestion (cold-read iteration 1, logged not applied): Edge cases bullet at the new Cross-reference convention entry cites "rule 6" without an in-file cross-link to `§"Reindex script"` → `§"Validation rules"`. The in-file drift bullet at the same level does the equivalent cross-link to `§"Reindex script"` → `§"Edge cases"`. Adding the symmetric link would improve navigability; not a blocker because the section name is mentioned by name elsewhere on the page.
- suggestion (cold-read iteration 1, logged not applied): The sub-section resolution rule and file-level union rule are stated in both the "Cross-file drift detection" paragraph and rule 6, with slightly different wording. The two statements agree on substance today; a future edit that tightens one and forgets the other would create a contradiction. Reviewer recommended canonicalizing on rule 6 (script-implementation detail) and cross-referencing from "Cross-file drift detection". Carried forward as low-priority polish; if a future mutation touches either site, fold the duplication then.

**Iterations**: 1 of 3 (PASS).

## Mutation 9 — 2026-05-28 — content-edit (design.md)

**Diff summary**: Corrected `§"Files and surfaces out of scope"` TL;DR's `.claude/workflow/` doc count from `30` to `31` to match the actual directory. Triggered by Phase 2 consistency-review finding CR3, which observed the same drift across implementation-plan.md (Component Map node label, Annotated content bullet, Track 4 entry header + body) and `plan/track-4.md` (title, purpose, files-in-scope section). The plan-side counts were corrected via `Edit` in the same Phase 2 fix batch; this mutation lines design.md up with the corrected plan.

**Mechanical checks** (target=design): iteration 1 surfaced a pre-existing blocker at `implementation-plan.md:145` — D10's `**Full design**` line cited `design.md §"Cross-reference convention" → §"In-file reference auto-stamping" → §"Cross-file drift detection"`, but the third level is a bold-paragraph subsection inside the auto-stamping H3, not a heading; the full-design-link-resolution check could not resolve it. The plan ref was rewritten to drop the unresolvable third level and name the bold-paragraph subsection in parenthetical prose instead: `... → §"In-file reference auto-stamping" (the "Cross-file drift detection" bold paragraph inside)`. Iteration 2: PASS, 0 findings.

**Cold-read** (scope: bounded — `Files and surfaces out of scope` + `Overview` + `Core Concepts` + `Annotation idiom and TOC region`): PASS on iteration 1, 0 findings. The cold-read verified the changed TL;DR is internally coherent at 31, no surrounding section carries a contradictory file count, the bootstrap-scope quadruple (7/11/20 → 38) in Overview and Core Concepts is unaffected, and every `**Full design**` ref in the plan resolves to an existing `##` heading in design.md.

**Findings**:
- (none)

**Iterations**: 2 of 3 (PASS — iteration 1 mechanical surfaced the pre-existing D10 link blocker; iteration 2 fixed the plan ref and re-ran clean).

## Mutation 10 — 2026-05-28 — content-edit (design.md)

**Diff summary**: Rewrote `§"Telemetry script"` → `§"Phase 4 integration"` and one `### Edge cases / Gotchas` bullet to reflect that the telemetry script's invocation hooks into `create-final-design.md` Step 3 §"Artifact 2: ADR" (the adr.md-write step), not Step 5 (the final-artifacts commit step). Triggered by Phase 2 consistency-review finding CR1 (a real blocker: the live prompt's Step 5 only stages files already on disk, so the previous wording "runs in Step 5 before writing adr.md" described an impossible execution). The companion `track-3.md` step-number rewrites land in the same Phase 2 fix batch (Step 4 description + Context-and-Orientation + Validation + Library/function signatures). The edge case bullet on workflow-modifying plans now acknowledges that telemetry in Step 3 precedes the promote-staged-workflow commit in Step 4 and explains that the script measures session-transcript token usage rather than on-disk workflow state, so the timing affects session-event coverage rather than measurement substance.

**Mechanical checks** (target=design): PASS on iteration 1 — 0 findings.

**Cold-read** (scope: whole-doc — periodic counter triggered at 10th mutation): PASS on iteration 1, 0 findings. The cold-read verified that the rewritten Phase 4 integration paragraph is internally consistent with §"Telemetry script" §"Edge cases / Gotchas" bullet on the same execution-order point, that `§"Migration replay semantics"` describes a separate axis (branch artifact migration vs. intra-session step ordering) without contradiction, and that every `**Full design**` reference in the plan and track files resolves to a real `^##` or `^###` heading in design.md. The Step 3 / Step 4 / Step 5 vocabulary stays unambiguous because line 449 explicitly disqualifies Step 5 ("only stages files already on disk") and line 588 names Step 4 separately as the promote-staged-workflow commit.

**Findings**:
- (none)

**Iterations**: 1 of 3 (PASS).

## Mutation 11 — 2026-05-28 — content-edit (design.md)

**Diff summary**: Closes iteration-2 gate-verification residues CR8 (design-side) and CR10. CR8 aligns design.md §"CI gate semantics" TL;DR + §"Pre-commit hook" subsection opening with track-2.md's already-corrected "extended" framing — the `.githooks/pre-commit` file exists today as a Spotless runner gated on JetBrains/youtrackdb origin (CR5's iteration-1 fix landed in track-2.md but not in design.md). CR10 extends §"Agent-side absorption" with a parenthetical naming D11 as the introduction source of the `WB<N>` prefix, so a reader landing on design.md without crossing the plan or track-2 sees the introduction status. Iteration 1 mechanical surfaced a fragmented-header should-fix at the rewritten §"Pre-commit hook" subsection (50% heading-word overlap with the new one-line paragraph through `pre-commit`); fix was to rename the heading to `### Pre-commit hook (local enforcement surface)`, expanding heading content words from 2 to 5 and dropping overlap to 20%. The local-enforcement-surface qualifier also makes the parallel with §"GitHub Actions step" (the CI enforcement surface) explicit. A trailing D11 References-footer entry was added to close the inline-parenthetical cite per cold-read suggestion 1.

**Mechanical checks** (target=design): iteration 1 surfaced 1 should-fix (fragmented-header at design.md:466). Iteration 2: PASS, 0 findings.

**Cold-read** (scope: bounded — §"CI gate semantics" + §"Telemetry script" + §"Migration replay semantics" + §"Overview" + §"Core Concepts"): PASS, 2 suggestions logged. Suggestion 1 — append D11 cite to §"CI gate semantics" References footer — was applied (closes loop on the inline D11 parenthetical). Suggestion 2 — pre-commit-hook snippet at design.md:472 omits `--diff-filter=ACMR` while the gotcha at line 504 documents the requirement — carried forward as known debt; pre-existing and outside CR8/CR10 scope, fold into the next mutation that touches §"Pre-commit hook" subsection or §"Edge cases / Gotchas".

**Findings**:
- (none — all iteration-1 mechanical findings resolved; cold-read suggestions noted above)

**Iterations**: 2 of 3 (PASS — iteration 1 mechanical surfaced the fragmented-header should-fix; iteration 2 fixed the heading expansion and re-ran clean).

## Mutation 12 — 2026-05-31 — content-edit (design.md)

**Diff summary**: D19 inline-replan body correction to §"Bootstrap protocol for agent system prompts" → §"Bootstrap block content" fenced block. Three changes to the embedded protocol: step 1 keys the TOC read on the `<!--Document index start-->`/`<!--Document index end-->` delimiters instead of "first ~30 lines" (large files like `conventions.md` exceed 30 lines); step 2's match rule expands a wildcard on either axis (a reader whose own role or phase is `any`, or a row whose Roles/Phases is `any`, matches); the closing line gains a half-sentence that backtick-wrapped refs carry no suffix. A new §"Edge cases / Gotchas" bullet records that the reader-side `any` expansion is distinct from the one-way citer-side cross-reference subset rule in §"Cross-reference convention". This is the design-side half of D19; the 38-file re-propagation plus staged `conventions.md §1.8(e)/(f)` alignment is Track 5 Step 5 (a later Phase B session).

**Mechanical checks** (target=design): iteration 1 surfaced 2 blockers + 1 should-fix. Both blockers were pre-existing dangling `design.md §"…"` cross-refs the whole-doc full-design-link check surfaced for the first time since the Track 2–5 episodes landed (no design mutation had run since): implementation-plan.md:285 (Track 2 episode) cited `§"Pre-commit hook"`, whose real heading gained the `(local enforcement surface)` qualifier back in Mutation 11; track-5.md:43 (this replan's own Decision Log) cited `§"Bootstrap protocol"`, real heading `…for agent system prompts`. Both citations were corrected to their resolving forms. Iteration 2: PASS, 1 should-fix remaining.

**Cold-read** (scope: bounded — §"Bootstrap protocol for agent system prompts" + §"Cross-reference convention" + §"Reindex script" + §"Overview" + §"Core Concepts"): PASS (cold reader can build a working mental model — YES). 0 blockers, 0 should-fix, 1 suggestion. The reviewer verified the new Edge-cases bullet correctly resolves the reader-side-vs-citer-side `any` ambiguity against staged `conventions.md §1.8`'s `any`-wildcard semantics.

**Findings**:
- should-fix (carried as known debt): fragmented-header at design.md:291 — `### CI enforcement` heading shares 50% content words ("CI") with its paragraph. Pre-existing; outside the `###` subsections this edit touched. A clean fix needs a `###` rename (ref-breakage risk) or rewording substantive prose, both beyond this focused D19 content-edit. Fold into the next mutation that touches §"Bootstrap protocol" → §"CI enforcement", or reconcile when `design-final.md` is authored at Phase 4. Same carry-forward shape as Mutation 11's suggestion 2.
- suggestion (recorded, not applied): the §"Cross-reference convention" Read-decision flowchart node still reads `Read TOC region (first ~20 lines)`, now in phrasing tension with the delimiter-bounded step-1 wording. Surrounding section, not the changed one; "20" is illustrative-for-a-small-file (a tension, not a contradiction). Suggestions are not retried per the iterate protocol. Phase 4 `design-final` reconciliation candidate.

**Iterations**: 2 of 3 (COMPLETE WITH SHOULD-FIX FINDINGS — iteration 1 cleared both blockers via cross-ref citation fixes; the lone remaining should-fix is pre-existing and unrelated to the D19 body change, carried forward as known debt).

## Mutation 13 — 2026-06-01 — phase4-creation (design-final.md)

**Diff summary**: Created `docs/adr/ytdb-1023-workflow-toc/design-final.md` (top-level final artifact, unstamped per §1.6(f)), the as-built design mirroring `design.md`'s 12-section shape (Overview, Core Concepts, Files and surfaces out of scope, Annotation idiom and TOC region, Role and phase enums, Cross-reference convention, Bootstrap protocol for agent system prompts, Reindex script, Telemetry script, CI gate semantics, Migration replay semantics, Phase 4 ADR template extension). Folds in the deferred-drift reconciliation the frozen `design.md` predated: the as-built bootstrap body (delimiter-bounded TOC read window, any-of/OR match on either axis, no-TOC read-window complement, no-rows-match terminal plus the flowchart loop-back and terminal edges, backtick-wrapped non-annotatable refs); three-anchor bootstrap placement (under-H1 / after-frontmatter / top-of-file); the updated Read-decision flowchart; the six shipped discovery globs (three live plus three staged) and the separate live-agent rules-6/7-only citing scope; the widened cross-file suffix convention (all in-scope workflow docs and prompts, non-annotatable targets backtick-wrapped) with staged-first / bare-basename / `<skill-dir>/SKILL.md` target resolution; the post-merge migration-acceptance framing; the telemetry methodology limits (char/4 vs API divergence, lifetime-window non-comparability, the symlinked-worktree repo-root fix in commit d00cd91f25) and the four skip-notice templates; and the locked "after `## Key Discoveries`" telemetry placement. Closes the two Mutation 12 carry-forwards: the `### CI enforcement` fragmented-header and the flowchart read-window phrasing are both reconciled in the as-built doc.

**Mechanical checks** (target=design, scope=whole-doc): iteration 1 surfaced 0 blockers + 10 should-fix (all `dsc-ai-tell`: one hyphenated-pair cluster plus nine fragmented-header echoes where an opener repeated its heading's words). All 10 cleared by rewording the flagged openers; re-run: PASS, 0 findings.

**Cold-read** (scope: whole-doc): PASS — cold reader can build a working mental model (YES). phase4-creation extra checks all clean: plan-deviation surfacing present (the Overview names the file-level suffix scope growth and the two-vs-three-commit Phase 4 shape), the Read-decision flowchart is faithful to the as-built protocol, and no leaked working-file identifiers. All four Human-reader rules (audience-fit, glossary-introduction, why-before-what, navigability) pass. PSI is N/A (Markdown / Python / YAML; no Java symbols, only a Mermaid flowchart and no class diagrams). Two should-fix em-dash-cap findings (paragraph-internal em-dash pairs in the bootstrap TL;DR and block-content paragraph) fixed by converting the appositives to parentheses; mechanical re-ran clean afterward.

**Findings**:
- (none remaining — 10 mechanical should-fix and 2 cold-read should-fix all resolved during iteration)

**Iterations**: 2 of 3 (PASS — iteration 1 cleared the 10 mechanical should-fix; the cold-read's 2 em-dash should-fix were fixed and the mechanical re-run confirmed clean).
