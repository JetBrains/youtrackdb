<!-- workflow-sha: 367f5f83f1bce0e98eaeb0679973f9728db64b61 -->
# Track 4: Universal annotation rollout (49 files)

## Purpose / Big Picture

After this track lands, every in-scope workflow doc and skill file carries a TOC region and per-section annotations. The reindex script's `--check` passes cleanly across the full file set.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Author per-section TOC + annotations for every in-scope file: 31 under `.claude/workflow/`, 11 under `.claude/workflow/prompts/`, and 7 workflow-referencing skill files. ~600 annotations, all author-written. Run `workflow-reindex.py --write` to scaffold TOC tables, then hand-correct per-section `roles=`, `phases=`, `summary=`. Land in a single logical batch so the schema becomes universally applicable on one commit (or a small adjacent group; squash-merge collapses anyway).

## Progress
- [x] 2026-05-29T03:51Z [ctx=warning] Review + decomposition complete (Technical + Risk + Adversarial reviews PASS iter-1; 6 steps decomposed; 2 design decisions resolved)
- [ ] Step implementation
- [x] 2026-05-29T04:50Z [ctx=info] Step 1 complete (commit 39d517c5c8) — dim review PASS iter-2 (5 agents; WC1/WI1 blocker fixed + 4 lower findings)
- [x] 2026-05-29T05:08Z [ctx=warning] Step 2 complete (commit 868f1f3d97) — batch 1 (9 files); rule_6 ADJUST surfaced (blocks Step 6 criterion) → paused for user decision + session refresh
- [x] 2026-05-29T06:16Z [ctx=warning] Inline replan (rule_6 ADJUST → D13): staged §1.8(e) widened, plan DRs/invariants (D6/D8/D9/D10/I7 + new D13) reconciled, Steps 3-6 re-decomposed to fold in link conversion; handoff resolved; `## Plan Review` reset to `[ ]` for State 0 re-validation.
- [x] 2026-05-29T07:14Z [ctx=info] Inline replan (build_file_lookup live-first → D14): Step 3 (old) implementer returned DESIGN_DECISION_NEEDED; user picked staged-aware lookup. New HIGH-risk script-fix step inserted (re-decomposed: new Step 3, old 3-6 → 4-7); D14 added, D13 no-third-reopen clause superseded; I7 + Plan of Work + Idempotence + Interfaces + Validation updated; `## Plan Review` reset to `[ ]` for State 0 re-validation.
- [x] 2026-05-29T09:25Z [ctx=info] Step 3 complete (commit 83a555854f) — staged-aware `build_file_lookup` (D14); dim review PASS iter-2 (consistency/hook-safety/context-budget; WC1 blocker fixed [bare-basename ambiguity guard → logical-path keying] + WC2/WH1/WH2); 103/103 reindex tests pass.
- [x] 2026-05-29T12:03Z [ctx=warning] Inline replan (build_file_lookup bare-basename collision → D15): Step 4 (old) implementer returned DESIGN_DECISION_NEEDED (`structural-review.md` workflow-root-vs-prompts collision; bare key resolves to the prompt); user picked Option A (script fix, bare → workflow-root). New HIGH-risk script-fix step inserted (re-decomposed: new Step 4, old 4-7 → 5-8); D15 added, D14 forward-note added, I7 + Plan of Work + Idempotence + Interfaces + Validation updated; staged §1.8(e) gained a collision note; `## Plan Review` reset to `[ ]` for State 0 re-validation.
- [x] 2026-05-29T13:03Z [ctx=info] Step 4 complete (commit c54ff41cbe) — D15 bare-basename collision fix (bare `structural-review.md` → workflow-root); dim review PASS iter-1 (1 suggestion WH1, applied + gate-verified iter-2); 109/109 reindex tests.
- [x] 2026-05-29T14:02Z [ctx=info] Step 5 complete (commit 6de25eba70) — batch 2 (22 workflow-root files, 202 headings) annotated + links converted; rules 2/3/4/5/8 green, 30 rule_6 dangling refs to Step 6 prompt targets + 22 rule_1 missing-stamp are the expected interim windows.
- [x] 2026-05-29T14:30Z [ctx=info] Step 6 complete (commit e8a0f7aca9) — 11 prompts annotated + links converted + create-final-design.md bare-§1.7 tokens fixed; Step 5's 30 dangling refs cleared; rules 2/3/4/5/8 green, one dangling rule_6 to edit-design/SKILL.md (Step 7) remains.
- [ ] Track-level code review
- [ ] Track completion

## Surprises & Discoveries
<!-- Continuous-log. Promoted by the orchestrator from per-step "What was
discovered" when the finding affects future steps or other tracks. Empty
at Phase 1. -->

- 2026-05-29T04:50Z Step 1 discovered: `design.md` still anchors the TOC "directly under the H1" at lines 20, 75, 285, which drifts from the implemented three-shape anchor schema — carry to Phase 4 design-synthesis (`design-final.md`) alongside the Track 3 design.md drift items. See Episodes §Step 1.
- 2026-05-29T04:50Z Step 1 discovered: the gap-scan accepts a non-fenced bootstrap block plus blank lines before the TOC delimiter under all three anchor shapes, so Track 5 can place `## Reading workflow files (TOC protocol)` above the TOC region on H1, after-frontmatter, and top-of-file files alike. See Episodes §Step 1.
- 2026-05-29T05:08Z Step 2 discovered: rule_6 in `workflow-reindex.py` flags Markdown link targets (`](x.md)`), link text, and bare prose mentions as cross-file refs missing a `:roles:phases` suffix — far broader than the design scope (agent files + SKILL read-lists only). Annotating targets cannot clear them, so the Step 6 full-set green `--check` is unreachable until rule_6 is rescoped. Reopens Track 2's script. See Episodes §Step 2 and Decision Log.
- 2026-05-29T05:08Z Step 2 operational rule for Steps 3-5: `--write` halts (exit 2) on a bare `§X.Y` following a backticked filename in prose; wrap the section token in one inline-backtick span. Never put a bare `name.md` token inside a `summary="..."`.
- 2026-05-29T06:16Z Inline replan (D13): the cross-file `:roles:phases` suffix convention is widened to all in-scope workflow docs/prompts, and non-annotatable targets are backtick-wrapped. The staged `conventions.md §1.8(e)` schema and the plan DRs/invariants now carry the widened contract, but `design.md` §"Cross-reference convention" and §"Files and surfaces out of scope" exclusion 1 still scope the suffix to agent files + SKILL read-lists — carry to Phase 4 design-synthesis (`design-final.md`) alongside the TOC-anchor and Track 3 design.md drift items. Recorded in `implementation-plan.md` D13's Full design line.
- 2026-05-29T07:14Z Step 3 (old) attempt discovered, verified against the live script: `workflow-reindex.py`'s `build_file_lookup` keys cross-file ref targets on the bare basename and iterates `IN_SCOPE_GLOBS` live-before-staged with first-match-wins, so a converted ref resolves to the un-annotated live copy and fails rule 6's subset check permanently. The staged-first precedence built for the §1.8 enum bootstrap probe (§1.7(d)/D12) was never applied to cross-file-ref target resolution. Resolved by D14 (new Step 3). See Decision Log.
- 2026-05-29T07:14Z Cross-track signal to Track 5: the same live-first lookup will fail Track 5's agent-file ref subset checks (their workflow-doc targets are staged during this branch), so Step 3's staged-aware lookup (D14) is a prerequisite for Track 5's agent-ref sweep, not only Track 4's conversion. See `implementation-plan.md` D14 Risks/Caveats.
- 2026-05-29T09:25Z Step 3 discovered (dim review): the multi-staged ambiguity guard must key on the logical `.claude/...` path, not the bare basename. The seven `SKILL.md` anchors and the workflow-root-vs-`prompts/` `structural-review.md` pair share a basename, so a bare-basename guard would falsely halt with exit 2 once Steps 4-6 stage them, breaking Step 7's full-set green `--check` and Track 5's agent-ref sweep. Fixed (logical-path-keyed guard) and pinned by tests. Related: the pre-fix lookup never emitted the `prompts/<name>.md` key for a staged prompt (now fixed), which Steps 5-6 depend on for staged-prompt-target ref conversion. See Episodes §Step 3.
- 2026-05-29T12:03Z Step 4 (batch-2 conversion attempt) discovered, verified empirically against the live script: `build_file_lookup` records the bare basename key for both the workflow-root `structural-review.md` and its `prompts/` namesake, and first-match-wins (the prompts path sorts first under the `.claude/workflow/**/*.md` glob) hands the bare key to the prompt — so a bare `structural-review.md` ref resolves to the prompt and the workflow-root doc has no reachable cross-file key. Step 3 (D14) noted this collision pair only for the multi-staged ambiguity guard; ref resolution for the collision was unaddressed, one layer deeper than D14. Resolved by D15 (new Step 4). Cross-track signal to Track 5: an agent ref to the workflow-root `structural-review.md` hits the same collision, so D15 is a prerequisite for Track 5's agent-ref sweep too. See Decision Log.
- 2026-05-29T13:03Z Step 4 (D15) landed: a bare `structural-review.md` cross-file ref now resolves to the workflow-root doc rather than the `prompts/` namesake, with the prompts-only-basename bare-key fallback preserved. This confirms the planned Track 5 unblock — an agent ref to the workflow-root `structural-review.md` (Track 5's sweep) and the converted refs in Steps 5-8 that mean it now resolve correctly. See Episodes §Step 4.
- 2026-05-29T14:02Z Step 5 discovered: the §1.8(e) cross-file-with-subsection ref form `name.md§X.Y:roles:phases` does not validate in live prose. The reindex in-file-ref scanner matches the `§X.Y:roles:phases` tail independently of the `name.md` prefix, so `--write` halts with a rule_8 unresolved-`§X.Y` against the citing file's own headings. Every §1.8(e) example of this form is fenced or inline-backticked, so live prose never exercised it. Workaround used this batch and required for every later sub-section pointer: whole-file form `name.md:roles:phases` (sub-section pinning is optional per §1.8(e)) plus a separate backticked `§X.Y` token, which the scanner skips. Affects Step 6 and Step 8 (including Step 8's batch-1 conversion pass) and likely Track 5's agent-ref sweep if any agent ref pins a sub-section. Phase 4 design-final reconciliation candidate for the §"Cross-reference convention" narrative (joins the D13/D14/D15 deferred-drift basket). See Episodes §Step 5.
- 2026-05-29T14:02Z Step 5 operational rule for Step 6: the staged `prompts/create-final-design.md` carries pre-existing bare `§1.7(b/e/f)` tokens after backticked filenames that halt a global `--write`; Step 6 must backtick-wrap them, and until then `--write` needs `--files` scoping. The per-line inline-backtick detector also breaks on a backtick span wrapped across two source lines — reflow onto one line. See Episodes §Step 5.
- 2026-05-29T14:30Z Step 6 discovered: the `### Finding <PREFIX><N> [blocker|should-fix|suggestion]` template headings in the review prompts carry literal pipe characters that the reindex TOC writer does not escape in a section cell, so rule_3 fires. Fixed by wrapping each Finding template in a fenced code block (matches the already-fenced sibling Premise/Edge-case/Integration templates and `review-iteration.md`'s identical template). Phase 4 design-final candidate note: the TOC writer has no pipe-escaping, so any future need to annotate (rather than fence) a pipe-bearing heading would require a script change. See Episodes §Step 6.
- 2026-05-29T14:30Z Step 6 → Step 7/8 dependencies: `create-final-design.md` cites `edit-design/SKILL.md:final-designer:4`, so Step 7 must annotate `edit-design/SKILL.md` with a union ⊇ `final-designer:4` to clear that dangling ref. Batch-1 `implementation-review.md` still carries bare `prompts/*.md` Markdown links; Step 8's deferred batch-1 conversion converts them now that the prompt targets are annotated. See Episodes §Step 6.

## Decision Log
<!-- Continuous-log. Execution-time decisions: inline-replan choices,
scope-downs, dependency reveals, gate-override reasons. -->

- **(Phase A) Fence-fix scope kept in Track 4.** The Phase A reviews ran `workflow-reindex.py` and found the parser counts fenced `##`/`###` headings and TOC delimiters as real (rules 2/3/4 ignore `compute_fenced_lines`; ~156 fenced headings in 20 files). The approved WB1 plan note ("rule_4 carve-out in `create-final-design.md`") was both mis-scoped (rules 2/3/4, not rule_4; 20 files, not 1) and mis-sequenced (prerequisite for Step 1, since `conventions.md` is annotated first). User chose to keep the widened fix in Track 4 as a front-loaded HIGH-risk step rather than ESCALATE to a Track 2 reopen. The `## 99.1 Demo section` annotate-vs-carve-out question resolves to carve-out, handled by the same parser fix.
- **(Phase A) H1-less skill files: TOC after frontmatter.** 5 of the 7 in-scope skill files (`edit-design`, `migrate-workflow`, `code-review`, `review-workflow-pr`, `review-plan`) have real `##` sections but no document H1, which §1.8(d)'s "directly under H1" rule did not anticipate. User chose to anchor the TOC immediately after the frontmatter block (over adding an H1 title or dropping skills to refs-only). Step 1 amends §1.8(d) + rule_2 accordingly; Track 5 inserts the bootstrap block above the TOC.
- **(Phase A) `--write` does not scaffold TOC regions.** Confirmed by reading `_rebuild_toc_region`: it rebuilds an existing delimiter pair, returning None when none exists. The per-file procedure now hand-places delimiters first, then runs `--write` to fill the table and auto-stamp in-file refs.
- **(Step 1, scope-refinement) Third TOC anchor shape added — top-of-file.** Dimensional review found 10 of the 11 prompts are prose-first (no real H1, no frontmatter), a shape the step's two-anchor plan did not cover and rule_2 silently accepted. The chosen convention extends the Phase A "anchor at the natural structural top" decision: such files anchor the TOC at the top of the file (delimiter as first content). §1.8(d) and rule_2 now enforce all three shapes (under-H1 / after-frontmatter / top-of-file) with stated H1-over-frontmatter-over-top precedence. See Episodes §Step 1.
- **(Step 2, ADJUST — RESOLVED by inline replan, 2026-05-29) rule_6 scope fix required before Step 6.** rule_6 flags Markdown link targets / link text / bare prose `name.md` mentions as cross-file refs missing a `:roles:phases` suffix, which makes the Step 6 full-set green `--check` unreachable (link targets cannot carry a suffix). The design scopes the mandatory cross-file suffix to agent files + SKILL.md startup read-lists only (D6, D8, Track 5, Non-Goals). Proposed ADJUST: add a rule_6-rescope step (reopening Track 2's script, as Step 1 already reopened it) before Step 6 — rule_6's "missing suffix" check fires only on the surfaces where the convention mandates the suffix, leaving workflow-doc/prompt prose and Markdown links unflagged; subset validation (I7) still applies to any ref that carries a suffix. **User decision (2026-05-29):** do NOT rescope rule_6 narrow; instead CONVERT workflow-doc/prompt Markdown links into bare cross-file refs carrying the `:roles:phases` suffix (e.g. `workflow.md:orchestrator:3B`), so the filter rides at every reference site (load-on-demand motivation). This widens the cross-file suffix convention from agent-files-only to all workflow docs/prompts (reopens Track 1 §1.8), makes rule_6's broad behavior the intended contract, and means Step 2's 9 annotated files need a link-conversion pass. Non-annotatable targets (`CLAUDE.md`, `MEMORY.md`, `design.md`, `output-styles/house-style.md`, etc.) cannot carry a valid suffix — proposed handling: backtick-wrap to exempt (to be confirmed during replanning). This is an ESCALATE: the next session runs inline replanning to formalize the §1.8 widening + re-decompose Steps 2-6. Paused at warning context for a session refresh; see `_workflow/handoff-track-4-phaseB.md`. See Episodes §Step 2.
- **(Inline replan, 2026-05-29) rule_6 ADJUST formalized as D13; widening confirmed, backtick-wrap chosen for non-annotatable targets.** The user confirmed the link→suffixed-bare-ref direction and chose backtick-wrap (over a script allowlist) for non-annotatable targets, so no third reopen of `workflow-reindex.py`. Formalized: `implementation-plan.md` D13 (new), with D6/D8/D9/D10/I7 reconciled to the widened scope; staged `conventions.md §1.8(e)` widened (cross-file suffix applies to all in-scope workflow docs/prompts; Markdown links to in-scope targets convert to bare suffixed refs; non-annotatable targets backtick-wrapped; rule_6 broad behavior is the intended contract). Steps 3-6 re-decomposed to fold link-conversion into each annotation step, with Step 2's 9 batch-1 files converted in Step 6 (renumbered to Step 7 by the subsequent D14 replan). design.md §"Cross-reference convention" and §"Files and surfaces out of scope" exclusion 1 still carry the narrow agent-files framing — reconciliation deferred to Phase 4 `design-final.md` (joins the existing deferred-drift basket; recorded in D13's Full design line). `## Plan Review` reset to `[ ]`; the next `/execute-tracks` re-runs State 0 against the revised plan, then Phase B resumes from Step 3.
- **(Inline replan, 2026-05-29) build_file_lookup live-first → D14 (staged-aware lookup).** The first conversion attempt (old Step 3) returned DESIGN_DECISION_NEEDED: `build_file_lookup` resolves a converted ref's target live-first to the un-annotated live copy, so every converted ref fails rule 6's subset check permanently. Verified against the live script — `IN_SCOPE_GLOBS` lists live before staged and the lookup is first-match-wins. This is the same class as the rule_6 ADJUST behind D13, one layer deeper: D13's "no third script reopen" rested on the assumption that a converted ref validates against the annotated target, which on a workflow-modifying branch is the staged copy. User chose Option A (make the lookup staged-aware) over (B) accept rule 6 RED and redefine the green criterion, or (C) revert D13 to agent-files-only. Formalized as D14 in `implementation-plan.md`, superseding D13's no-third-reopen clause; I7 gained a staged-resolution note. A new HIGH-risk script-fix step is inserted as Step 3 ahead of the conversion batches (old Steps 3-6 → 4-7); the fix reuses the §1.8 enum probe's multi-staged exit-2 guard and is forward-safe (pure-live on develop / non-workflow-modifying branches). `## Plan Review` reset to `[ ]`; the next `/execute-tracks` re-runs State 0 against the revised plan, then Phase B resumes from the new Step 3.
- **(Inline replan, 2026-05-29) build_file_lookup bare-basename collision → D15 (bare key resolves to workflow-root).** Step 4's first conversion attempt (batch-2 annotate + convert) returned DESIGN_DECISION_NEEDED: `build_file_lookup` records the bare basename key for both the workflow-root `structural-review.md` and its `prompts/` namesake, and first-match-wins hands the bare key to the prompt (the prompts path sorts first under the `.claude/workflow/**/*.md` glob), so the converted cross-file refs that mean the workflow-root doc (`planning.md` ×2, `review-iteration.md`, `conventions.md`, `implementation-review.md`) resolve to the wrong file or fail subset permanently. Verified empirically against the live script. Same class as the D13 rule_6 ADJUST and D14's live-first resolution, one layer deeper: §1.8(e) already states bare = workflow-root anchor, `prompts/` = prompt, but the lookup violates it for the collision. User chose Option A (script fix, bare key → workflow-root) over (B) a `workflow/<name>.md` disambiguating ref form or (C) backtick-wrapping those refs as non-annotatable. Formalized as D15 in `implementation-plan.md` (fourth reindex reopen); I7 gained a collision clause; staged `conventions.md §1.8(e)` gained a basename-collision note. A new HIGH-risk script-fix step is inserted as Step 4 ahead of the conversion batches (old Steps 4-7 → 5-8); the fix keeps the bare-key fallback for prompts-only basenames and is forward-safe (no-op where the globs yield one file per key). Half (1) of the batch-2 annotation work (all 22 files) was completed and validated by the implementer but reverted per the DESIGN_DECISION contract; it re-runs in Step 5. `## Plan Review` reset to `[ ]`; the next `/execute-tracks` re-runs State 0 against the revised plan, then Phase B resumes from the new Step 4.

<!-- Reserved for Move 1 — per-track inlined Decision Records. -->

## Outcomes & Retrospective
<!-- Continuous-log. Review iteration outcomes and the track-completion
summary at Phase C. -->

- [x] Technical: PASS at iteration 1 (7 findings — 3 blocker, 3 should-fix, 1 suggestion; all accepted and folded into Step 1 + the corrected procedure/decomposition).
- [x] Risk: PASS at iteration 1 (5 findings — 0 blocker, 3 should-fix, 2 suggestion; all accepted, mostly into Idempotence and the per-batch `--check` rule).
- [x] Adversarial: PASS at iteration 1 (5 findings — 1 blocker, 2 should-fix, 2 suggestion; all accepted; file counts 31+11+7=49 verified exact).

All three reviews converged on one root cause: the reindex parser counts fenced headings/delimiters as real, so the track cannot reach a green `--check` by authoring alone. Findings were concrete, script-verified facts (re-confirmed directly against staged `conventions.md` §1.8 and the skill-file H1 reality), so they were accepted wholesale and incorporated into the decomposition rather than re-litigated across iterations; the corrected track file is the durable trace. Two findings needed user decisions (fence-fix scope, H1-less TOC placement) — resolved in the Decision Log above. Carried to Track 5: the H1-adjacency hand-off (Track 5's bootstrap block sits above Track 4's after-frontmatter TOC in H1-less files). Carried to Phase C: annotation semantic accuracy (roles/phases/summary) on Steps 2-6 is the focal-point concern `--check` cannot fully validate.

## Context and Orientation

The file set as enumerated during Phase 0 research:

**`.claude/workflow/` root (31 files):**
branch-divergence-check.md, code-review-protocol.md, commit-conventions.md, conventions-execution.md, conventions.md, defensive-push-check.md, design-decision-escalation.md, design-document-rules.md, ephemeral-identifier-rule.md, episode-format-reference.md, finding-synthesis-recipe.md, implementation-review.md, implementer-rules.md, inline-replanning.md, mid-phase-handoff.md, planning.md, plan-slim-rendering.md, research.md, review-agent-selection.md, review-iteration.md, review-mode.md, risk-tagging.md, self-improvement-reflection.md, step-implementation.md, step-implementation-recovery.md, structural-review.md, track-code-review.md, track-review.md, track-skip.md, workflow-drift-check.md, workflow.md.

**`.claude/workflow/prompts/` (11 files):**
adversarial-review.md, consistency-gate-verification.md, consistency-review.md, create-final-design.md, design-review.md, dimensional-review-gate-check.md, review-gate-verification.md, risk-review.md, structural-gate-verification.md, structural-review.md, technical-review.md.

**Workflow-referencing skills (7 files):**
- `.claude/skills/create-plan/SKILL.md` (17 workflow-doc refs)
- `.claude/skills/execute-tracks/SKILL.md` (17 refs)
- `.claude/skills/edit-design/SKILL.md` (14 refs)
- `.claude/skills/migrate-workflow/SKILL.md` (18 refs)
- `.claude/skills/review-workflow-pr/SKILL.md` (8 refs)
- `.claude/skills/review-plan/SKILL.md` (7 refs)
- `.claude/skills/code-review/SKILL.md` (3 refs; borderline)

All 49 files route through §1.7 staging because they're under `.claude/workflow/**` or `.claude/skills/**`.

Section counts vary widely. `conventions.md` has ~10 H2 sections plus many H3s; smaller files like `track-skip.md` may have 2–3 H2s. Every `##` and every `###` heading carries an annotation per the locked density rule (no author-judged granularity; the bootstrap-block heading is the sole literal-heading exception). Annotation count estimate (~600) is the upper bound from per-file heading inventories.

### Files in scope

All 49 enumerated above. Staged copies under `_workflow/staged-workflow/.claude/workflow/**` and `_workflow/staged-workflow/.claude/skills/**`.

### Files out of scope

- `.claude/agents/**` — refs-only sweep, no per-section annotations. Track 5 territory.
- `CLAUDE.md` — general-purpose project guide, not workflow-specific. Out of scope for this plan; see Non-Goals.
- `.claude/scripts/**` — the scripts themselves are not in-scope for annotations.

## Plan of Work

Phase A revised the original six-batch plan after the track reviews ran `workflow-reindex.py` against the staged files and found that its parser treats `##`/`###` headings and `<!--Document index…-->` delimiters **inside fenced code blocks** as real (rules 2/3/4 never consult `compute_fenced_lines`; only rules 6/8 do). About 156 fenced headings span 20 of the 49 in-scope files — including `conventions.md` (annotated first) and 5 skill files — so a green `--check` is unreachable by authoring alone. The fence-exclusion fix in the reindex script, and the §1.8 schema clarifications it rests on, become Step 1 rather than a final cleanup. This reopens Track 2's script and Track 1's schema (the prior "No code paths change" framing no longer holds; see `## Interfaces and Dependencies`).

**Corrected per-file procedure.** `--write` rebuilds an existing TOC region; it does not create one. For each file: (1) hand-place the `<!--Document index start-->` / `<!--Document index end-->` delimiter pair at the file's TOC anchor — directly under H1 for files that have one (workflow docs and `design-review.md`), immediately after the frontmatter block for the 5 H1-less skill files, and at the top of the file for the 10 prose-first prompts with neither H1 nor frontmatter — the three anchor shapes Step 1 added to §1.8(d) and rule_2; (2) hand-write each `##`/`###` section's annotation comment; (3) run `workflow-reindex.py --write` to populate the TOC table body and auto-stamp in-file `§X.Y(z)` refs (rule 8); (4) run `workflow-reindex.py --check --files <batch>` for incremental signal. Files with no real (non-fenced) `##` headings carry no TOC region per §1.8(d) — `create-plan` and `execute-tracks` SKILL.md after fence exclusion.

**The `--no-verify` window.** The reindex pre-commit hook stays RED across the un-migrated tree until the rollout completes, so intermediate step commits use `git commit --no-verify`. Because `--no-verify` bypasses every pre-commit gate (not only reindex), each step runs `--check --files <batch>` manually before committing to recover per-batch signal; Step 8 is the single full-set green `--check`.

**Cross-ref ordering.** Rule 6 (cross-file subset) and rule 8 (in-file auto-stamp) both need the target's annotation to exist, so refs from an early batch to a not-yet-annotated target fail mid-rollout and clear only at Step 8's green run. The batches front-load the most-referenced targets (`conventions.md`, `workflow.md`) to keep the dangling-ref window small.

**Link conversion (D13, D14, D15).** rule_6 flags every non-backticked, non-fenced bare `name.md` token — Markdown link targets, link text, and bare prose mentions — so a green `--check` is unreachable while workflow docs carry plain links (Step 2 discovery; see Decision Log and `implementation-plan.md` D13). Each annotation step (5, 6, 7) folds in a link-conversion pass over its own batch: a Markdown link to an in-scope `.md` target becomes the bare `name.md:roles:phases` form (sub-section links keep the anchor, `conventions.md§1.6:roles:phases`); a reference to a non-annotatable target (`CLAUDE.md`, `MEMORY.md`, `design.md`, `design-mechanics.md`, the Phase 4 final artifacts, `output-styles/house-style.md`, `.claude/agents/*.md` cited as a target) is wrapped in an inline-backtick span, which §1.8(e)'s backtick exclusion exempts. The author derives a converted ref's `roles:phases` from the target's annotation; when the target is not yet annotated, the suffix is the author's best read of the target's eventual annotation and stays rule_6-RED until the target lands (same dangling-ref window as above). Step 8 runs the deferred conversion over Step 2's 9 batch-1 files once every target is annotated, then the full-set green `--check`. The conversion depends on two staged-aware resolution fixes: Step 3's staged-first `build_file_lookup` (D14), without which rule 6 resolves a converted ref's target to the un-annotated live copy; and Step 4's bare-basename collision fix (D15), without which a bare `structural-review.md` ref resolves to the prompt namesake rather than the workflow-root doc. The backtick-wrap itself needs no script change — it rides §1.8(e)'s existing inline-backtick exclusion.

Step sequencing (full roster in `## Concrete Steps`):
- Step 1 (HIGH) is the parser + schema prerequisite and must land before any annotation step.
- Step 2 annotated workflow-root batch 1 (9 files); its link-conversion is deferred to Step 8 (Step 5 onward folds conversion in).
- Step 3 (HIGH, D14) makes `build_file_lookup` staged-aware so a converted cross-file ref validates against the staged-annotated target, not the un-annotated live copy. It must land before any link-conversion step (5-8), or rule 6 stays subset-RED on every converted ref through Phase 4.
- Step 4 (HIGH, D15) makes `build_file_lookup` give the bare-basename key to the workflow-root file when a `.claude/workflow/prompts/` namesake exists, so a bare `structural-review.md` ref resolves to the workflow-root doc rather than the prompt. It must land before any link-conversion step (5-8), or the converted cross-file refs that mean the workflow-root `structural-review.md` (`planning.md` ×2, `review-iteration.md`, `conventions.md`, `implementation-review.md`) resolve to the wrong file.
- Step 5 annotates workflow-root batch 2 (the remaining 22 files); Steps 2 and 5 split the 31 workflow-root files by heading count, not file count (`conventions.md` alone carries ~54 headings), so each batch is a comparable hand-authoring and review unit. Step 5 folds link-conversion in.
- Step 6 covers the 11 prompts, including the already-staged `create-final-design.md`, with link conversion folded in.
- Step 7 covers the 7 skill files (after-frontmatter TOC for the 5 H1-less ones; no TOC for the 2 with no real headings), with link conversion folded in.
- Step 8 runs the deferred link-conversion over Step 2's 9 batch-1 files, then the full-set `--check` green run plus the house-style sweep on every `summary="..."` and the prose around converted refs.

Each annotated section needs: `roles=` (which agent types load it), `phases=` (which workflow phases pull it), `summary="..."` (≤120 chars, house-style compliant).

## Concrete Steps
1. Fence-exclusion in the reindex parser + §1.8 schema correctness: thread `compute_fenced_lines` into `parse_headings`, `parse_toc_region`, and the rule_2 delimiter count so rules 2/3/4 skip headings and TOC delimiters inside fenced blocks; add §1.8(e) prose stating fenced headings and TOC delimiters are excluded (today only refs are); add the §1.8(d) after-frontmatter TOC-anchor rule for H1-less files plus the matching rule_2 anchor logic; regression tests under `.claude/scripts/tests/`. Reopens `workflow-reindex.py` (live path) and staged `conventions.md` §1.8. — risk: high (architecture: reopens the reindex validation gate + the §1.8 schema that all of Track 4 and every future workflow-modifying branch depend on; a parser bug silently passes wrong annotations through CI)  [x] commit: 39d517c5c8
2. Annotate workflow-root batch 1, heading-count-balanced and anchored by `conventions.md` + the next most-referenced/largest root files. Hand-place TOC delimiters + per-section annotations → `--write` (fills TOC, auto-stamps in-file refs) → `--check --files <batch>` → commit `--no-verify`. — risk: medium (annotation semantic accuracy: roles/phases assignment and summary house-style are judgment calls `--check` cannot fully validate; Phase C focal point)  [x] commit: 868f1f3d97 (batch 1 = 9 files / 208 headings; rule_6 ADJUST surfaced — see Decision Log)
3. Make `workflow-reindex.py`'s `build_file_lookup` (and rule 6's cross-file-ref target resolution) staged-aware: when a cross-file ref target basename resolves to both a live and a staged in-scope file, prefer the staged copy keyed by its relative `.claude/...` path, reusing the §1.8 enum bootstrap probe's multi-staged exit-2 ambiguity guard; add regression tests under `.claude/scripts/tests/` covering a converted ref that subset-passes against a staged-annotated target while the live copy is un-annotated, plus the live-only (no staged copy) and multi-staged-ambiguity cases. Reopens `workflow-reindex.py` (live path) a third time (D14). — risk: high (architecture: reopens the reindex validation gate's cross-file-ref resolution that every converted ref in Steps 4-7 and Track 5's agent-ref sweep depend on; a wrong precedence silently validates refs against the wrong copy)  [x] commit: 83a555854f (dim review PASS iter-2; WC1 blocker fixed)
4. Make `workflow-reindex.py`'s `build_file_lookup` resolve a bare-basename cross-file ref to the workflow-root file when a `.claude/workflow/prompts/` namesake shares the basename: a prompts file claims the bare-basename key only if no workflow-root file shares it (keeping the bare-key fallback for prompts-only basenames so existing bare prompt refs still resolve), while a prompt keeps its `prompts/<name>` key unconditionally. Add regression tests under `.claude/scripts/tests/` covering the `structural-review.md` collision (bare → workflow-root, `prompts/` → prompt), a prompts-only basename still resolving bare, and a non-colliding basename unchanged. Reopens `workflow-reindex.py` (live path) a fourth time (D15). — risk: high (architecture: reopens the reindex validation gate's cross-file-ref resolution that every converted ref to the workflow-root `structural-review.md` in Steps 5-8 and Track 5's agent-ref sweep depend on; a wrong precedence silently validates refs against the wrong file)  [x] commit: c54ff41cbe (dim review PASS iter-1; one suggestion WH1 applied + gate-verified iter-2)
5. Annotate workflow-root batch 2 (the remaining 22 root files) AND convert this batch's Markdown links to in-scope `.md` targets into bare `name.md:roles:phases` cross-file refs, backtick-wrapping references to non-annotatable targets (D13). Same per-file procedure (hand-place TOC delimiters → per-section annotations → `--write` → `--check --files`), with the link-conversion folded in before `--write`. Depends on Step 3's staged-aware lookup and Step 4's collision-resolution fix so converted refs validate against the right staged targets. — risk: medium (annotation semantic accuracy + cross-file suffix/backtick-wrap judgment; Phase C focal point)  [x] commit: 6de25eba70
6. Annotate the 11 prompts under `.claude/workflow/prompts/` (including staged `create-final-design.md`, whose fenced `adr.md`-template headings Step 1's fence-exclusion skips) AND convert their links per D13. Same procedure. — risk: medium (annotation semantic accuracy + cross-file conversion; Phase C focal point)  [x] commit: e8a0f7aca9
7. Annotate the 7 workflow-referencing skill files (after-frontmatter TOC for the 5 H1-less ones — `edit-design`, `migrate-workflow`, `code-review`, `review-workflow-pr`, `review-plan`; no TOC region for the 2 with no real headings — `create-plan`, `execute-tracks`) AND convert their links per D13. Same procedure. — risk: medium (annotation semantic accuracy + after-frontmatter TOC anchor + cross-file conversion; Phase C focal point)  [ ]
8. Link-conversion pass over Step 2's 9 already-annotated batch-1 files (now that their cross-file targets are annotated), then the full-set `workflow-reindex.py --check` green run across all 49 files plus a house-style sweep on every `summary="..."` and on the prose around converted refs; fix residual enum/TOC/cross-ref findings; confirm a final clean `--check`. — risk: medium (cross-cutting verification gate + the deferred batch-1 conversion + house-style judgment; Phase C focal point)  [ ]

## Episodes
<!-- Continuous-log. Phase B sub-step 7 appends one block per
completed step, identified by step number + commit SHA. Empty at
Phase 1; Phase A does not populate. -->

### Step 1 — commit 39d517c5c8, 2026-05-29T04:50Z [ctx=info]
**What was done:** Threaded `compute_fenced_lines` into `parse_headings`, `parse_toc_region`, the rule_2 delimiter count, and `find_first_h1_line`, so rules 2/3/4 and `--write` skip `##`/`###` headings and `<!--Document index ...-->` delimiters inside fenced code blocks. Added the §1.8(d) after-frontmatter TOC anchor for H1-less skill files plus the matching rule_2 anchor logic (`find_first_h1_line` + `find_frontmatter_close_line`). Added §1.8(e) fenced-content-exclusion prose to staged `conventions.md`. The dimensional review (5 workflow-review agents, PASS at iteration 2) drove a `Review fix:` commit `39d517c5c8` that closed the WC1/WI1 blocker and four lower findings. Final state: 94/94 reindex tests pass.

**What was discovered:** The 11 prompt files split three ways by TOC anchor, not the two the step anticipated. Only `design-review.md` carries a real H1; the other 10 are prose-first with no H1 and no YAML frontmatter (including `create-final-design.md`, whose only `# ` is the fenced `adr.md`-template heading the new fence-aware `find_first_h1_line` correctly skips). rule_2's third branch had silently accepted any TOC placement for that shape, so `--check` could not have guarded Step 4's placement of those TOCs. The fix added a third anchor shape — top-of-file — to both §1.8(d) and rule_2, with the under-H1 and top-of-file branches sharing one gap-scan code path (`gap_start_idx = 0` vs the H1 line) so bootstrap-block tolerance and fence skipping apply uniformly. Separately, `design.md` still describes the TOC as anchored "directly under the H1" at three sites (lines 20, 75, 285), which now drifts from the implemented three-shape schema — a Phase 4 design-synthesis reconciliation item.

**What changed from the plan:** Step 1's scope grew by one anchor shape. The step description named only the H1-less-with-frontmatter case (the 5 skill files); the prose-first-no-frontmatter case surfaced during dimensional review and was folded into the same step. This sharpens the anchor placement Steps 4 and 5 must follow: TOC under the H1 for workflow docs and `design-review.md`, after the frontmatter close for the 5 H1-less skill files, and at the top of the file for the other 10 prompts.

**Key files:**
- `.claude/scripts/workflow-reindex.py` (modified)
- `.claude/scripts/tests/test_workflow_reindex.py` (modified)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/conventions.md` (modified, §1.8(d)/(e))

**Critical context:** rule_2 now enforces TOC placement positionally (previously it did no positional check for the third shape), so every annotation step must place each file's TOC at its correct anchor or rule_2 fires. Initial commit `8bcb1792f3`; review-fix commit `39d517c5c8`.

### Step 2 — commit 868f1f3d97, 2026-05-29T05:08Z [ctx=warning]
**What was done:** Annotated workflow-root batch 1 — 9 files, 208 of the 406 non-fenced root headings (51%), front-loading the two most-referenced targets. Batch 1: `conventions.md` (45), `workflow.md` (17), `implementation-review.md` (25), `review-mode.md` (24), `design-document-rules.md` (23), `track-code-review.md` (20), `step-implementation-recovery.md` (18), `self-improvement-reflection.md` (18), `risk-tagging.md` (18). Each file got the under-H1 TOC delimiter pair, one annotation comment after every non-fenced H2/H3 heading (roles/phases from the locked enums, house-style `summary=` ≤120 chars), then `--write` to fill the TOC tables and auto-stamp in-file refs, then `--check`. Batch annotations are well-formed: rules 2/3/4/5/8 pass clean on all 9 files.

**What was discovered:** Two findings, the first a hard blocker on the track's final acceptance criterion.
1. **rule_6 makes the Step 6 green-`--check` criterion unreachable as written.** rule_6's "missing suffix" half flags every non-backticked, non-fenced `name.md` token as a cross-file ref needing a `:roles:phases` suffix — but this catches **Markdown link targets** (`[`x.md`](x.md)` → the `(x.md)` target), link text, and bare prose mentions (`MEMORY.md`). A link target cannot carry a suffix without breaking the link. Workflow docs are saturated with such links, so annotating targets (the plan's "rule 6 RED clears at Step 6" model) cannot clear them. Verified by sampling: of 30 rule_6 findings on staged `workflow.md`, the cited lines are link targets (`L47 [`implementer-rules.md`](implementer-rules.md)`, `L165`, `L601`) and bare prose (`L407 MEMORY.md`). The design scopes the mandatory cross-file suffix to agent files + SKILL.md startup read-lists only (D6, D8, Track 5, Non-Goals) — never to workflow-doc prose/links — so rule_6 is over-broad relative to the design. This reopens Track 2's script and is the ADJUST recorded in the Decision Log.
2. **`--write` halts (exit 2) on bare `§X.Y` after a backticked filename in prose** (e.g. `` `conventions-execution.md` §2.1 ``): the bare `§X.Y` parses as an unresolvable in-file ref. Fix: wrap the section token in one inline-backtick span (§1.8(e) excludes backtick spans). The implementer fixed 11 such sites this batch; later batches will hit the same shape. Separately, a bare `name.md` inside a `summary="..."` trips rule_6 — never put a `.md` token in a summary.

**What changed from the plan:** The plan's Idempotence model — "Cross-file (rule 6) … refs to not-yet-annotated targets fail until Step 6's full-set run; normal interim state" — is wrong for link targets / prose mentions, which never carry a suffix. The remaining steps need an ADJUST: a rule_6-scope-fix step (reopening Track 2's script) before Step 6 can go green. Affects Steps 3, 4, 5 (all inherit rule_6 link-target noise) and Step 6 (acceptance criterion). Step 3 takes the complementary 22 root files: branch-divergence-check, code-review-protocol, commit-conventions, conventions-execution, defensive-push-check, design-decision-escalation, ephemeral-identifier-rule, episode-format-reference, finding-synthesis-recipe, implementer-rules, inline-replanning, mid-phase-handoff, planning, plan-slim-rendering, research, review-agent-selection, review-iteration, structural-review, track-review, track-skip, workflow-drift-check, step-implementation.

**Key files:**
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/conventions.md` (modified)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/workflow.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/implementation-review.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/review-mode.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/design-document-rules.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/track-code-review.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/step-implementation-recovery.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/self-improvement-reflection.md` (new staged)
- `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/risk-tagging.md` (new staged)

**Critical context:** A rule_1 finding fires on each staged copy (path under `docs/adr/` but no §1.6 line-1 stamp — structural, not an annotation defect) and persists through the rollout; reconcile at Step 6 / Phase C. The reusable per-file annotation procedure is established for the remaining batches.

### Step 3 — commit 83a555854f, 2026-05-29T09:25Z [ctx=info]
**What was done:** Made `workflow-reindex.py`'s `build_file_lookup` staged-aware so a converted cross-file ref validates against the branch's authored (staged) annotation rather than the un-annotated live copy (D14, I7). The lookup now derives keys from each file's logical `.claude/...` path (staged prefix stripped via a new `_STAGED_SUBTREE_PREFIX_RE`) and prefers the staged copy when both a live and a staged copy resolve to one key, extending the `conventions.md` §1.7(d) reads-precedence already used by the §1.8 enum bootstrap probe. A second staged copy of the same logical target halts with `AmbiguousBootstrapProbeError` (CLI exit 2), reusing the §1.8 probe's existing guard with no new CLI plumbing. The step-level dimensional review (consistency, hook-safety, context-budget; PASS at iteration 2) drove a `Review fix:` commit that corrected the ambiguity guard to key on the logical path, reworded an inaccurate docstring and comment, and added a `--check` CLI exit-2 test. Final state: 103/103 reindex tests pass.

**What was discovered:** The initial guard keyed on the bare basename, so two distinct staged logical targets sharing a basename would have falsely tripped the exit-2 ambiguity halt. Two such collisions exist in scope: the seven `SKILL.md` skill anchors, and `.claude/workflow/structural-review.md` against `.claude/workflow/prompts/structural-review.md`. The bug was latent here (no such pair is staged yet) but deterministic once Step 6 stages multiple `SKILL.md` files and Steps 4-5 stage both `structural-review.md` copies, at which point Step 7's full-set green `--check` and Track 5's agent-ref sweep would both break. The fix keys the guard on the logical `.claude/...` path, so it fires only on a genuine one-target-two-plan-dirs duplicate and distinct basenames resolve first-match-wins, matching the live side. Two related facts surfaced. First, the pre-fix code never produced the `prompts/<name>.md` lookup key for a staged prompt, because it tested the physical path, which for a staged file begins with `docs/adr/...`; Steps 5-6 would have hit that gap. Second, `build_file_lookup` has exactly one call site (`validate`, the `--check` path), not the two the initial handoff claimed: `compute_write_plan` does no cross-file resolution per D9. Both are now closed or corrected and pinned by tests.

**What changed from the plan:** none. Implemented as D14, I7, and the revised Step 3 prescribe. The plan's cited line numbers had drifted as the script grew through earlier steps; the current locations were verified against the live script before editing.

**Key files:**
- `.claude/scripts/workflow-reindex.py` (modified, live path per the plan Constraints)
- `.claude/scripts/tests/test_workflow_reindex.py` (modified, nine new regression tests)

**Critical context:** Forward-safety holds. On `develop` or any branch with no staged subtree the lookup is pure-live and behaviour is unchanged, pinned by the live-only regression test. The multi-staged exit-2 reuses the existing `AmbiguousBootstrapProbeError` (converted to exit 2 by `main`'s handlers at the single `validate` call site), so the guard needed no CLI change. Initial commit `4e0236e87b`; review-fix commit `83a555854f`.

### Step 4 — commit c54ff41cbe, 2026-05-29T13:03Z [ctx=info]
**What was done:** Implemented the §1.8(e) bare-basename collision fix in the live `workflow-reindex.py` `build_file_lookup` (D15). The lookup now gives the bare-basename key to a `.claude/workflow/` root doc whenever a `.claude/workflow/prompts/` namesake shares the basename, independent of parse order, closing the bug where the glob (which sorts the `prompts/` path first) handed the bare `structural-review.md` key to the prompt and left the workflow-root doc unreachable. A prompt claims the bare key only when no workflow-root namesake exists, so prompts-only basenames keep their bare key; the `prompts/<name>` key and the seven `SKILL.md` collisions stay first-match-wins. Added two helper predicates (`_is_workflow_root_doc`, `_is_workflow_prompt`) and an `is_bare_key`-scoped override branch threaded through `_record`. Six regression tests were added across the two commits: the collision in both record orders, through real discovery order, the prompts-only fallback, a non-colliding basename, the staged-vs-staged case in both orders, and the compositional override-then-staged-upgrade lock-in. Step-level dimensional review (consistency / hook-safety / context-budget; workflow-only diff, baseline group skipped) returned PASS at iteration 1 with one suggestion (WH1); WH1 was applied and gate-verified at iteration 2. 109/109 reindex tests pass.

**What was discovered:** The pre-existing collision test asserted the correct workflow-root result but for the wrong reason: it recorded the root copy before the prompt, so first-match-wins happened to land the right answer and masked the live bug, since real discovery sorts the prompt first. The new real-discovery-order test drives the actual discover/parse path, so the glob's prompt-first order is the order under test rather than a hand-ordered fixture. Both `structural-review.md` copies are live today (neither staged yet), so the collision is live-vs-live and the override resolves it regardless of order. The hook-safety review surfaced one untested compositional path, where a live root wins the bare key via the override and then a staged copy of the same root upgrades it through the Step 3 staged-precedence branch; it is now pinned by the WH1 lock-in test.

**What changed from the plan:** none. Implemented as the Step 4 acceptance criteria, DR D15, and invariant I7 prescribe; the four enumerated acceptance cases are covered, plus order-independence and the staged-vs-staged case as load-bearing extras.

**Key files:**
- `.claude/scripts/workflow-reindex.py` (modified, live path per the plan Constraints)
- `.claude/scripts/tests/test_workflow_reindex.py` (modified, six new regression tests)

**Critical context:** Forward-safe: where the globs yield one file per key (every non-colliding basename, and the whole live tree on `develop`) the override is a no-op, pinned by the non-colliding test. The override is scoped to the bare key via the new `is_bare_key` parameter; the `prompts/<name>` key path and the Step 3 logical-path multi-staged exit-2 ambiguity guard are untouched. With this fix landed, a bare `structural-review.md` ref resolves to the workflow-root doc, so both Track 4's converted refs (Steps 5-8) and Track 5's agent-ref sweep to that doc now resolve correctly. Initial commit `1da5894545`; review-fix commit `c54ff41cbe`.

### Step 5 — commit 6de25eba70, 2026-05-29T14:02Z [ctx=info]
**What was done:** Annotated the 22 remaining `.claude/workflow/` root files (batch 2): hand-placed the under-H1 TOC delimiter pair, wrote a `roles=`/`phases=`/`summary=` comment after every non-fenced `##`/`###` heading (202 annotations, all from the locked 15-role / 8-phase enums, summaries ≤120 chars), and converted each file's Markdown links to in-scope `.md` targets into bare `name.md:roles:phases` cross-file refs, backtick-wrapping references to non-annotatable targets (`design.md`, `CLAUDE.md`, `MEMORY.md`, `house-style.md`, `SKILL.md` paths, `_workflow/` artifact paths). Ran `workflow-reindex.py --write --files <batch>` to fill the TOC tables and auto-stamp in-file refs, then `--check --files` for per-batch signal. All writes routed to the staged subtree per §1.7; committed with `git commit --no-verify` (one commit, 22 files). The orchestrator re-verified the scoped `--check`: rules 2/3/4/5/8 are green on all 22 files; the only findings are 22 rule_1 (missing line-1 stamp, one per staged copy) and 30 rule_6 (all `target []`, dangling refs to not-yet-annotated Step 6 prompt targets) — both the expected interim windows.

**What was discovered:** The §1.8(e) cross-file-with-subsection ref form `name.md§X.Y:roles:phases` does not validate in live (non-fenced, non-backticked) prose. The reindex in-file-ref scanner matches the `§X.Y:roles:phases` tail independently of the `name.md` prefix, so `--write` halts atomically with a rule_8 "unresolved §X.Y" resolved against the citing file's own headings. Every §1.8(e) example of this form sits inside a fenced or inline-backtick span, so live prose had never exercised it. The batch used the whole-file form `name.md:roles:phases` (§1.8(e) makes sub-section pinning optional) and carried the section pointer as a separate backticked `§X.Y` token, which the scanner skips. Two recurring authoring traps confirmed at scale: a `.md` token inside a `summary="..."` trips rule_6 (summaries carry no `.md` tokens), and the per-line inline-backtick detector breaks on a backtick span wrapped across two source lines (reflow onto one line).

**What changed from the plan:** none. The per-file procedure ran as planned. The whole-file-ref-form choice is a tactical authoring decision inside §1.8(e)'s optional sub-section pinning, not a plan deviation, and needs no script change; Steps 6 and 8 inherit the same whole-file-ref plus backticked-section-token pattern for any sub-section pointer.

**Key files:** 22 new staged files under `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/`: branch-divergence-check, code-review-protocol, commit-conventions, conventions-execution, defensive-push-check, design-decision-escalation, ephemeral-identifier-rule, episode-format-reference, finding-synthesis-recipe, implementer-rules, inline-replanning, mid-phase-handoff, plan-slim-rendering, planning, research, review-agent-selection, review-iteration, structural-review, track-review, track-skip, workflow-drift-check, step-implementation. No live-path or script change (annotation authoring only).

**Critical context:** A global `--write` still halts until Step 6 backticks the pre-existing bare `§1.7(b/e/f)` tokens in the staged `prompts/create-final-design.md`; until then every `--write` needs `--files` scoping. The 30 rule_6 RED are converted refs to Step 6 prompt targets (`technical-review.md`, `risk-review.md`, `adversarial-review.md`, the prompts `structural-review.md`, the gate-verification prompts, `dimensional-review-gate-check.md`, `create-final-design.md`); their suffixes are the implementer's best read of each prompt's eventual annotation and clear when Step 6 annotates those targets (Step 6 should make each prompt's union ⊇ the citer slice). rule_1 RED on every staged copy reconciles at Phase 4. Initial commit `6de25eba70` (no separate review-fix commit — medium-risk step, no dimensional review).

### Step 6 — commit e8a0f7aca9, 2026-05-29T14:30Z [ctx=info]
**What was done:** Annotated all 11 prompts under `.claude/workflow/prompts/` (staged): a TOC region plus a `roles=`/`phases=`/`summary=` comment after every non-fenced `##`/`###` heading, with their cross-file refs converted per D13. `design-review.md` anchors its TOC under its H1; the other 10 are prose-first and anchor top-of-file. `create-final-design.md` has no real (non-fenced) `##` heading, so it carries no TOC region per §1.8(d); its three `###` sections are annotated directly. Each prompt's annotation union covers the batch-2 citer slice that already references it (all nine verified), so Step 5's 30 dangling cross-file refs from the workflow-root batch now resolve. Sub-section pointers use the whole-file ref form plus a separate backticked `§X.Y` token (the Step 5 workaround). The pre-existing bare `§1.7`/`§2.3`/`§1.2` in-file tokens that halt a global `--write` were backtick-wrapped in `create-final-design.md`, `consistency-review.md`, and `structural-review.md`. Ran `--write --files` then `--check --files`; committed `--no-verify` and pushed.

**What was discovered:** The five Finding-template headings (`### Finding T/R/A/S/CR<N> [blocker|should-fix|suggestion]`) carry literal pipe characters in their severity placeholders. The reindex TOC writer does not escape `|` in a section cell, so the TOC table row splits and rule_3 fires. The fix wraps each Finding template in a fenced code block (info string `markdown`), removing the pipe-bearing heading from the heading set — the same treatment the sibling Premise / Edge-case / Integration templates already get in these files and the identical template gets in `review-iteration.md`. The orchestrator's scoped re-check (the 22 batch-2 files plus the 11 prompts) confirms rules 2/3/4/5/8 green and all 30 batch-2 dangling refs cleared. Two annotation-union notes: `review-gate-verification.md`'s single section took the role union `orchestrator,reviewer-technical,reviewer-risk,reviewer-adversarial` (phase 3A) to cover the `orchestrator:3A` citer; `mid-phase-handoff.md`'s union has no `final-designer` role, so `create-final-design.md`'s converted ref to it uses `mid-phase-handoff.md:orchestrator:4` (subset-valid).

**What changed from the plan:** none. The whole-file-ref form (the Step 5 workaround) and the Finding-template fencing are author-side fixes inside the medium-risk step's scope; no script change.

**Key files:** 11 prompts under `docs/adr/ytdb-1023-workflow-toc/_workflow/staged-workflow/.claude/workflow/prompts/`: adversarial-review, consistency-gate-verification, consistency-review, create-final-design (modified — already staged from Track 3), design-review, dimensional-review-gate-check, review-gate-verification, risk-review, structural-gate-verification, structural-review, technical-review.

**Critical context:** One expected dangling rule_6 remains on this batch: `create-final-design.md` cites `edit-design/SKILL.md:final-designer:4`, and `edit-design/SKILL.md` is a Step 7 target (clears when Step 7 stages and annotates it with a union ⊇ `final-designer:4`). Batch-1 `implementation-review.md` still carries bare `prompts/*.md` Markdown links; converting them is Step 8's deferred batch-1 pass, now unblocked since the prompt targets are annotated. rule_1 RED on every staged copy reconciles at Phase 4. Initial commit `e8a0f7aca9` (no separate review-fix commit — medium-risk step, no dimensional review).

## Validation and Acceptance

After this track lands:

- Every in-scope file with at least one real (non-fenced) `^##` heading carries exactly one TOC region — directly under H1 for files that have one, immediately after the frontmatter block for the H1-less skill files, and at the top of the file for the prose-first prompts with neither H1 nor frontmatter. Files with no real `^##` headings carry no TOC region (§1.8(d)).
- Every real (non-fenced) `^##`/`^###` heading in every in-scope file is followed by an annotation comment; headings inside fenced code blocks are not annotated and not counted.
- `python3 .claude/scripts/workflow-reindex.py --check` exits 0 across the full file set, with no false positives on fenced headings/delimiters (the `## 99.1 Demo section` worked example and the `create-final-design.md` `adr.md`-template headings are skipped, not annotated).
- No Markdown link to an in-scope `.md` target remains in any workflow doc or prompt: every such link is converted to a bare `name.md:roles:phases` cross-file ref, and every reference to a non-annotatable target is backtick-wrapped (D13). rule_6 is green across the full set, with no missing-suffix or failed-subset findings.
- Step 1's regression tests pass: a fenced `## Heading` (both ``` and `~~~`) yields no rule_2/3/4 finding and no `--write` TOC row, a real heading still does, and an H1-less file with an after-frontmatter TOC validates.
- Step 3's regression tests pass: a converted cross-file ref subset-passes against a staged-annotated target while the live copy is un-annotated; a target with no staged copy resolves to live; multiple staged copies of one target halt with exit 2.
- Step 4's regression tests pass: a bare `structural-review.md` ref resolves to the workflow-root file (not the `prompts/` namesake); a prompts-only basename still resolves bare; a non-colliding basename is unchanged. The converted cross-file refs that mean the workflow-root `structural-review.md` (`planning.md` ×2, `review-iteration.md`, `conventions.md`, `implementation-review.md`) subset-pass against it.
- Annotation summary text passes house-style review (no banned vocabulary, ≤120 chars, plain prose).
- TOC tables match the per-section annotations 1:1 (rebuildable by `--write` with no diff).

<!-- Phase A placeholder for per-step EARS/Gherkin lines. -->

<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used
verbatim as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery

- **Step 1 must precede every annotation step.** Resuming into Step 2+ presupposes the fence-exclusion + H1-less-anchor fix has landed; running an annotation batch's `--check` against the un-patched script produces spurious rule_2/3/4 findings. A resume that finds Step 1 incomplete restarts from Step 1.
- **Step 3 must precede every link-conversion step (5-8) (D14).** Resuming into a link-conversion batch presupposes the staged-aware `build_file_lookup` fix has landed; without it, rule 6 resolves every converted ref's target to the un-annotated live copy and the subset check fails permanently — not the clearing dangling-ref window the rollout assumes. A resume that finds Step 3 incomplete restarts from Step 3 before any conversion.
- **Step 4 must precede every link-conversion step (5-8) (D15).** Resuming into a link-conversion batch presupposes the bare-basename collision fix has landed; without it, a bare `structural-review.md` ref resolves to the `prompts/` namesake, so the converted cross-file refs that mean the workflow-root doc validate against the wrong file (or fail subset). A resume that finds Step 4 incomplete restarts from Step 4 before any conversion. Steps 3 and 4 are independent script fixes; either order is valid, but both must land before Step 5.
- **Each step is one commit and independently revertable.** `git revert <step-commit>` rolls a batch back without disturbing earlier batches; re-running the batch reproduces the same staged content.
- **`--write` is idempotent.** Re-running it on an already-annotated file rebuilds the same TOC table and re-stamps the same in-file refs (no diff on a clean file), so resuming a partially-done batch is safe.
- **Staging isolation bounds partial-completion damage.** Annotation edits land under `_workflow/staged-workflow/`; the live `.claude/workflow/**` paths stay at develop state until Phase 4 promotion, so a half-annotated staged tree cannot break the live workflow machinery the branch itself runs on. The live-path changes are confined to `workflow-reindex.py` (Step 1's fence-exclusion + H1-less anchor, Step 3's staged-aware lookup, Step 4's bare-basename collision fix), each guarded by its regression tests.
- **Link conversion is re-runnable.** Converting a Markdown link to the bare suffixed form is a no-op on a ref already converted (it is no longer a link); backtick-wrapping a non-annotatable target is a no-op once wrapped. A re-run of a partially-converted batch reproduces the same staged content, so resuming a batch is safe.
- **Mid-rollout `--check` is expected RED; rule_6 RED has two sources (D13, D14, D15).** (1) A converted cross-file ref to a not-yet-annotated target fails subset validation until the target is annotated — this is the *clearing* window, which holds only once Step 3's staged-aware lookup (D14) and Step 4's bare-basename collision fix (D15) have landed; before Step 3 a converted ref resolves to the un-annotated live copy and never clears, and before Step 4 a bare `structural-review.md` ref resolves to the prompt namesake rather than the workflow-root doc. (2) An un-converted Markdown link or bare `name.md` mention in a batch not yet processed is flagged as a missing-suffix ref. Both clear progressively as batches land; the full-set green is reached only at Step 8, after Step 2's batch-1 files are converted and every target is annotated. rule_1 also stays RED on every staged copy (path under `docs/adr/` with no §1.6 line-1 stamp — structural, expected). None of these is a recovery trigger; they are the normal interim state, which is why intermediate commits use `git commit --no-verify` with a manual `--check --files <batch>` for per-batch signal.

## Artifacts and Notes
<!-- Continuous-log (rare). Cross-step artifact references that don't
belong to one specific step. Per-step episode content lives in
`## Episodes` above. Often empty. -->

## Interfaces and Dependencies

### In-scope file set

All 49 files enumerated above. Staged copies under `_workflow/staged-workflow/`.

### Out-of-scope

- `.claude/agents/**` — Track 5.
- `CLAUDE.md` — out of scope (general-purpose, not workflow-specific).
- `.claude/scripts/**` — not annotated.

### Inter-track dependencies

- **Depends on Track 1, and reopens it.** The schema in `conventions.md §1.8` must exist before authors can write annotations to it; Step 1 additionally amends §1.8(d) (after-frontmatter TOC anchor for H1-less files) and §1.8(e) (fenced headings/delimiters excluded). The inline replan (D13) further widens §1.8(e)'s cross-reference convention to all in-scope workflow docs/prompts and adds the backtick-wrap exemption for non-annotatable targets.
- **Depends on Track 2, and reopens it (three times).** The reindex script must exist for `--write` and `--check`; Step 1 fixes its fenced-heading/delimiter parsing (rules 2/3/4) and adds the H1-less anchor logic, Step 3 (D14) makes `build_file_lookup` / rule 6's cross-file-ref target resolution staged-aware, and Step 4 (D15) gives the bare-basename key to the workflow-root file when a `.claude/workflow/prompts/` namesake exists. Regression tests for all four behaviours are added to Track 2's suite.
- **Depends on Track 3** in execution order (not structurally). Track 3 lands first so `prompts/create-final-design.md` already carries the telemetry-invocation block when Track 4 annotates it.
- **Unblocks Track 5 (and its agent-ref validation).** Cross-reference suffixes in the 20 agent files point AT workflow docs whose annotated copies are staged during this branch; landing Track 4 first keeps the schema's surface coherent at every commit. Step 3's staged-aware lookup (D14) and Step 4's bare-basename collision fix (D15) are the load-bearing unblocks: without D14, Track 5's agent-ref subset checks resolve their workflow-doc targets to the un-annotated live copies; without D15, an agent ref to the workflow-root `structural-review.md` resolves to the `prompts/` namesake. Both fail exactly as Track 4's converted refs would.

### Library/function signatures touched

Step 1 edits `.claude/scripts/workflow-reindex.py` (a live-path change, not §1.7 staging): it threads `compute_fenced_lines` into `parse_headings`, `parse_toc_region`, and the rule_2 delimiter count so rules 2/3/4 skip fenced headings/delimiters, and it teaches rule_2 the after-frontmatter TOC anchor for H1-less files. This reopens Track 2's script and its test suite under `.claude/scripts/tests/` (regression tests for both behaviors are required per the track reviews). Step 1 also edits staged `conventions.md` §1.8(d)/(e) — Track 1's schema. Step 3 (D14) edits the same script again: `build_file_lookup` (and rule 6's target resolution) prefers a staged in-scope copy over its live namesake, reusing the §1.8 enum probe's multi-staged exit-2 guard, with regression tests. Step 4 (D15) edits it a fourth time: `build_file_lookup` gives the bare-basename key to the workflow-root file when a `.claude/workflow/prompts/` namesake shares the basename (keeping the bare-key fallback for prompts-only basenames), with regression tests. Steps 2 and 5-8 are per-section annotation authoring in Markdown only; no further code paths change.

## Base commit
5aa5917da7d8a57c78bcf2fe530c58686a07c3e0
