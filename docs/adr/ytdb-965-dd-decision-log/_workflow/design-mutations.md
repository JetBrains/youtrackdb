# Design Mutations — Phase 0/1 Decision Log + Design Philosophy

Append-only log of every `design.md` mutation: diff summary, mechanical-check result, cold-read verdict, iteration count. Read by `edit-design`'s `design-sync` step to find the last sync point.

## Mutation 1 — 2026-05-25 — structural-rewrite (design.md)

**Diff summary**: Split the design-philosophy material across two files. The `## Design philosophy subsection` H2 was renamed to `## Design philosophy` and restructured into two H3 subsections: `### Lean subsection in conventions.md` (seven principle names + one-sentence summaries + a pointer line to the detailed doc; no table, no failure modes, no citations) and `### Detailed doc at .claude/workflow/design-philosophy.md` (paragraph-length per-principle explanations, the seven-row workflow-mapping table as supplementary content, the six failure modes, the four external citations). The lean subsection is the orientation moment; the detailed doc is the deepening surface, loaded by reviewers grounding a rule or planners weighing approaches. Cross-references from the five workflow files still target `conventions.md § Design philosophy` (two-step). Propagated through Core Concepts (`Design philosophy subsection.` renamed to `Design philosophy.`), Overview (second bullet + structure-summary sentence), Class Design Mermaid diagram (added `design_philosophy_md` class with four attributes; added `conventions_md ..> design_philosophy_md : detailed-doc ref` edge; reworded `conventions_md`'s attribute from "Design philosophy subsection" to "Lean Design philosophy subsection (names + one-sentence summaries)"; expanded prose paragraph below the diagram to name the new artifact and the two-step targeting), the renamed Design philosophy section (TL;DR, two H3 bodies, Edge cases / Gotchas, References), and Cross-reference tier mapping (body sentence noting the two-step pointer; updated gotcha to mention lockstep with the detailed doc; expanded References list). Rationale (user): aligns with Principle 7 (Lean documents, load on demand) — the principle the subsection itself enforces; the workflow-mapping table is drift-prone, so it lives in the detailed doc only as supplementary information.

**Mechanical checks** (target=design): 0 blockers / 4 should-fix
**Cold-read** (scope: whole-doc): PASS / 3 suggestions

**Findings**:
- should-fix (mechanical, out of scope — pre-existing): line 39 `## Class Design` — one-line paragraph at line 41 shares 1/2 content words with heading (50% overlap; threshold 50%). Pre-existing; section not touched by this mutation.
- should-fix (mechanical, out of scope — pre-existing): line 228 `## Decision-log file shape` — one-line paragraph at line 230 shares 2/3 content words with heading (67% overlap). Pre-existing; section not touched by this mutation.
- should-fix (mechanical, out of scope — pre-existing): line 337 (inside §"Write triggers" → "Edge cases / Gotchas") — 2 em dashes form unbalanced cadence. Pre-existing; section not touched by this mutation.
- should-fix (mechanical, out of scope — pre-existing): line 369 `## Phase 1 design-iteration rationale` — one-line paragraph at line 371 shares 3/4 content words with heading (75% overlap). Pre-existing; section not touched by this mutation.
- suggestion (cold-read): `## Workflow` (lines 114-180) has no `### Edge cases / Gotchas` sub-section. Reviewer flagged as borderline — Workflow is structural content with two sequence diagrams; template-bound exemption likely applies. Pre-existing; section not touched by this mutation.
- suggestion (cold-read): `## Class Design` (lines 39-112) has no `### Edge cases / Gotchas` sub-section. Same borderline / template-bound consideration as above. Pre-existing; section not touched by this mutation.
- suggestion (cold-read): `### References` at line 226 cites `https://arxiv.org/abs/2603.01896` for Ugare & Chandra 2026. ArXiv prefix `2603` (March 2026 submission) is unusual relative to the cited 2026 publication year. URL was already in the pre-mutation file; reviewer flags for the author to double-check the identifier resolves before merge.

**In-scope findings cleared during iteration**:
- should-fix (mechanical, iteration 2): line 184 — TL;DR of `## Design philosophy` shared 2/2 content words ("design", "philosophy") with the H2 heading. Rewrote TL;DR to drop both words from the body ("Two artifacts share the load. A lean subsection sits in always-loaded `conventions.md`, ...").
- should-fix (mechanical, iteration 2): line 188 — paragraph under `### Lean subsection in conventions.md` shared 3/4 content words with the H3 heading. Rewrote paragraph to drop "Lean", "in", "conventions.md" from the body ("Seven principles, one sentence each, then a single pointer line ...").

**Iterations**: 2 of 3 (PASS — in-scope findings cleared; remaining should-fix and suggestions are pre-existing on sections this mutation did not touch and are out of scope per `design-document-rules.md § Mutation discipline (scope: new mutations only)`)

## Mutation 2 — 2026-05-25 — content-edit (design.md)

**Diff summary**: Applied three adversarial-review findings (A7, A9, A3) from the devil's-advocate pass on design.md. **A7** (Phase 4 ADR aggregation integration): added `prompts/create-final-design.md` to Overview "Restructured to fit" list; added a new Edge case in §"Decision-log file shape" naming the Phase 4 read contract (Decision Log → ADR key-decisions; Surprises → ADR narrative summary; Open Questions that landed in plan elements are not re-aggregated; without this read the durable-rationale benefit does not reach the final ADR). **A9** (lockstep mechanical enforcement): added a `grep -rn '<principle name>' .claude/ CLAUDE.md` recipe to §"Design philosophy" Edge cases, mirroring the YTDB-836 house-style section-names precedent at `conventions.md §1.5`. **A3** (Phase 0 handoff thinning was wrong): the handoff does NOT collapse to a one-line pointer; the research-shaped body is 5/6 in-flight content (What I was investigating, Already ruled out, Most promising lead, Raw notes, Resume notes) that the decision-log never receives by its own write-trigger criteria. Only `## Open questions` overlaps. Updated five sites: Overview "Restructured to fit" segment; Class Design diagram (`mid_phase_handoff_md` class attribute "Phase 0 handoff is thin pointer..." replaced with two attributes "Phase 0 handoff keeps in-flight body" + "Open questions section points at decision-log.md"); Workflow H3 "Phase 0 → Phase 1 transition" closing paragraph; Decision-log file shape Edge case (handoff threshold trigger); Phase 0 → Phase 1 transition Edge case (handoff write). The two files now own complementary tiers: durable commitments in the log; in-flight investigation state in the handoff. Rationale (user) — option (a) of the adversarial-review proposal: keep the full research-shaped handoff body, with the single overlap (`## Open questions`) becoming a pointer to the log; the decision-log's clean three-tier commitment-knowledge contract is preserved; the handoff stays useful for what it was designed for. The other adversarial findings (A1, A2, A4, A5, A6, A8, A10, A11) require design decisions and are queued for separate discussion.

**Mechanical checks** (target=design): 0 blockers / 4 should-fix (all carried from Mutation 1; same pre-existing sites, all out of scope)
**Cold-read** (scope: whole-doc): PASS / 1 suggestion

**Findings**:
- should-fix (mechanical, out of scope — carried from Mutation 1): line 39 `## Class Design` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutation 1): line 230 `## Decision-log file shape` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutation 1): line 340 (inside §"Write triggers" → "Edge cases / Gotchas") em-dash density. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutation 1): line 372 `## Phase 1 design-iteration rationale` fragmented header. Pre-existing.
- suggestion (cold-read): wording inconsistency — Workflow H3 paragraph at line 158 uses "Raw notes / partial findings" (full canonical handoff template field name) while the two Edge cases at lines 294 and 365 use "Raw notes" (short form). Three sites are mutually consistent in meaning; the reviewer notes a future tightening pass could harmonize to one phrasing. Suggestion severity — not retried in this mutation per `edit-design` skill rules; recorded for next-pass consideration.

**Iterations**: 1 of 3 (PASS — no in-scope findings; all four mechanical should-fix are pre-existing carry-overs from Mutation 1; cold-read suggestion is below should-fix threshold)

## Mutation 3 — 2026-05-25 — content-edit + template-internal section-rename (design.md)

**Diff summary**: Applied the remaining eight adversarial-review findings (A1, A2, A4, A5, A6, A8, A10, A11) decided one-by-one with the user after Mutation 2. **A1 (a)**: kept the bundled YTDB-965 + YTDB-842 ship and added a "Why these ship together" paragraph to the Overview citing file-geography overlap (both edit `conventions.md`) and rebase-risk dominance. **A2 (b)**: kept `decision-log.md` and `design-mutations.md` as two files and strengthened §"Phase 1 design-iteration rationale" with the different-readers justification (Phase 4 reads the log for ADR aggregation; the mutations file is internal to `edit-design`). **A4 (a)**: kept the articulated-only rule and added an Edge case in §"Phase 1 design-iteration rationale" documenting the captured / uncaptured boundary (the `because` / `in order to` / `to avoid` trigger phrases vs implicit rationale reconstructed at ADR-write time from the diff). **A5 (a)**: timestamp-on-refinement-append rule. First paragraph in `## Initial request` stays bare-anchor (the verbatim aim); subsequent refinement paragraphs carry an `_appended <YYYY-MM-DD HH:MM>_` marker; latest-wins on contradictions. Rule restated across §"Initial-request write contract" body, the file-template comment in §"Decision-log file shape", and the contradiction edge case. **A6 (a)**: renamed three sections in the `decision-log.md` template inside design.md from the per-track names (`## Decision Log` / `## Surprises & Discoveries` / `## Open Questions`) to `## Plan-time Decisions` / `## Plan-time Surprises` / `## Plan-time Open Questions` to disambiguate from the per-track `plan/track-N.md` section names sharing the same vocabulary. Renames propagated through all 16 in-document references; the H1 of `design.md` and the file-template H1 inside the code fence (both using "Decision Log" as the feature/file-title name) are preserved deliberately. **A8 (a)**: kept seven principles and added a defending sentence to the detailed doc's "Per-principle paragraphs" naming why each is load-bearing (Principle 1 single-source-of-truth, Principle 7 lean documents load on demand, etc.). **A10 (b)**: kept `decision-log.md` as a separate file (not folded into `implementation-plan.md`) and added a three-reasons paragraph to §"Decision-log file shape" naming writes-before-plan-exists (Phase 0 fires before Phase 1 plan generation), single-file-pointer (handoff and plan both reference one canonical log path), and coherent identity (the artifact survives plan rewrites and Phase 4 cleanup as `design-final.md` and `adr.md` siblings). **A11 (e)**: Step 4 transition confirmation. The sequence diagram in §"Phase 0 → Phase 1 transition" was updated with an `ack or refine` arrow on the user side, and a new paragraph in the body explains the read-and-ack mechanic (the agent reads the current log back to the user before plan-file generation; the user either ack-confirms or supplies a refinement; refinement appends as a timestamped paragraph per A5 with no LLM heuristic involved).

**Mechanical checks** (target=design): 0 blockers / 4 should-fix (all carried from Mutations 1+2; same pre-existing sites at lines 41 `Class Design` fragmented header, 234 `Decision-log file shape` fragmented header, 352 `Write triggers` em-dash density, 386 `Phase 1 design-iteration rationale` fragmented header; all out of scope per `design-document-rules.md § Mutation discipline (scope: new mutations only)`)
**Cold-read** (scope: whole-doc): PASS / 3 suggestions

**Findings**:
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 41 `## Class Design` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 234 `## Decision-log file shape` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 352 (inside §"Write triggers" → "Edge cases / Gotchas") em-dash density. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 386 `## Phase 1 design-iteration rationale` fragmented header. Pre-existing.
- suggestion (cold-read): §"Initial-request write contract" body and the §"Decision-log file shape" file-template comment both define the timestamped-append rule (lines 319 vs lines 255-265). Mutually consistent; a future tightening pass could nominate one as the canonical citation. Both restatements are needed at their respective sites (the contract section is the rule; the template comment is the surface a writer copies), so not a should-fix.
- suggestion (cold-read): §"Phase 0 → Phase 1 transition" sequence diagram (lines 152-154) shows the Step-4 `ack or refine` arrow without a caption noting the refinement uses the default append rule (no LLM heuristic). The connection is made in the body paragraph at line 367, but a one-line diagram caption would help diagram-first readers.
- suggestion (cold-read): §"Decision-log file shape" Edge cases at line 304 carries a long sentence bundling three concepts (threshold trigger / handoff body retention / pointer-to-log mechanic / complementary-tier framing). A sentence break before "The two files now own complementary tiers" would lower cognitive load. Soft cap exempt under template-bound shape.

**Iterations**: 1 of 3 (PASS — no in-scope findings; all four mechanical should-fix are pre-existing carry-overs from Mutations 1+2; the three cold-read suggestions are below should-fix threshold and not rule-mandated)

## Mutation 4 — 2026-05-25 — structural-rewrite (design.md)

**Diff summary**: Folded YTDB-975 (Split Phase 1: design-first authoring with feasibility-review gate; plan derivation in a fresh session) into the existing YTDB-965 + YTDB-842 bundle. Two new top-level sections added: §"Phase 1a feasibility-review gate" (sub-agent gate after `design.md` is authored, running a new feasibility-review agent at `.claude/agents/feasibility-review.md` plus the existing `prompts/adversarial-review.md` plus zero or more domain-shaped reviewers when the design's content triggers a recommendation; gate PASS requires every sub-agent PASS or a written `accepted-open-risks` block; session-end on PASS; file shape for new `_workflow/feasibility-review.md` artifact specified in template form) and §"Phase 1b plan derivation and ESCALATE back-edge" (separate `/create-plan` session that auto-resumes from gate PASS by checking `design.md` exists, latest cycle in `feasibility-review.md` records gate PASS, and `implementation-plan.md` does not yet exist; derives Architecture Notes + Decision Records + tracks + plan/track-N.md files; ESCALATEs to Phase 1a on fundamental contradiction via an entry in `feasibility-review.md`, an explicit user-facing message, and session end). H2 section renames: `## Phase 0 → Phase 1 transition` → `## Phase 0 → Phase 1a transition` and `## Phase 1 design-iteration rationale` → `## Phase 1a design-iteration rationale` (also on the matching H3 sub-sections under §Workflow). Existing §"Phase 0 → Phase 1a transition" rewritten as handshake-only (TL;DR + body + edge cases + references); the log-to-plan mapping prose moved out of this section and into the new §"Phase 1b plan derivation and ESCALATE back-edge". §Workflow sub-section `### Phase 0 → Phase 1a transition` sequence diagram truncated (the four plan-derivation arrows moved out); new §Workflow sub-section `### Phase 1a gate → Phase 1b derivation (with ESCALATE)` added with a new sequence diagram + prose. Overview rewritten: "two artifacts" → "two artifacts plus one Phase 1 reorder"; third bullet added for YTDB-975; "Why these ship together" paragraph extended to cover YTDB-975's heavy file overlap with the existing bundle (`planning.md`, `research.md`, `create-plan/SKILL.md`, `workflow.md`, `implementation-review.md`) plus YTDB-975's explicit dependency on YTDB-842; "Restructured to fit" list updated with the new workflow-file edits and the new `.claude/agents/feasibility-review.md` agent; doc structure roadmap updated from "seven topic sections" to "nine topic sections"; enabling-primitives paragraph rewritten to enumerate three primitives (`decision-log.md`, `feasibility-review.md`, session boundary). Core Concepts intro bumped from "seven" to "ten" load-bearing ideas; existing `decision-log.md` bullet updated to mention Phase 1a + Phase 1b; three new bullets added (Phase 1a feasibility-review gate, Phase 1b plan derivation, ESCALATE back-edge). Class Design Mermaid diagram extended with three new classes (`feasibility_review_md`, `feasibility_review_agent_md`, `workflow_md`); updated attributes on `conventions_md`, `create_plan_skill`, `planning_md`, `mid_phase_handoff_md`, `implementation_review_md`, `research_md`; new edges for `create_plan_skill ..> feasibility_review_agent_md`, `feasibility_review_agent_md ..> feasibility_review_md`, `create_plan_skill ..> feasibility_review_md`, `workflow_md ..> create_plan_skill`; prose summary updated from "two file artifacts" to "three file artifacts" / "four workflow documents" to "five workflow documents" / "complementary halves" to "three complementary roles" (iter-2 fix on the L147 cold-read should-fix). §"Decision-log file shape" TL;DR + template blockquote + `## Plan-time Decisions` template comment + Lifecycle "Written by" / "Read by" lines updated to cover Phase 1a + Phase 1b writes / reads; new Edge case for the `(Phase 1a)` / `(Phase 1b)` body annotation convention (Phase 0 entries carry no annotation). §"Phase 1a design-iteration rationale" body updated to note `edit-design` Step 7 fires only inside Phase 1a + the `(Phase 1a)` annotation guidance. §"Initial-request write contract" cross-reference rename (Phase 1 → Phase 1a). §"Write triggers" body cross-reference renames (Phase 1 → Phase 1a). §"Cross-reference tier mapping" planning.md row's Reason text updated to mention Phase 1a / Phase 1b split; new Edge case noting YTDB-975 touches additional files but doesn't add a sixth philosophy cross-reference (the five-site table stays at five).

**Mechanical checks** (target=design): 0 blockers / 4 should-fix (all pre-existing carry-overs from Mutations 1+2 at lines 48 `Class Design` fragmented header, 310 `Decision-log file shape` fragmented header, 431 `Write triggers` em-dash density, 527 `Phase 1a design-iteration rationale` fragmented header post-rename; all out of scope per `design-document-rules.md § Mutation discipline (scope: new mutations only)`)
**Cold-read** (scope: whole-doc): PASS / 2 suggestions (after iter-2 cleared the L147 in-scope should-fix)

**Findings**:
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 48 `## Class Design` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 310 `## Decision-log file shape` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 431 (inside §"Write triggers" → "Edge cases / Gotchas") em-dash density. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2): line 527 `## Phase 1a design-iteration rationale` fragmented header (post-rename, content unchanged from Mutation 3).
- suggestion (cold-read iter-2): three different phrasings for the third runtime flow across the Overview structure roadmap (line 22), the Workflow intro paragraph (line 151), and the H3 sub-section heading (line 216): `Phase 1a gate → Phase 1b derivation with ESCALATE` / `Phase 1a gate → Phase 1b plan derivation handshake with the optional ESCALATE back-edge` / `Phase 1a gate → Phase 1b derivation (with ESCALATE)`. Cross-references still resolve on slug match. Recorded for a future harmonization pass.
- suggestion (cold-read iter-2): the Overview structure-roadmap sentence at line 22 carries the three-flow list plus nine topic-section descriptors in one sentence. Splitting after "(with ESCALATE)" would lower cognitive load. Soft cap exempt under template-bound shape.

**In-scope findings cleared during iteration**:
- should-fix (mechanical, iter-1): three TL;DR paragraphs flagged as fragmented headers (≥50% content-word overlap with their H2 headings) — line 440 `Phase 0 → Phase 1a transition` (50% overlap), line 463 `Phase 1a feasibility-review gate` (100% overlap), line 553 `Phase 1b plan derivation and ESCALATE back-edge` (50% overlap). Rewrote each TL;DR to drop the heading words while preserving the technical content; all three cleared in iter-2.
- should-fix (cold-read iter-1, line 147 Class Design prose summary): "Three file artifacts ... own complementary halves" — the prose enumerated three roles after "halves" (knowledge / mechanics / feasibility verdicts), so "halves" no longer matched the count. Replaced with "three complementary roles" via a one-word edit (iter-2).

**Iterations**: 2 of 3 (PASS — all in-scope findings cleared; remaining mechanical should-fix are pre-existing carry-overs from Mutations 1+2; cold-read suggestions are below should-fix threshold and not rule-mandated)

## Mutation 5 — 2026-05-26 — content-edit (design.md)

**Diff summary**: Restructured the Phase 1a review machinery in response to five points the user raised about the review-cycle mechanics. (1) The feasibility-review reviewer is now committed to a written prompt at `.claude/workflow/prompts/feasibility-review.md`, replacing the prior "agent under `.claude/agents/`" placeholder that left the gate's behavior under-specified. (2) The gate loop now plugs into `.claude/workflow/review-iteration.md` verbatim: max 3 iterations, severity ladder (blocker / should-fix / suggestion), finding format `### Finding <PREFIX><N>`, gate-check verdicts on iter-2/3 (`VERIFIED` / `STILL OPEN` / `REJECTED` / `MOOT` / `REGRESSION`), max-iters ESCALATE — replacing the prior "bounded by user patience, not by a hard limit" wording. (3) Both reviewers are now required to follow Principle 6 (Semi-formal reasoning for reviewers) — premises, structural traversal, conclusion with Survival test; certificate-shape output; finding-cites-certificate-entry contract; existing precedent in `prompts/adversarial-review.md § Semi-Formal Reasoning Protocol` and `prompts/technical-review.md`. (4) The existing track-level `prompts/adversarial-review.md` (scoped to Phase A track structure: Decision Records / Invariants / Integration Points / Non-Goals — categories that do not exist at design time) is left unchanged; a separate adversarial-design-review prompt at `.claude/workflow/prompts/adversarial-design-review.md` is introduced for design-time devil's-advocate review. (5) The loop now auto-fires immediately after `edit-design phase1-creation` seeds `design.md` with no user "design ready" signal required; iteration is autonomous, applying mechanical findings via `edit-design` mutations and halting to ask the user only on decision-shaped findings (alternative-A-or-B; missing-mechanism; under-specified gap with no clear default). Two new finding ID prefixes `FD` (feasibility-design) and `AD` (adversarial-design) added to `review-iteration.md`'s prefix table (in scope for the implementation plan; not landed in this mutation). Propagated through Overview (3rd YTDB-975 bullet updated; enabling-primitives parenthetical extended with gate-check verdicts and the auto-fire / decision-shaped halt-to-ask clause; "Restructured to fit" paragraph's closing clause replaced — drops the `.claude/agents/feasibility-review.md` agent definition and the `prompts/adversarial-review.md` reuse, adds the two new prompt files at `.claude/workflow/prompts/` plus the `review-iteration.md` plug-in plus the `FD`/`AD` prefixes), Core Concepts (Phase 1a feasibility-review gate bullet rewritten end-to-end), Class Design Mermaid diagram (class `feasibility_review_md` renamed to `feasibility_review_log_md` with attributes extended for per-iter gate-check verdicts and the new `PASS (with accepted open risks)` verdict; class `feasibility_review_agent_md` renamed to `feasibility_review_prompt_md` with attributes rewritten for the new prompt-file location and the certificate-shape + `FD` prefix contract; new class `adversarial_design_review_md` added with four attributes; `create_plan_skill`'s Phase 1a attribute updated to mention auto-fire and the `review-iteration.md` plug-in; three edges rewritten — `create_plan_skill ..> feasibility_review_prompt_md`, new `create_plan_skill ..> adversarial_design_review_md`, `create_plan_skill ..> feasibility_review_log_md` consolidating cycle-log writes; old edge `feasibility_review_agent_md ..> feasibility_review_md` removed), Class Design prose paragraph below the diagram (file-artifact count framing updated for the rename and the new prompt-file pair; the "one new agent definition" sentence dropped; `feasibility_review_md` references replaced with `feasibility_review_log_md` throughout), Workflow sub-section §"Phase 1a gate → Phase 1b derivation (with ESCALATE)" sequence diagram (initial user "design looks ready" arrow replaced with two CP-internal self-arrows `Auto-fire Phase 1a gate` and `Auto-detect domain triggers`; per-sub-agent loop replaced with an `iter 1..3` outer loop containing a `par` block for the two anchor sub-agents plus an optional domain-trigger reviewer, followed by an `alt` block for mechanical vs decision-shaped findings; post-loop `alt` block extended with a `blockers remain after 3 iters` branch and an `accepted-open-risks` opt sub-block), and §"Phase 1a feasibility-review gate" — major rewrite covering new TL;DR, new H3 subsections "Auto-fire trigger and loop ownership", "Two anchor sub-agents" (three bulleted slots: feasibility-review with `FD` prefix and `prompts/adversarial-review.md § Output Format` shape; adversarial-design-review with `AD` prefix and design-content scope; optional domain-shaped reviewers with auto-detection and the "not reused from `.claude/agents/`" note), "Semi-formal reasoning protocol (Principle 6) — required for both prompts", "Iteration loop (per `review-iteration.md`)" with explicit per-iteration cycle steps and the PASS-condition restatement, "Decision-shaped finding criterion" with three concrete shapes (alternative-A-or-B, missing-mechanism, under-specified gap), "Max-iters halt and `accepted-open-risks`" with the user-facing prompt and the two option paths; `feasibility-review.md` file-shape template rewritten to show iter-1 full-review block plus iter-2/3 gate-check blocks plus the new gate-verdict line "PASS | PASS (with accepted open risks) | ESCALATE-to-Phase-1a-via-manual-refinement"; Edge cases / Gotchas restructured to eight bullets (PSI unavailable; mid-loop user mutation; iter-2 regression on mechanical fix; domain-trigger auto-detection false positive; `design-review.md` cold-read still fires per-mutation — explicit non-replacement clause; single sub-agent disagreement; Phase 1b ESCALATE re-enters via Phase 1a; mid-gate pause); References footer expanded to nine entries (YTDB-975, YTDB-813, the two new prompt files, the unchanged track-level prompt, `design-review.md`, `review-iteration.md`, `workflow.md § Session Boundary Rules`, and the in-doc pointer to §"Phase 1b plan derivation and ESCALATE back-edge"). Rationale (user): five concrete review-cycle improvements raised in conversation — written prompts (no agent improvisation), structured review-iteration.md plug-in, semi-formal reasoning enforcement for both prompts, separate design-time adversarial reviewer distinct from the track-level one, and an autonomous review loop that auto-fires after design.md is seeded and halts only on decision-shaped findings.

**Mechanical checks** (target=design): 0 blockers / 4 should-fix (all pre-existing carry-overs from Mutations 1+2+3+4; same pre-existing sites)
**Cold-read** (scope: whole-doc): PASS / 1 should-fix (length-signal soft-cap trigger on §"Phase 1a feasibility-review gate" — cold-read reviewer's own verdict treats it as soft-cap-flag for closer review rather than a hard violation under the Padding-based finding criterion) / 2 suggestions

**Findings**:
- should-fix (mechanical, out of scope — carried from Mutations 1+2+3+4): line 452 (inside §"Write triggers" → "Edge cases / Gotchas") em-dash density. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2+3+4): line 48 `## Class Design` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2+3+4): line 331 `## Decision-log file shape` fragmented header. Pre-existing.
- should-fix (mechanical, out of scope — carried from Mutations 1+2+3+4): line 606 `## Phase 1a design-iteration rationale` fragmented header. Pre-existing.
- should-fix (cold-read, in scope — soft-cap trigger): §"Phase 1a feasibility-review gate" is ~2,284 words (lines 484–604), above the 200-word soft cap for an H2 section. Cold-read reviewer's verdict: "Section length cap exception only applies to ExecPlan episodes, edit-list subsections, state-machine tables, file:line citation blocks, or multi-step derivations under `design-mechanics.md`; this is free-form prose under `design.md`. The padding-based finding criterion requires a banned-vocabulary / banned-pattern / restatement hit before length alone becomes a finding — I do not see a concrete padding pattern in the section, so the cap-trigger flags it for closer review rather than as a hard violation." Two near-overlap pairs flagged for next-mutation consolidation: (a) TL;DR's autonomy clause overlaps with §"Auto-fire trigger and loop ownership" body; (b) §"Two anchor sub-agents" prompt disambiguation overlaps with the Edge cases bullet on prompt-vs-track-level distinction. Recorded for next-pass consideration; not retried in this mutation per cold-read PASS verdict.
- suggestion (cold-read): embedded `feasibility-review.md` template inside §"Phase 1a feasibility-review gate" emits H2 headings (`## Cycle 1`, `## Cycle N`) inside a fenced code block; outer-heading grep picks them up as document-level H2s. Functionally inert (inside the fence) but a reader scanning the structural outline may briefly conflate them with body sections. Not a rule violation. Recorded for next-pass consideration.
- suggestion (cold-read): Overview's penultimate paragraph (line 18) is dense — load-bearing but long. A second sentence break around "Two new sub-agent prompts under `.claude/workflow/prompts/`..." would lower per-sentence parse cost without losing content. Recorded for next-pass consideration.

**In-scope findings cleared during iteration**:
- should-fix (mechanical, iter-1): line 16 hyphenated-pair cluster `(alternative-A-or-B, missing-mechanism, under-specified gap with no clear default)`. Rewrote to break the comma-separated cluster: "pick between alternatives, define a missing primitive, or resolve a gap whose default is not clear from surrounding context".
- should-fix (mechanical, iter-1): line 486 TL;DR of §"Phase 1a feasibility-review gate" shared 3/4 content words ("phase", "feasibility-review", "gate") with the H2 heading. Rewrote TL;DR opening to drop "gate" (replaced with "loop" / "review pass") and "Phase 1b" (replaced with "plan derivation") and `phase1-creation` (replaced with bare `edit-design`); residual match is only the `feasibility-review.md` file path. New overlap: 1/4 (25%).
- should-fix (mechanical, iter-1): line 492 paragraph under H3 "Auto-fire trigger and loop ownership" shared 3/4 content words with the heading. Rewrote to drop "auto-fire", "trigger", "loop" from the body; "auto-fire" file-shape reference dropped in favor of "opens a fresh cycle header".
- should-fix (mechanical, iter-1): line 496 paragraph under H3 "Two anchor sub-agents" shared 2/3 content words. Replaced "The two anchor slots..." with "Both reviewer slots cover distinct dimensions; each runs on every cycle." — 0/3 match.
- should-fix (mechanical, iter-1): line 504 paragraph under H3 "Semi-formal reasoning protocol (Principle 6) — required for both prompts" shared 7/8 content words. Rewrote opening to drop the `Semi-Formal Reasoning Protocol` filename anchor (precedent reference is now generic: `prompts/adversarial-review.md` and `prompts/technical-review.md`), replaced "Both reviewers" with "Each reviewer", dropped "the prompt" in favor of "the template". Residual matches: "principle" (1, in "per Principle 6"), "6" (1), "prompts" (1, in file paths) = 3/8 (37.5%).
- should-fix (mechanical, iter-1): line 508 paragraph under H3 "Iteration loop (per `review-iteration.md`)" shared 4/5 content words. Added a non-matching lead sentence "Each cycle through the gate follows the standard workflow review protocol."; replaced "iterations" / "loop" with "cycles" in the rewritten body; dropped one `review-iteration.md` filename reference (replaced with `§ Gate-check verdict handling in that same file`).
- should-fix (mechanical, iter-1): line 521 paragraph under H3 "Decision-shaped finding criterion" shared 2/3 content words. Rewrote to drop "decision-shaped" and "finding" — "A reviewer's proposed fix runs autonomously when surrounding context makes the resolution clear, and stops to ask the user when the resolution requires picking between alternatives or defining a new primitive. Three concrete shapes need user input:".

**Iterations**: 2 of 3 (PASS — all 7 in-scope findings cleared; the 4 remaining mechanical should-fix are pre-existing carry-overs from Mutations 1+2+3+4; the cold-read should-fix is a soft-cap trigger that the cold-read reviewer's own verdict treats as non-blocking under the Padding-based finding criterion; the two cold-read suggestions are below should-fix threshold and not rule-mandated)

## Mutation 6 — 2026-05-26 — structural-rewrite (design.md)

**Diff summary**: Replaced §"Phase 1a feasibility-review gate" (lines 484-604, ~120 lines) with a new §"Per-mutation design review fan-out" section. The new section folds the gate machinery into `edit-design`'s Step 4 per-mutation fan-out (eliminates the "Phase 1a gate" as a distinct phase-level mechanism). Names mandatory reviewer slots (cold-read existing, feasibility-review FD, adversarial-design-review AD), four content-triggered domain reviewers (crash-safety CS, concurrency CC, performance PF, workflow-changes — the last expands to four sibling reviewers WCC/WCB/WCI/WCP mirroring `.claude/agents/review-workflow-*` at design-doc scope). Adds the aggregator sub-agent that consolidates per-reviewer files in `_workflow/design-reviews/cycle-N-iter-M/`. Defines cycle/iter semantics over autonomous chained mutations. Names the user-review checkpoint protocol for batching decision-shaped findings + user observations + optional autonomous-mutation reverts. References the new `conventions.md §1.4.X` prompt-by-reference spawn protocol. Retains the decision-shaped finding criterion (alternative-A-or-B, missing-mechanism, under-specified-gap) and the max-iters `accepted-open-risks` halt path. References footer enumerates new prompt files (feasibility-review, adversarial-design-review, three domain-design files, four workflow-changes design siblings, aggregator) plus the code-side `.claude/agents/review-*` sources cited for dimensional taxonomy.

**Mechanical checks** (target=design): 11 findings, 0 blockers
**Cold-read** (scope: whole-doc): NEEDS REVISION, 6 blockers + 5 should-fix + 2 suggestions

**Findings**:
- blocker (cold-read, in scope — pending downstream mutations): 6 structural-contradiction findings between the new §"Per-mutation design review fan-out" and the rest of the document. The new section names the per-mutation fan-out as canonical; the Overview (lines 12, 16), Core Concepts (line 40), Class Design diagram (lines 76-82, 140-142), the Phase 1a → Phase 1b workflow sequence diagram (lines 225-282), §"Decision-log file shape" (lines 345-348), and §"Phase 1b plan derivation and ESCALATE back-edge" (lines 661, 683, 691) all still describe the single-gate-after-phase1-creation architecture and reference the now-deprecated `feasibility-review.md` artifact. These blockers cannot close within this mutation alone — the document's other sections must be re-coordinated to match. They are scheduled to close via the next mutations in the planned restructure sequence: Mutation 7 (Class Design rewrite), Mutation 8 (Overview rewrite), Mutation 9 (Core Concepts rewrite), Mutation 10 (Phase 1a design-iteration Workflow diagram), Mutation 11 (Phase 1a → 1b Workflow diagram), Mutation 12 (§"Phase 1b plan derivation" rewrite), Mutation 13 (§"Decision-log file shape" content-edits adding gate-verdict / transition annotations). Plus the planned addition of §"Design-doc review directory shape" between this section and §"Phase 1a design-iteration rationale" via a separate section-add mutation.
- should-fix (cold-read, in scope — pending Core Concepts mutation): novel-construct-not-in-core-concepts — the new section introduces "cycle", "iter", "iteration_budget", "aggregator sub-agent", "user-review checkpoint", "domain-triggered reviewer", "prompt-by-reference protocol", "decision-shaped finding criterion"; Core Concepts (lines 26-46) does not list them. Scheduled to close with the Core Concepts rewrite.
- should-fix (cold-read, in scope — pending Overview mutation): unsourced-protocol-ref — the new section cites `conventions.md §1.4.X` but the Overview's "Restructured to fit" list (line 18) does not include the §1.4 edit. Scheduled to close with the Overview rewrite.
- should-fix (cold-read, in scope — pending Class Design + Overview mutations): novel-output-directory-not-in-class-design — `_workflow/design-reviews/cycle-N-iter-M/` is not in the Class Design diagram nor in the Overview's §1.2 directory layout enumeration. Scheduled to close with Class Design + Overview rewrites.
- should-fix (cold-read, in scope — pending Overview mutation): finding-id-table-drift — Overview line 18 names only FD and AD as new prefixes; the new section adds 7 more (CS, CC, PF, WCC, WCB, WCI, WCP). Scheduled to close with the Overview rewrite.
- should-fix (cold-read, in scope — addressable here): cold-read-reviewer-double-counting — line 494 step 1 names cold-read as a slot the orchestrator spawns; ambiguous whether `edit-design`'s existing Step 4 cold-read invocation is preserved or replaced. To be tightened in a follow-up content-edit mutation that pins the ownership boundary explicitly.
- should-fix (mechanical, in scope): line 542 hyphenated-pair cluster in the aggregator-output tuple description. To be addressed by a follow-up content-edit.
- should-fix (mechanical, out of scope — carried from prior mutations and the new section's TL;DRs): 9 fragmented-header findings spanning Class Design (line 48), Decision-log file shape (line 331), new section TL;DR (line 484), four H3 sub-sections inside the new section (lines 490, 537, 550, 572, 600), and Phase 1a design-iteration rationale (line 633). The H3 sub-section fragmented headers in the new section are a structural feature of the section's TL;DR-per-subsection shape; consistent with existing design.md sections that share the same pattern. Recorded as known debt; not retried in this mutation.
- should-fix (mechanical, out of scope — carried from prior mutations): line 452 em-dash density inside §"Write triggers" → "Edge cases / Gotchas". Pre-existing.
- suggestion (cold-read, in scope — pending Edge cases extension): aggregator-double-write-on-decision-log — both the aggregator and `edit-design` Step 7 emit Plan-time Decisions entries; ordering and PASS-with-zero-findings suppression not stated. Recorded for next-pass consideration in the Edge cases list.
- suggestion (cold-read, in scope — pending Edge cases extension): missing-edge-case-aggregator-failure — the Edge cases list does not cover aggregator failure modes (parse error, timeout, malformed structured output). Recorded for next-pass consideration.

**In-scope findings cleared during iteration**: none cleared in this mutation. The structural contradictions surfaced by cold-read are inherent to a multi-mutation restructure: they require subsequent mutations to land before re-running cold-read will return PASS. Per SKILL.md Step 6 "Budget exhausted with blockers remaining" path, the action does not succeed in isolation; the edits stay on disk; manual resolution is the planned mutation sequence. The follow-up Mutation 7+ batch closes the blockers.

**Iterations**: 1 of 3 (BLOCKER REMAINS — manual resolution required; blockers close via planned downstream mutations 7-13 of the YTDB-965+842+975 restructure)

## Mutation 7 — 2026-05-26 — section-add (design.md)

**Diff summary**: Added §"Design-doc review directory shape" between §"Per-mutation design review fan-out" and §"Phase 1a design-iteration rationale" (inserted at what was line 633 in the prior state). The new section names the `_workflow/design-reviews/cycle-N-iter-M/<reviewer>.md` path layout, the per-reviewer file naming convention (`feasibility.md`, `adversarial.md`, `cold-read.md`, three domain files, four workflow-changes design siblings), optional `summary.md` and `<reviewer>-inputs.md`, the cycle / iter incrementing semantics, the stamp-exclusion under `conventions.md §1.6(f)`, the lifecycle (created lazily by orchestrator; written by reviewer sub-agents + aggregator; read by aggregator + Phase 4 audit; removed by Phase 4 cleanup commit), the Forward-extension sub-section naming `_workflow/plan-reviews/`, `_workflow/track-reviews/`, `_workflow/code-reviews/` as out-of-scope future directories that follow the same naming pattern, three Edge cases (Partial fan-out on session pause, Reviewer file naming collisions, Empty cycle-N-iter-M/ directory), and a References footer pointing back to §"Per-mutation design review fan-out" and `conventions.md §1.6(f)`.

**Mechanical checks** (target=design): iter-1 1 finding (fragmented header at the "Forward extension to other review surfaces" sub-section, in scope). Iter-1 fix: rewrote the lead paragraph from "The kind-scoped directory pattern extends forward to other review surfaces as those phases adopt the per-reviewer-file pattern" to "The kind-scoped directory pattern carries over to other phases as those adopt the per-reviewer-file approach" (drops "forward", "review", "surfaces", "extends" from the paragraph; overlap goes from 4/5 → 0/4 with the heading). Iter-2 mechanical: PASS, 0 findings.

**Cold-read** (scope: bounded — new section + Overview + Core Concepts + structure roadmap): PASS, 0 blockers, 0 should-fix, 5 suggestions.

**Findings**:
- suggestion (cold-read): "Forward extension" wording — change "established here so future plans don't reinvent it" to "suggested here as a pattern future plans can follow without reinventing it." Recorded for next-pass consideration.
- suggestion (cold-read): Phase 4 reader of `_workflow/design-reviews/**` is not echoed in `prompts/create-final-design.md`. Either drop the Phase 4 reader from the lifecycle "Read by" list, or add a forward-pointer + ensure the Phase 4 prompt names this read in a separate workflow file edit. Recorded for next-pass consideration.
- suggestion (cold-read): Example directory tree shows `summary.md` only inside `cycle-1-iter-1/` — other entries end with `...`. Adding `summary.md` placeholders to the other cycles would make the per-iter-M aggregator output visible across the example. Recorded for next-pass consideration.
- suggestion (cold-read): Cross-link the aggregator's two outputs explicitly — the directory-local `summary.md` is the verbose mirror of the one-line `decision-log.md ## Plan-time Decisions` gate-verdict entry. Recorded for next-pass consideration.
- suggestion (cold-read): Edge case heading "Empty `cycle-N-iter-M/` directory" rule-out body suggests renaming to "Minimum reviewer set per iter-M" to match. Recorded for next-pass consideration.

**In-scope findings cleared during iteration**:
- should-fix (mechanical, iter-1): line 669 "Forward extension to other review surfaces" fragmented header. Lead paragraph rewritten as documented above; overlap 4/5 → 0/4.

**Iterations**: 2 of 3 (PASS — all in-scope mechanical findings cleared; cold-read suggestions recorded for next-pass consideration per SKILL.md Step 6 "suggestion findings are not retried")

## Mutation 8 — 2026-05-26 — structural-rewrite (design.md)

**Diff summary**: Replaced §"Phase 1b plan derivation and ESCALATE back-edge" (lines 716-756, ~41 lines) with a new ~60-line version that re-homes the auto-resume detector and the ESCALATE back-edge from the eliminated `feasibility-review.md` onto `decision-log.md ## Plan-time Decisions` tag scans. New TL;DR names the `(Phase 1a → 1b)` tag (written by the Phase 1a user-review checkpoint) and the `(Phase 1b ESCALATE)` tag as the durable transition signals. New `### Auto-resume detection` H3 lists the routing-table over five startup conditions (`(Phase 1a → 1b)` + plan absent → 1b auto-resume; `(Phase 1b ESCALATE)` → 1a re-entry; no matching entry → fresh start; tag + plan exists → resume 1b mid-derivation; malformed → ask user). New `### Plan-derivation mapping from decision-log.md` H3 maps `## Plan-time Decisions` / `## Plan-time Surprises` / `## Plan-time Open Questions` to DR / Architecture Notes / Integration Points + names the four annotated tags (`(Phase 1a gate-verdict)`, `(Phase 1a accepted-open-risks)`, `(Phase 1a → 1b)`, `(Phase 1b ESCALATE)`) and how each is consumed. New `### ESCALATE shape` H3 carries the four-sub-bullet entry format + the user-facing message text. Edge cases reframed for the new flow (auto-resume false negative reads `decision-log.md`; pre-YTDB-975 path detects absence of `(Phase 1a → 1b)` tag). References footer updated: drops `review-iteration.md`; adds §"Per-mutation design review fan-out" and §"Decision-log file shape" pointers; drops the now-vanished §"Phase 1a feasibility-review gate" cite. Closes Mutation-6 blockers F4 (auto-resume detector pointing at vanished `feasibility-review.md`) and F5 (ESCALATE entry shape).

**Mechanical checks** (target=design, scope=whole-doc): PASS, 0 blockers, 14 should-fix
**Cold-read** (scope: whole-doc): PASS, 0 blockers, 6 should-fix (all tagged "scheduled for downstream Mutation N" per the brief), 2 suggestions

**Findings**:
- should-fix (cold-read, scheduled for downstream Mutation 9): §Overview ¶2 (line 12), ¶3 (line 16), ¶4 (line 18), and roadmap sentence ¶6 (line 22) still describe a `feasibility-review.md`-gated handshake. Closes with the Overview rewrite.
- should-fix (cold-read, scheduled for downstream Mutation 10): §Core Concepts (lines 40, 42, 44) defines "Phase 1a feasibility-review gate", names `feasibility-review.md` as the auto-resume signal, and routes ESCALATE writes to `feasibility-review.md`. Closes with the Core Concepts rewrite.
- should-fix (cold-read, scheduled for downstream Mutation 11): §Class Design diagram nodes (`feasibility_review_log_md` at lines 76-82; `feasibility_review_prompt_md` at lines 83-87; `create_plan_skill` lines 97, 99; arrows at lines 140, 142) plus post-diagram prose (line 154) still depict `feasibility-review.md` as a durable artifact. Closes with the Class Design rewrite.
- should-fix (cold-read, scheduled for downstream Mutation 13): §Workflow "Phase 1a gate → Phase 1b derivation (with ESCALATE)" diagram (lines 225-280) plus closing prose (line 282) models the handshake through a `feasibility-review.md` participant. Closes with the Phase 1a → 1b sequence diagram rewrite.
- should-fix (cold-read, scheduled for downstream Mutation 9/10 — 14b coverage): §"Decision-log file shape" template comment block (lines 345-348) cites "gate-PASS handshake". Closes with the Mutation 14b annotation-list edit.
- should-fix (cold-read, scheduled for downstream Mutation 9/10 — 14c coverage): §"Phase 0 → Phase 1a transition" References footer (line 481) cites §"Phase 1a feasibility-review gate". Closes with the Mutation 14c post-ack paragraph rewrite (also touches the predecessor-section citation).
- suggestion (cold-read, in scope — recorded for next-pass consideration): §Phase 1b Auto-resume detection (after line 724) doesn't name the writer event for the `(Phase 1a → 1b)` tag. Suggested edit: add one sentence pointing at §"Per-mutation design review fan-out" § User-review checkpoint as the writer event.
- suggestion (cold-read, in scope — recorded for next-pass consideration): §Phase 1b References footer (lines 770-775) doesn't cite §"Decision-log file shape", though both the auto-resume detector and the ESCALATE shape depend on its per-section format. Suggested edit: add the bullet to the References footer.
- should-fix (mechanical, out of scope — known debt, structural feature of the TL;DR-per-subsection shape): 3 new fragmented-header findings introduced by the rewrite (line 716 main heading + TL;DR overlap 4/6; line 734 `### Plan-derivation mapping from decision-log.md` + lead-paragraph overlap 2/4; line 745 `### ESCALATE shape` + lead-paragraph overlap 1/2). Same pattern Mutation 6 recorded as "structural feature of the section's TL;DR-per-subsection shape; consistent with existing design.md sections that share the same pattern. Recorded as known debt; not retried in this mutation."
- should-fix (mechanical, out of scope — carried from prior mutations): 11 pre-existing findings unchanged by this mutation — line 48 (Class Design), line 331 (Decision-log file shape), line 452 (em-dash density in Write triggers), line 484 (Per-mutation design review fan-out), line 490 (Integration into edit-design Step 4), line 537 (Aggregator sub-agent), line 542 (hyphenated-pair cluster in aggregator description), line 550 (Cycle and iteration semantics), line 572 (User-review checkpoint), line 600 (Spawn protocol), line 690 (Phase 1a design-iteration rationale). Already enumerated in Mutation 6 + 7 logs.

**In-scope findings cleared during iteration**: none cleared in this mutation. The cross-section contradictions (F4 + F5 close at this rewrite's text site, but the downstream symbolic-reference cleanup at §Overview / §Core Concepts / §Class Design / §Workflow diagrams remains) require Mutations 9-13 to land before whole-doc cold-read returns clean. Per Mutation 6 precedent: scheduled-for-downstream findings are not retried within the originating mutation. The two cold-read suggestions specific to this section are non-rule-mandated and recorded for next-pass consideration per SKILL.md Step 6 "suggestion findings are not retried".

**Iterations**: 1 of 3 (PASS — closes F4 + F5 at this section's text site; cross-section contradictions tracked downstream per the planned mutation sequence)

## Mutation 9 — 2026-05-26 — structural-rewrite (design.md)

**Diff summary**: Replaced §"Overview" (lines 4-22, 19 lines) with an 18-line version that re-frames the four gaps in current workflow (philosophy unnamed, Phase 0 chat evaporates, mechanics-only design log, plan-then-design ordering) and the four ship-together changes (philosophy artifact pair, durable `decision-log.md`, Phase 1a/1b split with ESCALATE back-edge, per-mutation review fan-out integrated into `edit-design` Step 4). The four-bullet inventory drops the deprecated "auto-firing feasibility-review gate" bullet and adds the per-mutation fan-out bullet with explicit reviewer enumeration (cold-read existing + 2 mandatory: feasibility-review FD + adversarial-design-review AD + content-triggered domain reviewers: crash-safety CS + concurrency CC + performance PF; workflow-changes expands to four siblings WCC + WCB + WCI + WCP). The "These changes ship together" paragraph now cites the new `conventions.md §1.4` spawn protocol sub-section as a shared touch-point. The "enabling primitives" paragraph (previously line 16) is replaced with a tighter "Phase 1a exits on a user signal recorded as a Plan-time Decisions entry; Phase 1b auto-resumes by reading that entry from `decision-log.md`" framing that explicitly names the new artifact set (`decision-log.md`, `design-mutations.md`, `_workflow/design-reviews/cycle-N-iter-M/`) and the no-separate-`feasibility-review.md` rule. The "Restructured to fit" paragraph (previously line 18) is dropped in favor of the new "These changes ship together" + "Phase 1a exits on a user signal" pair. The closing roadmap (previously line 22) is rewritten to "Core Concepts → Class Design → Workflow → ten topic sections" reflecting the new section list after Mutation 7's §"Design-doc review directory shape" addition.

**Mechanical checks** (target=design, scope=whole-doc): iter-1 16 findings, 0 blockers (1 new in-scope em-dash density at line 17 + 1 new in-scope hyphenated-pair cluster at line 13 + 14 pre-existing carried over). Iter-1 fix: collapsed three em dashes in the "Phase 1a exits on a user signal..." paragraph (line 17) into prose: `(knowledge — rationale, gate verdicts, transition signals)` → `(knowledge: rationale, ...)`; `(mechanics — unchanged)` → `(mechanics, unchanged)`; `No separate feasibility-review.md — gate verdicts live in` → `No separate feasibility-review.md; gate verdicts live in`. Iter-2 mechanical: PASS, 15 findings, 0 blockers (1 new in-scope hyphenated-pair cluster remaining + 14 pre-existing).
**Cold-read** (scope: whole-doc): PASS, 0 blockers, 6 should-fix (all tagged "scheduled for downstream Mutation 10-14" per the brief), 0 suggestions specific to §Overview.

**Findings**:
- should-fix (mechanical, in scope — known debt, dimensional-name reference): line 13 hyphenated-pair cluster on "consistency, context-budget, instruction-completeness, prompt-design" — these are the literal sibling-reviewer names mirroring code-side `.claude/agents/review-workflow-*` files. The script categorizes them as adjectival hyphenated pairs but they are nouns here (dimension names). Rewriting to drop hyphens breaks the byte-for-byte mirror to code-side filenames. Recorded as known debt; not retried.
- should-fix (cold-read, scheduled for downstream Mutation 10): §Core Concepts line 39 ("Phase 1a feasibility-review gate" still describes auto-firing gate-after-phase1-creation), line 41 ("Phase 1b plan derivation" still names `feasibility-review.md` as resume signal), line 43 ("ESCALATE back-edge" still writes to `feasibility-review.md`). Closes with the Core Concepts structural rewrite.
- should-fix (cold-read, scheduled for downstream Mutation 11): §Class Design diagram still carries `feasibility_review_log_md` + `feasibility_review_prompt_md` nodes per Mutation 8 cold-read evidence. Closes with the Class Design rewrite.
- should-fix (cold-read, scheduled for downstream Mutations 12 + 13): §Workflow diagrams still model the old gate handshake per Mutation 8 cold-read evidence. Closes with the Phase 1a fan-out and Phase 1a → 1b diagram rewrites.
- should-fix (cold-read, scheduled for downstream Mutation 14b): §"Decision-log file shape" template comment block still cites "gate-PASS handshake". Closes with the Mutation 14b annotation-list edit.
- should-fix (cold-read, scheduled for downstream Mutation 14c): §"Phase 0 → Phase 1a transition" References footer still cites §"Phase 1a feasibility-review gate" (now-vanished section name). Closes with the Mutation 14c post-ack paragraph rewrite.
- should-fix (mechanical, out of scope — carried from prior mutations): 14 pre-existing findings unchanged by this mutation — line 47 (Class Design), line 330 (Decision-log file shape), line 451 (em-dash density in Write triggers), line 483 (Per-mutation design review fan-out), line 489 (Integration into edit-design Step 4), line 536 (Aggregator sub-agent), line 541 (hyphenated-pair cluster in aggregator description), line 549 (Cycle and iteration semantics), line 571 (User-review checkpoint), line 599 (Spawn protocol), line 689 (Phase 1a design-iteration rationale), line 715 (Phase 1b plan derivation), line 733 (Plan-derivation mapping), line 744 (ESCALATE shape). All enumerated in Mutation 6 + 7 + 8 logs; line numbers shifted by -1 from Mutation 8's state because Mutation 9 shrank §Overview by one line.

**In-scope findings cleared during iteration**:
- should-fix (mechanical, iter-1): line 17 em-dash density (3 em dashes in the "Phase 1a exits..." paragraph). Collapsed to prose as documented above; density 3 → 0 in that paragraph.

**Iterations**: 2 of 3 (PASS — closes F1 + F2 + F7 + F9 + F10 at this section's text site; one new in-scope mechanical finding retained as known debt with documented rationale; cross-section contradictions tracked downstream per the planned mutation sequence)

## Mutation 10 — 2026-05-26 — structural-rewrite (design.md)

**Diff summary**: Replaced §"Core Concepts" (lines 23-45, 23 lines) with a 28-line version. The header text moved from "ten load-bearing ideas" through an iter-1 "twelve" (inherited from the drafts file header) to "fourteen load-bearing ideas" after the iter-2 cold-read flagged the count drift and the one-token follow-up Edit landed. The `**decision-log.md.**` entry expanded to include gate-verdict entries, Phase 1a → 1b transition signal, and ESCALATE entries from Phase 1b (absorbing the role the discarded `feasibility-review.md` was going to play). The `**Phase 1a feasibility-review gate.**` entry was removed entirely. Five new Core Concepts entries added: `**Per-mutation design review fan-out.**` (Step 4 reviewer enumeration with finding-ID prefixes), `**Design-doc-scoped reviewer prompts.**` (six new prompts under `.claude/workflow/prompts/`), `**Aggregator sub-agent.**` (consolidates per-reviewer files in `cycle-N-iter-M/`), `**User-review checkpoint.**` (autonomous mechanical-fix chain boundary), `**Sub-agent prompt-by-reference spawn protocol.**` (`conventions.md §1.4` rule). The `**Phase 1b plan derivation.**` entry rewritten to scan `decision-log.md ## Plan-time Decisions` for the `(Phase 1a → 1b)` tag (replacing the prior `feasibility-review.md gate PASS` check). The `**ESCALATE back-edge.**` entry rewritten to write a `(Phase 1b ESCALATE)`-tagged entry to `decision-log.md ## Plan-time Decisions` (replacing the prior `feasibility-review.md` ESCALATE entry). Closes Mutation-9 cold-read findings against §Core Concepts (lines 39, 41, 43).

**Mechanical checks** (target=design, scope=whole-doc): PASS, 0 blockers, 15 should-fix (1 new in-scope hyphenated-pair cluster at line 13 carrying over from Mutation 9 as known debt + 14 pre-existing carried over).

**Cold-read** (scope: whole-doc): PASS, 0 blockers, 5 should-fix (1 in-scope count-mismatch closed in iter-2; 4 tagged "scheduled for downstream Mutation N" per the M8 + M9 pattern), 1 suggestion (shape-exemption note for §Core Concepts having no Edge cases / References footer; no action). Comprehension answer: YES — a cold reader who reads §Overview + §Core Concepts has the vocabulary to navigate the rest of the document.

**Findings**:
- should-fix (cold-read, in scope — closed in iter-2): §Core Concepts line 25 intro sentence said "twelve load-bearing ideas" but the section enumerates fourteen entries. The "twelve" originated from the drafts file header which carried the same miscount; the applier mirrored it on iter-1; the iter-2 cold-read flagged the drift; the one-token Edit ("twelve" → "fourteen") landed before this entry was finalized.
- should-fix (cold-read, scheduled for downstream Mutation 11): §Class Design Mermaid diagram and surrounding prose still carry `feasibility_review_log_md` and `feasibility_review_prompt_md` nodes (lines 83-100, 143-161). Contradicts the new §Core Concepts "No separate `feasibility-review.md`" framing. Closes with the Class Design rewrite.
- should-fix (cold-read, scheduled for downstream Mutations 12 + 13): §Workflow sub-sections "Phase 1a design-iteration rationale capture" diagram and "Phase 1a gate → Phase 1b derivation (with ESCALATE)" diagram (lines 230-289) still model the old `feasibility-review.md`-gated handshake. Closes with the Phase 1a fan-out and Phase 1a → 1b diagram rewrites.
- should-fix (cold-read, scheduled for downstream Mutation 14b): §"Decision-log file shape" template comment block cites "gate-PASS handshake" wording at line 353. Closes with the Mutation 14b annotation-list edit.
- should-fix (cold-read, scheduled for downstream Mutation 14c): §"Phase 0 → Phase 1a transition" References footer (line 488) cites §"Phase 1a feasibility-review gate" (now-vanished section name). Closes with the Mutation 14c post-ack paragraph rewrite.
- suggestion (cold-read, in scope — shape-exemption note, no action): §Core Concepts has no Edge cases / Gotchas sub-section and no References footer. The reviewer noted this matches the same-design shape-exempt treatment of §Class Design and §Workflow under house-style §Why-before-what's exemption list ("Core Concepts, Class Design, Workflow, Part-level TL;DR"). Recorded for trace; no edit required.
- should-fix (mechanical, in scope — known debt carried over from Mutation 9): line 13 hyphenated-pair cluster on the literal sibling-reviewer names mirroring code-side `.claude/agents/review-workflow-*` files. Recorded as known debt; not retried.
- should-fix (mechanical, out of scope — carried from prior mutations): 14 pre-existing findings unchanged by this mutation. Same set as Mutation 9, with line numbers shifted by +N (Core Concepts grew by 5 entries).

**In-scope findings cleared during iteration**:
- should-fix (cold-read, iter-2): §Core Concepts line 25 count-mismatch. Edit applied: "twelve" → "fourteen". Closes the drafts-file inherited miscount; the section's bullet enumeration is the authoritative count. Cold-read re-run on the post-fix state was skipped per the M9 precedent for single-token text fixes that do not affect comprehension.

**Iterations**: 2 of 3 (PASS — closes the §Core Concepts text-site rewrite plus the iter-2 count-mismatch fix; cross-section contradictions against §Class Design, §Workflow, §"Decision-log file shape", and §"Phase 0 → Phase 1a transition" tracked downstream per the planned mutation sequence)

