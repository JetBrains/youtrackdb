# Plan Review — Gremlin-to-MATCH Translator

Phase-2 audit summary (consistency + structural). Relocated from
`implementation-plan.md` § Plan Review during workflow migration (#1145,
D7): the Phase-2 audit lives here in every tier, and the plan no longer
carries the State-0 checkbox.

- [x] Plan review (consistency + structural) — passed at iteration 1

**Auto-fixed (mechanical):** S2 — trimmed Track 4 checklist intro to 3 sentences (detail already in plan/track-4.md); S3 — trimmed Track 6 checklist intro to 3 sentences (detail already in plan/track-6.md).

**Escalated (design decisions):** S1 — missing Decision Record for the class-`count(*)` fast-path unification (the one engine-surface change beyond the additive ctor, reversing the pre-YTDB-609 decline rationale). User approved adding D11, rationale distilled from the frozen design.md §"Aggregation barrier semantics".

Consistency review: no findings (28 verification certificates, all current-state symbols confirmed). Review files: `plan/reviews/consistency-iter1.md`, `plan/reviews/structural-iter1.md`.

## Re-validation 2026-07-15 (7-track plan, after the Track 4 split inline replan)

- [x] Plan review (consistency + structural) — passed at iteration 1

**Auto-fixed (mechanical):** S1 — trimmed the Track 6 checklist intro from ~6 sentences to 2 (over the 1–3-sentence cap after the renumber; the class/flag inventory is preserved in the entry's `**Scope:**` line and in `plan/track-6.md`).

**Escalated (design decisions):** S2 — Tracks 2 & 3 (completed) carry Strategy-refresh notes naming pre-split downstream ranges (`Tracks 3–6`, `Tracks 4–6`) that the A1 split superseded. User chose **leave-as-is**: they are as-of-completion historical assessments, editing completed-track content is user-pause-gated, and widening the ranges would assert a broader "unchanged" claim the split made false. The split is documented in live prose (Track 3 ADJUST note, Track 4 Outcomes, the Implementation-state paragraph, D5→Track 5).

**Deferred to Phase 4 (design.md frozen):** CR1 — the frozen `design.md` class diagram lags the as-built Track 2/3 surface (7-param `addEdge` + separate `addEdgeAsNode` vs the diagram's 8-param `addEdge`; the current `WalkerContext` field set; `NotFilterStepRecogniser`). Recorded only, no plan/track impact; reconciled in the Phase-4 `design-final.md`.

Consistency review: PASS (gate-verified; CR1 deferred). Structural review: PASS (gate-verified; the 1→(2,3)→4→5→6→7 chain is acyclic, cross-track references and the D5→5 / D11→6 / D8→7 reassignments consistent). PSI was non-responsive this session (IDE stuck indexing) — references verified via grep + direct source Reads with the reference-accuracy caveat recorded in the review evidence bases. The four `plan/reviews/*-iter1.md` files were regenerated for this 7-track re-validation, superseding the Jul-14 6-track evidence (preserved in git history).

## Re-validation 2026-07-27 (9-track plan, after the Track 6 split inline replan)

- [x] Plan review (consistency + structural) — passed at iteration 1

**Auto-fixed (mechanical):** S2 — the `### Invariants` idempotency bullet named only `YTDBMatchPlanStep`; broadened it to name both boundary steps (`YTDBMatchPlanStep` or `MultiPlanMatchStep`), matching the sibling boundary-step invariant above it and D7 (the scan detects both after Track 8).

**Escalated (design decisions):** CR1 (consistency) — Track 7 named `GremlinToMatchTranslator`, `GremlinStepWalker`, and `GremlinToMatchStrategy` all "construction sites" of `YTDBMatchPlanStep`, but only `GremlinToMatchStrategy.replaceAllStepsWithBoundary` constructs it; the other two produce the upstream `TranslationResult`. User chose **correct current-state wording**: the plan Checklist scope line and `track-7.md` Plan of Work item 2 / Interfaces / Context reference-accuracy note now name the sole construction site and describe the two upstream producers, leaving the rewire footprint deferred to the base-shape choice at decomposition. S1 (structural) — Track 6's `**Strategy refresh:** ADJUST` line asserted two premises the post-Track-6 split (D8) overturned (`MultiPlanMatchStep` inherits the `YTDBMatchPlanStep` constructor; three order-less post-process flags). User chose **append superseding note**: the historical line is kept and a correcting parenthetical points to D8 + Tracks 7/8/9 (`MultiPlanMatchStep` extends the shared boundary base; the post-process is an ordered `List`), mirroring the Track 2 refresh convention.

Consistency review: PASS (gate-verified; CR1 fixed and re-confirmed). Structural review: PASS (gate-verified; the 7→8→9 chain rooting on completed Track 6 is acyclic, sizing within the soft two-sided bound, the supply/consume contract holds). The frozen `design.md` `MultiPlanMatchStep : "Track 6"` / `extends YTDBMatchPlanStep` provenance is expected pre-split freeze state, reconciled in the Phase-4 `design-final.md` — recorded, not a finding. PSI `steroid_execute_code` timed out on the cold kotlinc index this session; symbol facts (the sole `new YTDBMatchPlanStep` site, the walker/translator non-references) were verified via grep + Read + fork-jar inspection with the reference-accuracy caveat recorded in the evidence bases. The four Jul-27 12:21 pre-split `plan/reviews/*-iter1.md` files were archived under `plan/reviews/pre-split/`; this re-validation's fresh files are `plan/reviews/consistency-iter1.md`, `consistency-gate-verification-iter1.md`, `structural-iter1.md`, and `structural-gate-verification-iter1.md`.

## Re-validation 2026-08-01 (10-track plan, after the post-Track-8 inline replan that inserted Track 10)

- [x] Plan review (consistency + structural) — passed at iteration 2

**Auto-fixed (mechanical):** CR1 — `## Implementation state` still described the pre-replan world (Track 8 Phase C pending, no Track 10 anywhere); narrative, table row 8, the new Track 10 row, and the decision-conformance sentence brought current. CR3 — `plan/track-10.md` `**Signatures:**` named `ExecutionStep.containsStepOfType`, which does not exist; re-attributed to `YTDBQueryMetricsStrategyTest.containsStepOfType` (a private test helper recursing through `getSubSteps()` only) plus `ExecutionStepInternal.getSubSteps()` / `getSubExecutionPlans()`. CR4 — added a fifth `### Integration Points` bullet for `YTDBQueryMetricsStep.capturedExecutionPlan()` reading the compiled plan off the boundary step, the integration Track 10 exists to repair. CR5 — Track 9 never mentioned Track 10; its `**Inter-track dependencies:**` line and the plan entry's `**Depends on:**` now name it, and the stale "all six prior tracks" count is gone. CR6 — the CR2 fix named `UnionStepRecogniser` as the home of the post-union allow-list; it is `GremlinStepWalker.POST_UNION_RECOGNISERS` (`:193-197`, read by `dispatchAll` and `postUnionSuffixTranslatable`), corrected at four sites. CR7 — the plan entry's `**Depends on:**` line lagged the track file. S1 — Track 9's checklist intro had grown to five sentences against the 1–3 cap; trimmed to three. S2 — Track 10's `**Scope:**` line carried no file footprint; added `~5 files`, with the under-floor size already justified in writing on both sides. S3 — `plan/track-10.md` had its Phase-1 sketch under `## Concrete Steps` and a two-sentence `## Plan of Work`; the four items moved to `## Plan of Work` and `## Concrete Steps` returned to the Phase A placeholder. S7 — the S5 Component Map bullet ran over the ~5-line intent cap; re-wrapped.

**Escalated (design decisions):** CR2 — Track 8's DR-U4 and DR-U1 assign Track 9 the post-union suffix relaxation for the list-shaping terminators, and Track 9's `**Out of scope:**` line excluded union outright; Track 9 is the last Phase 1 track, so nothing downstream recovers the gap. User chose **Track 9 absorbs the relaxation**: new Plan of Work item 4 extending `GremlinStepWalker.POST_UNION_RECOGNISERS` to the four terminators, a `union(...).fold()` multiset-parity acceptance line, the out-of-scope entry narrowed to the union recogniser and `MultiPlanMatchStep` internals, and the Scope figure moved ~14–20 → ~15–21 files. Track 8's own split between "Track 9 relaxes this" and "Track 9 **may** relax" was left as-is — editing a completed track's content is user-pause-gated, and Track 9's file is now the authoritative carrier. S4 — the plan's "Engine surface is preserved" constraint freezes "the execution steps" with two named exceptions, while Track 10 item 3 contemplates `MatchFirstStep` introspection overrides, and `MatchFirstStep` is a MATCH execution step; a decomposer reading the constraint as binding picks the test side silently and the branch loses the only Gremlin-level index-usage assertion in the tree. User chose **leave it to Track 10 Phase A**: item 3 now states the collision, both admissible resolutions, and an ESCALATE condition on defaulting to the test side, with a cross-reference from that file's `## Invariants & Constraints`. The plan's Constraints bullet is deliberately unamended. S5 — the Component Map had no node for the query-metrics capture layer Track 10 touches. User chose **add the node and bullet**: a `Metrics` node fed by `Boundary` over a `read by` edge, plus a seventh annotated bullet.

**Deferred to Phase 4 (design.md frozen):** S6 — two gaps Track 10 opened, both new since the 2026-07-27 pass. First, `design.md` § "Boundary-step lifecycle" enumerates stream open, three close paths, and `clone()` isolation but states no `reset()` / re-iteration contract, which is exactly what Track 10 step 1 must settle (`toList()` drives `AbstractMatchPlanStep` to `CLOSED`; `reset()` re-arms only from `OPEN` / `DRAINED`, so re-iteration yields `[]` where the native `YTDBGraphStep` path re-executes). Second, § "Observable behavior changes" lists three user-visible changes and omits a fourth live since `6e657ce2b1` — a translated traversal carries no `YTDBGraphStep`, so `YTDBQueryMetricsStep.capturedExecutionPlan()` changed what the monitoring layer sees. `design-final.md` should carry the settled reset contract in the first section and the metrics-capture change in the second; Track 10's Phase C feeds its step-1 DR into that queue. Joins the existing deferrals (CR1 of 2026-07-15; the `MultiPlanMatchStep : "Track 6"` / `extends YTDBMatchPlanStep` provenance of 2026-07-27).

Consistency review: PASS (gate-verified at iteration 2 — the iteration-1 gate caught CR6, a wrong class name in the orchestrator's own CR2 fix, and CR7, a site the CR5 fix missed). Structural review: PASS (gate-verified at iteration 1 plus the S7 re-wrap). The non-ascending Checklist order (Track 8 → Track 10 → Track 9) is intended and was verified mechanically sound: `workflow-startup-precheck.sh` walks `## Checklist` for the first `[ ]` in document order and reads the number off `Track <N>:` without sorting, so Track 10's entry position makes it the resume target. PSI `steroid_execute_code` timed out again on the cold kotlinc index; symbol facts (`POST_UNION_RECOGNISERS`, `MatchFirstStep`'s absent overrides, the `containsStepOfType` test-local helper) were verified via grep plus direct source Reads with the reference-accuracy caveat recorded in the review evidence bases. The four `plan/reviews/*-iter1.md` files were regenerated for this 10-track re-validation, superseding the Jul-27 9-track evidence (preserved in git history).
