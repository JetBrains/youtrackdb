<!-- MANIFEST
findings: 6   severity: {blocker: 0, should-fix: 4, suggestion: 2}
index:
  - {id: S1, sev: should-fix, loc: "implementation-plan.md § Checklist (Track 9 entry — intro paragraph)", anchor: "### S1 ", cert: "", basis: "Track 9 checklist intro runs 5 sentences against the 1-3 cap; the just-landed POST_UNION relaxation sentence pushed a 4-sentence intro further over"}
  - {id: S2, sev: should-fix, loc: "implementation-plan.md § Checklist (Track 10 entry — **Scope:** line)", anchor: "### S2 ", cert: "", basis: "Track 10's Scope line carries no ~N-file footprint; every other Scope line in the plan does, and the footprint is the plan-time sizing signal"}
  - {id: S3, sev: should-fix, loc: "plan/track-10.md § Plan of Work + § Concrete Steps", anchor: "### S3 ", cert: "", basis: "Track 10's four work items sit in the Phase-A-owned ## Concrete Steps slot while ## Plan of Work holds only a 2-sentence strategy note; inverted against the 15-section template and against track-9.md"}
  - {id: S4, sev: should-fix, loc: "implementation-plan.md § High-level plan → ### Constraints (Engine surface bullet) vs plan/track-10.md § Plan of Work item 3", anchor: "### S4 ", cert: "", basis: "the plan constraint says MATCH execution steps are not modified except two named exceptions; Track 10 item 3 may add getSubSteps/getSubExecutionPlans overrides to MatchFirstStep, a MATCH execution step"}
  - {id: S5, sev: suggestion, loc: "implementation-plan.md § Architecture Notes → #### Component Map", anchor: "### S5 ", cert: "", basis: "the Component Map carries no node for the query-metrics capture layer or MatchFirstStep, the components Track 10 modifies; the CR pass added the Integration Points bullet but not the map node"}
  - {id: S6, sev: suggestion, loc: "design.md § Boundary-step lifecycle + § Observable behavior changes (frozen — record only)", anchor: "### S6 ", cert: "", basis: "the frozen design's lifecycle section states no reset()/re-iteration contract (the contract Track 10 step 1 must settle) and Observable behavior changes omits the metrics-capture change"}
evidence_base: {section: "## Evidence base", certs: 0, matches: 0}
cert_index: []
flags: [CONTRACT_OK]
-->

## Findings

### S1 [should-fix]
**Location**: `implementation-plan.md` § Checklist, Track 9 entry — the intro paragraph preceding `**Scope:**` (plan lines 599–610).
**Issue**: The intro runs five sentences against the 1–3-sentence cap:
1. "Completes and validates the feature."
2. "Adds the four list-shaping terminators … mid-traversal use declines under D3."
3. "Relaxes `GremlinStepWalker.POST_UNION_RECOGNISERS` to admit the same four terminators after a union (Track 8 DR-U4)."
4. "Then the full TinkerPop Cucumber suite green … pinned to verified-recognised shapes."
5. "Detail in plan/track-9.md."

The entry was already at four sentences; this session's consistency pass added sentence 3. Every sentence past the cap is re-read at every `/execute-tracks` session startup for the rest of the plan's life, and the same rule was enforced against Track 6 in the 2026-07-15 re-validation (S1) and Tracks 4 and 6 in the first pass (S2/S3). All the displaced detail already has a home: the POST_UNION relaxation is on the `**Scope:**` line ("the post-union suffix allow-list relaxation"), in `plan/track-9.md` Plan of Work item 4, and in its `## Interfaces and Dependencies`; the `tail`/`fold` mechanics are in that file's `## Context and Orientation`.
**Proposed fix**: Trim to three sentences — keep sentence 1, compress sentence 2 to the terminator list plus the last-step rule (drop the `projectOrSkip` / `TailGlobalStepContract` / `n=0` / `n<0` parentheticals, which are Plan-of-Work item 2–3 detail), drop sentence 3 entirely (the `**Scope:**` line already names it), and merge sentence 4 with the "Detail in plan/track-9.md" pointer. Target shape: intro (3 sentences) → `**Scope:**` → `**Depends on:** Tracks 7, 8, and 10.`
**Classification**: mechanical
**Justification**: §`mechanical` — "All BLOAT findings are `mechanical` by construction"; the fix moves nothing new, it deletes plan-checklist text that is already carried in the Scope line and the track file.

### S2 [should-fix]
**Location**: `implementation-plan.md` § Checklist, Track 10 entry — the `**Scope:**` line (plan lines 585–590).
**Issue**: The Scope line carries no planned file footprint. It reads `**Scope:** settle the AbstractMatchPlanStep.reset()-from-CLOSED contract …, settle the g.V(rid) plan-capture contract, decide the MatchFirstStep introspection question …, and close the detection hole` — a restatement of the Plan-of-Work items with no `~N files` figure. Every other Scope line in the plan carries one (Track 1 `~9 files`, Track 2 `~19 files`, Track 6 `~20 files`, Track 9 `~15–21 files`). The SCOPE INDICATORS criterion requires "an approximate planned file footprint and a brief list of what those files cover"; without the figure the two-sided sizing bound cannot be applied at all, and Phase A has no plan-time effort signal to reconcile its decomposition against.
**Proposed fix**: Derive the footprint from `plan/track-10.md` `## Interfaces and Dependencies` **In scope** list — `YTDBQueryMetricsStep`, `AbstractMatchPlanStep` (conditional), `MatchFirstStep` (conditional), `YTDBQueryMetricsStrategyTest`, plus the CI / draft-PR detection artefact — and write `**Scope:** ~5 files covering the metrics plan capture, the `AbstractMatchPlanStep` reset-from-`CLOSED` contract, `MatchFirstStep` introspection, the `YTDBQueryMetricsStrategyTest` expectations, and the detection-hole fix.` **No sizing finding follows from adding the number**: ~5 files is under the ~12 floor, but the merge candidacy is already refused in writing on both sides — the checklist's `**Why a separate track:**` block and `plan/track-10.md` DR-M1 refuse the fold back into Track 8, and the Purpose paragraph plus `## Interfaces and Dependencies` refuse the fold forward into Track 9 (its Cucumber and JMH gates need a green baseline). Do not escalate a sizing question off this fix.
**Classification**: mechanical
**Justification**: §`mechanical` — "Scope-indicator format issues (missing `**Scope:**` line, missing `**Depends on:**` annotation …)"; the figure is read off the track file's own in-scope list, so the edit changes no plan intent.

### S3 [should-fix]
**Location**: `plan/track-10.md` § Plan of Work (line 56–58) and § Concrete Steps (lines 60–66).
**Issue**: The two sections are inverted against the 15-section template. `## Plan of Work` holds only a two-sentence strategy note ("Establish what the correct contract is before changing code … Then fix, then close the hole"), while the four numbered work items — settle `reset()`, settle `byId` capture, decide the introspection question, close the detection hole — sit under `## Concrete Steps` behind a `<!-- Phase A decomposes this. The list below is the Phase 1 sketch, not a decomposed roster. -->` disclaimer. `conventions-execution.md` §2.1 assigns §7 `## Plan of Work` the Phase 1 "prose sequence of edits and additions" and §8 `## Concrete Steps` the Phase-A-written roster, "immutable after Phase A except for the status checkbox flip". `plan/track-9.md` follows that split exactly (7 numbered Plan-of-Work items; `## Concrete Steps` is `<!-- Phase A placeholder. -->`), so Track 10 is the only outlier.

Two consequences. The Phase A decomposer and the Phase A review trio read `## Plan of Work` for what the track changes and would find a strategy sentence there. And Phase A must overwrite planner-authored content inside a slot the template declares Phase-A-owned, so the sketch's four items and the decomposed roster can silently diverge in numbering. There is **no** startup-state hazard: `workflow-startup-precheck.sh` short-circuits to `decomposition-pending` off the `## Progress` `- [ ] Review + decomposition` entry before it quantifies the roster (script comment at line 1081–1086), and the sketch items carry no `[ ]`/`[x]`/`[!]` glyphs.
**Proposed fix**: Move the four numbered items verbatim into `## Plan of Work`, under the existing two-sentence lead. Restore `## Concrete Steps` to `<!-- Phase A placeholder. -->`, matching `plan/track-9.md`. Keep the cross-references in `## Validation and Acceptance` ("The index-usage question from step 3") and `## Interfaces and Dependencies` ("if step 1 lands on the product side", "if step 3 lands on the product side") pointing at the same numbers — the renumbering is identity, so no reference changes.
**Classification**: mechanical
**Justification**: §`mechanical` — "a single unambiguous edit that doesn't change plan intent … a missing required heading"; the content moves between two sections of the same file with no renumbering and no semantic change.

### S4 [should-fix]
**Location**: `implementation-plan.md` § High-level plan → `### Constraints`, the "Engine surface is preserved" bullet (plan lines 40–44), against `plan/track-10.md` § Plan of Work item 3 and its `## Interfaces and Dependencies` **In scope** list.
**Issue**: The plan constraint reads: "The only addition to the MATCH execution surface is one new public `MatchExecutionPlanner(MatchPlanInputs)` constructor (D2). Existing constructors, the IR classes, **the execution steps**, the grammar, and the evaluators are not modified — except the two new string-predicate AST nodes in D-TEXT-OPS and the count short-circuit refactor."

Track 10 item 3 contemplates exactly such a modification: "If `MatchFirstStep` should expose its nested plan through `getSubSteps()` / `getSubExecutionPlans()`, that fixes both scan tests and restores the only Gremlin-level index-usage assertion in the tree." `MatchFirstStep` lives at `core/src/main/java/com/jetbrains/youtrackdb/internal/core/sql/executor/match/MatchFirstStep.java` — a MATCH execution step, squarely inside the constraint's "the execution steps", and not covered by either named exception. `plan/track-10.md` `## Interfaces and Dependencies` lists "`MatchFirstStep` introspection overrides if step 3 lands on the product side" as in scope, so the track intends the product-side option as live.

The failure mode is silent, not loud: a Phase A decomposer that treats the Constraints bullet as binding picks the test-side closure without escalating, and the branch permanently loses "the only Gremlin-level index-usage assertion in the tree" — the exact assertion Track 10 exists to restore. Track 10's own `## Invariants & Constraints` guards a different axis ("Do not change what translates") and does not resolve this one.
**Proposed fix**: The user picks one of two. (a) Extend the Constraints bullet with a third exception naming the Track 10 introspection override as a behavior-neutral addition — something like "and the `MatchFirstStep` sub-step / sub-plan introspection overrides in Track 10, which add no execution behavior" — leaving item 3's product-side option open. (b) Pin item 3 to the test side in `plan/track-10.md` and record in its `## Decision Log` why the engine-surface constraint outweighs restoring the index-usage assertion. Either resolution should also reach the acceptance line "The index-usage question from step 3 is answered explicitly, not left open."
**Classification**: design-decision
**Justification**: §`design-decision` — "Track contradictions — Track 1 assumes X, Track 3 assumes not-X. Which is right is a design call"; whether the engine-surface freeze admits a behavior-neutral introspection override is the planner's call, not a mechanical edit.

### S5 [suggestion]
**Location**: `implementation-plan.md` § Architecture Notes → `#### Component Map` (the Mermaid `flowchart TB` and its annotated bullet list, plan lines 64–117).
**Issue**: The map carries no node for the components Track 10 modifies. Its six bullets cover the strategy, the walker + registry, `WalkerContext`, the shared builders, the boundary base and its two subclasses, and the preserved engine; the query-metrics capture layer (`YTDBQueryMetricsStep` / `YTDBQueryMetricsStrategy`) and `MatchFirstStep` appear nowhere. This session's consistency pass added the fifth `### Integration Points` bullet for `YTDBQueryMetricsStep.capturedExecutionPlan()` but left the map untouched, so the plan's one cross-track impact-assessment artefact still shows a pending track with an empty blast radius. The criterion is "Does it include only touched components plus immediate neighbors? Is every component annotated with what changes and why?" — the map is now incomplete on the first half.

Filed at suggestion because an execution agent has the fact from the Integration Points bullet; the map node is completeness for impact assessment rather than a correctness gap.
**Proposed fix**: Add one node and one bullet. Node: a `Metrics["YTDBQueryMetricsStep\n(plan capture)"]` box fed by the `Boundary` node (the capture reads the compiled plan off the boundary step, falling back to `YTDBGraphStep`). Bullet: one short paragraph naming what changes — the capture source moved from `YTDBGraphStep` to the boundary step when traversals began translating, and Track 10 settles the resulting `reset()` / `byId` / introspection contracts. Keep it inside the ~5-line component-intent budget.
**Classification**: design-decision
**Justification**: §`design-decision` — "Architecture Notes gaps — a missing plan Component Map, or a track missing Invariants … where the scope boundary is ambiguous. Filling these requires the user's rationale."

### S6 [suggestion]
**Location**: `design.md` § "Boundary-step lifecycle" (lines 1858–1941) and § "Observable behavior changes" (lines 1942–1986). **Frozen after the Phase 1 gate — recorded only, no mutation proposed; carry to Phase 4.**
**Issue**: Two gaps that Track 10 opened, both new since the 2026-07-27 re-validation (Track 10 did not exist then), and distinct from the track-attribution provenance drift that pass already recorded as expected frozen state.

1. § "Boundary-step lifecycle" enumerates stream open, three close paths, and `clone()` isolation, but states no `reset()` / re-iteration contract. That contract is precisely what Track 10 step 1 must settle: `plan/track-10.md` records that `toList()` drives `AbstractMatchPlanStep` to `CLOSED`, `reset()` re-arms only from `OPEN` / `DRAINED`, and re-iteration then yields `[]` where the native `YTDBGraphStep` path re-executes. The frozen section gives the decomposer no design-side anchor for the "is the step wrong or the test wrong?" question, and whichever way it resolves, the answer belongs in this section's Phase-4 successor.
2. § "Observable behavior changes" lists three user-visible changes (result ordering, `.explain()` rendering, `profile()` declines) and omits a fourth that has been live on the branch since `6e657ce2b1`: a translated traversal no longer carries a `YTDBGraphStep`, so `YTDBQueryMetricsStep.capturedExecutionPlan()` changed what the monitoring layer sees. The plan's `### Integration Points` now records it; the design's dedicated section for exactly this class of change does not.
**Proposed fix**: None against `design.md` — it is frozen. Record both in the Phase-4 reconciliation queue alongside the existing deferrals (CR1 of 2026-07-15; the `MultiPlanMatchStep : "Track 6"` / `extends YTDBMatchPlanStep` provenance of 2026-07-27), so `design-final.md` carries the settled reset/re-iteration contract in § "Boundary-step lifecycle" and the metrics-capture change in § "Observable behavior changes". Track 10's Phase C should feed its DR for step 1 into that queue.
**Classification**: design-decision
**Justification**: §`design-decision` — "Design document gaps (design gate yes only) … missing dedicated section for concurrency / crash recovery / performance. The content of those sections is a design call"; the reset contract's content is Track 10's decision to make.

## Evidence base

No certificates — plan-internal structural review, no codebase read beyond one path confirmation for S4 (`core/src/main/java/com/jetbrains/youtrackdb/internal/core/sql/executor/match/MatchFirstStep.java`). `certs: 0`.

Checked and cleared, recorded so the next pass does not re-litigate:

- **Descending track order (10 before 9) is documented robustly and works mechanically.** Nine positions carry it: the `## Checklist` physical order; Track 10's "**Runs before Track 9** despite the higher number" opener; its `**Why a separate track:**` numbering rationale; `**Depends on:** Tracks 7, 8, and 10` on Track 9; the `## Implementation state` prose ("in that execution order") and its 10-then-9 table rows; `plan/track-10.md`'s `**Numbering note.**` and its `## Interfaces and Dependencies`; and `plan/track-9.md`'s inter-track dependency line. Verified against the machine path: `workflow-startup-precheck.sh` (lines 2103–2160) walks `## Checklist` for the *first* `[ ]` in document order and reads the number off `Track <N>:` — the walk is positional, never sorted, so Track 10's entry position makes it the resume target. No ordering finding.
- **Dependency graph is acyclic.** 8 → 10 → 9, rooted on completed Track 8. No earlier-executing track depends on a later-executing one.
- **Tracks 10 and 9 both touch `AbstractMatchPlanStep`** (Track 10's `reset()` contract, conditionally; Track 9's `LIST` materialization + ordered op application). They are consecutive in execution order, so the non-consecutive-overlap rule does not fire.
- **Track 9's `## Decision Log` carries no formal DR** — it delegates to plan D3 and Track 7's carrier and lists two open realization notes for decomposition. This is the same shape `plan/track-8.md` carried before its Phase A, which then resolved the open choices into DR-U1…DR-U5; Track 8 passed the 2026-07-27 structural gate in that state. Not filed.
- **Track 9's absorption of the post-union relaxation is consistent with its source.** `plan/track-8.md` DR-U4 ("List-shaping terminators (`fold`/…) remain Track 9"), its Surprises entry ("Track 9 relaxes this for the list-shaping terminators only"), its `## Outcomes & Retrospective` ("the extension point a later list-shaping track edits", after `order()` left the allow-list), and its `## Interfaces and Dependencies` out-of-scope line all agree with Track 9's Plan-of-Work item 4, acceptance line, in-scope/out-of-scope split, and signatures block. The `count` / `range` / `dedup` membership Track 9 states matches Track 8's post-repair state.
- **Sizing is in bounds.** Track 9 at ~15–21 in-scope files sits inside the soft two-sided range (floor ~12, ceiling ~20–25). Track 10 is under the floor with written justification on both fold directions (see S2).
- **Component-intent budget.** The 7-line "Boundary base / YTDBMatchPlanStep / MultiPlanMatchStep" bullet is 2 lines over the ~5-line soft cap, but commit `3c4f17d752` ("Apply structural-preview fixes to the Track 7/8/9 split") already trimmed it from 9 lines to this state as a prior structural fix. Not re-raised. All five other bullets are 2–4 lines.
- **Plan-file budget.** 638 lines / ~41 KB (~11K tokens), well under the ~1,500-line / ~30K-token roll-up.
- **No `- [ ] Step:` items and no *(provisional)* markers** anywhere in the plan file or either pending track file.
- **Design-document diagram sizes exceed the soft caps** — the `classDiagram` carries 21 classes against ~12, the `sequenceDiagram` 11 participants against ~8. Both are frozen, unchanged since the freeze, and passed the 2026-07-14, 2026-07-15, and 2026-07-27 structural gates without a finding. Unactionable before Phase 4; not filed.
