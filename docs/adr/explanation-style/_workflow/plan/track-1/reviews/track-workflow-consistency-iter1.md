<!-- MANIFEST
findings: 1   severity: {Critical: 0, Recommended: 1, Minor: 0}
index:
  - {id: WC1, sev: Recommended, loc: ".claude/skills/readability-feedback/SKILL.md:36,70", anchor: "### WC1 ", cert: n/a, basis: "new always-on ## Orientation rule omitted from the readability-audit prompt's house-style section list and GAP-routing targets; audit agents will misclassify too-terse findings as GAP"}
evidence_base: {section: "## Evidence base", certs: 0}
flags: [CONTRACT_OK]
-->

## Findings

### WC1 [Recommended] readability-feedback audit prompt omits the new `## Orientation` rule from its house-style section list and GAP-routing targets

- **File:** `.claude/skills/readability-feedback/SKILL.md` (line 70, the audit sub-agent prompt's STEP 1 "note especially" list; line 36, the GAP-routing target-section guidance; line 76 STEP 4 classification). Referent: `house-style.md § Orientation` (added this track at `house-style.md` step 2).
- **Axis:** cross-file rule restatement (summary-vs-source drift between the new always-on rule and a consuming prompt's section list).
- **Cost:** the one workflow surface whose job is to audit prose readability and classify findings against house-style sections does not point its agents at the rule built to catch the failure it audits for; a too-terse passage gets misclassified as a GAP (no rule addresses it) when `## Orientation` now does, defeating the readability-feedback loop's convergence premise.
- **Issue:** this track adds `## Orientation` as a top-level, always-on house-style rule whose explicit purpose is the too-terse direction — "prose a reader cannot follow without opening the code," and per its three moves, "disconnected one-line assertions with no motivation." The `readability-feedback` skill dispatches audit sub-agents (line 63 `## Audit sub-agent prompt`) that read `house-style.md` in full and classify each obscure passage as `CAUGHT by § <section>` or `GAP` (line 76). STEP 3 of that prompt hunts exactly "disconnected one-line assertions with no motivation" (line 74) — the failure `## Orientation` now governs. But the prompt's STEP 1 "note especially" section list (line 70: `§ Banned vocabulary, § Banned sentence patterns, § Banned analysis patterns (and its subsections), § Punctuation and typography, § Structural rules (and its subsections), and § Document-shape rules`) omits `## Orientation`, and the skill's GAP-routing guidance (line 36: "general tells go under Banned vocabulary / Banned sentence patterns / Banned analysis patterns / Structural rules; design-doc-shape tells go under Document-shape rules") gives the agent no `## Orientation` bucket. The file has no reference to `## Orientation` or `### Explanatory register` anywhere (grep-confirmed). This is not a closed-set AI-tell-subset enumeration, so it is correctly outside D1's four→five flip roster; it is a different consistency axis — a source rule added without propagating to the consumer prompt whose function it directly extends. Track scope confirms the gap is unassigned: Track 1 touches `readability-feedback/SKILL.md` only at the `:54` governance grep (R1, kept at four), Track 2 only at the `## Rule sync map` design-review row (D4); the audit prompt's section list and GAP-routing targets are named in neither track's roster (implementation-plan.md:71/101/143/147, track-1.md:226/232).
- **Suggestion:** add `§ Orientation` to the STEP 1 "note especially" list at line 70 and to the GAP-routing target-section guidance at line 36 (general too-terse / one-line-assertion tells now route to `§ Orientation`). This is a one-line addition on each of two lines and does not touch the governance grep (which stays four-string per R1) or the Track 2 sync-map row. If the author prefers to keep all `readability-feedback` Orientation propagation in one place, fold it into the Track 2 commit that already edits this file's `## Rule sync map` — but flag it now so it is not lost between the two tracks' rosters, since both currently treat the file as a two-touch (grep + sync-map) surface and overlook the audit prompt.

## Evidence base
