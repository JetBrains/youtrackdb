<!-- workflow-sha: 26f990ed824d113fdb5fcb930361e69378f0f12a -->
# Track 2: Over-dense prose enforcement

## Purpose / Big Picture
After this track lands, the over-dense AI-tells YTDB-1084 names — run-on mechanism traces, lists spliced into one sentence, inflated-abstraction labels — are caught: the regex-expressible cases by two new `dsc-ai-tell` patterns, the judgment cases by a new cold-read block that runs for both design and track prose.

<!-- Reserved for Move 2 — ADDED/MODIFIED/REMOVED triad. Empty until Move 2 lands. -->

Adds the YTDB-1084 over-dense enforcement that does not enumerate the subset: a judgment-layer `### Prose AI-tell additions` block in the `design-review.md` cold-read running for both `target=design` and `target=tracks`, plus two `dsc-ai-tell` regexes (inflated-abstraction labels and the "X, not Y" negative-parallelism variant) with tests. Ships demotable, calibrated against this branch's own authoring.

## Progress
- [x] Review + decomposition
- [x] Step implementation
- [x] Track-level code review (PASS at iteration 1)
- [x] Track completion
- [x] 2026-06-12T17:02Z [ctx=info] Review + decomposition complete
- [x] 2026-06-12T17:32Z [ctx=safe] Step 1 complete (commit 40d1285f29)
- [x] 2026-06-12T18:03Z [ctx=safe] Step 2 complete (commit e4f38cb77b; Review fix 92b86d5df5 for WH1)
- [x] 2026-06-13T04:40Z [ctx=safe] Track-level code review iteration 1 complete (1/3 iterations)
- [x] 2026-06-13T04:45Z [ctx=safe] Track-level code review complete (PASS, 1 iteration; gate-checks VERIFIED)
- [x] 2026-06-13T04:45Z [ctx=safe] Track complete

## Surprises & Discoveries
<!-- Continuous-log. Empty at Phase 1. -->

- **`dsc-ai-tell` inflated-abstraction is a closed set, not a wildcard.** The inflated-abstraction-label regex matches a curated closed inflation set, not an open `[a-z]+ing`/`[a-z]+ed` participle — WH1 showed the open arm self-flags concrete-mechanism prose ("The locking mechanism is held…"). A future genuine inflation word will not fire until added to the set; extend the set, do not reintroduce the wildcard. The "X, not Y" over-dense regex likewise matches only the emphatic-intensifier form ("not just/merely/simply"), so plain contrastive "A, not B" passes. Both bear on the Phase-4 `design-final.md` calibration acceptance (zero findings of any severity from the two new patterns). See Episodes §Step 2.

## Decision Log
<!-- The track-canonical live decision carrier (D7). Full inline Decision
Records this track owns. The regex severity follows D5's A9 clause, owned
by Track 1; cross-referenced below, not duplicated as authority. -->

#### D4: New cold-read block runs for design AND tracks
- **Alternatives considered**: design-only (the `### Prose AI-tell additions` block runs only for the `design.md` cold-read); both `target=design` and `target=tracks` (chosen).
- **Rationale**: track prose carries the same over-dense / too-terse failure as design prose at **creation time**, so scanning only `design.md` leaves the plan-at-start track sections unchecked. The block sits sibling to `### Human-reader cold-read additions` (`design-review.md:165`) and instructs the reviewer to scan the changed sections against `§ Banned analysis patterns`, `§ Mechanism traces and inline citations`, lists-disguised-as-sentences, and inflated-abstraction labels — the judgment cases regex cannot catch — plus the too-terse direction (the `## Orientation` rule). One block covers both axes.
- **Risks/Caveats**: the claim is bounded to **creation-time** prose. The `target=tracks` cold-read runs once, at Step 4b before the plan commit, so it does **not** cover the YTDB-1106 exemplar surface (live `## Decision Log` entries, episodes, review findings) that accrues during Phase 3 — nothing re-runs a cold-read there. The Phase-3 surface is held by the always-on subset wiring (D1/D2) on the writers, not by this block. The sibling `### Human-reader cold-read additions` applies only to the three design-target mutation kinds (phase1-creation / phase4-creation / design-sync), so this block **cannot copy its applies-to line** — it needs its own statement covering `target=design` (those three kinds) **and** `target=tracks`.
- **Implemented in**: this track (`design-review.md`); step references added during execution
- **Full design**: design.md §"Over-dense prose enforcement" (the applies-to asymmetry, the three sync sites)

#### D4b: Two dsc-ai-tell regexes (severity per D5/A9)
- **Alternatives considered**: add the over-dense rules to the cold-read only (judgment-only, no regex); add regexes for the cleanly-detectable cases (chosen).
- **Rationale**: `dsc-ai-tell` (`design-mechanical-checks.py`) gains the inflated-abstraction labels ("the enabling primitive", "the key abstraction", "the underlying mechanism", and the participle-plus-category-noun shape) and the "X, not Y" rhetorical-elevation variant of negative parallelism (a distinct pattern from the existing `NEGATIVE_PARALLELISM_RE`; **not** to be labelled "faux-symmetry", which `house-style.md:341` already uses for a different structural ban — A4). These two regexes are YTDB-1084's deliverable; **D5's A9 clause governs only their severity** — both ship at the rule's documented demotable severity (this is a cross-reference to Track 1's D5, not a re-decision).
- **Risks/Caveats**: the "X, not Y" pattern risks firing on legitimate contrastive "A, not B" prose (a `, not <lowercase>` probe finds ~8 such sentences on this branch's own `design.md`, none AI-tells — T2); the inflated-abstraction-label regex collides with the design-doc Overview template, which prescribes naming "the enabling primitive(s)" (`design-document-rules.md § Overview`). The regex must target the **subject-slot** inflated label, not the Overview's sanctioned enumeration element, or every conforming design Overview self-flags. Both findings cite an existing `house-style.md §` (`§ Banned sentence patterns` and `§ Banned analysis patterns`) per the rule's citation convention — this track adds no new rule text. The demotable severity is `should-fix`; `dsc-ai-tell` has **no blocker path** (`design-mechanical-checks.py` exits 1 only on blockers), so the earlier "a blocker false positive would exit 1 and block the loop" framing was wrong (A5/T1) — a false positive pollutes the review with a spurious `should-fix`, it does not halt `edit-design`. The calibration target is therefore **zero findings of any severity** from the two new patterns on this branch's own Phase-4 `design-final.md` authoring (where the live regexes self-apply through `edit-design`).
- **Implemented in**: this track (`design-mechanical-checks.py` + `test_dsc_ai_tell.py`); step references added during execution
- **Full design**: design.md §"Over-dense prose enforcement" (the Overview-template collision, the demotable calibration)
- **Realized form (Step 2 refinement).** Two within-step refinements of the loosely-framed regexes, both holding the calibration corpus at zero. (1) The "X, not Y" pattern matches **only** the emphatic-intensifier form ("X, not just/merely/simply Y") — the "not just A" half of the house-style canonical "Not just A, but B." Plain contrastive "A, not B" is genuine prose and passes (a `, not just|merely|simply` probe returns zero across `design.md`, the calibration ADRs, and the plan/track files). (2) The inflated-abstraction-label regex matches a **curated closed inflation set** (enabling, driving, defining, unifying, guiding, governing, underpinning, animating, plus the inflated adjectives), not an open participle arm; the open `[a-z]+ing`/`[a-z]+ed` arm self-flagged concrete-mechanism prose (WH1) and was removed. A future inflation word must be added to the set to fire. See Episodes §Step 2.

## Outcomes & Retrospective
<!-- Continuous-log. Empty at Phase 1. -->

- [x] Technical: PASS at iteration 2 (4 findings T1–T4, 4 accepted; 0 blocker / 2 should-fix / 2 suggestion). Drove the demotable-severity reframe (T1), the X-not-Y false-positive guard set (T2), and the WC1 anchor + Tone-count corrections (T3/T4). Gate-verification VERIFIED all four.
- [x] Adversarial: PASS at iteration 2 (7 findings A1–A7, 6 accepted + A7 confirmed the under-floor split holds; 0 blocker / 5 should-fix / 2 suggestion). Narrowed to track-realization (scope/sizing, cross-track-episode reality, invariant violation). Caught the false "Track 1 flipped the readability grep" claim (A1), the missed `design-document-rules.md` sync site (A2), the cold-read block activation-wiring gap (A3), and the "faux-symmetry" naming collision (A4). Gate-verification VERIFIED all seven.
- [x] Gate-verification (iter 2, consolidated technical+adversarial): PASS — 10/10 VERIFIED, 0 regressions; one new suggestion T5 (stale "~4" in-scope numeral after A2 bumped the roster to ~5) fixed in place, plus the second in-file docstring sync site folded into step 2.
- [x] Track-level code review (Phase C): PASS at iteration 1, 0 blockers. Workflow-only diff, so the four code-correctness baselines skipped and all six workflow reviewers ran; consistency and context-budget came back clean. WP1/WI1 (should-fix, one issue from two angles: the new Prose AI-tell cold-read block told the reviewer to scan for over-dense/too-terse prose but the output template carried no emit slot and said nothing about the `target=tracks` invocation) closed by one edit (`e15e97f174`) adding an `over-dense:`/`too-terse:` slot covering both `target=design` and `target=tracks`; both gate-checks VERIFIED and the T4 "five Human-reader rules" count held. WP2 (suggestion: the over-dense bullet wrongly listed inflated-abstraction labels as a class the regex cannot catch after Step 2 made the subject-slot label regex-detectable) reworded in the same commit. Three suggestions left unaddressed by design: WH1 (the per-line scan misses a subject-slot label split across a soft-wrap break — accepted, consistent with the closed-set "a missed tell is foregone noise" trade-off, and touching the calibration-locked script for a suggestion risks the Phase-4 zero-findings contract); WS1 (an em dash inside a fenced prompt block — fence-excluded, so not a mechanical violation); WS2 (em-dash density in a `_workflow/` review artifact Phase 4 sweeps). No plan corrections: no finding belongs to another track, and Track 2 is the last track.

## Context and Orientation

This track adds the two YTDB-1084 combining moves that do **not** enumerate the AI-tell subset, so they sit outside Track 1's atomic flip. State of the surfaces (verified on the branch tip `26f990ed82`):

- **`design-review.md`** — the cold-read prompt with two targets (`target=design` / `target=tracks`). `### Human-reader cold-read additions` at line 165 is the sibling pattern, and it shows a block needs **two** things to run, not one: its own applies-to line **and** explicit activation pointers inside each per-target cold-read body (the "verify the Human-reader cold-read additions (§ below)" lines near 126 / 135 / 148). The new `### Prose AI-tell additions` block must be wired the same way — an applies-to line alone leaves it defined-but-never-run (A3). So its edit set is: the block body + applies-to line, the activation pointers at every cold-read invocation site for `target=design` (the three design kinds) **and** `target=tracks`, the TOC row near line 22, and a reviewer-tone clause in `§ Tone and depth`. That last item is **not** a five→six count bump: the "five Human-reader rules require evidence" count at line 406 names the sibling block's five rules specifically, so the new block needs its **own** evidence-tone clause alongside it, leaving the count at five (T4).
- **`design-mechanical-checks.py`** — holds the `dsc-ai-tell` rule (the narrow regex set). Not a subset-enumeration site. The two new regexes extend it.
- **`test_dsc_ai_tell.py`** — exercises `dsc-ai-tell`. Not a subset-enumeration site; it gains cases for the two new regexes (positive matches + the false-positive guards: contrastive "A, not B" prose, the Overview "the enabling primitive(s)" enumeration).
- **`readability-feedback/SKILL.md`** — its `## Rule sync map` (line 41) is the codified add-a-rule procedure; its design-review bullet already exists (line 47, listing the Human-reader sync targets) and Track 2 **extends** that bullet to name the new cold-read block (it is not a new row to add). The same file's `:54` grep is a rename-enumeration tool Track 1 deliberately kept four-string (R1 — it counts pointer sites, so it is not a subset-membership site and never gains `## Orientation`); **Track 2 does not touch the grep** (A1 — an earlier draft wrongly said Track 1 "flipped" it). **Plan correction (WC1, from Track 1 Phase C review):** this file's audit sub-agent prompt omits the `## Orientation` rule Track 1 adds — its STEP 1 "note especially" house-style section list (line 70) and its STEP 4 GAP classification (line 76, where each finding is marked `CAUGHT by § <section>` or `GAP`) name the other house-style sections but not `## Orientation` (T3 — the audit's GAP routing is STEP 4 at line 76, not the rule-author target taxonomy at line 36). A too-terse passage, the failure `## Orientation` now governs, therefore misroutes as a GAP ("no rule addresses it") when one now does, defeating the readability-feedback convergence premise. The fix location sits in neither track's original roster; the Track 1 reviewer endorsed folding it here, since this track already edits the file.

Non-obvious terminology: **dsc-ai-tell** is the mechanical AI-tell check inside `design-mechanical-checks.py`, run by `edit-design` on every `design.md` mutation; **demotable severity** means a finding the rule may down-grade to `suggestion` rather than fail the mutation, the documented fallback when a pattern risks false positives.

## Plan of Work

1. **The cold-read block (D4).** Add `### Prose AI-tell additions` to `design-review.md` sibling to `### Human-reader cold-read additions`, with its **own** applies-to line covering `target=design` (phase1-creation / phase4-creation / design-sync) and `target=tracks`. The block scans both axes — over-dense (the four judgment cases) and too-terse (the `## Orientation` rule). **Wire it to run (A3):** add an activation pointer at every cold-read invocation site the sibling Human-reader block is pointed from (near lines 126 / 135 / 148 for the design kinds) plus the `target=tracks` invocation site — an applies-to line without these pointers is defined-but-never-run. Sync sites: the new TOC row (near line 22); a **new** reviewer-tone clause for the block in `§ Tone and depth` (leave the "five Human-reader rules" count at five — T4); and **extend the existing design-review sync bullet** at `readability-feedback/SKILL.md:47` to name the block (T3 — it is a bullet to extend, not a row to add).
2. **The two regexes (D4b).** Add to `dsc-ai-tell` in `design-mechanical-checks.py`: (a) the inflated-abstraction-label pattern, targeting the **subject slot**, not the Overview's sanctioned "the enabling primitive(s)" enumeration element; (b) the "X, not Y" rhetorical-elevation pattern — a variant of negative parallelism, **distinct from** the existing `NEGATIVE_PARALLELISM_RE` ("not X, but Y"). **Naming + citation (A4):** do not label the second regex "faux-symmetry" (that term is already taken by `house-style.md:341` for a different, structural ban); both findings must cite an **existing** `house-style.md §` per the `dsc-ai-tell` convention — the "X, not Y" variant cites `§ Banned sentence patterns`, the inflated-abstraction label cites `§ Banned analysis patterns` — since this track adds no new rule text. **Severity (A5/T1):** both findings emit at the rule's documented demotable severity, which **is** `should-fix` (like the rule's existing eight patterns); build **no** new severity mechanism. **Doc sync (A2):** `design-document-rules.md:284` enumerates the `dsc-ai-tell` patterns as "nine" by name — bump the count to eleven and add the two new patterns to that list; also bump the in-file `check_dsc_ai_tell` docstring near `design-mechanical-checks.py:1771` ("Nine patterns fire").
3. **Tests.** Extend `test_dsc_ai_tell.py` with positive matches and the false-positive guards. The "X, not Y" guard set must include the legitimate contrastive constructions a `, not <lowercase>` probe surfaces on this branch's own `design.md` (~8 real contrastive sentences, none AI-tells — T2), so the elevation regex does not regress them; plus the sanctioned "the enabling primitive(s)" Overview enumeration for the inflated-abstraction-label regex.
4. **Propagate `## Orientation` into the readability audit prompt (WC1 plan correction).** Add `§ Orientation` to `readability-feedback/SKILL.md`'s audit sub-agent prompt — its STEP 1 "note especially" house-style section list (`:70`) and its STEP 4 GAP classification (`:76`, where a finding is marked `CAUGHT by § <section>` or `GAP`; a general too-terse / one-line-assertion tell now routes to `§ Orientation`) — so the audit recognises the too-terse rule rather than misclassifying its hits as a GAP (T3 — the audit's GAP routing is STEP 4 at `:76`, not the rule-author target taxonomy at `:36`). It does not touch the `:54` grep (kept four-string, R1) or the step-1 design-review sync bullet above. Folded here per the Track 1 reviewer's endorsement; keep all readability-feedback `## Orientation` propagation in this track's single edit of the file.

Ordering: this whole track lands after Track 1 (its cold-read scans the `## Orientation` rule, which Track 1 adds; its live edits rely on Track 1's `§1.7` opt-out). Internal order: the regexes (step 2) and their tests (step 3) are one coupled unit — same commit, since the tests gate the regexes (A6); the cold-read block (step 1) and the WC1 propagation (step 4) are independent of that unit and of each other.

## Concrete Steps

1. Add the `### Prose AI-tell additions` cold-read block to `design-review.md` — block body + its own applies-to line covering `target=design` (the three design kinds) and `target=tracks`, **plus activation pointers at the cold-read invocation sites for both targets** (mirror the sibling Human-reader block's pointers near :126/:135/:148, A3), the new TOC row (~:22), and a new `§ Tone and depth` reviewer-tone clause for the block (leave the "five Human-reader rules" count at five, T4); and propagate `## Orientation` into `readability-feedback/SKILL.md` (extend the existing design-review sync bullet `:47`; add `§ Orientation` to the audit prompt's STEP 1 list `:70` and STEP 4 GAP classification `:76`, WC1/T3). Do not touch the `:54` grep (R1, four-string). — risk: medium (workflow machinery — bounded behavioral: changes the cold-read reviewer's observable behavior across two prompt files) — size: ~2 files; (a) the only other unit is the high regex step, which stays isolated  [x] commit: 40d1285f29 *(parallel with Step 2)*
2. Add the two `dsc-ai-tell` regexes to `design-mechanical-checks.py` — (a) inflated-abstraction label in the **subject slot** (not the Overview's "the enabling primitive(s)" element), citing `§ Banned analysis patterns`; (b) the "X, not Y" rhetorical-elevation variant, **distinct from** `NEGATIVE_PARALLELISM_RE`, citing `§ Banned sentence patterns`; both emit `should-fix` (the rule's demotable severity), no new severity mechanism (A4/A5) — and sync the count nine→eleven at the in-file docstring `:1771` and `design-document-rules.md:284` (A2); extend `test_dsc_ai_tell.py` with positive matches plus the false-positive guards (the ~8 contrastive `design.md` sentences a `, not <lowercase>` probe surfaces, T2; the sanctioned "the enabling primitive(s)" Overview enumeration). Calibration target: zero findings of any severity from the two patterns on this branch's `design.md` / `design-final.md`. — risk: high (workflow machinery — edits `design-mechanical-checks.py`, a script `edit-design` runs automatically on every design mutation)  [x] commit: e4f38cb77b *(parallel with Step 1)*

## Episodes
<!-- Continuous-log. Empty at Phase 1. -->

### Step 1 — commit 40d1285f2939f1b95bf65a761ecdb897dff6e220, 2026-06-12T17:32Z [ctx=safe]

**What was done:** Added the `### Prose AI-tell additions` cold-read block to `design-review.md`, sibling to `### Human-reader cold-read additions`. The block carries its own applies-to line covering the three `target=design` kinds (phase1-creation / phase4-creation / design-sync) and `target=tracks`, and scans two axes: over-dense prose against `§ Banned analysis patterns`, `§ Mechanism traces and inline citations`, lists-disguised-as-sentences, and inflated-abstraction labels; too-terse prose against `## Orientation`. Activation pointers run at the three design-kind invocation sites and the `target=tracks` Step-4b site, so the block fires instead of sitting defined-but-unrun (A3). The TOC row regenerated via `workflow-reindex.py`, and a separate `§ Tone and depth` evidence clause names the block while the "five Human-reader rules" count stays at five (T4). In `readability-feedback/SKILL.md`, the existing design-review sync bullet now names the block, and `§ Orientation` joins the audit prompt's STEP 1 section list and STEP 4 GAP classification, so a too-terse passage routes to `§ Orientation` rather than misclassifying as a GAP (WC1/T3). The `:54` rename-enumeration grep stays four-string (R1).

**What was discovered:** The 120-char TOC-summary cap fired — the first-draft annotation summary ran 141 chars and `workflow-reindex.py --check` rejected it; the fix shortened it to 119 chars with plain (non-backtick) `§` references, which also clears the ref-stamping trap. The `target=tracks` activation pointer is genuinely new wiring, not a mirror of an existing line: the sibling Human-reader block has no pointer inside the Step-4b track-scoped section, so the new pointer landed after that section's target-set paragraph. One consistency fact for Step 2: this block's over-dense bullet quotes "the enabling primitive" / "the key abstraction" / "the underlying mechanism" as the inflated-label examples, so Step 2's inflated-abstraction regex must target the subject slot and spare the design-doc Overview's sanctioned "the enabling primitive(s)" enumeration (already pinned in D4b).

**What changed from the plan:** None. Every step-described site was hit as scoped.

**Key files:** `.claude/workflow/prompts/design-review.md` (new cold-read block, four activation pointers, TOC row, Tone clause); `.claude/skills/readability-feedback/SKILL.md` (sync bullet + audit-prompt `§ Orientation`).

### Step 2 — commit e4f38cb77b, 2026-06-12T18:03Z [ctx=safe]

**What was done:** Added the two over-dense `dsc-ai-tell` regexes to `design-mechanical-checks.py`. (a) An inflated-abstraction-label pattern fires on the subject slot — a capital "The" plus an inflation-signalling word plus a category noun plus a finite verb — and skips the `## Overview` section so the design-doc template's sanctioned "the enabling primitive(s)" enumeration does not self-flag; it cites `§ Banned analysis patterns`. (b) A trailing-negation "X, not just/merely/simply Y" pattern, distinct from the existing `NEGATIVE_PARALLELISM_RE` ("not X, but Y"), cites `§ Banned sentence patterns`. Both emit the rule's existing `should-fix` demotable severity through `make_finding`; no new severity path. The pattern count moved nine→eleven in the in-file `check_dsc_ai_tell` docstring and in `design-document-rules.md`, with both new patterns named at the enumeration. `test_dsc_ai_tell.py` and its fixture gained positive anchors for both patterns plus the false-positive guards: the ~8 contrastive sentences a `, not <lowercase>` probe surfaces on this branch's `design.md`, the sanctioned Overview enumeration, and (from the WH1 fix) a concrete-mechanism negative block.

**What was discovered:** Plain "X, not Y" is not a usable AI-tell signal: it fires on legitimate contrast such as "a defensive guard, not dead code," so the regex narrowed to the emphatic-intensifier form ("not just/merely/simply"), the "not just A" half of the house-style canonical "Not just A, but B." A `, not just|merely|simply` probe returns zero across `design.md`, the three calibration ADRs, and the plan/track files. Step-level hook-safety review then caught WH1 (should-fix): the inflated-label regex's open `[a-z]+ing`/`[a-z]+ed` participle arms, crossed with concrete nouns like "The locking mechanism is held…", over-matched ordinary prose that would self-flag on every future design mutation, including this branch's Phase-4 `design-final.md`. The fix replaced the open arms with a curated closed inflation set (enabling, driving, defining, unifying, guiding, governing, underpinning, animating, plus the inflated adjectives); `enabling` and `driving` needed explicit entries because the genuine positives had depended on the open arm. After the fix all six concrete-mechanism sentences pass and all six genuine inflated-label positives still fire. The inflated-abstraction slot is now a closed set, so a future genuine inflation word will not fire until added. That is the intended trade: a missed tell is foregone review noise, a false positive is avoided review noise. A later track wanting broader coverage should extend the set rather than reintroduce the wildcard.

**What changed from the plan:** None on scope or sites. One within-step refinement of D4b's loosely-framed "X, not Y": the regex pins the emphatic-intensifier form, the only discriminator that holds the calibration corpus at zero. Consistent with T2's false-positive-guard intent and the cited `§ Banned sentence patterns`. See Decision Log D4b.

**Key files:** `.claude/scripts/design-mechanical-checks.py` (two regexes + count-sync docstring); `.claude/scripts/tests/test_dsc_ai_tell.py` + `.claude/scripts/tests/fixtures/dsc-ai-tell-fixture.md` (positives + false-positive guards + concrete-mechanism negative); `.claude/workflow/design-document-rules.md` (nine→eleven enumeration).

**Review:** hook-safety step-level review — 1 should-fix (WH1), 0 blockers; PASS at gate-check iteration 2 (WH1 VERIFIED).
- Review fix commit: `92b86d5df5` (WH1)

## Validation and Acceptance

Track-level behavioral acceptance:

- `design-review.md` carries a `### Prose AI-tell additions` block with its own applies-to line naming `target=design` (the three design kinds) and `target=tracks`, **and** activation pointers at the cold-read invocation sites for both targets so the block actually runs (A3). The TOC row is synced; the existing design-review sync bullet at `readability-feedback/SKILL.md:47` names the block; `§ Tone and depth` carries a new reviewer-tone clause for the block with the "five Human-reader rules" count left at five (T4).
- `dsc-ai-tell` flags an inflated-abstraction label in a subject slot and an "X, not Y" rhetorical-elevation sentence, both at `should-fix` (the rule's demotable severity), each citing an existing `house-style.md §`; it does **not** flag a contrastive "A, not B" sentence, the ~8 contrastive constructions in this branch's `design.md`, or the design-doc Overview's "the enabling primitive(s)" enumeration.
- `design-document-rules.md`'s `dsc-ai-tell` description names eleven patterns (was nine), including the two new ones (A2).
- `test_dsc_ai_tell.py` passes with the new positive and false-positive cases (including the ~8 contrastive guards).
- `readability-feedback/SKILL.md`'s audit sub-agent prompt names `§ Orientation` in both its STEP 1 "note especially" section list (`:70`) and its STEP 4 GAP classification (`:76`), so a too-terse passage routes to `§ Orientation` rather than GAP (WC1 plan correction; T3).
- Running `edit-design` on this branch's own `design-final.md` in Phase 4 produces **zero findings of any severity** from the two new regexes — the real demotable-calibration contract, since `dsc-ai-tell` has no blocker path and "no blocker-severity false positive" was vacuous (A5/T1).

<!-- Phase A placeholder for per-step EARS/Gherkin lines. -->

<!-- Reserved for Move 3 — EARS or Gherkin acceptance lines used verbatim
as test method names. Empty until Move 3 lands. -->

## Idempotence and Recovery
<!-- Phase A placeholder. -->

## Artifacts and Notes
<!-- Continuous-log (rare). Often empty. -->

## Interfaces and Dependencies

**In-scope files** (~5):
- `.claude/workflow/prompts/design-review.md` — the `### Prose AI-tell additions` cold-read block + its activation pointers at the `target=design` and `target=tracks` invocation sites (A3) + the TOC row + a new `§ Tone and depth` reviewer-tone clause (count stays five — T4).
- `.claude/scripts/design-mechanical-checks.py` — the two `dsc-ai-tell` regexes; also the in-file count sync (the `check_dsc_ai_tell` docstring near `:1771` says "Nine patterns fire" — bump to eleven alongside the `design-document-rules.md:284` count; A2 blast radius).
- `.claude/scripts/tests/test_dsc_ai_tell.py` — tests for the two regexes (positive + false-positive guards, including the ~8 contrastive cases).
- `.claude/workflow/design-document-rules.md` — the `dsc-ai-tell` description at `:284` (the "nine patterns" enumeration → eleven, with the two new patterns named; A2).
- `.claude/skills/readability-feedback/SKILL.md` — extend the existing design-review sync bullet (`:47`) to name the block, **plus** the audit sub-agent prompt's `## Orientation` references (STEP 1 section list `:70` + STEP 4 GAP classification `:76`, WC1 plan correction; T3). The `:54` rename-enumeration grep stays four-string (R1); Track 2 does not touch it.

**Out-of-scope**: the AI-tell subset enumeration (Track 1's atomic flip); `house-style.md` rule text (the over-dense rules the block scans against — `§ Banned analysis patterns`, `§ Mechanism traces and inline citations` — already exist; this track adds enforcement, not new rule text); every `_workflow/**` artifact schema.

**Inter-track dependency**: depends on Track 1 (the `## Orientation` rule must exist for the block to scan the too-terse direction; the `§1.7` opt-out sanctions these live edits).

**Overlap note (planning.md §"Justify any overlap-split").** `readability-feedback/SKILL.md` appeared in both tracks' nominal scope at planning time, but the overlap dissolved in realization: Track 1's only association with the file was its `:54` rename-enumeration grep, which R1 left four-string (a no-op — the grep is a pointer-counting tool, not a subset-membership site), so Track 1 committed **no** edit to the file (verified: `git log` shows only the pre-branch `#1125` commit). Track 2 is therefore the sole editor — it extends the design-review sync bullet (`:47`) and adds the WC1 audit-prompt `## Orientation` references (`:70` / `:76`). The sync bullet documents this track's cold-read block, so it could not have moved earlier without forward-referencing a block that does not yet exist; ordering Track 2 adjacent to Track 1 keeps rebase distance on every shared house-style surface minimal.

**Sizing justification (argumentation gate, under floor).** This track is ~5 in-scope files, under the ~12 merge-candidate floor. It is **not** folded into Track 1 because Track 1 is already over the ~20-25 ceiling (the atomic subset flip forces it), so folding would worsen the breach with no reviewability gain. This track is the one independently-mergeable seam split off from over-ceiling Track 1 (planning.md §"Then clamp with a two-sided bound" sanctions the split), and its review surface — regex false-positive calibration and both-axes cold-read coverage — is orthogonal to Track 1's atomic-flip review focus.

## Base commit

e3503a10d9ccc7e60e54a1acd4bb276dd00a0e1f
