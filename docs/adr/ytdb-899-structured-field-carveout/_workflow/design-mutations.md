# Design mutations log

## Mutation 1 — 2026-05-20 — phase1-creation (design.md)

**Diff summary**: Initial seed of `design.md` for the YTDB-899 ExecPlan structured-field carveout work. Four `##` sections: Overview (concept-first elevator pitch), Workflow (before/after Mermaid flowcharts of the reviewer's decision tree), Exemption categories (five shapes of template-bound content), and Padding-based finding criterion (the post-cap quality predicate for free-form prose). No `design-mechanics.md` companion seeded — single-file design per the skill default for designs under ~5 sections with no Parts and no anticipated long-form derivations.

**Mechanical checks** (target=design): PASS after 2 iterations. Initial run flagged 3 should-fix findings (overview-body line count, dsc-ai-tell negative parallelism in a quoted example, fragmented-header overlap on the "Exemption categories" TL;DR). All three resolved by splitting Overview paragraph 2 into two paragraphs, rewriting the Padding-based TL;DR to drop the literal "It's not X, it's Y" phrase, and rewording both Exemption categories and Padding-based finding criterion TL;DR openings to avoid heading content-word overlap.
**Cold-read** (scope: whole-doc): NEEDS REVISION returned with 5 findings (0 blockers, 4 should-fix, 1 suggestion). 1 applied (F5 partial: added `**Full design**: design.md §"Exemption categories"` to D1 in `implementation-plan.md`). 4 recorded as disputed — applying them would conflict with the design-doc shape rules themselves:

**Findings**:
- should-fix (DISPUTED): F1 — cold-read flagged `## Workflow` for missing Edge cases / Gotchas and References footer per `house-style.md § Edge cases sub-section required` / `§ References footer shape`. Workflow is on the shape-exempt list in `design-document-rules.md § Mechanical checks` (Per-section shape compliance row: "excluding shape-exempt: Overview, Core Concepts, Class Design, Workflow, Part-level TL;DR"). Mechanical check correctly skipped this section; cold-read applied a rule that does not govern Workflow.
- should-fix (DISPUTED): F2 — cold-read flagged Overview for missing `**TL;DR.**` opener. Overview is shape-exempt and `§ Overview concept-first` requires concept-first ordering (baseline → change → primitive → restructured → roadmap). Adding `**TL;DR.**` would violate that rule. The Overview itself is the document-level TL;DR.
- should-fix (DISPUTED): F4 — cold-read flagged the absent `design-mechanics.md` companion pointer in Overview. Per the skill workflow Step 1 default and `design-document-rules.md § Length-triggered split into design-mechanics.md` ("Default is single file"), no companion is needed for a small single-file design. The companion-file pointer rule is conditional ("when `design-mechanics.md` exists") — no pointer required when no companion exists.
- suggestion (DISPUTED): F3 — cold-read flagged Exemption categories (376 words) and Padding-based finding criterion (353 words) as exceeding length tiers. The mechanical length-cap rule in `design-document-rules.md § Mechanical checks` is line-based (≤300 lines), not word-based. Both sections are ~25 lines, well under threshold.
- should-fix (PARTIALLY APPLIED): F5 — plan and track files lack `**Full design**` cross-references. Added `**Full design**: design.md §"Exemption categories"` to D1 in `implementation-plan.md` (D1 drives the non-trivial design that this design.md walks through). D2 and D3 omit the link per `planning.md § Decision Records` ("omit when the four lines above are the whole story"). Track file's `## Decision Log` is a Phase 1 placeholder and carries no DRs to link from.

**Iterations**: 2 of 3 (COMPLETE WITH SHOULD-FIX FINDINGS — 4 cold-read findings recorded as disputed; mechanical PASS clean)

## Mutation 2 — 2026-05-20 — phase4-creation (design-final.md)

**Diff summary**: Created `docs/adr/ytdb-899-structured-field-carveout/design-final.md` (top-level, outside `_workflow/`) reflecting the actual implementation. Four `##` sections: Overview (concept + propagation surface + Phase C deviations), Workflow (before/after Mermaid flowcharts unchanged from `design.md`), Exemption categories (five categories + new `### Unit of evaluation` subsection promoted from the original inline tie-breaker paragraph), and Padding-based finding criterion (unchanged from `design.md`). No `design-mechanics-final.md` — the original design had no mechanics companion, so `target=design`. Cross-file ref propagation skipped per the `phase4-creation` rule. Plan-deviation surfacing in the Overview names the Phase-C additions: `CLAUDE.md:102` compressed to a brief pointer, four Tier-A pointer sites rewritten to "soft section length cap with template-bound exemptions" phrasing, and the unit-of-evaluation tie-breaker added during track-level review.

**Mechanical checks** (target=design): PASS after 2 iterations. Initial run flagged 1 should-fix (`overview-body` 4 non-empty lines, needs ≥5). Resolved in iteration 1 by splitting the propagation inventory into two paragraphs (declarative restatements vs back-references + Tier-A pointer sites), bringing Overview to 5 paragraphs.

**Cold-read** (scope: whole-doc): NEEDS REVISION returned with 2 should-fix + 3 suggestions + 6 pass items. Of the 2 should-fix items, both were misapplications of the soft-cap rule — the cold-read agent applied the ≤200-word soft cap (which `house-style.md:262` explicitly scopes to `### subsections`) to `##` sections, and used the line-based 301-400 should-fix tier against word counts. Overview is shape-exempt per `design-document-rules.md § Per-section mandatory shape` ("The shape-exempt sections are Overview, Core Concepts, Class Design, Workflow…"). One structural improvement applied anyway: promoted the inline "Unit-of-evaluation tie-breaker" paragraph in Exemption categories to a `### Unit of evaluation` subsection — clearer hierarchy, addressable from a TOC, no content lost. Three suggestions (navigability roadmap as bullets, document-level References footer, padding section ~214 words) deferred as non-rule-mandated and outside the soft-cap scope. Phase 4 specific checks all PASS: (a) plan-deviation surfacing complete, (b) diagrams implementation-grounded (after-flow matches `house-style.md:263-264` verbatim), (c) no leaked working-file identifiers (no track/step/finding labels; YTDB-899 allowed). Human-reader additions (audience-fit, glossary-introduction, why-before-what, navigability) all PASS.

**Findings**:
- should-fix (DISPUTED): Overview length 390 words flagged as "301-400 = should-fix". The 200-word soft cap targets `### subsections` per `house-style.md:262`; Overview is a `##` section AND shape-exempt per `design-document-rules.md`. The line-based 301-400 should-fix tier in the severity rubric counts lines, not words. Mechanical line cap PASS (Overview is ~13 source lines).
- should-fix (PARTIALLY APPLIED): Exemption categories length 348 words flagged. Same misapplication. Promoted the unit-of-evaluation tie-breaker to a `### Unit of evaluation` subsection anyway for clearer hierarchy — addresses the spirit (over-stuffed parent) without trimming content the design needs to surface.
- suggestion (DEFERRED): Padding-based finding criterion 214 words — same rule misapplication; section is well within line cap.
- suggestion (DEFERRED): navigability — Overview's roadmap on the last line is prose rather than a bulleted Parts list. Acceptable for a 4-section doc; bullets would add visual weight without information.
- suggestion (DEFERRED): document-level References footer absent. `design-document-rules.md § Per-section mandatory shape` requires per-section References blocks, which the doc has; no document-level footer is mandated by the rule set.

**Iterations**: 2 of 3 (COMPLETE WITH SHOULD-FIX FINDINGS — 1 structural improvement applied; remaining findings disputed as misapplications of `### subsection` rules to `##` sections)
