# Issue reports (local, not committed)

Agents write issue / bug / friction reports here as Markdown files.

Everything in this folder is gitignored except this README, so reports
stay local to your working tree and are never committed or pushed.

Suggested file naming: `YYYY-MM-DD-short-slug.md`.
